# 数据可信上下文

本上下文描述业务事实数据被怀疑、人工核实和保留追溯时使用的统一语言。它与利润异常、员工表现和主数据治理是不同问题。

## Language

**数据提示**：
导入或计算过程中附在事实行上的技术或经营信号，不等于数据有错，也不触发排除。
_Avoid_: 数据错误、违规

**数据疑点**：
一条需要人工核实的采购或销售事实行；在核实前仍属于正式统计样本。
_Avoid_: 错误单、异常员工

**规则校准预览**：
用只读查询模拟某条候选规则可能圈出的数据规模和样本；不保存阈值，不创建数据疑点，也不改变经营统计。
_Avoid_: 异常检测、规则启用

**待核实候选**：
在校准预览中命中模拟条件的一对相邻事实；它只用于估算人工工作量和收集业务解释，不等于数据疑点或数据错误。
_Avoid_: 错误采购、违规记录

**确认数据正确**：
核实人确认源记录符合真实业务；该记录继续参与全部统计。
_Avoid_: 忽略问题

**确认源数据错误**：
核实人依据源表或真实单据确认该事实行的数据有误；原始记录仍保留并可追溯。
_Avoid_: 删除记录、排除型号

**数据已变化**：
事实行在核实后被重新导入或修正，旧结论不再自动适用，需要重新核实。
_Avoid_: 自动恢复、沿用旧结论

**正式参考口径**：
仅排除已确认源数据错误的价格参考统计；待核实记录仍然计入。
_Avoid_: 清洗后数据

**原始追溯口径**：
包含已确认源数据错误在内的全部原始事实，用于还原当时上传和记录的内容。
_Avoid_: 错误口径

## 维保项目语言

**互通 PN 池**：
状态为 active 的一组可互通 PN；人工创建和历史生成的有效池都可参与维保缺失成本参考，并固化使用时的池 ID、版本和样本窗口。
_Avoid_: 只认人工池、自动替代关系、约束价集合

**池均价**：
同一参考自然月内，active 池全体成员（明确包含当前待补价的目标 PN）的有效价格事实先逐条归一含税和未税，再分别按数量加权得到的价格。采购最高价、销售最低价等人工约束不属于均价。
_Avoid_: 成员均价的算术平均、人工指导价

**缺失成本参考**：
专属采购、近日采购和当月采购均未命中后，按“有效池采购 → 有效池销售 → 本 PN 采购 → 本 PN 销售”顺序使用的三个月内低置信参考。超过三个月不再自动引用，继续进入人工成本回填。
_Avoid_: 无限期历史追溯、实际成本、历史重估

**双口径备件成本**：
同一维保行同时持久化的含税成本和未税成本。业务计算统一按 13% 归一；来源表的原始税率、含税标记和原始金额只作审计保留。兼容用的混合原值金额不得进入正式毛利公式。
_Avoid_: 按来源税率计算业务毛利、把含税与未税原值直接相加

**合同级毛利**：
一张销售合同的收入减去归集到该合同的完整双口径成本。同一合同挂多个项目且没有收入分摊规则时，只能计算合同整体，不能伪造各项目毛利。
_Avoid_: 共用合同的单项目毛利

**合同级贡献毛利**：
合同收入减备件成本并进一步扣除生效维保报销。只有不带日期范围的完整往返工作簿成功导入并建立合同费用完整水位后才可发布；范围导入和零散报销行不能自行证明费用完整。
_Avoid_: 只扣备件成本、用局部报销推断全量、预算余额

**统一双税展示策略**：
管理员分别为采购、销售、项目维保设置的含税、未税或同时显示口径，销售默认未税。普通用户只能读取，不能保留个人开关；策略不改变后端同时计算和保留的两套事实。
_Avoid_: 用户个人口径、计算开关、删除另一税口径

**维保往返工作簿**：
带固定 Table、协议版本、行签名、实体版本和显式操作列的可编辑 Excel。缺行不代表删除，只有 CREATE、UPDATE、VOID 会写库；整本先预检，再以单事务写入并重算。
_Avoid_: 任意格式 Excel、按缺失行隐式删除、逐行部分成功

## 只读智能体语言

**智能体任务（Agent Task）**：
用户一次业务目标的可追踪执行单元；它包含版本化计划、步骤、预算、证据引用和终止原因。聊天消息可以触发任务，但聊天轮次本身不等于任务。
_Avoid_: 一轮问答、无限自主循环

**任务计划（Task Plan）**：
服务端校验过的有向无环步骤集合。计划只能引用当前身份可用的已登记能力，且受步骤数、时间、Token、查询行数和文件大小预算约束。
_Avoid_: 模型自由发挥的思考文本、未经校验的工具列表

**执行步骤（Agent Step）**：
一次明确能力调用及其输入引用、输出引用、尝试次数和状态；只有依赖步骤成功后才能运行。
_Avoid_: 不可追踪的后台动作

**工具能力（Capability）**：
由服务端登记的最小操作，显式声明输入 schema、效果类型、权限条件和资源预算。未登记或未声明效果的能力默认拒绝。
_Avoid_: 只在提示词里承诺安全、任意 Shell、任意网络请求

**业务只读（Business Read）**：
AI 只能读取经过当前登录身份的行级、字段级和页面权限过滤后的采购、销售、库存、项目与维保事实；不得创建、修改、删除这些事实，也不得改变申请或审批状态。
_Avoid_: AI 只读数据库等于所有业务数据可读、自动审批

**文件制品（Artifact）**：
由确定性制品服务基于不可变输入生成的新文件；拥有稳定 ID、归属、真实 MIME、大小、SHA-256、来源、状态和生命周期。只有 ready 制品可下载。
_Avoid_: 覆盖源文件、模型手写下载链接、本地临时路径

**证据包（Evidence Package）**：
支撑建议结论的结构化事实快照，固化查询窗口、正式实体 ID、统计结果、规则版本和来源引用；模型只能解释证据，不能改写硬规则结果。
_Avoid_: 无来源的 AI 判断、模型自行补数

**补库申请评审（Replenishment Review）**：
对维保备件补库申请给出“建议通过、建议驳回、需补充信息”的只读评审流程。正式审批仍由既有业务流程和人工完成。
_Avoid_: 自动审批、AI 采购单

**人工模板（Human Template）**：
由业务人员维护的字段、映射和清洗规则版本，是描述清洗的权威输入；待处理表格中的内容始终视为数据而非指令。
_Avoid_: 从任意单元格学习系统指令、模型临时发明字段口径

**变更提案（Change Proposal）**：
AI 对表格或描述提出的结构化 Patch/Diff；只有通过确定性校验后才能生成新的 Artifact，不能原地覆盖源文件或直接回写业务事实。
_Avoid_: 自动修改原表、静默清洗
