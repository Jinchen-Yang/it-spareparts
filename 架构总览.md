# IT 备件智能管理系统 · 当前架构与功能总览

> **本文用途**：从**当前代码**反向整理的"现状架构 + 全部功能"权威总览（不是规划稿）。
> 与合同附件 `架构报告_合同附件.md`(v1.0, 2026-05-25, 规划口径) 的区别：那份描述一/二期规划，
> 本文描述**截至 2026-06-30 仓库里真实已落地的系统**——二期 AI 已用 DeepSeek 落地、三期 RBAC 已启用、
> PN 主数据治理已上线、采购分析面板 / 账号管理 / 服务端会话等均已实现。
>
> 生成方式：通读 `backend/app`、`frontend/src`、`docker-compose.yml`、迁移链与现有设计文档后综合。

---

## 0. 一图速览

```
┌──────────────────────────────────────────────────────────────────────┐
│  浏览器 (Nginx 静态托管 React/Vite/AntD SPA)                            │
│   登录 · 数据导入 · 型号查询 · 采购记录 · AI助手 · 利润分析            │
│   库存查询 · 备件主数据 · 数据治理 · 账号管理                          │
│   ── 状态式分页(无 react-router)，菜单按 page_* 权限过滤 ──            │
└───────────────────────────────┬──────────────────────────────────────┘
                                │ HTTPS / JSON · Bearer HMAC token
┌───────────────────────────────┴──────────────────────────────────────┐
│  FastAPI 后端 (Python 3.11+, uv)                                      │
│                                                                       │
│  API 层  app/api/*       auth accounts import parts profit inventory  │
│                          substitutes governance agent chat_sessions   │
│                          purchases   (+ /health, /health/db)          │
│  ───────────────────────────────────────────────────────────────     │
│  横切     auth.py(HMAC token/PBKDF2/吊销)  permissions.py(RBAC 模板)   │
│           security.py(字段脱敏/行级匿名)   config.py(运行配置+业务口径) │
│  ───────────────────────────────────────────────────────────────     │
│  服务层  app/services/*  成本/利润 · 型号全景 · 检索解析 · 库存 ·       │
│                          主数据治理(合并/改名/规格/质量) · 采购分析 ·   │
│                          会话存储 · 智能体文件                         │
│  智能体  app/agent/*     provider(OpenAI兼容) runtime(多轮工具循环)    │
│                          tools(10个工具) prompts(场景手册 A-E)         │
│  ETL     app/etl/*       reader→cleaner→mapping→transform→loader       │
│                          (+anomaly, pipeline 编排)                     │
│  模型    app/models/*    SQLAlchemy 2.0 声明式  ·  Alembic 迁移        │
└───────────────────────────────┬──────────────────────────────────────┘
                                │
┌───────────────────────────────┴──────────────────────────────────────┐
│  PostgreSQL 15  (+pg_trgm 生成列/GIN 近似检索)                         │
│   维度 dim_*  ·  事实 f_*  ·  库存/替代 inventory/part_substitute      │
│   主数据 brands/product_*  ·  系统 sys_*  ·  会话 chat_*               │
└───────────────────────────────────────────────────────────────────────┘
  原始上传 Excel 按 SHA256 哈希名归档于命名卷 uploaded_files
```

部署：Docker Compose 三服务 **db + app + frontend**；CI 跑 **后端 pytest(含迁移链验证) + 前端 tsc&vite build**。

---

## 1. 技术栈

| 层 | 选型 |
|---|---|
| 后端语言/运行时 | Python 3.11+，`uv` 管理依赖 |
| Web 框架 | FastAPI + Uvicorn(standard) |
| ORM / 迁移 | SQLAlchemy 2.0(声明式) + Alembic |
| 数据库 | PostgreSQL 15（含 `pg_trgm` 扩展，生成列 + GIN 索引做近似检索）|
| 数据处理 | pandas + openpyxl（`.xlsx`）；pdfplumber / python-docx / pillow（智能体文件解析）|
| LLM 接入 | `openai` SDK，**OpenAI 兼容 provider 抽象**：对话默认 DeepSeek，视觉默认通义 Qwen-VL |
| 前端 | React 18 + Vite + TypeScript + Ant Design 5 |
| 前端托管 | Nginx（`frontend/nginx.conf`，静态 + 反代 `/api`）|
| 部署 | Docker Compose（app + db + frontend + 命名卷）|
| CI | GitHub Actions（`.github/workflows/ci.yml`）|

后端 Python 依赖（`backend/pyproject.toml`）：fastapi · uvicorn · sqlalchemy · alembic · psycopg[binary] · pydantic / pydantic-settings · pandas · openpyxl · python-multipart · **openai** · python-docx · pdfplumber · pillow；dev: pytest · httpx。

---

## 2. 部署拓扑（`docker-compose.yml`）

| 服务 | 镜像/构建 | 端口 | 说明 |
|---|---|---|---|
| `db` | postgres:15 | `127.0.0.1:5432`（仅本机）| 命名卷 `postgres_data`；`pg_isready` 健康检查 |
| `app` | 构建自 `backend/` | `expose 8000`（仅 docker 内网）| 命名卷 `uploaded_files` 挂 `/app/data/raw`；`depends_on db healthy` |
| `frontend` | 构建自 `frontend/`(Nginx) | `${FRONTEND_PORT:-8080}:80` | 浏览器入口；反代到 `app` |

要点：
- **默认 `ENVIRONMENT=prod`**：忘配 `.env` 时若仍是默认弱口令/密钥则**拒绝启动**（`config.check_security` + `main.py` 启动自检），本地用 `.env` 设 `ENVIRONMENT=dev` 放行。
- 日志统一 json-file 轮转 `10m × 5`（每服务 ≤50MB）。
- 数据库不暴露公网；app 不暴露主机端口，只经 frontend Nginx 出网。
- 无自动迁移：部署时手动 `alembic upgrade head`（部署顺序见 `docs/DEPLOY.md`：备份→build→迁移→起容器）。

CI（`.github/workflows/ci.yml`）两 job：
1. **后端**：起 postgres:15(:5433) → `uv run --extra dev pytest -q`（conftest 先 `alembic upgrade head`，**迁移链一并验证**）。
2. **前端**：`npm ci && npm run build`（tsc 类型检查 + vite 构建）。

---

## 3. 后端分层与横切

### 3.1 入口与装配（`app/main.py`）
- 创建 `FastAPI`，挂 CORS（来源由 `cors_origins` 配置），统一 `/api` 前缀挂载 11 个路由：auth · accounts · import · parts · profit · inventory · substitutes · governance · agent · chat_sessions · purchases。
- 启动期安全自检：prod 下默认口令/密钥直接 `RuntimeError` 拒启；dev 仅告警。
- 探针：`GET /health`（不依赖 DB）、`GET /health/db`（连通性，异常不泄露细节）。

### 3.2 配置（`app/config.py`）
两类集中在一处：
- **`Settings`（环境变量驱动）**：数据库、上传目录、登录密钥/TTL、CORS、LLM（provider/base_url/model/key/extra_body/工具轮次/超时/重试）、Vision（key/base_url/model/页数上限）等。`LLM_EXTRA_BODY` 在**启动期**就校验是否合法 JSON（非法即拒启）。
- **§8 业务口径开关**（待客户确认项集中，改配置不改逻辑）：成本法 `moving_avg`/`fifo`、FIFO 排队范围、期初成本兜底、计入成本/营收的业务类型、含税口径 `ex_tax`、目标毛利率、近 N 天采购窗口、成交参考价加权口径、导入模式/上限、品牌同义词、解析置信阈值、合并候选门槛、采购分析阈值与来源渠道分类、**RBAC 字段组 `FIELD_GROUPS`** 等。

### 3.3 横切安全三件套
- **认证 `app/auth.py`**：
  - 口令 **PBKDF2-SHA256**（带盐/迭代）存储校验。
  - 令牌 **HMAC-SHA256** 自签：payload `{role, sub, name, exp, tv}`，无第三方依赖（`{base64}.{sig}` 形式）。
  - **即时吊销**：实名 token 的 `tv` 必须等于用户当前 `token_version`，且账号 active；改密/停用/改权限 → 递增 `token_version` → 旧 token 立即失效（即时踢线，`verify_token_db`）。
  - **登录锁定**：失败次数锁定（迁移 `e5b2c9f4a1d6_login_lockout`）。
  - **guest 回退**：共享口令/认证失败兜底身份，绝不为 admin；该身份**禁用会话功能**（报价隐私）。
- **权限 `app/permissions.py`**：见 §8 RBAC。
- **脱敏 `app/security.py`**：据 `data_*` 开关算出要隐藏的 `FIELD_GROUPS` 组 → `apply_field_visibility` 在所有接口出参把命中字段抹成 `null`；逐单销售成交明细另由 `anonymize_sales_rows` + `own_customers_only` 收紧。**脱敏靠精确 key 匹配**，服务层每个派生价格/毛利键名都已在 `FIELD_GROUPS` 逐字登记。

### 3.4 数据访问（`app/db.py`）
SQLAlchemy 2.0 `engine` + `SessionLocal`；FastAPI 依赖注入 `get_db`。流式对话期间注意连接纪律（请求级连接先 rollback 还池，worker 每次工具调用后 rollback）。

---

## 4. 功能全景（按业务域）

### A. 数据导入 ETL（`app/etl/*`、`app/api/imports.py`）
氚云三类 Excel（采购订单 / 销售订单 / 产品库存）自动识别并入库。
- **reader**：读 `.xlsx`，双表头解析（跳过 `F\d{7}` 编码行、识别中文列名），多明细订单主表字段 ffill 续行补齐。
- **cleaner**：PN 标准化（去氚云内部 V 码 token）、金额定点化（修浮点毛刺）、税率统一（`13.0%`/`0.13`→`0.1300`）、日期容错、供应商名去全角括号后缀、采购类型标准化。
- **mapping**：氚云列名 → 内部字段，三类文件按列特征判定。
- **transform / loader**：行变换 → 维度/事实记录；落库时 dim upsert、**幂等去重**（以氚云"数据ID(不可修改)"为唯一键，同文件 SHA256 拦重传）、**墓碑重定向**（已合并 part 不复活，沿 `merged_into` 链重定向）、两种导入模式 `skip`(默认)/`upsert`(修复)。
- **anomaly**：异常行检测与预览。
- **pipeline**：编排全流程 + 写审计 + 产出导入报告（源行/新增/更新/跳过/错误/未生效计数）。
- 落地表：`sys_import_job`(批量任务) / `sys_import_batch`(批次) / `sys_import_error`(错误明细) / `sys_raw_file`(原文件 SHA256 归档)。
- API：`POST /import/upload`、`POST /import/precheck`、`POST /import/upload-batch`、`GET /import/jobs[/{id}]`、`GET /import/batches[/{id}]`。

### B. 型号全景查询 / 检索解析（`part_overview` · `part_resolver` · `app/api/parts.py`）
- **检索**：`GET /parts/search` 按 `pn_std`/描述命中，支持品牌同义词召回（`BRAND_SYNONYMS`，中英混写）、规格区间过滤（part_type/interface/capacity）；零命中落 `sys_audit_log`(search_miss) 作治理工单来源。
- **解析器 `part_resolver`**：近似检索 + 消歧 + 置信度（低于 `RESOLVE_LOW_CONFIDENCE` 引导消歧）——主数据治理候选发现与智能体的共同基建，统一以 **part_id** 为身份锚点。
- **全景 `part_overview`**：聚合基本信息（品牌/品类/规格/`needs_review`）、采购史、销售史、库存、替代料、**两种成本法毛利并排**、近期采购价窗口（quick_pricing，整机拆解/批量查价用）、成交参考价。含 merged 重定向与 `redirected_from`。
- API：`GET /parts/overview`、`GET /parts/purchases`、`GET /parts/sales`。

### C. 成本与利润引擎（`services/cost.py` · `services/profit.py` · `app/api/profit.py`）
- **两种计价法并排**：移动加权平均 与 FIFO，每笔销售两套单位成本同时算并落库（`f_sales_line` 上 `cost_moving_avg`/`cost_fifo` 等，迁移 `9ab193c1387c`）。
- **COGS 严格按已售扣减**；未售部分作库存资产价值另列，不计毛利。
- **税口径** 默认 `ex_tax`（不含税算毛利）；**维保采购不计成本**（`COST_PURCHASE_TYPES`）。
- **兜底分级**：`computed`(真实匹配) / `fallback`(最近采购价估算) / `none`(无成本)。
- **重算**：`POST /profit/recompute`（采购/销售导入成功后自动触发，按 part_id 口径）。
- **三维度聚合**：型号 / 销售员 / 客户；`GET /profit`。
- **导出**：`GET /profit/export` CSV，含两法全列，防 Excel 公式注入（`=+-@` 转义）。

### D. 库存与替代料（`services/inventory.py` · `services/substitute.py`）
- 库存查询：仓库筛选 + PN/描述搜索 + 分页；`GET /inventory`、`GET /inventory/warehouses`。
- 人工修正：`PUT /inventory/{id}` 改 `manual_qty`/`safety_stock`，必填原因，写审计，可撤销恢复源值（`source_qty` 与人工值并存）。
- 单位成本/资产价值自动回填（按型号加权平均不含税采购价，无采购记录留空不臆造）。
- 替代料：`(part_id_a, part_id_b)` 排序唯一 + `direction`/`type`/`status`，自关联拒绝，写审计；`GET/POST /substitutes`。

### E. PN 主数据治理（`services/{master_data,master_edit,merge,part_rename,match_candidates,taxonomy,spec_extract,normalize,batch_normalize,classify}.py` · `app/api/governance.py` · `app/api/parts.py` master/*）
治理闭环：**候选发现 → 人工审核 → 合并/改名/独立**，全部以 part_id 为身份，事实行 pn 文本永不改。
- **品牌/品类/规格字典**：`brands` / `product_categories`(扁平两级) / `product_specs`(EAV + numeric 区间)。
- **合并候选 `match_candidates`**：两路召回（pn_compact 重复组 + needs_review 跑 resolver），score≥`CANDIDATE_MIN_SCORE` 入队，垃圾 compact 过滤；**任何分数都不自动合并**。
- **合并/反合并 `merge`**：`merge_parts`（锁序防并发互合、恒等别名 UPSERT、路径压缩、合并后自动重算利润+库存成本）+ `unmerge`（前镜像 LIFO 回滚）。
- **受控改名 `part_rename`**：`rename_part` 改 `pn_std` 文本不动身份；占用则转合并候选；复合外键 `(part_id, pn_std)` 同事务批量同改防漂移；旧写法 UPSERT 为 active 别名。
- **质量问题 `product_data_quality_issues`**：缺品牌/规格、占位品牌等；`dismissed` 不重开。
- **新建/编辑 PN `master_edit`**：`GET /parts/master/check`、`POST/PATCH /parts/master`、`/master/suggest`、`/master/categories`、`/master/batch-preview`、`/master/batch-apply`（批量规格归一化，前端 `BatchNormalizeModal`）。
- **治理工作台 API**：`GET /governance/{summary,metrics,parts,candidates,issues,aliases,merges}`、`POST /governance/{refresh,parts/merge,parts/rename,parts/unmerge,parts/confirm-batch,candidates/{id}/review,issues/{id}/resolve,aliases/{id}/review}`、`PUT /governance/exclude`。

### F. 采购分析面板（`services/{purchase_analysis,purchase_query}.py` · `app/api/purchases.py`）
早会/周会用：识别频发应急采购→转批量、按来源渠道拆分。
- `GET /purchases/recent`（最近采购记录，合同重点"销售/采购都能查"）。
- `GET /purchases/cancellation-stats`（取消统计）。
- `GET /purchases/analysis`、`GET /purchases/analysis/part`：窗口内频发型号（阈值 `ANALYSIS_FREQ_THRESHOLD`）、逐日 sparkline、来源渠道分类（淘宝/京东/个人/正规供应商…），含税缺税率时按行业 13% 兜底换算未税；带取数硬上限/over_limit 保护。

### G. AI 智能体（`app/agent/*` · `services/{chat_store,agent_files}.py` · `app/api/{agent,chat_sessions}.py`）
对标扣子/DeepSeek 网页版的内部智能体（路线见 `docs/智能体平台架构与演进路线.md`，现状=P0/P1）。
- **provider**：OpenAI 兼容抽象，换厂商只改 `base_url+model+key`；对话默认 DeepSeek、视觉默认 Qwen-VL；显式化 429/5xx 退避重试。未配 key 时返回降级提示。
- **runtime**：多轮工具循环（流式/非流式，最多 `LLM_MAX_TOOL_ITERS` 轮）；思考模型 `reasoning_content` 流式回前端（可折叠灰显）。
- **tools（10 个）**：`search_parts` · `get_part_overview` · `get_profit_ranking` · `list_recent_purchases` · `inspect_file` · `read_file_rows` · `read_document` · `lookup_prices_bulk` · `write_excel` · `write_report`。**数据诚实**戒律：模型只能引用工具返回的库内数据；越权数据不进 LLM 上下文（脱敏/行级过滤在工具产出处）。
- **prompts**：场景手册式系统提示词——A 报价 / B 压价 / C 解释型号 / D 询价单批量 / D2 整机拆解 / D3 整机配置 / E 经营分析。
- **服务端会话 `chat_store`**：`chat_session`/`chat_message` 持久化，归属 `token.sub` 本人可见（管理员也看不到别人对话）；历史窗口服务端截取（20 条/单条 8000 字符）；**中断语义**：点停止→已生成部分 `stopped=True` 落库；断网/关页→worker 跑到底完整落库；同会话同时只允许一轮（409）。
- **文件 `agent_files`**：上传解析 docx/pdf/xlsx/图片(→视觉) + ACL；`POST /agent/upload`、`GET /agent/files/{id}[/preview]`、会话级 `POST /agent/sessions/{id}/chat/attach`。
- API：旧无状态 `POST /agent/chat[/stream]`；会话式 `app/api/chat_sessions.py`：`GET/POST /agent/sessions`、`PATCH/DELETE /agent/sessions/{id}`、`GET /{id}/messages`、`POST /{id}/chat/stream`(SSE)、`POST /{id}/chat/cancel`。

### H. 账号 / 认证 / RBAC / 审计（`app/api/accounts.py` · `app/auth.py` · `app/permissions.py` · `app/security.py`）
- 账号管理（admin）：`GET /accounts/_meta`、`GET/POST /accounts`、`PUT /accounts/{u}`、`PUT /accounts/{u}/password`、`PUT /accounts/{u}/active`、`GET /accounts/{u}/activity`。
- 见 §8 RBAC 详解。
- 审计：`sys_audit_log`（排除/恢复/库存修正/替代料/合并/改名等写操作的实体/前后值/原因/操作人）；`sys_access_log`（谁查了什么型号/客户，`ENABLE_ACCESS_LOG`，含 IP/UA）。

---

## 5. 数据模型（PostgreSQL，约 28 张表）

| 域 | 表（文件） | 要点 |
|---|---|---|
| **维度** | `dim_part`, `part_alias`, `dim_supplier`, `dim_customer`（`models/dimensions.py`）| `dim_part`=商品主键(id 即 product_id)，含 `pn_std`(唯一)/`pn_compact`/`search_doc`(trgm 生成列)/`status`/`merged_into_id`/`brand_id`/`category_id`/`data_quality_score`/`is_excluded`；`part_alias` 复合外键 `(part_id,pn_std)→dim_part(id,pn_std)` 防漂移 |
| **事实** | `f_purchase_order`/`f_purchase_line`、`f_sales_order`/`f_sales_line`、`f_part_inquiry`（`models/{purchase,sales,inquiry}.py`）| 头/行拆分；幂等键=氚云数据ID；金额 `NUMERIC`；`f_sales_line` 带 `cost_moving_avg`/`cost_fifo`/`matched_cost` 等；采购行 `tax`/`supplier_channel`（迁移 `d7a4f1c9b3e2`）；事实行 part_id NOT NULL |
| **库存/替代** | `inventory`, `part_substitute`（`models/inventory.py`）| `source_qty`(源)/`manual_qty`+`is_qty_overridden`(人工)/`unit_cost`/`inventory_value`；替代料 `(a,b)` 规范序 + direction/type/status |
| **主数据** | `brands`, `product_categories`, `product_specs`, `product_match_candidates`, `product_merge_logs`, `product_data_quality_issues`（`models/master_data.py`）| 候选部分唯一防重复待审；合并日志含前镜像+受影响行 id；质量问题 entity 定位 |
| **系统** | `sys_user`, `sys_import_job`, `sys_import_batch`, `sys_import_error`, `sys_raw_file`, `sys_audit_log`, `sys_access_log`（`models/system.py`）| `sys_user` 含 `permissions`(JSONB)/`token_version`/`is_active`/锁定字段 |
| **会话** | `chat_session`, `chat_message`（`models/chat.py`）| 服务端会话持久化（迁移 `85424bb7cc56`）|

迁移链（Alembic，单头，约 20 个）从 `fb60839c775d_initial_schema_v1_1` 起，含：pg_trgm 生成列+GIN（`c3a51f8d2e07`）、sys_user/RBAC（`bad10309d7ec`、`a3f1c9e2d7b8`）、主数据新表/改表（`a1f3c9d2e8b4`、`b2e7d5c1f9a3`、`a8f3c1d5e7b9`）、FIFO/移动均价列（`9ab193c1387c`）、登录锁定（`e5b2c9f4a1d6`）、token 版本（`f6c4a2e9b1d3`）、采购税/渠道（`d7a4f1c9b3e2`）、订单状态/日期索引（`e9d4c2b7a1f5`）、会话持久化（`85424bb7cc56` 等）。

---

## 6. API 全量端点清单（前缀 `/api`）

| 路由 | 端点 |
|---|---|
| auth | `POST /auth/login` |
| accounts | `GET /accounts/_meta` · `GET/POST /accounts` · `PUT /accounts/{u}` · `PUT /accounts/{u}/password` · `PUT /accounts/{u}/active` · `GET /accounts/{u}/activity` |
| import | `POST /import/upload` · `POST /import/precheck` · `POST /import/upload-batch` · `GET /import/jobs[/{id}]` · `GET /import/batches[/{id}]` |
| parts | `GET /parts/search` · `GET /parts/overview` · `GET /parts/purchases` · `GET /parts/sales` · `GET /parts/master/check` · `POST/PATCH /parts/master` · `GET /parts/master/categories` · `GET /parts/master/suggest` · `GET /parts/master/batch-preview` · `POST /parts/master/batch-apply` |
| profit | `POST /profit/recompute` · `GET /profit` · `GET /profit/export` |
| inventory | `GET /inventory` · `GET /inventory/warehouses` · `PUT /inventory/{id}` |
| substitutes | `GET /substitutes` · `POST /substitutes` |
| governance | `GET /governance/{summary,metrics,parts,candidates,issues,aliases,merges}` · `POST /governance/{refresh,parts/merge,parts/rename,parts/unmerge,parts/confirm-batch}` · `POST /governance/candidates/{id}/review` · `POST /governance/issues/{id}/resolve` · `POST /governance/aliases/{id}/review` · `PUT /governance/exclude` |
| agent | `POST /agent/chat` · `POST /agent/chat/stream` · `POST /agent/upload` · `GET /agent/files/{id}` · `GET /agent/files/{id}/preview` |
| agent/sessions | `GET/POST /agent/sessions` · `PATCH/DELETE /agent/sessions/{id}` · `GET /agent/sessions/{id}/messages` · `POST /agent/sessions/{id}/chat/stream` · `POST /agent/sessions/{id}/chat/cancel` · `POST /agent/sessions/{id}/chat/attach` |
| purchases | `GET /purchases/recent` · `GET /purchases/cancellation-stats` · `GET /purchases/analysis` · `GET /purchases/analysis/part` |
| 探针 | `GET /health` · `GET /health/db` |

---

## 7. 前端结构（`frontend/src`）

- **应用壳 `App.tsx`**：AntD `Layout`，**状态式分页**（非 react-router，`useState(page)`）；菜单按 `page_*` 权限过滤（`can()`），admin 多"账号管理"；版本号/更新日志 Modal + 升版"本次更新"提示。
- **页面 `pages/*`**：`LoginPage` · `ImportPage`(数据导入) · `PartSearchPage`(型号查询) · `PurchasesPage`(采购记录) · `ChatPage`(AI 助手, SSE 会话) · `ProfitPage`(利润分析) · `InventoryPage`(库存) · `MasterDataPage`(备件主数据) · `GovernancePage`(数据治理) · `AccountsPage`(账号管理)。
- **组件 `components/*`**：`BatchNormalizeModal`(批量规格归一化) · `ErrorBoundary`(防整页白屏) · `PageHeader` · `ResizableTable`。
- **基础**：`api.ts`(fetch 封装 + Bearer token) · `theme.ts`(`COLORS`) · `version.ts`(`APP_VERSION`/`CHANGELOG`) · `utils/format.ts` · `main.tsx`。
- **权限快照**：登录后把 `permissions` 存 localStorage，读坏时回退空（不白屏）。
- 构建/托管：`vite.config.ts` 构建；`nginx.conf` 静态托管 + `/api` 反代 app。

---

## 8. RBAC 与脱敏（三期已启用 `ENABLE_RBAC=True`）

防御在**数据层**（行级匿名化 + 字段脱敏），不靠提示词。

- **角色模板 `permissions.ROLE_TEMPLATES`**（单一真值源）：

  | 角色 | 数据可见(data_*) | 页面 | 备注 |
  |---|---|---|---|
  | `admin` | 全开（恒全开，不可自锁）| 全部 | |
  | `boss` | 全开 | 全部 | |
  | `readonly` | 全开 | 除 import/governance/master_data | |
  | `sales` | 客户✓ 成本✓ 利润✓，**供应商✗** | parts/purchases/inventory/chat | `own_customers_only=True`（只看自己成交客户，防恶性竞争）|
  | `purchaser` | 供应商✓ 成本✓，**客户✗ 利润✗** | parts/purchases/inventory/chat/master_data | |

- **权限维度**：
  - `data_*`（驱动 `security.apply_field_visibility`）：`data_supplier`/`data_customer`/`data_purchase_cost`/`data_profit` → 关掉则对应 `FIELD_GROUPS` 字段在所有接口被抹 null。
  - `page_*`（驱动前端菜单 + 后端 `require_page` 准入）：parts/purchases/profit/inventory/chat/import/governance/master_data。
  - `own_customers_only`：销售逐单成交明细收紧。
- **每账号** 可在 `sys_user.permissions`(JSONB) 逐项覆盖模板；`effective(role, custom)` = 模板打底 + 自定义覆盖（admin 例外恒全开）。
- **脱敏机制**：`FIELD_GROUPS`(config) 把字段按"组"管控（避免漏字段被反推）；**所有派生价格/成本/毛利键名逐字登记**（任何新派生键不登记即对越权角色静默泄漏——代码注释反复强调的历史教训）。

---

## 9. 安全基线
- prod 默认弱口令/密钥**拒启**；DB 端口仅 `127.0.0.1`；`/health/db` 不泄露细节。
- 口令 PBKDF2、token HMAC 自签 + 版本吊销 + 登录锁定；guest 回退绝不为 admin 且禁会话。
- CSV 导出防公式注入；上传大小/行数上限（`MAX_UPLOAD_MB=100`、`IMPORT_MAX_ROWS=300k`）防内存撑爆。
- 关键写操作全审计；越权数据不进 LLM 上下文。

---

## 10. 测试与质量
- 后端 `pytest`（`backend/tests/`，约 40+ 用例）：RBAC、审计、认证锁定/回退、导入鲁棒性/双表头容错、成本/利润 part_id 等价回归、合并/改名/重定向、智能体运行时/工具/文件 ACL、采购分析、会话等。
- CI 跑后端 pytest（含 `alembic upgrade head` 迁移链验证）+ 前端 tsc&build。
- 评测脚本 `backend/scripts/pn_*`（PN 同款匹配/合并/规模化评测）。

---

## 11. 演进路线与设计文档索引

**分期现状**：一期(数据底座/查询/治理/库存) **已交付**；二期(AI 定价助手) **已落地**（DeepSeek，非合同稿写的 Anthropic）；三期(RBAC 防恶性竞争) **已启用**。

**智能体平台路线（`docs/智能体平台架构与演进路线.md`）**：
- **P0/P1 已落地**：服务端会话 + 多轮工具循环 + provider 抽象 + 10 工具 + 场景手册 A-E。
- **P2 知识库(RAG)**：设计先行——`kb_collection/document/chunk` + pgvector + `search_knowledge`（需换 pgvector 镜像，部署注意）。
- **P3 工作流**：DAG 定义存库 + 执行器（tool/llm/branch/human 节点），`wf_definition/run/run_step`。
- **P4 Skill/Tools 可插拔**：`_REGISTRY` 升级带元数据注册器 + skill 表化（场景手册→可配置），远期对接 MCP。

**其他设计/方案文档**：
| 文档 | 内容 | 状态 |
|---|---|---|
| `架构报告_合同附件.md` | 主架构与交付报告（一/二期规划口径）| 合同附件 v1.0 |
| `docs/PN主数据整改方案-定稿.md` | PN 主数据整改（part_id 身份/additive/人工审核/合并语义）| 已实施 + 30 回归 |
| `docs/PN标准化Agent设计方案.md` | 联网治理 Agent（自动核对同款 + 分级自治 L0–L2 + 证据表 + 联网安全）| 设计稿，未实施 |
| `docs/采购分析面板-部署说明.md` | 采购分析面板部署 | 已实施 |
| `docs/DEPLOY.md` | 部署手册（备份→build→迁移→起容器）| — |
| `docs/交接_型号同款准确率测试.md` | 同款准确率测试交接 | — |
| `架构质量体检报告_2026-06-29.md`·`系统全面审计报告_2026-06-28.md`·`架构逻辑漏洞报告.md` | 审计/体检/漏洞报告 | 审计产物 |

---

> 设计不变量（贯穿全系统）：① **数据诚实**（模型只引用库内数据）；② **权限在数据层**（越权不进上下文）；③ **服务端为唯一事实源**；④ **商品身份=dim_part.id**（禁 pn 文本作键）；⑤ 通用原语优于预设管道。
