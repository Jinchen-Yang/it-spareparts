# 技术实现详解

> 中石化完井研究中心 · IT 备件智能管理系统

---

## 目录

1. [技术栈](#1-技术栈)
2. [API 端点总览](#2-api-端点总览)
3. [核心服务层函数](#3-核心服务层函数)
4. [ETL 流水线](#4-etl-流水线)
5. [AI 智能体](#5-ai-智能体)
6. [数据模型（主要表）](#6-数据模型主要表)
7. [权限与安全](#7-权限与安全)

---

## 1 技术栈

### 后端

| 组件 | 版本 / 说明 |
|------|------------|
| Python | 3.11 |
| FastAPI | 0.111 |
| SQLAlchemy | 2.0 ORM |
| Alembic | 数据库迁移 |
| PostgreSQL | 15，启用 `pg_trgm` 扩展（模糊搜索） |
| 驱动 | psycopg3 |
| 数据验证 | pydantic v2 |
| 认证 | python-jose（HMAC-SHA256 令牌）+ passlib（PBKDF2 密码哈希） |
| HTTP 客户端 | httpx（异步 LLM 调用） |
| Excel I/O | openpyxl |
| 包管理 | uv（`pyproject.toml`） |

### 前端

| 组件 | 版本 / 说明 |
|------|------------|
| 框架 | React 18 + TypeScript |
| 构建工具 | Vite 5 |
| UI 库 | Ant Design 5 |
| HTTP 客户端 | Axios |
| 流式响应 | EventSource / fetch（SSE streaming） |
| 包管理 | npm |

### 部署

```
Docker Compose — 3 个服务:
  db        postgres:15
              绑定 127.0.0.1:5432
  app       FastAPI
              仅暴露内部端口 8000
  frontend  nginx
              代理 /api → app:8000

日志: json-file 驱动，单文件 10 MB × 5 轮转
CI:   GitHub Actions (.github/workflows/ci.yml)
        后端: pytest
        前端: tsc + vite build
```

---

## 2 API 端点总览

所有路由挂载于前缀 `/api`。

### 认证 (`auth.py`)

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/auth/login` | 请求体 `{username, password}` → 返回 `{token, 用户信息}` |
| POST | `/api/auth/logout` | 吊销当前令牌 |
| GET  | `/api/auth/me` | 返回当前用户资料 |

### 账户管理 (`accounts.py`)

| 方法 | 路径 | 说明 |
|------|------|------|
| GET    | `/api/accounts` | 用户列表（仅管理员） |
| POST   | `/api/accounts` | 创建用户 |
| PUT    | `/api/accounts/{id}` | 更新用户（角色、密码等） |
| DELETE | `/api/accounts/{id}` | 删除用户 |
| POST   | `/api/accounts/{id}/reset-password` | 重置密码 |

### 数据导入 (`imports.py`)

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/imports/upload` | multipart form，上传 Excel 文件 → 触发 ETL 流水线 |
| GET  | `/api/imports` | 导入历史列表 |
| GET  | `/api/imports/{id}` | 导入任务状态/结果 |

### 型号主数据 (`parts.py`)

| 方法 | 路径 | 说明 |
|------|------|------|
| GET  | `/api/parts` | 型号搜索（支持 `q`、`page`、`part_type`、`interface`、`capacity_min`、`capacity_max` 过滤） |
| GET  | `/api/parts/{id}` | 型号详情（含规格、别名、替代品） |
| PUT  | `/api/parts/{id}` | 更新型号元数据 |
| POST | `/api/parts/{id}/rename` | 重命名 PN（创建别名，复合 FK 延迟校验） |
| GET  | `/api/parts/{id}/aliases` | 列出别名 |
| POST | `/api/parts/aliases/{id}/approve` | 审批通过别名 |
| POST | `/api/parts/aliases/{id}/reject` | 驳回别名 |
| POST | `/api/parts/aliases/{id}/reassign` | 将别名重新归属到其他型号 |

### 利润分析 (`profit.py`)

| 方法 | 路径 | 说明 |
|------|------|------|
| GET    | `/api/profit/overview` | 聚合利润（三维：型号/销售员/客户） |
| GET    | `/api/profit/ranking` | 利润 Top-N 型号 |
| POST   | `/api/profit/recompute` | 触发利润重算 |
| GET    | `/api/profit/exclusions` | 排除列表 |
| POST   | `/api/profit/exclusions` | 新增排除 |
| DELETE | `/api/profit/exclusions/{id}` | 移除排除 |

### 库存 (`inventory.py`)

| 方法 | 路径 | 说明 |
|------|------|------|
| GET  | `/api/inventory` | 库存列表（含成本估值） |
| GET  | `/api/inventory/{part_id}` | 单型号库存详情 |
| POST | `/api/inventory/backfill-costs` | 按 `part_id` 触发成本回填 |

### 替代品 (`substitutes.py`)

| 方法 | 路径 | 说明 |
|------|------|------|
| GET    | `/api/parts/{id}/substitutes` | 列出替代品 |
| POST   | `/api/parts/{id}/substitutes` | 新增替代关系（`direction`: `both`/`a_to_b`/`b_to_a`，`type`，`status`） |
| PUT    | `/api/substitutes/{id}` | 更新替代关系 |
| DELETE | `/api/substitutes/{id}` | 删除替代关系 |

### 治理工作台 (`governance.py`)

| 方法 | 路径 | 说明 |
|------|------|------|
| GET  | `/api/governance/metrics` | 全部治理 KPI |
| GET  | `/api/governance/candidates` | 待合并候选（分页，按交易量排序） |
| POST | `/api/governance/candidates/{id}/merge` | 审批合并 |
| POST | `/api/governance/candidates/{id}/reject` | 驳回候选 |
| POST | `/api/governance/candidates/{id}/independent` | 标记为独立型号 |
| POST | `/api/governance/candidates/bulk-independent` | 批量确认独立型号 |
| GET  | `/api/governance/quality-issues` | 数据质量问题列表 |
| POST | `/api/governance/quality-issues/{id}/dismiss` | 关闭质量问题 |
| POST | `/api/governance/refresh` | 刷新候选生成 + 质量扫描 |

### AI 智能体 (`agent.py`)

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/agent/chat` | 非流式对话（请求体 `{messages, scenario}`） |
| POST | `/api/agent/chat/stream` | SSE 流式对话 |
| POST | `/api/agent/chat/cancel` | 取消正在生成的响应（设置 `stopped=True`） |
| POST | `/api/agent/files/upload` | 上传文件供智能体处理（inspect/read 工具） |
| GET  | `/api/agent/files` | 列出已上传文件 |

### 会话管理 (`chat_sessions.py`)

| 方法 | 路径 | 说明 |
|------|------|------|
| GET    | `/api/chat-sessions` | 当前用户的会话列表 |
| POST   | `/api/chat-sessions` | 创建新会话 |
| GET    | `/api/chat-sessions/{id}` | 会话详情 + 消息历史 |
| DELETE | `/api/chat-sessions/{id}` | 删除会话 |
| POST   | `/api/chat-sessions/{id}/messages` | 发送消息（SSE 流式响应） |
| POST   | `/api/chat-sessions/{id}/cancel` | 取消生成 |

### 采购记录 (`purchases.py`)

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/purchases` | 采购单列表（可过滤） |
| GET | `/api/purchases/{id}` | 采购单详情 |

---

## 3 核心服务层函数

### `cost.py` — 成本引擎

```python
replay(db, part_id) -> list[LineCost]
    # 按时间重放购销事件流，同时维护 moving-average 和 FIFO deque
    # 返回每条销售行的 {cost_moving_avg, cost_fifo, fallback_price}
    # fallback_price: 库存归零时用最近入库价兜底

class PurchaseEvent:
    part_id: int
    quantity: int
    unit_price: Decimal
    event_date: date

class SaleEvent:
    part_id: int
    quantity: int
    sale_price: Decimal
    event_date: date

class LineCost:
    sale_line_id: int
    cost_moving_avg: Decimal
    cost_fifo: Decimal
    fallback_price: Decimal
```

### `profit.py` — 利润服务

```python
recompute(db, part_id=None) -> None
    # 触发成本 replay，按事件流重算所有（或指定）型号的利润列
    # is_excluded: 排除集按 part_id 判定（已切 part_id，非 pn 文本）
    # 更新 FSalesLine.cost_moving_avg, cost_fifo, gross_profit_*

aggregate(db, group_by, filters) -> list[dict]
    # 三维聚合：group_by in (part, salesperson, customer)
    # filters: date_range, part_id, salesperson_id, customer_id
    # 返回: revenue, cost, gross_profit, margin, quantity
```

### `part_overview.py` — 型号概览

```python
get_overview(db, part_id, user_ctx) -> dict
    # 合并后型号走 merged_into 链重定向（最多 10 跳）
    # 返回: part info, sales stats, inventory, price history, specs, aliases
    # redirected_from: 若经过重定向则附原始 part_id

resolve_part(db, part_id) -> dim_part | None
    # 沿 merged_into_id 链找活跃型号（墓碑跟随，链长恒≤1 因路径压缩）

quick_pricing(db, part_id) -> dict
    # 最近 90 天销售均价、最低/最高成本、毛利率估算
```

### `part_resolver.py` — 型号匹配引擎

```python
resolve(db, pn_raw) -> list[ResolveResult]
    # 两阶段召回 + Python 重排序
    # Stage 1: pg_trgm GIN 相似度查询 (pn_compact)
    # Stage 2: alias 表精确查找
    # rerank: _score(candidate, query)

_score(candidate, query) -> float
    # PN 相似度  55%
    # 精确匹配奖励 25%
    # 子串匹配   20%
    # 文档覆盖率 15%
    # 别名命中   10%

preprocess(pn) -> str
    # 大写、去符号、提取 pn_compact（与 pg_trgm 同款处理）

class ResolveResult:
    part_id: int
    pn_std: str
    score: float
    match_type: str
```

### `merge.py` — 合并服务

```python
merge_parts(db, keep_id, merge_id, operator) -> merge_log_id
    # 1. SELECT FOR UPDATE 升序 id 锁（防并发互合死锁）
    # 2. 把 merge_id 的所有事实行 part_id → keep_id
    # 3. 别名 UPSERT（ON CONFLICT (pn_raw) DO UPDATE，恒等别名不撞键）
    # 4. 路径压缩：所有指向 merge_id 的 merged_into_id → keep_id（链长恒≤1）
    # 5. spec/substitute 迁移
    # 6. merge_id.status = 'merged', merged_into_id = keep_id
    # 7. 写 product_merge_logs（前镜像 + 受影响行 id）
    # 8. 触发 recompute(keep_id) + backfill_costs(keep_id)

unmerge(db, merge_log_id, operator) -> None
    # LIFO 回滚最近一次合并
    # 按 merge_log 前镜像恢复，受影响行 part_id 回拨
    # 回滚后重算利润 + 库存成本
```

### `match_candidates.py` — 候选生成

```python
generate(db) -> int
    # 两路召回候选对：
    # Path 1: pn_compact 重复组（≥5 位非纯数字，score≥0.70）
    # Path 2: needs_review 标记型号的 part_resolver 召回
    # 写 product_match_candidates（部分唯一索引防重复待审）

review(db, candidate_id, action, operator) -> None
    # action: merge | reject | independent
    # merge       → 调 merge_parts()
    # reject/independent → 更新候选状态

confirm_independent(db, candidate_ids, operator) -> None
    # 批量确认独立型号（出口：管理员无需逐条点击）
```

### `part_rename.py` — 重命名服务

```python
rename_part(db, part_id, new_pn, operator) -> None
    # 1. 检查 new_pn 未被占用（或已是候选）
    # 2. 旧 pn_std 写入 part_alias（type=historical）
    # 3. dim_part.pn_std = new_pn（复合 FK 延迟校验）
    # 4. 若旧 pn 已有候选 → 状态改为 occupied
```

---

## 4 ETL 流水线

### `reader.py`

```python
read_excel(path, sheet=0) -> DataFrame
detect_header(df) -> int        # 找第一个含 F\d{7} 氚云 ID 的行
peek_columns(path) -> list[str]
_check_workbook_size(wb)        # 防超大文件
```

### `cleaner.py`

```python
standardize_pn(raw) -> str
    # Strategy B: 去 V/v 前缀版本号，大写，去特殊符号
    # 例: "V2.0-RJ45" → "2.0-RJ45"（保留连字符）

parse_money(s) -> Decimal | None    # 万元/元单位识别
parse_qty(s) -> int | None          # 数量解析
parse_date(s) -> date | None        # 多格式日期
parse_rate(s) -> Decimal | None     # 税率/费率
normalize_supplier_name(s) -> str   # 去括号/简称标准化
classify_source_channel(row) -> str # 销售渠道分类
```

### `loader.py`

```python
load(db, df, import_job_id, mode) -> LoadResult
    # mode: skip（跳过重复）| upsert（覆盖）
    # 重复判定: sha256(氚云数据 ID + file_hash)

_resolve_line_parts(db, rows) -> dict[pn_raw, part_id]
    # 三层解析: alias 精确 → merge 重定向 → upsert 新建
    # upsert 加 WHERE status != 'merged'（防墓碑复活）
    # 合并后路径压缩: pn→part_id 映射层订单/库存共用

_upsert_facts(db, rows, part_id_map, mode) -> (inserted, skipped, updated)

sha256_file(path) -> str
```

### `pipeline.py`

```python
run_import(db, file_path, import_job_id, user_ctx) -> ImportResult
    # 完整编排: reader → mapping → cleaner → transform → loader
    # 导入后自动执行: 品牌/品类回填 + 质量扫描 + 质量分更新
```

---

## 5 AI 智能体

### `provider.py`

```python
chat(messages, tools, model, stream=False) -> ChatResult
chat_stream(messages, tools, model) -> AsyncIterator[delta | thinking | tool_call | done]
    # 支持 reasoning_content 流式输出（DeepSeek-R1 思考链）
vision_extract(image_bytes, prompt) -> str   # Qwen-VL 图片解析

class ToolCall:
    id: str
    name: str
    arguments: dict

class ChatResult:
    content: str
    tool_calls: list[ToolCall]
    usage: dict
    stop_reason: str
```

### `runtime.py`

```python
run(db, messages, scenario, user_ctx, file_ctx) -> ChatResult
run_stream(db, messages, scenario, user_ctx, file_ctx) -> AsyncIterator[SSE events]
    # SSE 事件类型:
    #   delta      — 增量文字
    #   thinking   — 思考链片段
    #   tool       — 工具调用
    #   tool_done  — 工具返回
    #   done       — 生成结束
cancel(session_id) -> None   # 设置 stopped=True，worker 收束

_agent_loop(db, messages, tools, provider, max_iters=LLM_MAX_TOOL_ITERS)
    # 多轮工具循环，直到模型不再调工具或达到最大迭代次数
```

### `tools.py` — 10 个工具

```python
search_parts(q, part_type?, interface?, capacity_min?, capacity_max?) -> list[part]
    # pg_trgm 模糊搜索 + 规格过滤

get_part_overview(part_id) -> dict
    # 调 part_overview.get_overview()，含合并重定向

get_profit_ranking(top_n, date_from?, date_to?) -> list[{part, profit, margin}]

list_recent_purchases(part_id?, days=90) -> list[purchase]

inspect_file(file_id) -> {columns, row_count, sample_rows}   # Excel 文件预览

read_file_rows(file_id, start_row, end_row) -> list[row]     # 分批读取

read_document(file_id) -> str    # 读取文档内容（PDF/Word/TXT）

lookup_prices_bulk(part_ids) -> dict[part_id, price_info]    # 批量查价

write_excel(data, filename) -> file_url    # 生成 Excel 并返回下载链接

write_report(title, sections) -> file_url  # 生成 Word 报告

dispatch(tool_name, args, db, user_ctx) -> tool_result
    # 统一分发入口，含审计日志记录
```

### 场景手册 (`prompts.py`)

| 场景 | 代号 | 说明 |
|------|------|------|
| 报价助手 | A | 单件查价、历史采购价、利润率查询 |
| 压价助手 | B | 供应商谈判价格分析 |
| 型号解释 | C | 参数规格解析、兼容性说明 |
| 询价单批量处理 | D | 上传 Excel、批量查价、回填结果 |
| 拆整机清单 | D2 | 整机拆解为零部件清单 |
| 配整机清单 | D3 | 根据需求配置整机方案 |
| 经营分析 | E | 利润趋势、客户/品类分析 |

---

## 6 数据模型（主要表）

### 主数据

```sql
-- 型号主表
dim_part (
    id                 SERIAL PRIMARY KEY,
    pn_std             TEXT UNIQUE NOT NULL,       -- 标准料号（冗余）
    pn_raw             TEXT,                       -- 原始料号
    brand_id           INT REFERENCES brands,
    category_id        INT REFERENCES product_categories,
    status             TEXT CHECK (status IN ('active','merged','needs_review')),
    merged_into_id     INT REFERENCES dim_part,    -- 自指 FK，墓碑链
    data_quality_score NUMERIC,
    reviewed_at        TIMESTAMP,
    created_at         TIMESTAMP
)

-- 别名表
part_alias (
    id           SERIAL PRIMARY KEY,
    part_id      INT REFERENCES dim_part,
    pn_raw       TEXT UNIQUE,
    pn_std       TEXT,
    alias_type   TEXT,
    status       TEXT,
    -- 复合 FK (part_id, pn_std) → dim_part(id, pn_std)，防文本漂移
    FOREIGN KEY (part_id, pn_std) REFERENCES dim_part (id, pn_std)
)

-- 替代品关系
part_substitute (
    id         SERIAL PRIMARY KEY,
    part_a_id  INT REFERENCES dim_part,
    part_b_id  INT REFERENCES dim_part,
    direction  TEXT CHECK (direction IN ('both','a_to_b','b_to_a')),
    type       TEXT,
    status     TEXT,
    CHECK (part_a_id < part_b_id)   -- 规范序，防重复对
)

-- 规格表
product_specs (
    id            SERIAL PRIMARY KEY,
    part_id       INT REFERENCES dim_part,
    spec_key      TEXT,
    text_value    TEXT,
    numeric_value NUMERIC   -- 范围查询专用
)

-- 品牌
brands (
    id              SERIAL PRIMARY KEY,
    name            TEXT,
    normalized_name TEXT UNIQUE
)

-- 品类（两级树）
product_categories (
    id        SERIAL PRIMARY KEY,
    name      TEXT,
    parent_id INT REFERENCES product_categories,   -- 自指
    level     INT CHECK (level IN (1, 2))
)
```

### 治理表

```sql
-- 合并候选
product_match_candidates (
    id          SERIAL PRIMARY KEY,
    part_a_id   INT REFERENCES dim_part,
    part_b_id   INT REFERENCES dim_part,
    score       NUMERIC,
    status      TEXT,
    recall_path TEXT,
    -- 部分唯一：(part_a_id, part_b_id) WHERE status = 'pending'
    UNIQUE (part_a_id, part_b_id) WHERE status = 'pending'
)

-- 合并日志
product_merge_logs (
    id              SERIAL PRIMARY KEY,
    keep_id         INT,
    merged_id       INT,
    operator        TEXT,
    before_snapshot JSONB,    -- 前镜像
    affected_row_ids JSONB,   -- 受影响行 id 列表
    created_at      TIMESTAMP
)

-- 数据质量问题
product_data_quality_issues (
    id          SERIAL PRIMARY KEY,
    entity_type TEXT,
    entity_id   INT,
    issue_type  TEXT,
    detail      TEXT,
    status      TEXT CHECK (status IN ('open','dismissed','auto_closed')),
    -- 同一 part 同类问题唯一；dismissed 状态不重开
)
```

### 事实表

```sql
-- 销售行
FSalesLine (
    id               SERIAL PRIMARY KEY,
    part_id          INT REFERENCES dim_part NOT NULL,
    pn_raw           TEXT,
    pn_std           TEXT,          -- 保留导入原文
    quantity         INT,
    unit_price       NUMERIC,
    sale_date        DATE,
    cost_moving_avg  NUMERIC,
    cost_fifo        NUMERIC,
    gross_profit_ma  NUMERIC,
    gross_profit_fifo NUMERIC,
    sha256_key       TEXT UNIQUE    -- 幂等键
)

-- 采购行
FPurchaseLine (
    id            SERIAL PRIMARY KEY,
    part_id       INT REFERENCES dim_part NOT NULL,
    pn_raw        TEXT,
    quantity      INT,
    unit_price    NUMERIC,
    purchase_date DATE,
    sha256_key    TEXT UNIQUE
)

-- 库存行
FInventoryLine (
    id               SERIAL PRIMARY KEY,
    part_id          INT REFERENCES dim_part NOT NULL,
    pn_raw           TEXT,
    quantity         INT,
    warehouse        TEXT,
    unit_cost        NUMERIC,
    valuation_method TEXT
)
```

### 系统表

```sql
-- 用户
sys_user (
    id              SERIAL PRIMARY KEY,
    username        TEXT UNIQUE,
    password_hash   TEXT,
    role            TEXT,
    token_version   INT,            -- 递增即吊销所有旧令牌
    failed_attempts INT DEFAULT 0,
    locked_until    TIMESTAMP,
    created_at      TIMESTAMP
)

-- 审计日志
sys_audit_log (
    id          SERIAL PRIMARY KEY,
    user_sub    TEXT,
    action      TEXT,
    entity_type TEXT,
    entity_id   INT,
    detail      JSONB,
    created_at  TIMESTAMP
)

-- 会话 / 消息
chat_session (
    id         SERIAL PRIMARY KEY,
    user_sub   TEXT,
    title      TEXT,
    created_at TIMESTAMP
)

chat_message (
    id         SERIAL PRIMARY KEY,
    session_id INT REFERENCES chat_session,
    role       TEXT,
    content    TEXT,
    tool_calls JSONB,
    stopped    BOOLEAN DEFAULT FALSE,
    tokens     INT,
    created_at TIMESTAMP
)
```

---

## 7 权限与安全

### 令牌机制

```
算法:  HMAC-SHA256 自签令牌
Payload 字段:
  sub      — 用户标识（user_id）
  version  — 对应 sys_user.token_version；递增即吊销所有旧令牌（即时生效）
  fb       — 共享口令回退登录标志；fb=True 时禁用会话功能，相关接口返回 403

登录失败锁定策略:
  failed_attempts >= 5  →  locked_until = now() + 15 分钟
```

### RBAC 角色模板

| 角色 | 权限说明 |
|------|---------|
| `admin` | 全部权限 |
| `boss` | 全量数据查看，无管理操作 |
| `sales` | 自己客户的行级过滤（`own_customers_only`），不可见成本字段 |
| `purchaser` | 采购相关权限，可见成本字段 |
| `readonly` | 只读，字段组受限 |

### 字段级权限 (`apply_field_visibility`)

```python
# 字段分组定义（FIELD_GROUPS）
FIELD_GROUPS = {
    "data_cost":     [...],   # 成本相关字段
    "data_price":    [...],   # 价格相关字段
    "data_margin":   [...],   # 毛利相关字段
    "data_supplier": [...],   # 供应商相关字段
}

# 机制
# 1. 递归置空 response 中受限字段
# 2. 所有派生价格/成本/毛利键必须在 FIELD_GROUPS 中注册
# 3. 在响应序列化阶段统一过滤，业务层无需感知
```

### 行级权限

```
own_customers_only 标志位:
  查询语句自动附加 WHERE customer_id IN (user.customers)
  适用于 sales 角色，确保销售人员只能看到自己负责的客户数据
```
