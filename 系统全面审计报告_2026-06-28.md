# IT 备件智能管理系统 · 全方位审计报告

> **审计日期**：2026-06-28　**审计对象**：`cloudlay/epic-elgamal-e59595` 分支（与 main 对齐）
> **方法**：9 个维度并行深审（安全 / 导入鲁棒性 / 业务正确性 / 生产运维 / 前端 UX / 工程质量），高危项对抗式逐行核验 + 多处端到端复现。
> **范围**：FastAPI 后端（9.5K 行）+ React/Vite/AntD 前端（3.7K 行）+ PostgreSQL，Docker Compose 部署，交付甲方（中石化完井研究中心）。

---

## 一、执行摘要

**系统整体工程素质明显高于同类业务项目**——鉴权内核（token 绑定用户 + token_version 即时吊销 + 登录锁定 + 时序抹平）、销售可见性三层兜底、文件 ACL 防 IDOR/路径穿越、Decimal 计价、迁移链干净单 head、备份脚本带完整性校验与恢复演练，这些都做得扎实。**未发现 P0（数据损坏/越权/宕机当场触发）级缺陷。**

但存在一批 **P1 级真实可触发问题，且高度集中在「数据导入」这条甲方最关心的链路上**：一份格式略有偏差的正常导出文件，可能被**整文件拒收**、**整批 0 入库**，或**静默写入错误日期/错误金额而毫无报错**。这些不是"会不会"的问题，多条已端到端复现。

| 严重度 | 数量 | 含义 |
|--------|------|------|
| **P0** | 0 | 无当场数据损坏/越权/宕机 |
| **P1** | 10 | 严重 bug / 安全弱点 / 体验阻断 / 关键路径零测试 |
| **P2** | 18 | 明显缺陷 |
| **P3** | 22 | 改进项 |

**各维度健康度速览：**

| 维度 | 评级 | 一句话 |
|------|------|--------|
| 安全·认证权限 | 🟡 良好有缺口 | 内核优秀，但「默认密钥仅 prod 拒启 + compose 默认 dev」是结构性不安全默认值 |
| 安全·数据访问/Agent | 🟢 强 | 销售可见性三层兜底、文件 ACL、prompt injection 根防御均到位，**无 P0/P1** |
| **导入·读取层** | 🔴 **薄弱** | 空表头误判重复列整文件拒收（P1）、非流式读取 OOM（P1） |
| **导入·清洗转换** | 🟡 主干严谨有洞 | Excel 日期序列号静默变 1970（P1）、金额/字符串溢出毒化整批（P1） |
| **导入·落库事务** | 🟡 设计成熟有洞 | 后置重算逃逸出锁与事务、并发覆盖 + 全表入内存（P1） |
| 业务·成本利润 | 🟡 正向正确，退货失守 | 退货/负数量口径全栈未定义，毛利被系统性高估（P1） |
| 生产·运维 | 🟢 成熟 | 迁移/备份/护栏都好，仅归档卷无保留策略等边角（无 P1） |
| 前端·UX | 🟡 主流程稳，错误态弱 | 无 ErrorBoundary 整页白屏（P1）、利润页静默吞错（P1） |
| 工程·质量测试 | 🟢 纪律好有盲区 | 226 测试全绿，但成本引擎零真值测试（P1）、合同文档脱节一月 |

---

## 二、跨维度系统性根因（比单条 bug 更重要）

九个维度的 50 条发现，可归并到 **5 条系统性根因**。修根因比逐条打补丁更划算：

### 根因 A：「退货 / 负数量行」口径全栈未定义 —— 一根因 4 条发现
`cleaner.py` 注释自己承认"退货抵扣"是真实存在的脏数据，但成本引擎、利润聚合、型号全景三处对负数量各行其是：成本回放**静默丢弃**、利润营收**计入负值但成本不冲回**、全景均价**缺 qty>0 过滤**。结果：**同一型号的成本/毛利，在三个界面是三个对不上的数，且毛利被系统性高估**。这是给中石化算利润的系统里最该统一的口径。

### 根因 B：「静默失败」模式贯穿全栈 —— 最危险的模式
财务系统最怕"看起来成功、实则错了且无痕"。本系统多处如此：日期序列号→1970（无报错）、列归一冲突丢列（仅 warning）、库存多重定向丢量（仅 warning）、后置 backfill/refresh 失败（静默 rollback 无日志）、利润页加载失败（无 catch，显示陈旧数据）。**这些比直接崩溃更难发现、危害更大。**

### 根因 C：导入「坏行隔离」设计目标在边界处被绕过 —— 一根因 3 条 P1/P2
系统宣称"坏行隔离、好行照常"，但在 reader/loader 边界处失效：金额/字符串溢出 → 未捕获 `DataError` → 单文件上传整批 500；空表头 → 误判重复列 → 整文件 ReaderError；合并链成环 → 批次记录随回滚丢失无痕。**一行/一列坏数据让整个文件 0 入库**，与设计目标相悖。

### 根因 D：「不安全 / 不一致的默认值」散落在并行代码路径
默认密钥只在 prod 拒启；公式注入只在 CSV 路径防了、.xlsx 路径漏防；`require_page` 后端准入加在 purchases 上、parts/inventory 漏加；`qty>0` 过滤有的成本查询有、有的没有。**同一种防护在并行代码路径上贴得不一致**（与历史「18 并行 PR 撞车」的技术债同源）。

### 根因 E：缺全局兜底层 —— 单点失败级联放大
前端无 ErrorBoundary（任一子组件抛错 → 整页白屏）、后端无全局异常 handler（非 ReaderError 异常 → 裸 500）、容器无 app 级 healthcheck（假死不自愈）。**缺少"最后一道防线"，使局部故障级联成全局。**

---

## 三、只修三件事的话，按这个优先级

> 依据：影响面 × 触发概率 × 甲方关注度 × 修复成本。

### 🥇 第一优先：导入「整文件失败」类 P1（根因 C） —— 甲方最痛、最高频、改动最小
三条直接导致"甲方一份正常文件导不进/0 入库"：
1. **空表头误判重复列**（`reader.py:46`）：**一行大小写 bug，改动最小、影响面最大**。`("", "NAN", "None")` 应改为大小写不敏感 + 空列名一律排除。
2. **非流式读取 OOM**（`reader.py:32`）：`read_excel` 加 `read_only` 流式 + 行数硬上限，防大文件拖垮单 worker 生产。
3. **金额/字符串溢出毒化整批**（`cleaner.py:87` + `imports.py:79`）：`_parse_decimal` 加范围保护走坏行隔离；单文件上传路径补 `except DataError` 转 4xx。

### 🥈 第二优先：Excel 日期序列号 → 1970 静默损坏（根因 B 的标志性案例）
`cleaner.py:115`。**已端到端复现**：日期列丢失日期格式（氚云不同导出视图常见）→ `order_date` 静默写成 1970-01-01 → 该单从所有分析窗口"凭空消失"或污染 FIFO 计价基准，且无错误行留痕。按 Excel 纪元（`origin="1899-12-30"`）转换 + 补单测。

### 🥉 第三优先：两个「单点级联」防线（根因 E）
- **前端 ErrorBoundary + `App.tsx:35` 的 `JSON.parse` 兜底**：当前任一渲染异常或 localStorage 损坏 → 非技术用户整页白屏、无自救路径。
- **默认密钥护栏改为"默认即拒"**（`config.py` + `main.py:19`）：与环境解耦，把 compose 默认 `ENVIRONMENT` 改 `prod`，让"忘配 .env"倒向更安全的一侧。

---

## 四、导入鲁棒性专项（甲方重点 · 深挖）

> 结论：**读取层是当前最薄弱环节**。清洗/落库主干设计成熟（坏行隔离、Decimal 计价、hash 幂等去重、advisory 锁串行、坏行入 `SysImportError`），但在"非标但合法的真实导出文件"面前容差不足，且多处**静默**而非可见地失败。

### P1 级（5 条）

| # | 标题 | 位置 | 触发条件 → 后果 | 状态 |
|---|------|------|----------------|------|
| I-1 | **空表头单元格被误判"重复列名"→ 整文件拒收** | `reader.py:46` | 表头行有 ≥2 个空单元格（合并单元格表头 / 字段视图未导满 / 表头上方有标题行）→ 两个小写 `'nan'`，但排除集写的是大写 `"NAN"` 恒不匹配 → 判重复列 → 整文件 0 入库，错误信息误导为"重复列名" | ✅ 已复现+核验 |
| I-2 | **`pd.read_excel` 未用 `read_only` → 内存放大 OOM** | `reader.py:32` | 100MB 上限内的 xlsx 解压可达数 GB；实测 2.87MB/12 万行就额外吃 ~77MB RSS。读取还在持有全局导入锁期间 → 一次大导入卡死所有人/重启容器 | ✅ 已复现 |
| I-3 | **Excel 日期序列号被当纳秒 → 静默变 1970-01-01** | `cleaner.py:115` | 日期列丢失日期格式 → `pd.to_datetime(45658)` 当纳秒纪元 → 1970-01-01，`pd.isna` 不触发故无报错、无错误行 → 下游按日期过滤"凭空消失"/污染计价 | ✅ 已复现（`uv run`） |
| I-4 | **金额/字符串超列限 → 未捕获 `DataError` → 整批 500 毒化** | `cleaner.py:87`、`_types.py:4`、`imports.py:79` | 金额超 `Numeric(14,2)` 或文本超 `String(16/128)` → 落库抛 `DataError`，单文件上传路径只 catch `DuplicateFileError`/`ReaderError` → 穿透成 500、整批回滚，"坏行隔离"被绕过 | ✅ 已核验 |
| I-5 | **导入后置利润/成本重算逃逸出锁与事务** | `imports.py:90`、`profit.py:79` | `pg_advisory_xact_lock` 在 commit 时已释放，之后才 recompute；并发导入下两次全表 UPDATE 交错、按谁先 commit 的过期快照算 → 成本/利润不一致；且 recompute 全表 `.all()` 入内存无上限，可 OOM | ✅ 已核验 |

### P2 级（6 条）

| # | 标题 | 位置 | 要点 |
|---|------|------|------|
| I-6 | 数字内嵌空格/货币符被静默拼接成错值 | `cleaner.py:18` | `'1 234'→1234`、`'12$34'→1234`，且全角逗号反而被拒；静默错值比报错危险 |
| I-7 | 仅读第一个 sheet | `reader.py:32` | 数据在第二 sheet（首页是封面/说明）→ 整文件"无法识别类型" |
| I-8 | 列归一冲突静默丢列 | `mapping.py:142` | 同名列带不同注解并存 → 后者原样保留 → transform 取空，无报错 |
| I-9 | 硬异常（MergeChainError/DB错）批次记录随回滚丢失 | `pipeline.py:107` | 只 catch `ReaderError`；落库层失败"什么都不留"，审计断链 |
| I-10 | 重复文件跳过被计入 error_files | `imports.py:124,144` | 一批全是重复 → `done==0` → 作业误判 `failed` 红色报错，用户误以为失败 |
| I-11 | amount_mismatch 容差对大数量误报 | `anomaly.py` | 固定 0.05 绝对容差在大金额/大数量行上易误报（run1 发现，详见附注） |

### P3 级（4 条）
- I-12 双表头探测仅凭"第 0 行 ≥3 个 `F\d{7}`"，脆弱易误判（`reader.py:16`）
- I-13 重复检测在加锁前进行，竞态下报 500 而非 409（`pipeline.py:54`）
- I-14 后置 backfill/refresh 失败静默 rollback 无日志（`imports.py:54`）
- I-15 库存按 (pn_std,warehouse) 聚合时多型号重定向只 warning、丢量无持久留痕（`loader.py:468`）

**导入链已验证为正确的部分**（避免误判系统全坏）：扩展名/MIME 伪装与路径穿越已防、`tempfile.mkstemp` 无并发命名冲突、清理路径异常安全、hash 幂等去重无 race、`raw_*_id` 唯一约束 + `ON CONFLICT` 幂等、单事务整批回滚无半截脏数据、坏行入 `SysImportError`、后台线程独立 `SessionLocal` 不跨线程共享。

---

## 五、各维度详细发现

### 5.1 安全 · 认证与权限内核

| # | 严重度 | 标题 | 位置 |
|---|--------|------|------|
| S-1 | **P1** | 默认 SECRET_KEY/ADMIN_PASSWORD 仅 prod 硬拒、compose 默认 dev → 易带默认签名密钥上线，token 可离线伪造为 admin | `config.py:33-34`、`main.py:19`、`docker-compose.yml` |
| S-2 | P2 | 库存/替代料/治理写操作审计记成角色字符串"admin"而非实际用户名 → 多 admin 无法追责 | `inventory.py:51`、`substitutes.py:37`、`governance.py:106` |
| S-3 | P2 | parts/inventory 读接口缺 `require_page` 后端准入，关了菜单仍可打 API 读（仅靠字段脱敏兜底） | `parts.py:13-83`、`inventory.py:23-39` |
| S-4 | P3 | 登录审计无条件信任 X-Forwarded-For 首段 → 可伪造审计 IP（不影响锁定） | `auth.py:138` |
| S-5 | P3 | 审计日志把 write_excel/write_report 的完整客户报价数据原样落库 → 数据足迹放大 | `tools.py:422` |
| S-6 | P3 | 字段脱敏靠"精确 key 名逐字登记"，新增派生键易静默漏配泄漏 | `security.py:152`、`config.py:280` |
| S-7 | P3 | 共享口令回退登录的文件归属可被同名碰撞 | `auth.py:200` |
| S-8 | P3 | tools.py 注释称 readonly 属全量角色，与实际 `FULL_SCOPE_ROLES` 不符（误导性注释，防回归） | `tools.py:294` |

**数据访问/Agent 攻击面：未发现 P0/P1。** 销售看逐单成交明细——工具层、服务层、数据层三层兜底全部命中（受限销售逐单明细整段返回 `[]`，profit 维度直接 `raise PermissionError`）；路径穿越被 `^[a-f0-9]{12}$` 白名单封死；prompt injection 身份只取服务端 token 不信对话自报；Agent 异常统一脱敏不外泄；provider 密钥不进客户端；governance 端点一律 `require_admin`；无任意 SQL/代码执行工具。历史多轮对抗审计（TOOLS-1~6、IDOR PR#16/#18）已补主要洞。

### 5.2 业务正确性 · 成本 / 利润 / 库存 / 合并

| # | 严重度 | 标题 | 位置 |
|---|--------|------|------|
| C-1 | **P1** | 退货行计入营收（负值）但永不计入成本/已配成本营收 → 维度毛利率被系统性高估 | `profit.py:69-91,185-244` |
| C-2 | P2 | 退货/负数量在成本回放中被静默丢弃，不回补 FIFO 层与移动加权在库量 | `cost.py:69-119` |
| C-3 | P2 | 型号全景"加权平均采购成本"缺 `qty>0` 过滤，与 backfill/利润引擎口径分叉 → 同型号三处三个成本数 | `part_overview.py:207-218` |
| C-4 | P2 | AI 定价/全景的 90 天均售价 `sum(qty*price)/sum(qty)` 含退货负数量，价格失真 | `part_overview.py:421-431,222-233` |
| C-5 | P3 | 无日期事件被排为"最早" → 无日期销售在任何采购前结算，恒走兜底成本 | `cost.py:41-50` |
| C-6 | P3 | 聚合层 `_gp/_margin` 先转 float 再算，与逐行 Decimal 口径有一分级抖动 | `profit.py:222-228` |

**已验证健壮：** 移动加权扣穿公式自洽、FIFO 批次层无跨调用污染、合并的成本/历史迁移用 `returning(id)` 精确记录可无损回滚、merge 并发用 `with_for_update` 按 id 升序锁防成环、除零边界均有保护、inventory_value 在导入后随 backfill 全表重算保持一致。**C-1~C-4 同根于"退货口径未定义"（根因 A），建议与甲方先敲定口径再统一修三处。**

### 5.3 生产 / 部署 / 运维

| # | 严重度 | 标题 | 位置 |
|---|--------|------|------|
| O-1 | P2 | 上传归档 `raw_file_dir` 永不清理、无保留策略 → 慢性占满与 DB 同盘的持久卷 → 拖垮 Postgres | `pipeline.py:35-41`、`docker-compose.yml` |
| O-2 | P3 | 只有 db 有 healthcheck，app/frontend 假死不自愈（`/health` 探针现成未接） | `docker-compose.yml`、`main.py:54` |
| O-3 | P3 | 容器以 root 运行（前后端 Dockerfile 均无 USER） | `backend/Dockerfile`、`frontend/Dockerfile` |
| O-4 | P3 | DB 密码轮换不生效是已知陷阱，文档给的解法会清库（缺不删库轮换步骤） | `docs/DEPLOY.md:341-351` |
| O-5 | P3 | 单 uvicorn worker + 同步阻塞导入/对话线程共用一进程（2G 内存下合理取舍） | `backend/Dockerfile:22`、`db.py:16` |

**已验证健壮（运维成熟度高）：** 迁移链单 head `d7a4f1c9b3e2`、分叉已正确 merge、可线性 upgrade；CORS 非通配、生产经 nginx 同源代理；prod 弱口令护栏拒启、密钥全 env 驱动且 `.env` 已 gitignore；流式上传 + 大小上限 + finally 清理；错误对客户端统一脱敏；日志轮转已配；备份脚本带完整性校验 + sha256 + 14 天保留 + 恢复演练；DEPLOY.md 明确"先备份→先迁移→再起容器"。

### 5.4 前端 UI / UX / 可用性

| # | 严重度 | 标题 | 位置 |
|---|--------|------|------|
| U-1 | **P1** | 全应用无 ErrorBoundary + `App.tsx` 裸 `JSON.parse` → 渲染异常/存储损坏即整页白屏，非技术用户无自救路径 | `App.tsx:35`、`main.tsx:13` |
| U-2 | **P1** | 利润分析加载失败被静默吞掉（无 catch）→ 领导看到陈旧/空数据却无提示，可能误判经营 | `ProfitPage.tsx:46-54` |
| U-3 | P2 | 利润导出 CSV 无错误处理 → 失败时静默下载损坏文件；token 过期触发整页刷新丢状态 | `ProfitPage.tsx:74-82` |
| U-4 | P2 | 治理"主数据指标"深层解构无可选链 → 后端字段不全即崩 Tab，叠加无 ErrorBoundary → 整页白屏 | `GovernancePage.tsx:402-432` |
| U-5 | P2 | 多处把任意失败一律提示"需要管理员权限" → 网络断/500 时有权限的管理员也被误导 | `ProfitPage.tsx:68`、`InventoryPage.tsx:86`、`PartSearchPage.tsx:96` |
| U-6 | P3 | 全站静态 `message.xxx` 脱离 AntD `<App>` 上下文 → 丢主题/locale + 控制台告警 | `main.tsx:13-18` |
| U-7 | P3 | 导入大文件期间只有按钮转圈，上传无进度条，长等待疑似卡死 | `ImportPage.tsx:85-92` |
| U-8 | P3 | 导入批次详情把后端原始英文/下划线键名直接当标签给业务用户看 | `ImportPage.tsx:230-232` |
| U-9 | P3 | 别名/质量问题队列"已处理"单击即发、无 catch → 失败也提示成功 | `GovernancePage.tsx:237-241` |
| U-10 | P3 | token 过期统一 `location.reload()` → 丢失当前页/筛选/半填表单，突兀 | `api.ts:17-20` |

**已验证健壮：** 危险写操作（型号排除/合并/回滚/账号停用/库存撤销）均有 Popconfirm/Modal 二次确认；大表格均已分页；竞态有 `seqRef`/`alive`/`ctlRef` 防护；登录页错误分支正确；无硬编码后端地址；ChatPage 已专门规避 401 整页刷新（存草稿 + fetch）。

### 5.5 工程质量 / 可维护性 / 测试

| # | 严重度 | 标题 | 位置 |
|---|--------|------|------|
| E-1 | **P1** | 成本计价引擎 `cost.replay`（甲方核心诉求）无真值数学测试，唯一相关测试是"自我等价"（期望值也调同一函数生成） | `cost.py:45-133`、`test_profit_part_id.py:47-83` |
| E-2 | P2 | 两份合同附件级架构文档停留 2026-05-26、漏洞报告读起来像一堆未修 P0（实多已修）；`CONTEXT.md`/`docs/adr` 不存在但 CLAUDE.md 引用 | `架构报告_合同附件.md`、`架构逻辑漏洞报告.md` |
| E-3 | P2 | Agent 生成的 .xlsx 报表无公式注入防护（CSV 路径已防、Excel 路径漏防） | `agent_files.py:346,415` |
| E-4 | P2 | `test_overview_roundtrip` 因测试库被 truncate 始终命中 `pytest.skip` → 伪覆盖 | `test_agent_tools.py:29-37` |
| E-5 | P3 | `app.auth` ↔ `app.security` 循环依赖（已用函数内延迟导入规避，全仓 16 处隐藏边） | `auth.py:23`、`security.py:69` |
| E-6 | P3 | 8 处未用 import + profit.py 重复 import date + 导入后置失败静默无日志 | `loader.py:15` 等 |

**已验证健壮：** 226 测试全绿、CI 跑真 Postgres 并验证迁移链、conftest 强制独立测试库；26 处 `except Exception` 全带 `# noqa: BLE001` 且都 `_log`/re-raise/脱敏返回，无裸 `except`、无 `type: ignore`、无 `TODO/FIXME`；配置高度集中在 `config.py`；最大文件 loader.py 534 行但拆成 12 个小函数。

---

## 六、修复路线图建议

**第一波（阻断甲方导入 / 财务正确性 / 安全 —— 立即）**
- I-1 空表头误判（一行 bug，最高性价比）、I-2 read_only 流式、I-4 DataError 坏行隔离、I-3 日期序列号 1899 纪元转换
- S-1 默认密钥改"默认即拒" + compose 默认 prod
- U-1 ErrorBoundary + JSON.parse 兜底、U-2 利润页补 catch

**第二波（口径统一 / 静默失败可见化 —— 短期）**
- 与甲方敲定退货口径后统一修 C-1~C-4（根因 A）
- I-5 后置重算纳入锁覆盖 + 分块流式、I-14/E-6 静默失败补日志（根因 B）
- I-9/I-10 导入失败留痕与重复不计错、E-3 .xlsx 公式注入

**第三波（健壮性 / 工程债 —— 中期）**
- E-1 成本引擎补 golden-number 测试、E-2 刷新合同文档加状态戳、E-4 修伪覆盖测试
- O-1 归档保留 cron、O-2 app healthcheck、O-3 容器降权
- S-2/S-3 审计归属与 require_page 一致化（根因 D）、U 系列错误态打磨

---

## 七、审计方法与可信度说明

- 9 个维度各由独立 agent 用 Read/Grep/Bash **真实打开源码**审计，每条发现附真实 `file:line`。
- 高危项经对抗式核验：导入 P1（日期 1970、金额溢出）在项目 `uv` 环境**端到端复现**；read_only OOM 用 12 万行实测 RSS；空表头误判、默认密钥护栏、前端无 catch/无兜底由审计员复核 + 本人对源码二次核验，全部属实。
- 报告刻意保留"已验证健壮"清单，避免把"未覆盖"误读为"有问题"，便于甲方验收时区分真实缺陷与既有防护。
- 局限：本轮为**静态 + 单元级复现**审计，未做并发压测、未跑全链路端到端导入回归；C-1~C-4 的退货口径"对错"取决于甲方业务定义，本报告只指出"三处口径不一致"这一客观事实。
