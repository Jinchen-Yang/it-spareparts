# IT 备件管理系统 · 架构质量体检报告

> **日期**：2026-06-29　**问题**：系统设计是否过度/不足？有没有设计成可复用组件？逻辑冗长吗？有没有更简单的实现？是不是高效系统？有没有屎山代码？
> **方法**：5 个并行 Agent 真实读码（后端服务层 / ETL 管线 / 前端复用 / 效率性能 / 横切关注点），每条发现带真实 `file:line`，并被要求先肯定再批评、区分"必要/可简化/应删除"。

---

## 一、直接回答你的六个问题

| 你的问题 | 结论 |
|---|---|
| **有没有屎山代码？** | **没有。** 这是少见的"被审计反复打磨过"的克制代码库（处处带 PR/审计编号、教训注释）。综合健康分 **~7.7/10**。 |
| **系统设计有没有过度？** | **几乎没有过度设计。** 反而极少数是"空壳仪式性代码"该删（死钩子/空目录/假环注释）。 |
| **有没有设计成可复用组件？** | **部分到位、部分缺。** 算法/危险操作层复用优秀；**横切口径过滤散落复制是最大缺口**（同一计价口径抄了 5~14 份）。 |
| **代码逻辑冗长吗？** | **个别巨型函数**（`analysis()` 100+ 行、`_load_orders` 100 行），非普遍现象。 |
| **有没有更简单的实现？** | **有，且方向是"做减法 + 抽 3-4 个共享 helper"**，不是推倒重来。 |
| **是不是高效系统？** | **是，7.5/10。** 核心设计对（利润物化、聚合下推 SQL）；**唯一真瓶颈＝两个高频列缺索引**。 |

**一句话总评**：这不是屎山，是一座**克制、纪律性极强、被打磨过**的系统。它真正的风险不是"乱"，而是**反向**——① 防御性注释/空壳扩展点开始累积"仪式性代码"，② 横切安全/计价口径靠"逐处手工接线 + 注释纪律"维持，缺一道结构性兜底，散落复制有漂移风险。**改进方向是减法 + 收敛，而非新增抽象。**

各维度健康分（均已归一为"越高越好"）：

| 维度 | 健康分 | 一句话 |
|---|---|---|
| 后端服务层 | 8/10 | 仓库里设计最好的部分；问题全是"该抽的横切口径没抽" |
| ETL 导入管线 | 7.5/10 | 链路分层清楚、不是屎山；`is_sales` 差异散落 ~10 处可声明式收口 |
| 前端复用 | 7/10 | 展示组件拆得好；缺"数据访问层"hook，工具函数散落 |
| 效率性能 | 7.5/10 | 利润物化是定海神针；唯一真瓶颈＝缺索引 |
| 横切/整洁度 | 8/10 | 小而干净；少数空壳抽象与人肉维护的安全不变量 |

---

## 二、贯穿全局的根因（比单条更重要）

五份报告的发现高度收敛到 **3 条主线**：

### 主线 A：横切口径"散落复制" —— 全局最大、且代价最高的技术债
同一份业务规则被复制到多处，靠人记得同步，漂移即出错（尤其甲方核心计价口径）：
- `data_status='已生效'` 过滤散落 **14 处**（含一处退化成 SQL 字符串），`_ACTIVE` 常量 5 个文件各定义一遍。
- 含税/未税换算"金额÷(1+税率)"有 **3 份实现**（profit / purchase_analysis / inventory SQL）。
- "计入成本的采购行"过滤（`unit_price>0 & qty>0 & source_type IN COST_PURCHASE_TYPES & active`）复制 **5 处**。
- 前端同构：9 处页面各手写 `load/loading/error/page/seqRef`；`money/pct` 工具 4 份（精度还不一致）；角色字典/权限读取 2 份。
- 脱敏靠**黑名单精确 key + 人肉登记派生键**（`config` 注释自承"差一字即漏""见 2026-06-15 越权教训"）——**已酿过越权事故**。

> 这不是"设计糟"，是"少抽了几个共享 helper"。但因为涉及计价与权限口径，**漂移代价最高、修复最机械、风险最低**——是性价比第一的改进。

### 主线 B：仪式性/空壳代码累积 —— 该做减法
防御性工程过头，留下一批"看着像有用、实则空转"的代码，增加认知负担：
- `apply_data_scope` 是 **7 处调用的纯 pass-through 死钩子**（`return query` 原样返回，制造"这里有行级安全"的假象）。
- `schemas/` 是**空目录**（约定半途而废）。
- `master_data.py` 两处"局部导入避免环"是 **cargo-cult 假注释**（`SysAuditLog` 是纯模型无环，另 7 个 service 都顶层 import）。
- `parts.py` 3 处 `_: Depends(current_role)` 注入后丢弃，与已有 `ctx` 鉴权**重复**，每请求多一次 DB 往返。
- 前端 `agentChat`/`agentChatStream` 疑似**死代码**（ChatPage 已只用会话版）。

### 主线 C：安全口径"靠纪律"而非"靠结构" —— 与第一次审计呼应
- 脱敏黑名单人肉登记（主线 A 的安全面）→ 应转**白名单投影**（只输出该角色允许字段，新派生键默认不外露），从根上消除"漏登记即泄漏"。
- `require_page` 后端准入只挂了 purchases/agent/chat，**parts/inventory/governance/profit 没挂**——与系统自述"前端藏菜单 ≠ 后端拦接口"矛盾（这条与第一次审计的 S-3 一致）。

---

## 三、效率：唯一真瓶颈

系统效率本身良好（**7.5/10**），核心做对了：利润**物化**到 `f_sales_line`（不每请求回放）、聚合**下推 SQL**、批量 upsert（chunk 1000）、连接池调过、窗口查询有 LIMIT 护栏。

**唯一会随数据增长真实变慢的**：`f_purchase_order` / `f_sales_order` 的 **`order_date` 与 `data_status` 全表无索引**（初始迁移只建了 `order_no`）。而这两列是全站最高频的过滤+排序列，4.5万/4.8万行上每次退化成 seq scan + sort。**加一条索引迁移即可**（建议 `(data_status, order_date)` 复合，或 `WHERE data_status='已生效'` 部分索引），低风险、收益最高、随增长放大。

储备项（量级翻倍前再做）：`recompute` 全量入内存（~100-150MB，当前可扛）；agent 批量定价最坏 60×7≈420 次往返可批量化。

---

## 四、做什么（按性价比排序）

### 第一梯队 · 高收益低风险（建议优先）
1. **加索引**：`f_purchase_order(data_status, order_date)`、`f_sales_order(data_status, order_date)`。一条 alembic 迁移。〔效率真瓶颈〕
2. **抽 3 个共享口径 helper**：`active_orders(stmt, model)` / `to_ex_tax(amount, rate, fallback)` / `cost_basis_purchase_lines()`，替换散落的 14+3+5 处。`_ACTIVE` 统一进 config。〔主线 A·甲方核心计价口径单一真值源〕
3. **前端抽 `usePagedQuery` hook + `utils/{format,auth}.ts`**：消除 ~200 行重复，GovernancePage 5 个 Tab 是重灾区（各从 ~30 行缩到 ~8 行）。〔主线 A·前端〕
4. **做减法**：删 `apply_data_scope` 死钩子(7 处)、空 `schemas/`、cargo-cult 假环注释(2 处)、`parts.py` 冗余 `current_role`(3 处)、疑似死代码 `agentChat`。〔主线 B〕

### 第二梯队 · 中收益中风险（需测试托底）
5. **脱敏黑名单→白名单投影**：根除"漏登记即泄漏"（已出过事故）。〔主线 C〕
6. **`require_page` 对称化**：router 级统一挂准入，消除前后端权限口径分裂。〔主线 C，与第一次审计 S-3 同源〕
7. **`loader._load_orders` 拆小 + `FILE_SPEC` 声明式**：把散在 ~10 处的"采购/销售/库存"`is_sales` 分支收敛成一张表。〔ETL〕
8. **`purchase_analysis.analysis()` 拆聚合函数**：100+ 行巨型循环拆成 `_aggregate_parts` / `_source_composition`，可独立测。〔服务层〕

### 储备 · 量级翻倍前
9. `recompute` 按 part_id 分块流式回放（降峰值内存）。
10. agent 批量定价改 `WHERE part_id IN (...)` 批量聚合。

---

## 五、明确"不要动"（避免误伤）

- **`cost.py` replay**：移动加权+FIFO 的本质复杂度，纯函数引擎，单测友好 —— 教科书级，别动。
- **`merge.py`/`unmerge`**：危险写操作的锁/审计/回滚是必要复杂度，不是屎山。
- **`ChatPage` 流控状态机**：后台续跑/409 退避/attach 续播是真实难题，强拆会割裂状态机。
- **`config.py` 307 行**：是"集中"不是"杂物抽屉"，按域强拆是数字焦虑；仅 `SOURCE_CHANNEL_*` 关键词可下沉。
- **`agent/tools.py` `_REGISTRY` dispatch**：10 个工具的合理抽象，别引框架重写。
- **`ResizableTable`**：前端最值钱的复用核心，18 处统一受益。
- **ETL 双表头探测 / ffill / advisory 锁 / 幂等去重**：都是被真实脏数据逼出来的必要设计。

---

## 六、结论

**这是一个健康、克制、高效的系统，不是屎山。** 它的复用做得"对了大半"——算法引擎、危险操作、ETL 容差、`ResizableTable` 都是恰当抽象。短板集中且明确：**横切口径散落复制（主线 A）** 是性价比第一的改进，**做减法去仪式性代码（主线 B）** 几乎零风险，**安全靠结构而非纪律（主线 C）** 是中期该补的防线。加一条索引解决唯一效率瓶颈。

按第一梯队 4 项推进，能在低风险下显著提升一致性、可维护性与性能，且**不引入任何新框架或新抽象层**——符合"小而干净"的系统调性。
