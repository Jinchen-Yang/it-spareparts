# 架构与逻辑漏洞说明报告

生成时间：2026-05-25  
范围：后端 FastAPI/SQLAlchemy、前端 API 调用、Docker 配置、当前 Postgres 数据状态。  
说明：本报告只基于只读检查和数据库统计生成，未修改任何代码文件或数据库数据。

## 摘要

当前系统的主要问题不是 SQL 注入这类单点漏洞，而是“最小可用版本”的认证、权限、导入幂等和成本口径被直接用于真实数据后带来的系统性风险。最需要优先处理的是默认口令/默认签名密钥、RBAC 实际未生效、只读账号仍能看到成本/供应商/利润敏感数据，以及导入采用 `skip` 逻辑导致历史错误数据无法通过重新导入修正。

当前数据库统计显示：

- `dim_part`: 22,962 条
- `f_purchase_line`: 45,440 条
- `f_sales_line`: 47,648 条
- `inventory`: 28,165 条
- `sys_import_batch`: 9 个成功批次
- 销售行中 `matched_cost is null`: 6,041 条
- 销售行中 `revenue_amount is null`: 845 条
- 库存行中 `unit_cost is null`: 28,165 / 28,165 条
- 库存行中 `inventory_value is null`: 28,165 / 28,165 条
- 销售异常标记：`no_cost` 5,196，`neg_margin` 4,789，`excluded_business_type` 3,902，`zero_price` 1,703，`amount_mismatch` 120

## 高风险问题

### 1. 默认管理员口令和默认 Token 签名密钥可直接形成管理员权限

证据：

- `backend/app/config.py:27-28` 默认 `admin_password = "admin"`、`secret_key = "change-me-in-env"`。
- `docker-compose.yml:23-24` 未提供环境变量时继续使用 `ADMIN_PASSWORD:-admin`、`SECRET_KEY:-change-me-in-env`。
- `backend/.env` 当前只覆盖了 `DATABASE_URL` 和 `RAW_FILE_DIR`，未覆盖管理员口令和签名密钥。
- `backend/app/auth.py:63-69` 只要用户名是 `admin` 且密码匹配共享口令，就签发 `role=admin` token。

影响：

攻击者或内部普通使用者如果知道默认口令，可以获得管理员权限，执行导入、库存修正、替代料新增、利润重算、利润导出等操作。由于签名密钥也是默认值，若服务实际使用默认密钥，token 还可以被离线伪造。

建议：

- 启动时强制校验 `ADMIN_PASSWORD` 和 `SECRET_KEY`，发现默认值直接拒绝启动。
- `SECRET_KEY` 使用至少 32 字节随机值，并支持密钥轮换。
- 管理员账号从单共享密码改为真实用户表或外部身份源。

### 2. Token 只包含 role，不绑定用户、密码版本、会话或撤销状态

证据：

- `backend/app/auth.py:41-45` token body 只有 `role` 和 `exp`。
- `backend/app/auth.py:48-58` 验证时只校验签名和过期时间。
- `backend/app/auth.py:78-80` 管理权限只判断 `role == "admin"`。

影响：

已签发 token 在有效期内无法吊销；管理员密码泄露后，即使修改口令，旧 token 仍可继续使用到过期。审计也无法定位具体操作者，因为 token 中没有用户身份。

建议：

- token 至少包含 `sub/user_id`、`iat`、`jti`、`token_version`。
- 修改密码、禁用账号、切换角色后让旧 token 失效。
- 写操作审计记录真实用户，而不是只记录 `admin` / `readonly` 角色字符串。

### 3. RBAC 和字段脱敏钩子存在，但默认完全绕过

证据：

- `backend/app/config.py:82` `ENABLE_RBAC = False`。
- `backend/app/security.py:41-42` RBAC 关闭时返回 `phase1_full_access`。
- `backend/app/security.py:62-64` 行级数据范围实际原样返回 query。
- `backend/app/security.py:84-85` 字段脱敏实际原样返回 payload。
- `backend/app/parts.py`、`backend/app/inventory.py` 等查询接口虽然调用 `apply_field_visibility`，但开关关闭时无效果。

影响：

任何持有有效 token 的用户都能通过型号全景、采购明细、库存列表看到供应商、采购单价、库存成本字段、客户销售数据等敏感信息。代码里的角色矩阵目前只是配置预留，不是实际保护。

建议：

- 不要把 `ENABLE_RBAC=False` 的版本用于含真实业务数据的环境。
- 先实现最小可用数据范围：销售只能看自己的客户/订单，采购只能看供应商/采购，老板/管理员看全量。
- 字段权限需要按接口语义区分采购价和销售价，不能只按同名字段 `unit_price` 做统一遮蔽。

## 中高风险问题

### 4. “只读用户”不是低权限用户，仍可查看成本和利润敏感信息

证据：

- `backend/app/auth.py:63-69` 任意非 `admin` 用户名只要密码正确就得到 `readonly`。
- `backend/app/config.py:109-115` `readonly` 的所有字段组均为 `True`。
- `backend/app/api/parts.py:41-62` 登录用户可查看采购和销售明细。
- `frontend/src/api.ts:42-52` 前端 overview 类型中包含 `purchases_recent.unit_price`、`avg_purchase_cost`、`avg_cost_moving`、`avg_cost_fifo`、毛利率等字段。

影响：

名义上的只读账号可以读取大量商业敏感信息。即使利润聚合接口要求 admin，型号全景接口已经足够反推出采购成本、销售价格和部分利润口径。

建议：

- 将 `readonly` 改为默认最小权限，不开放成本、供应商和利润字段。
- 对型号全景拆分接口：基础资料、库存、采购成本、销售利润分别授权。
- 前端展示不是安全边界，后端必须脱敏。

### 5. 数据库端口暴露到宿主机，且默认数据库密码为弱口令

证据：

- `docker-compose.yml:6` 默认 `POSTGRES_PASSWORD` 为 `spareparts`。
- `docker-compose.yml:10-11` 将 Postgres `5432` 暴露到宿主机所有地址。
- `backend/app/config.py:21` 默认数据库连接串也使用 `spareparts:spareparts`。

影响：

如果部署机器网络暴露或本机有其他不可信进程，数据库可被直接连接，绕过应用层认证、权限和审计。

建议：

- 生产环境不要映射数据库端口到公网/宿主机，至少绑定 `127.0.0.1`。
- 使用强随机数据库密码，并限制数据库账号权限。
- 应用账号和迁移/管理账号分离。

### 6. 健康检查接口泄露数据库连接错误细节

证据：

- `backend/app/main.py:37-45` `/health/db` 在异常时返回 `detail: str(exc)`。

影响：

未登录访问者可能获得数据库主机、端口、账号、驱动错误或网络结构信息，辅助后续攻击或弱口令猜测。

建议：

- 对外只返回 `"db": "unreachable"`。
- 详细异常写服务端日志，不回传给客户端。
- 如需内部诊断，给 `/health/db` 加内部网络限制或鉴权。

## 业务逻辑漏洞

### 7. 导入幂等策略只跳过重复 raw id，无法修正已经入库的错误事实数据

证据：

- `backend/app/etl/loader.py:114-122` 事实表统一 `ON CONFLICT DO NOTHING`。
- `backend/app/etl/loader.py:180` 订单头也是只插入、不更新。
- `backend/app/config.py:71-72` 存在 `DEFAULT_IMPORT_MODE = "skip"`，但当前 loader 没有 upsert 修复路径。
- 当前数据库批次显示销售批次 13 跳过 3,197 行、销售批次 14 跳过 8 行、采购批次 11 跳过 57 行。

影响：

当源系统导出补充了字段、修正了订单状态、税率、数量、单价、客户/供应商等，重新导入不会更新已有订单和明细，系统会长期保留旧错误。后续利润重算使用旧数据，报表结果会偏离真实业务。

建议：

- 对事实表建立可控 `upsert` 模式，按 `raw_line_id/raw_order_id` 更新允许修复的字段。
- 每次导入记录字段级变更摘要，避免静默覆盖。
- 对订单状态、税率、数量、单价等影响财务口径的字段优先支持修正。

### 8. 利润重算在导入接口内二次提交，事务边界不清晰

证据：

- `backend/app/api/imports.py:46-48` 导入成功后先 `db.commit()`。
- `backend/app/api/imports.py:58-62` 随后调用 `profit.recompute(db)`，失败只返回错误信息，不回滚导入。
- `backend/app/services/profit.py:135-137` `recompute()` 内部再次 `db.commit()`。

影响：

采购/销售导入成功但利润重算失败时，系统会处于“事实数据已更新、利润字段未同步”的中间状态。当前数据库已有 `revenue_amount is null` 845 行，说明利润字段并非总是完整覆盖。用户如果此时查看报表，会拿到不一致口径。

建议：

- 导入和利润重算状态分开建模，例如 `import_status` 与 `profit_recompute_status`。
- 利润报表读取前校验是否存在待重算批次。
- 重算失败应进入任务表或告警，而不是只在上传响应里返回一次。

### 9. 库存成本字段未由导入或利润重算统一维护，库存金额全为空

证据：

- `backend/app/etl/loader.py:235-255` 库存导入只写 `source_qty`、描述、品牌、批次等，不写 `unit_cost` / `inventory_value`。
- `backend/app/services/inventory.py:82-84` 只有人工修正库存时，如果已有 `unit_cost` 才重算 `inventory_value`。
- 当前数据库 `inventory.unit_cost is null` 为 28,165 / 28,165，`inventory.inventory_value is null` 为 28,165 / 28,165。

影响：

库存列表和型号全景中的库存价值字段没有实际意义。若业务用这些字段判断库存资金占用、低周转、补货或报价，会产生误导。

建议：

- 明确库存成本来源：最近采购价、移动加权结存价、FIFO 剩余层、或人工维护。
- 每次采购/库存导入和利润重算后同步刷新库存成本。
- 前端在成本未计算时显示“未计算”，不要展示空值当作正常结果。

### 10. 成本回放按型号全局扣减，不按仓库/订单链路匹配，容易造成利润错配

证据：

- `backend/app/config.py:48-49` `FIFO_SCOPE = "global"`。
- `backend/app/services/profit.py:33-55` 采购事件只按 `pn_std` 收集。
- `backend/app/services/profit.py:72-83` 销售事件也只按 `pn_std` 分组回放。
- `backend/app/services/cost.py:45-50` 事件排序只按日期和采购/销售类型。

影响：

不同仓库、不同销售订单对应采购、不同业务来源的成本被混在同一个型号池里，利润可能被跨仓库、跨订单链路摊平。当前数据库 `neg_margin` 4,789 条、`no_cost` 5,196 条，需进一步核对是否由成本池错配、采购类型过滤或导入跳过导致。

建议：

- 如果采购有 `linked_sales_order_no`，优先按销售订单链路匹配成本。
- 如果采购没有仓库字段，至少在报表中明确“全局型号成本池”口径。
- 对异常毛利行提供成本来源、采购批次、是否 fallback 的明细追溯。

### 11. fallback 使用“最近采购价”补期初/扣穿成本，可能美化或扭曲历史毛利

证据：

- `backend/app/config.py:51-53` `OPENING_COST_POLICY = "fallback_recent"`。
- `backend/app/services/profit.py:46-54` 每个型号选最近采购价作为 fallback。
- `backend/app/services/cost.py:76-89` 库存扣穿或无库存时使用 fallback 成本。

影响：

历史销售早于采购、或销售数量超过采购数量时，会用最近采购价补成本。这可能与真实期初库存成本差异很大，导致毛利被错误计算。报表如果不区分 `computed` 和 `fallback`，用户容易把估算成本当成真实成本。

建议：

- 报表默认区分真实匹配成本和 fallback 成本。
- fallback 行不要参与核心毛利 KPI，或单独列出估算毛利。
- 导入期初库存成本或期初批次，替代最近采购价兜底。

### 12. CSV 导出存在公式注入风险

证据：

- `backend/app/api/profit.py:58-69` 直接将维度值等字段写入 CSV。
- 维度可能来自 `pn_std`、销售员、客户名等源系统可控字段。

影响：

如果客户名、销售员或型号以 `=`, `+`, `-`, `@` 开头，Excel 打开 CSV 时可能执行公式，造成数据外传或本地命令/链接诱导风险。

建议：

- CSV 写出前对所有字符串单元格做转义：以危险字符开头时前置单引号或制表符。
- 导出文件增加安全测试用例。

## 设计缺口

### 13. 审计开关关闭，且操作者身份不精确

证据：

- `backend/app/config.py:83` `ENABLE_ACCESS_LOG = False`。
- `backend/app/security.py:100-107` 访问审计关闭时直接 no-op。
- `backend/app/services/inventory.py:88-90` 库存审计的 `operated_by` 来自 role，而不是用户。

影响：

查询、导出、利润重算、导入等关键行为缺少可追溯记录。即使库存修改有审计，也无法区分具体是谁操作。

建议：

- 查询敏感数据、导出、导入、利润重算都应记录访问审计。
- 审计主体应为用户 id，而不是角色名。
- 审计日志要包含请求来源、过滤条件摘要、影响行数和批次号。

### 14. 前端将 token 存在 localStorage，扩大 XSS 后果

证据：

- `frontend/src/api.ts:5-7` 从 `localStorage` 读取 token 并放入 `Authorization`。

影响：

一旦前端出现 XSS，攻击者可直接读取 token 并调用所有接口。考虑当前 token 无撤销机制，这个风险会被放大。

建议：

- 优先使用 HttpOnly、Secure、SameSite Cookie。
- 配合 CSRF 防护和较短 access token。
- 前端避免渲染未净化的业务字段。

## 数据库表设计不足

### 15. 缺少租户/组织边界，后续多公司、多部门隔离成本高

证据：

- `dim_part`、`dim_supplier`、`dim_customer`、采购/销售/库存事实表均没有 `tenant_id`、`org_id` 或数据域字段。
- 当前 RBAC 预留了 `department_id`、`team_id`，但数据表没有对应可过滤字段。

影响：

如果后续要支持不同事业部、仓库主体、法人公司或客户团队的数据隔离，只能靠业务字段模糊过滤，难以做强隔离。历史数据补 `tenant_id` 也会涉及大量回填和唯一约束重建。

建议：

- 在核心维表和事实表上预留 `tenant_id/org_id`。
- 所有唯一键改为租户内唯一，例如 `(tenant_id, pn_std)`、`(tenant_id, raw_line_id)`。
- RBAC 的行级权限应基于表字段，而不是只停留在上下文对象。

### 16. 维度表缺少主数据生命周期和人工确认状态

证据：

- `backend/app/models/dimensions.py` 中 `dim_part` 只有 `needs_review`，没有审核人、审核时间、停用状态、合并关系、来源优先级。
- `dim_supplier`、`dim_customer` 只有 raw/name/type 等基础字段，没有主数据状态。

影响：

型号、供应商、客户名称一旦被错误标准化，很难追踪谁确认、何时确认、是否已经废弃。后续如果要合并重复客户/供应商，事实表外键已指向旧 id，缺少合并映射会造成报表口径割裂。

建议：

- 给主数据增加 `status`、`review_status`、`reviewed_by`、`reviewed_at`、`source_priority`。
- 增加主数据合并表，例如 `dim_customer_merge(from_id, to_id, reason)`。
- 对人工修订建立独立审计，而不只依赖导入自动 upsert。

### 17. 供应商/客户以 raw name 唯一，normalized name 只建索引，容易形成重复维度

证据：

- `backend/app/models/dimensions.py` 中 `DimSupplier.name_raw`、`DimCustomer.name_raw` 唯一。
- `name_normalized` 只建普通索引，不唯一。
- loader 中 `_upsert_named_dim()` 按 `name_raw` upsert。

影响：

同一供应商/客户只要源系统名称带后缀、空格、简称差异，就会生成多个维度 id。按客户、供应商统计时会被拆散，权限过滤也可能漏数据。

建议：

- 建立别名表：`supplier_alias`、`customer_alias`，raw name 映射到 canonical id。
- normalized name 不一定直接唯一，但应进入人工候选合并流程。
- 报表聚合优先使用 canonical id，而不是 raw name。

### 18. PartAlias 没有外键约束，PN 映射可能指向不存在的标准型号

证据：

- `backend/app/models/dimensions.py` 中 `PartAlias.pn_std` 是普通字符串，只建 `ix_alias_std`，没有 FK 到 `dim_part.pn_std` 或 `dim_part.id`。

影响：

后续人工修订别名或清理型号时，别名可能悬空，导致同一个原始 PN 在不同流程里被映射到不存在或过期的标准型号。

建议：

- `part_alias` 改为保存 `part_id` 外键，同时保留 `pn_std_snapshot` 便于审计。
- 对人工 alias 设置唯一约束和状态字段，支持禁用、替换和历史追踪。

### 19. 库存表覆盖当前值，没有库存快照历史

证据：

- `backend/app/etl/loader.py:243-255` 库存导入按 `(pn_std, warehouse)` upsert，覆盖 `source_qty`、`snapshot_date`、`import_batch_id`。
- `inventory` 表只有当前库存，没有 `inventory_snapshot` 历史表。

影响：

无法回看某天库存、无法做库存变动趋势、无法复盘某次导入导致的库存变化。人工修正和源系统覆盖混在当前表里，也不利于审计。

建议：

- 拆分 `inventory_current` 和 `inventory_snapshot`。
- 每次导入写入快照明细，再刷新 current。
- 人工修正单独建 `inventory_adjustment`，不要只在 current 表覆盖展示值。

### 20. 财务计算结果直接回填销售明细，但缺少规则版本和重算批次

证据：

- `f_sales_line` 保存 `matched_cost`、`cost_moving_avg`、`cost_fifo`、`gross_profit`、`gross_margin` 等派生字段。
- `backend/app/services/profit.py:126-137` 重算后直接 update 销售行。
- 表中没有 `cost_rule_version`、`recompute_batch_id`、`computed_at`、`computed_by`。

影响：

当成本口径、采购类型、税率口径、fallback 策略变化后，旧报表和新报表无法并存，也无法知道某行利润是按哪个规则算出来的。排查历史报表差异时缺少依据。

建议：

- 增加 `profit_compute_run` 表记录规则快照、执行时间、执行人、统计结果。
- 销售行派生字段增加 `compute_run_id` 或拆到 `f_sales_line_profit`。
- 重要财务报表按 compute run 固化，避免口径漂移。

### 21. 事实表缺少足够的业务约束，异常数据主要靠应用层识别

证据：

- 采购/销售明细的 `qty`、`unit_price`、`line_amount` 允许为空。
- `data_status`、`business_type`、`source_type` 是自由字符串。
- 异常只落在 `anomaly_flags`，数据库层没有枚举或检查约束。

影响：

只要绕过 ETL 或 ETL 漏校验，数据库可以接受大量不符合业务口径的数据。后续报表必须反复处理空值、未知状态和异常类型。

建议：

- 对稳定枚举建立字典表或 CHECK 约束。
- 对财务关键字段区分“允许为空”和“导入失败”，不要混用 NULL 表达多种含义。
- 对 `anomaly_flags` 建立枚举说明表，方便统计和治理。

### 22. 原始数据留痕不完整，无法完整复现导入转换过程

证据：

- `sys_raw_file` 只保存原文件路径。
- `sys_import_error.raw_row` 只保存错误行。
- 成功行没有标准化前后的逐行映射留痕。

影响：

当用户质疑某条订单、型号标准化或金额计算时，只能回头解析原 Excel 文件，难以直接定位“原始字段是什么、清洗后是什么、为什么这样变”。如果原文件丢失或模板变化，复盘成本很高。

建议：

- 增加导入 staging 表，保存每行原始 JSON、清洗 JSON、错误/警告、目标表 id。
- 关键清洗规则记录版本号。
- 原始文件归档增加完整性校验和保留策略。

## 系统架构设计不足

### 23. 导入、清洗、入库、利润重算都在同步请求中完成

证据：

- `backend/app/api/imports.py:46-64` 上传接口内同步执行 `pipeline.run_import()` 和 `profit.recompute()`。
- `profit.recompute()` 会扫描并更新所有已生效销售行。

影响：

大文件上传或利润重算会占用 HTTP 请求线程，容易超时、重复提交或让用户误判状态。失败也缺少可恢复任务状态。

建议：

- 引入后台任务队列：导入任务、清洗任务、利润重算任务分阶段执行。
- 前端轮询任务状态，展示当前阶段、进度、失败原因和可重试动作。
- 大计算任务加幂等任务 id 和分布式锁。

### 24. 业务规则集中在 Python 常量中，缺少可配置、可审计和灰度能力

证据：

- `backend/app/config.py:46-75` 成本法、采购类型、营收业务类型、税口径、导入模式等均为代码常量。

影响：

业务口径调整需要改代码、重启服务、重新计算。更关键的是，历史报表无法知道当时使用的是哪一组配置。

建议：

- 建立 `business_rule_set` 表，保存规则 JSON、版本、生效时间、审批人。
- 利润重算引用具体 rule set。
- 允许先在测试规则集下试算，对比后再切换生效。

### 25. 应用缺少分层的“数据质量治理”机制

证据：

- 当前 ETL 只有行级 errors 和 `anomaly_flags`，没有质量任务、质量分、修复闭环。
- `needs_review` 只存在型号层，未形成审核流程。

影响：

`no_cost`、`neg_margin`、`zero_price` 等异常会持续堆积，但系统没有明确负责人、处理状态和关闭原因。数据质量问题会逐渐变成报表可信度问题。

建议：

- 建立 `data_quality_issue` 表，记录对象类型、对象 id、问题类型、严重级别、状态、负责人。
- 从导入和利润重算自动生成质量问题。
- 支持人工确认：真实异常、已修复、忽略、规则待确认。

### 26. 缺少统一的领域服务边界，查询服务混合了权限、聚合和展示拼装

证据：

- `part_overview` 同时拼装型号、采购、销售、库存、替代料、利润、询价参考。
- 权限脱敏在 API 层最后处理，部分子查询如 `_inventory()`、`_profit_summary()` 没有接受 data scope。

影响：

随着权限和业务口径变复杂，一个全景接口容易变成“所有数据都查出来再遮蔽”。这对性能和安全都不理想，也容易出现某个子块漏过滤。

建议：

- 按领域拆分查询服务：主数据、库存、采购、销售、利润、询价。
- 权限过滤下沉到查询构造阶段，而不是只在最终 payload 脱敏。
- 对全景接口定义清晰的数据契约和权限矩阵。

### 27. 缺少生产级观测和告警

证据：

- 当前只看到 `/health` 和 `/health/db`。
- 导入失败、重算失败、异常数据激增没有任务表或告警通道。

影响：

系统可以“接口正常但数据错误”。例如利润重算失败后上传接口只是返回一次错误，后续无人感知。

建议：

- 增加结构化日志、任务状态表、错误指标、慢查询指标。
- 对导入失败、重算失败、`no_cost/neg_margin` 异常比例设置告警。
- 对关键报表展示“数据新鲜度”和“最近重算状态”。

## 业务设计不足

### 28. 销售换货、整机销售、维保需求等业务类型的财务口径尚未闭环

证据：

- `backend/app/config.py:57` 成本采购类型排除 `维保需求`。
- `backend/app/config.py:60` 营收业务类型只包含 `备件销售`，排除 `销售换货/整机销售`。
- 当前数据库异常中 `excluded_business_type` 为 3,902 条。

影响：

排除规则可能适合一期报表，但如果换货、整机、维保会影响库存、成本或售后毛利，当前报表会低估或漏算部分业务结果。

建议：

- 为每类业务定义：是否计营收、是否计成本、是否扣库存、是否进售后费用、是否影响毛利。
- 不确定业务类型进入“待确认”队列，不应静默排除。
- 报表显示排除金额和排除原因明细。

### 29. 退货、负数数量、换货冲销缺少明确模型

证据：

- 成本回放以采购入库、销售出库为主。
- `qty <= 0` 在 `backend/app/services/cost.py:69-70` 会导致移动成本为空。

影响：

退货、换货、冲销、红字订单如果以负数进入系统，可能无法正确回补 FIFO 层或移动加权库存，利润和库存都会失真。

建议：

- 明确交易方向字段：销售出库、销售退货、采购入库、采购退货、调拨、盘点。
- 退货要能关联原销售或按退货日期/原成本入库。
- 成本引擎支持反向事件，而不是把负数视作无成本。

### 30. 替代料只保存直接关系，没有替代组、方向和适用条件

证据：

- `part_substitute` 只有 `part_id_a`、`part_id_b`、`source`、`note`。
- 服务层按 A/B 双向查询。

影响：

现实替代关系可能有方向、品牌限制、机型限制、客户限制、质量等级和有效期。简单双向关系会让不可逆替代被误用。

建议：

- 增加替代组 `substitute_group`，并记录关系方向、适用条件、优先级、有效期。
- 对人工替代料建立审核流程。
- 报价和库存推荐时显示替代依据，而不是只给列表。

### 31. 询价参考缺少时效性、客户场景和成交转化维度

证据：

- `f_part_inquiry` 只保存 `apply_time`、`parts_type`、`pn_std`、`money` 等基础字段。
- 型号全景 `_inquiry_ref()` 只返回最小价、最大价、最近价和数量。

影响：

历史询价价格可能受客户、数量、时间、货期、品牌、成色影响。只展示 min/max/last 容易误导报价。

建议：

- 增加询价来源、客户类型、数量、品牌/成色、是否成交、有效期。
- 报价参考按时间衰减，只默认展示近 N 天有效样本。
- 对异常高低价做剔除或标记。

### 32. 库存业务缺少可用库存、占用库存和安全库存闭环

证据：

- 当前库存表主要有 `source_qty`、`manual_qty`、`safety_stock`。
- 没有预占、在途、锁定、待出库、待入库等字段或事实表。

影响：

展示库存不等于可销售库存。销售报价、采购建议和库存预警如果直接用 `display_qty`，可能出现超卖或误判缺货。

建议：

- 区分账面库存、可用库存、锁定库存、在途库存。
- 建立库存流水或库存事件表，而不只依赖快照。
- 安全库存需要维护责任人、生效范围和补货策略。

## 优先级建议

1. 立即处理默认口令、默认签名密钥、数据库弱密码和数据库端口暴露。
2. 启用或实现最小 RBAC：先保护采购成本、供应商、利润、客户数据。
3. 修复导入 `skip` 导致不可修正的问题，补充 upsert 修复模式。
4. 将利润重算改为可观测任务状态，避免导入成功但利润失效。
5. 明确成本口径，区分真实匹配成本、fallback 成本、无成本。
6. 补齐库存成本计算，否则隐藏或标注库存价值字段。
7. 增加 CSV 导出转义、访问审计和 token 撤销机制。
8. 补充主数据治理：客户/供应商/型号别名、合并、审核、停用、追溯。
9. 增加库存快照、库存流水和利润计算版本，保证历史可复盘。
10. 把导入和重算改为后台任务，并建立数据质量闭环。
