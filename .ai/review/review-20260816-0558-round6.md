# 维保模块重建分支第六轮独立代码审核报告

报告标识时间：2026-08-16 05:58（Asia/Shanghai，按用户指定文件名）

完成复核时间：2026-08-16 06:35（Asia/Shanghai）

最终结论：**❌ 不可合并；不可进入生产或灰度。**

本轮固定对象为 `07be37b61094f5e5ea1abe4b493f505ea60ca396`。Round-5 的 13 项 Blocker 中，只有 **3 项真实闭环**（#1 模型注册与零漂移、#12 F2 CKD 有效头门与 owner HTTP、#13 旧三条 downgrade 的并发窗口）；其余 **10 项仍为 ❌**。AI accept 的真实 API 预读链确定具备 SQLAlchemy identity-map 旧状态条件；front-stock 的作废路径保留同类丢更新风险，尚缺真实 PostgreSQL 调用链反例。现有“加锁”测试不能证明两者安全。

新迁移审计为两项 ❌、一项 ⚠️：任何已先落地 c3 且产生业务事实的环境中，`d7f1a3c5e8b2` 不回填 RKD head 项目归属，`e3c5a7f9d1b2` 会把未扣账的旧变卖虚标为已扣账；`f1b3d5e7a9c2` 的唯一索引会对重复 active 凭证安全失败并完整回滚，但缺少发布前 preflight/runbook/test。生产 `origin/main` 尚无这些新表，首次全链部署不受“c3 存量”反例直接影响；空库单 head 和 `alembic check` 零漂移通过，也不能替代受影响环境的增量升级验收。

## 一、审核范围（提交列表）

### 1. 固定基线、锚点与目标

- 已执行 `git fetch origin`。
- 生产基线：`origin/main = 4f8b6881a0ff367e73f8dc108ada137d3d2e2346`。
- Round-5 固定对象/本轮锚点：`9adffd0f21b1017da28fc57a131732d9e9067ddb`，见 `.ai/review/review-20260816-0344-round5.md:7-11,102-178`。
- 本轮最终对象：`origin/feat/maintenance-ledger-import = 07be37b61094f5e5ea1abe4b493f505ea60ca396`；本地 `HEAD` 与远端一致。
- 实际新增范围：`9adffd0..07be37b`，39 个文件，1,945 行新增、304 行删除；`git diff --check` 退出码 0。

### 2. Round-5 之后的提交（时间正序）

1. `07be37b61094f5e5ea1abe4b493f505ea60ca396` — `fix(maintenance): round-5 review — 13 项 Blocker 修复`（2026-08-16 05:57:17 +08:00）。

本提交同时加入 Round-5 报告和自身的“13 项已修”处置表；该处置表是开发方声明，不作为关闭证据。结论只取固定 SHA 的实现、业务权威文档、真实 PostgreSQL/SQLAlchemy 反例和测试结果。

### 3. 工作树与测试机边界

- 审核开始时工作树干净；本 reviewer 没有修改 `backend/`、`frontend/`、`docs/`、迁移或测试。
- 按用户要求同步的是**仓库根目录**，不是只同步 `backend/`；WSL 中 `.deploy/` 等根目录文件与本地 SHA-256 一致。
- 所有代码行号均以固定对象 `07be37b` 为准。本轮唯一仓库写入是本报告。

## 二、逐项结论（五维度）

| 维度 | 结论 | 决定性证据 |
|---|:---:|---|
| 业务口径一致性 | ❌ | F3 仍按项目无条件汇总 RKD、错 PN 只警告：`backend/app/services/maintenance_bad_returns.py:878-890,909-979`；F5 无 RKD/结存事实仍可登记：`backend/app/services/maintenance_bad_salvage.py:124-147,172-252`；D1 未按 `as_of`/有效合同聚合且缺值形成部分比率：`backend/app/services/maintenance_project_workbook_v3.py:252-265,332-390`；C4 重传批次仍重复累计：`backend/app/services/maintenance_expense_reconcile.py:59-160`。 |
| 数据模型与迁移安全 | ❌ | 对已先落地 c3 的环境，`d7` 无存量回填且 downgrade 直接删项目归属：`backend/alembic/versions/d7f1a3c5e8b2_maintenance_doc_head_project.py:16-44`；`e3` 给该环境的全部旧变卖默认 `stock_deducted=true`：`backend/alembic/versions/e3c5a7f9d1b2_maintenance_bad_salvage_cost_basis.py:28-54`。`f1` 唯一索引能安全失败回滚，但缺重复 preflight：`backend/alembic/versions/f1b3d5e7a9c2_maintenance_collection_evidence_dedup.py:15-27`。 |
| 安全与权限 | ❌ | F4/F5/D1 的权限实现已补，但三组测试仍只有 admin happy path：`backend/tests/test_maintenance_recovery.py:102-155`、`backend/tests/test_maintenance_bad_salvage.py:131-175`、`backend/tests/test_maintenance_project_workbook_v3.py:241-270`；C3 仅凭 sheet 名认可首行为表头并外发原文：`backend/app/api/maintenance_ai_fallback.py:73-105`、`backend/app/services/maintenance_ai_fallback.py:98-102`。 |
| 测试质量 | ❌ | 新测试覆盖顺序重放和 fresh Session 并发，但没有实际 API 预读对象后的双 Session 竞争：`backend/tests/test_maintenance_ai_fallback.py:333-375`、`backend/tests/test_maintenance_front_stock.py:385-483`；三条新 revision 在 `backend/tests/` 中无生命周期命中；权限拒绝、迁移存量、文件/DB 故障、重复 canonical 和 sharedStrings 峰值均缺反例。 |
| 回归 | ❌ | 单 head、模型注册和空库零漂移已恢复：`backend/app/models/__init__.py:90-99`、`backend/tests/test_migration_manual_pool.py:252-256`；但真实 c3→head 探针复现 `head_project_id=None` 和“无流水却 `stock_deducted=True`”，AI/front-stock 又保留 identity-map 旧对象机制，说明绿色空库门没有覆盖存量升级与真实调用链并发。 |

### 2.1 Round-5 的 13 项 Blocker 真实闭环矩阵

| # | Round-5 Blocker | Round-6 结论 | 决定性证据 |
|---:|---|:---:|---|
| 1 | F5/F6 模型未注册、Alembic 漂移 | ✅ | `backend/app/models/__init__.py:90-99` 已注册 RKD、BadSalvage、CollectionEvidence；WSL 独立 `tests/test_migration_manual_pool.py -q` 为 15 passed，内含 `alembic check`；`alembic heads` 仅 `f1b3d5e7a9c2 (head)`。 |
| 2 | F4/F5/D1 成本、利润与合同/回款越权 | ❌ | 实现基本补齐：F4 实时成本脱敏 `backend/app/api/maintenance_recovery.py:23-35,60-69`，F5 要求 `data_profit` `backend/app/api/maintenance_bad_salvage.py:51-68,94-130`，D1 同时要求两数据组 `backend/app/api/maintenance_project_workbook_v3.py:28-50,68-80`。但验收仍只有 admin happy path：`backend/tests/test_maintenance_recovery.py:102-155`、`backend/tests/test_maintenance_bad_salvage.py:131-175`、`backend/tests/test_maintenance_project_workbook_v3.py:241-270`；没有任一 flag=false、单侧授权或低权限 HTTP 反例，未达到 Round-5 明确的真实关闭门。 |
| 3 | F3 官方返还率分子不可信、伪 0% | ❌ | 分子仍按项目 `SUM(RKD.qty)`，不按 PN/义务勾稽：`backend/app/services/maintenance_bad_returns.py:878-890`；PN 错配仅 warning 仍发布：`:909-979`。义务 PN 的生成链见 `backend/tests/test_maintenance_bad_returns_api.py:136-164,1655-1661` 与 `backend/tests/test_site_issue_v2_api.py:93-121`，实际为 `PN-rkd-rate`；测试却插入 PN=`PN-rate-1` 的 RKD（`backend/tests/test_maintenance_bad_returns_api.py:1687-1697`）并断言 75% available（`:1701-1709`），直接固化错配。返还类未知/空测试结果在 `backend/app/services/maintenance_doc_import.py:640-644` 静默跳过，批次仍在 `:697-700` applied；项目只要出现任一已归属的 applied RKD head 就会被视为已导入：`backend/app/services/maintenance_bad_returns.py:892-907`，可发布伪 0%。当前类别白名单还在 `backend/app/config.py:244-250` 明示“待业务最终确认”，不能据此发布权威 KPI。 |
| 4 | F5 变卖不减账本、毛利可被未来事实改写 | ❌ | 冻结成本、`salvage_out/salvage_in` 已补，但 `part_id` 仍可空：`backend/app/api/maintenance_bad_salvage.py:30-40`；服务仅在非空时校验 PN，零结存时直接跳过扣账：`backend/app/services/maintenance_bad_salvage.py:124-147,199-202`，整个登记路径未绑定 RKD 坏件事实或剩余可售量：`:172-252`。fixture 与断言表明在无 RKD/前置库事实时登记 2 件仍成功：`backend/tests/test_maintenance_bad_salvage.py:45-90`。幂等键仍可空，冲突只 409：API `:33-40,150-152`；作废 body 的 reason 被丢弃：`:43-48,183-185`。 |
| 5 | F6 上传即关闭、并发去重、文件/DB 原子性 | ❌ | 自动 handle 和 DB 部分唯一索引已补；但 binary/yml 在最终 DB commit 前进入正式路径：`backend/app/services/maintenance_collection_evidence.py:175-259`，commit 在 `backend/app/api/maintenance_collection_evidence.py:93-126`。service 对 binary/sidecar 写入失败已有 `BaseException` 本地清理（service `:210-247`），但 API commit 阶段补偿仅覆盖进程内 `Exception`：API `:141-164`；进程 crash/kill 会留 orphan，commit 结果不确定时还可能删除已提交行引用的文件。顺序 replay 固定 `closed=False`：`:109-124`，节点后来变为可关闭也不会重试。测试 `backend/tests/test_maintenance_collection_evidence.py:108-194` 没有双事务、落盘故障或 commit 模糊失败。 |
| 6 | D1 返还关联、回款聚合与缺失值 | ❌ | obligation 关联已修：`backend/app/services/maintenance_project_workbook_v3.py:176-192`；但回款查询仅按项目+confirmed，没有 `as_of`、`included_in_total` 和合同生效区间：`:252-265`，违反 `docs/maintenance/import-field-contract.md:188-195`。计入合同缺金额时仍对已知合同求部分和并输出回款进度/成本率，成本 null 又按 0/部分和：service `:332-390`。测试把未来 `2026-10` 快照纳入当前业务日导出并断言成功：`backend/tests/test_maintenance_project_workbook_v3.py:127-135,216-220`。附加 D3 协议缺口：99 表仍无行级稳定 ID/token/HMAC，见 service `:491-500`。 |
| 7 | C4 未确认口径、重传与假分页 | ❌ | `unresolved` 替代 matched/mismatch 是正确部分修复；但 ledger/BXD 仍汇总所有 applied 批次：`backend/app/services/maintenance_expense_reconcile.py:59-126`，而同一文件换 key 可新建第二 applied 批次：`backend/tests/test_maintenance_ledger.py:264-275`。所谓分页在三趟 `.all()`、全量 dict 后才切 keys：service `:59-160`；测试只验返回切片：`backend/tests/test_maintenance_expense_reconcile.py:318-334`。`_status()` 又按金额非空而非来源事实存在判断：service `:40-48,166-188`，金额 null 的 ledger-only 可误标 formal_only。 |
| 8 | AI accept 非原子/锁连接风险 | ❌ | batch+proposal 单事务本身已修；但真实 API 在锁前 `db.get()` 缓存 proposal：`backend/app/api/maintenance_ai_fallback.py:208-215`，后续 `FOR UPDATE` 没有 `populate_existing`/expire/refresh：`backend/app/services/maintenance_ai_fallback.py:293-303`。后到事务等待锁后仍可读 identity map 中的旧 pending，再用不同 key 建第二批。SQLAlchemy 2.0.49 + 内存 SQLite 的顺序双 Session 机制探针输出：`database_after=accepted; selected_status=pending; after_expire=accepted`；它不冒充 PostgreSQL barrier 测试，但真实 API 的预读强引用链明确具备该 ORM 条件。现有测试只是同一 Session 顺序调用：`backend/tests/test_maintenance_ai_fallback.py:333-375`。 |
| 9 | front-stock savepoint/并发 | ❌ | advisory xact lock 已补，但锁后的查询没有刷新 Session 中已缓存的 stock：`backend/app/services/maintenance_front_stock.py:97-118`。至少作废路径在调用 movement 前查询 stock，并由循环变量保留强引用：`backend/app/services/maintenance_bad_salvage.py:270-301`。SQLAlchemy 2.0.49 + 内存 SQLite 的顺序双 Session identity-map 探针证明：数据库由 10 变 3 后，后续 `select(...).with_for_update()` 仍返回缓存值 10，显式 expire 才刷新；它证明 ORM 机制，不冒充真实 PostgreSQL void 并发复现。结合上述实际路径，两个不同 salvage 并发 void 存在覆盖回冲的丢更新风险。新增并发测试从 fresh Session 直接调用且两线程使用相同 source：`backend/tests/test_maintenance_front_stock.py:385-483`，未覆盖“预加载既有 stock + 不同 source”。 |
| 10 | C3 伪表头外发、alias/重复 canonical | ❌ | 未知识别 sheet 已 422；但只检查 sheet 名，不验证首行业务表头：`backend/app/api/maintenance_ai_fallback.py:73-105`。把业务行放在名为 `01_项目与合同` 的首行，主解析失败后该行仍会作为 headers 原文进入 prompt：`backend/app/services/maintenance_ai_fallback.py:98-102`。`apply_column_aliases` 只在“不同源字符串映射同一 target”时报错：`backend/app/services/import_safety.py:195-206`；完全相同 canonical 或 `订单编号`+`订单编号(必填)` 仍由 `_header_index` 静默取首：`backend/app/services/maintenance_ledger.py:107-117`，违反 `docs/maintenance/import-field-contract.md:31-35`。 |
| 11 | C5 隐藏首 sheet 与严格内存界 | ❌ | 正常 relationships 路径已跳过 hidden；但关系文件缺失/损坏被 `backend/app/services/import_safety.py:125-135` 吞掉后回退排序首 XML，不再检查 visibility。删除 `workbook.xml.rels` 的隐藏首 sheet 反例仍输出 `MALFORMED_REL_RESULT SECRET-HIDDEN`，证明流式路径仍会错误读取隐藏 sheet。sharedStrings 仍全量 list：service `:137-147`；峰值随解压体积线性增长且上限可逼近 2 GiB：`:16-18,59-64`，缺少进程可承受的独立内存预算；AI 路径仍 16 MiB 压缩文件、`read_only=False` 并全表 `list(iter_rows)`：`backend/app/api/maintenance_ai_fallback.py:25,78,95-103`。 |
| 12 | F2 CKD 有效头门与 owner HTTP | ✅ | CKD 成本查询已联结 batch/head，并要求 applied、维保供货、已生效、有日期且不晚于业务日、无 head issue、正数量：`backend/app/services/maintenance_replenishment_evidence.py:155-178`。owner/other-owner/admin 对 evidence/export 的 200/404 HTTP 矩阵存在：`backend/tests/test_maintenance_replenishment_evidence.py:449-527`。测试尚未逐个覆盖 future/status/issues 负例，列入建议但不推翻源码和核心 HTTP 闭环。 |
| 13 | 三条旧 downgrade guard 的检查后并发写窗口 | ✅ | `a7c3e5f9b2d1:66-87`、`b9d1e7c3f5a8:83-104`、`c3e9d1b7f5a2:69-90` 均先 `ACCESS EXCLUSIVE` 锁事实表，再 EXISTS、再 DROP；既有写会先完成、后续写会被阻塞，原竞态已消失。尚无双事务迁移测试，列入建议。 |

结论统计：**✅ 3 项，❌ 10 项。** “代码中写了已修”或现有 happy-path 变绿不能替代反例验收。

### 2.2 三条新迁移：守卫、单 head 与零漂移

| Revision | 结论 | Upgrade、downgrade 与存量证据 |
|---|:---:|---|
| `d7f1a3c5e8b2` | ❌ | upgrade 只增加 nullable `project_id`、FK 和索引，没有回填既有 applied RKD head：`backend/alembic/versions/d7f1a3c5e8b2_maintenance_doc_head_project.py:16-32`。在**已先落地 c3 并产生事实**的 disposable DB 中预置 applied head 和已有 `MaintenanceRkdReturnLine(project=probe-project)` 后升 head，实测 `head_project_id=None`；而就绪判断只认 `head.project_id`：`backend/app/services/maintenance_bad_returns.py:892-955`。downgrade `:35-44` 无历史事实 guard，直接删除升级后解析的项目归属；零返还 head 还不能从明细重建。生产 main 首次全链部署没有这类 c3 存量，但受影响环境的迁移兼容性未闭环。 |
| `e3c5a7f9d1b2` | ❌ | upgrade 给**已先落地 c3 环境中的全部旧 salvage**增加 `NOT NULL DEFAULT true`：`backend/alembic/versions/e3c5a7f9d1b2_maintenance_bad_salvage_cost_basis.py:41-46`；但 `9adffd0` 的旧服务只写变卖事实、从未写 `salvage_out`。c3→head 探针实测 `stock_deducted=True; salvage_ledger_count=0`，该环境的旧事实语义被虚标。downgrade `:57-100` 的双表 `ACCESS EXCLUSIVE` 和 salvage/salvage_in guard 本身正确，不能抵消错误 upgrade。生产 main 首次全链部署没有旧 salvage 行。 |
| `f1b3d5e7a9c2` | ⚠️ | upgrade 直接创建 active `(milestone_id,md5)` 部分唯一索引：`backend/alembic/versions/f1b3d5e7a9c2_maintenance_collection_evidence_dedup.py:15-21`。c3 预置两条 active 重复凭证时，实测 `UniqueViolation`；建索引锁下检查并使整事务安全回滚，version 仍 c3、两行原样，属于正确 fail-closed，不是静默损坏。发布准备仍缺只读 preflight、可操作错误/runbook 和存量迁移测试。生产 main 尚无该表，首次全链部署时 c3→f1 之间没有应用写入窗口。模型约束一致；downgrade `:24-27` 只删索引、不删事实。 |
| 迁移链/metadata | ✅（仅此子项） | `alembic heads` 为 `f1b3d5e7a9c2 (head)`；独立 `tests/test_migration_manual_pool.py -q` 为 15 passed，包含 `alembic check`，说明**空库全链 + 当前 metadata** 单 head、零漂移。三条 revision 在 `backend/tests/` 中均无命中，门禁没有覆盖 c3 存量升级/回填/downgrade。 |

## 三、Blocker 清单（必须修，按严重性排序）

### Blocker 1 — P0（限已落地 c3 且有事实的环境）：`d7/e3` 会错误迁移存量业务事实

- `d7` 不回填已应用 RKD head；在已经运行 c3 并产生数据的环境里，项目即使已有明确 RKD 明细，也会被新 readiness 误判未导入：`backend/alembic/versions/d7f1a3c5e8b2_maintenance_doc_head_project.py:16-32`、`backend/app/services/maintenance_bad_returns.py:892-955`。
- `e3` 把同类环境中由旧实现创建、从未扣过账的变卖标成已扣账：`backend/alembic/versions/e3c5a7f9d1b2_maintenance_bad_salvage_cost_basis.py:41-46`。
- 必须以 c3 存量 fixture 做 upgrade：能确定的项目归属须可审计回填，不能确定的显式进入异常；`stock_deducted` 必须由真实 `salvage_out` 事实推导，不能 blanket true。
- 生产 main 尚无这些表，首次全链部署没有上述存量；这里阻断的是已使用 c3 的预演/开发环境兼容性和 `d7` downgrade 的历史保护，不把探针泛化成生产已有污染。

### Blocker 2 — P0：F3 仍可发布跨 PN 或不完整 RKD 的错误官方返还率

- 项目级 SUM 与 PN 错配只警告：`backend/app/services/maintenance_bad_returns.py:878-979`。
- 未知/空检测结果静默丢行，批次仍 applied：`backend/app/services/maintenance_doc_import.py:640-644,697-700`。
- 类别白名单在实现自身仍标“待业务最终确认”：`backend/app/config.py:244-250`；开发方本次处置表也把类别映射列为待确认：`docs/superpowers/plans/2026-08-15-maintenance-rebuild-plan.md:159-162`。该处置表中的数据自述未在本轮独立验证，不作为数量证据；口径未确认本身已足以禁止发布权威 KPI。

### Blocker 3 — P0：F5 允许无坏件来源、无结存、无必填幂等键的任意变卖

- `part_id=None`/零结存绕过扣账，且不绑定 RKD 坏件或剩余可售量：`backend/app/api/maintenance_bad_salvage.py:30-40`、`backend/app/services/maintenance_bad_salvage.py:124-147,172-252`。
- 作废理由输入后丢弃：API `:43-48,183-185`；迁移又有历史 `stock_deducted` 污染。
- 这会产生无来源收入、错误毛利和不可勾稽库存，不是“坏件已消耗所以可不扣”的充分证据。

### Blocker 4 — P0：front-stock 仍存在旧 ORM 对象导致的丢更新路径

- `_get_or_create_stock()` 的锁读没有强制 refresh：`backend/app/services/maintenance_front_stock.py:97-118`。
- 至少并发作废路径会在 movement 前查询 stock，循环变量保留最后一个 ORM stock 强引用：`backend/app/services/maintenance_bad_salvage.py:270-301`。
- WSL SQLAlchemy 2.0.49 + 内存 SQLite 的顺序 identity-map 探针证明 `select(...).with_for_update()` 不会自动覆盖已缓存属性；结合实际 void 路径可推出覆盖回冲风险，但该探针不是 PostgreSQL barrier 并发或完整 API 复现。必须在锁读上 `populate_existing`/expire/refresh，并用两个不同 salvage、预加载同一 stock、不同 source 的真实 PostgreSQL 测试验收。

### Blocker 5 — P0：AI accept 行锁读到 identity map 旧状态，可二次建批

- API 锁前缓存 proposal：`backend/app/api/maintenance_ai_fallback.py:208-215`；service 的 `FOR UPDATE` 不刷新：`backend/app/services/maintenance_ai_fallback.py:293-303`。
- 同一 Session 顺序测试不能模拟两个请求均预读 pending：`backend/tests/test_maintenance_ai_fallback.py:333-375`。
- 必须用两个真实 Session/barrier、不同 Idempotency-Key，断言恰一 batch；并覆盖提交异常。

### Blocker 6 — P0：F6 文件与数据库仍非崩溃一致，重放也不能补关闭

- 正式文件早于 DB commit 落盘：`backend/app/services/maintenance_collection_evidence.py:175-259`、`backend/app/api/maintenance_collection_evidence.py:93-164`。
- service 已覆盖 binary/sidecar 写入失败的本地清理；API commit 阶段仍无法覆盖进程 crash/kill 或 commit 结果不确定，当前顺序测试没有故障注入。
- 同 md5 replay 永远 `closed=False`：API `:109-124`，第一次因 incomplete 未关闭后不能在状态修复后自动闭环。

### Blocker 7 — P0：D1 仍会导出未来回款和部分合同/成本比率

- 查询不按 `as_of` 与合同有效范围：`backend/app/services/maintenance_project_workbook_v3.py:252-265`。
- 缺合同金额/成本时仍输出部分合同总额、回款进度和成本率：service `:332-390`，违反 `docs/maintenance/import-field-contract.md:188-195`。
- 现有测试明确把未来 `2026-10` 快照纳入：`backend/tests/test_maintenance_project_workbook_v3.py:127-135,216-220`。

### Blocker 8 — P0：C4 重传重复累计，分页仍全量加载

- ledger/BXD 所有 applied 批次一起累加：`backend/app/services/maintenance_expense_reconcile.py:59-126`；同文件新 key 会产生第二 applied 批次：`backend/tests/test_maintenance_ledger.py:264-275`。
- 三源均 `.all()` 后才做 offset/limit：service `:59-160`，不是有界服务端分页。
- null 金额还会抹掉“来源存在”语义：service `:40-48,166-188`。

### Blocker 9 — P0：C3 仍可把首行业务原文当表头外发，重复 canonical 静默择一

- 已知 sheet 名不等于首行已验证为 header：`backend/app/api/maintenance_ai_fallback.py:73-105`。
- prompt 仍携带原始 headers：`backend/app/services/maintenance_ai_fallback.py:98-102`。
- alias helper 与 `_header_index` 组合没有对规范化后重复做统一 fail-closed：`backend/app/services/import_safety.py:195-206`、`backend/app/services/maintenance_ledger.py:107-117`。

### Blocker 10 — P1：C5 malformed relationships 仍会错误读取隐藏 sheet，进程内存预算仍不成立

- 缺失/损坏 `workbook.xml.rels` 回退排序首 XML：`backend/app/services/import_safety.py:125-135`；反例仍读出 `SECRET-HIDDEN`，直接违反“首个可见 sheet”契约，但不把该探针夸大为已证明越权外发。
- sharedStrings 全量驻留，峰值随解压体积线性增长且可逼近 2 GiB：service `:16-18,59-64,137-147`；AI 路径继续全表物化：`backend/app/api/maintenance_ai_fallback.py:25,78,95-103`。

### Blocker 11 — P1：F4/F5/D1 权限实现没有负向 HTTP 验收门

- 三个 endpoint 的实现静态未发现绕过，但测试只造 admin：`backend/tests/test_maintenance_recovery.py:102-155`、`backend/tests/test_maintenance_bad_salvage.py:131-175`、`backend/tests/test_maintenance_project_workbook_v3.py:241-270`。
- 需要 `data_purchase_cost/data_profit` 四组合，以及 owner/other-owner/admin/anonymous；不只验状态码，还要验响应体、汇总值、缓存头均不侧漏。

## 四、建议清单（可修可不修）

1. 给三条新迁移建立 c3 存量升级 fixture：旧 RKD 有/无返还行、旧 salvage 有/无对应流水、active evidence 重复；同时验证失败回滚后的 `alembic_version` 和事实行数。`f1` 目前是安全 fail-closed，发布前补只读重复 preflight、可操作 runbook 和迁移测试即可，不单列合并 Blocker。
2. F3 把“导入完成”建模为可审计覆盖事实，不以“项目出现任一 applied head”代替全量完成；类别、状态、测试结果未确认时输出 `not_ready/incomplete`。
3. F5 变卖绑定具体 RKD/义务事实和可售剩余量，`part_id`、Idempotency-Key、作废 reason 均成为必需审计字段；扣账/回冲用不可变流水。
4. F6 采用 staging + durable outbox/reconciler + 原子 promote，明确 commit 不确定时的幂等恢复；同 md5 重放应重算“是否需要关闭”，而不是永久返回 false。
5. D1 复用统一的“截至 as_of 的有效合同 + 每合同最新 confirmed 累计快照”投影；任一计入合同缺金额时总额和派生比率均为 null，成本缺失只展示下界。
6. C4 在数据库层先选稳定版本/最新批次，再按 key 做真正 cursor/offset 分页；来源存在与金额是否为空使用两个独立字段。
7. C3 先验证模板签名/受控完整 header 集，再允许 AI；对移除 `(必填)/(不可修改)/(含税)` 后的 canonical 统一检测重复。
8. C5 对 malformed workbook fail-closed；sharedStrings/worksheet/AI 都用统一解压字节、行数、列数、耗时和峰值内存预算。
9. 为 #12 CKD 门补 future date、invalid status、head issues 三个独立负例；为 #13 旧 downgrade lock 补两个连接的真实 PostgreSQL 竞争测试。

## 五、复跑测试记录（命令 + 结果尾部）

所有测试均针对固定对象 `07be37b`。WSL 工作目录为 `~/Workspaces/it-data-pm/it-spareparts-b2`。

### 1. 全仓同步（含 `.deploy` 等根目录文件）

```bash
rsync -az --itemize-changes --stats \
  --exclude .git \
  --exclude node_modules \
  --exclude .venv \
  --exclude __pycache__ \
  /Users/yangjinchen/Code/IT_data/ \
  wsl:~/Workspaces/it-data-pm/it-spareparts-b2/
```

结果：979 个文件；本次 `Number of regular files transferred: 0`（WSL 已是相同内容）。随后逐个比较根目录 `.deploy/` 文件及审核关键文件 SHA-256，本地/WSL 一致。

### 2. 单 head 与独立零漂移门

```bash
.venv/bin/python -m alembic heads
unset DATABASE_URL TEST_DATABASE_URL PYTEST_DATABASE_BASE_URL
.venv/bin/python -m pytest tests/test_migration_manual_pool.py -q
```

结果尾部：

```text
f1b3d5e7a9c2 (head)
...............                                                          [100%]
15 passed, 1 warning in 25.11s
```

该测试包含 `alembic check`。默认配置直接运行曾因 WSL hostname `db` 无法解析而失败，共享 localhost base 又停在旧 revision；两者均是环境连接/共享状态，不作为 drift 结论。最终以上“清空外部 DB 变量 + disposable DB”的独立进程结果为准。

### 3. 定向回归

```bash
.venv/bin/python -m pytest \
  tests/test_maintenance_doc_import.py \
  tests/test_maintenance_ai_fallback.py \
  tests/test_maintenance_replenishment_evidence.py -q
```

```text
33 passed in 15.77s
```

```bash
.venv/bin/python -m pytest \
  tests/test_maintenance_bad_salvage.py \
  tests/test_maintenance_collection_evidence.py -q
```

```text
11 passed in 9.08s
```

绿色结果证明已有断言通过，不覆盖本报告中的 identity-map、故障注入、存量迁移或业务口径反例。

### 4. 全量 pytest

```bash
unset DATABASE_URL TEST_DATABASE_URL PYTEST_DATABASE_BASE_URL
.venv/bin/python -m pytest -q
```

结果尾部：

```text
11 failed, 3296 passed, 7 skipped, 2 warnings in 1419.82s (0:23:39)
```

11 个失败与用户给定环境基线逐项一致，没有新增业务测试失败：

- `tests/test_https_rollback.py` 9 项：均先停在测试机 root runtime `Caddyfile` ownership/safety 环境门；
- `tests/test_v120_release_control.py::test_ci_pins_setup_uv_action_and_binary_version` 1 项：release-control 固定版本断言；
- `tests/test_v122_collection_reminders_release_control.py::test_static_release_self_test_is_runnable_without_production_access` 1 项：静态脚本仍固定旧 migration graph head。

按 reviewer-prompt 和用户说明，这 11 项记为“已知环境用例”，不冒充本轮业务回归；同时也不把它们计为绿色通过。两条 warning 分别是既有 Starlette/httpx deprecation 与 `alembic check` 的 `dialect_options` SAWarning，不改变零漂移结论。

### 5. Identity-map、OOXML 与迁移存量反例

- AI/front-stock 使用 WSL 的 SQLAlchemy 2.0.49 + `sqlite+pysqlite:///:memory:` 顺序双 Session 探针；它们验证 ORM identity-map 不会因后续 `select(...).with_for_update()` 自动刷新，**不是** PostgreSQL barrier 并发或完整 API 复现。front-stock 输出采用负向扣减模型，只证明旧值覆盖机制，不冒充实际 `salvage_in` 回冲复现。
- OOXML 探针在内存中构造隐藏首 sheet 并删除 `workbook.xml.rels`，不使用数据库。
- `d7/e3/f1` 使用 disposable PostgreSQL 做 c3→head 增量升级；临时数据库在 `finally` 删除。
- 以上均未写仓库。

```text
AI identity-map:
cached_before=pending
database_after=accepted
selected_status=pending
after_expire=accepted

front-stock identity-map:
db_after_first=3
late_locked_seen=10
final_qty=3 expected=-4_or_reject

malformed OOXML relationship:
MALFORMED_REL_RESULT SECRET-HIDDEN

c3 -> head transition:
head_project_id=None
stock_deducted=True
salvage_ledger_count=0

f1 duplicate-active transition:
IntegrityError UniqueViolation: could not create unique index
alembic_version=c3e9d1b7f5a2
active_rows=2
```

`f1` 是安全失败（事务回滚、事实不丢），因此记 ⚠️ 发布准备而非合并 Blocker。两个 identity-map 探针直接反证“加锁查询必然重读”的假设；AI 的真实 API 预读链可确定触发该机制，front-stock 则仍需真实 PostgreSQL void 并发测试把风险闭合成调用链复现。OOXML 和 PostgreSQL 迁移探针分别直接反证 #11、`d7/e3` 的闭环声明。

### 6. Diff 格式检查

```bash
git diff --check 9adffd0..07be37b
```

结果：退出码 0、无输出。
