# 维保模块重建分支第二轮独立代码审核报告

审核时间：2026-08-16 00:24（Asia/Shanghai）  
指定审核提交：`d5fd3764ff65bd798f9bcd6428fd9c7a33db1700`  
最终结论：**❌ 不可合并；更不可进入生产或灰度。**

本轮按首轮报告的关闭门执行：不仅要改到实现，还必须新增能证明反例被封住的失败路径测试。复核结果为：**15 项 Blocker 中 0 项完整关闭，15 项仍未达到关闭条件**。下文中的 `✅` 只表示该 Blocker 的某个子问题已修，不能替代整项关闭结论。

现有绿色测试没有覆盖本轮复现出的关键反例：含税金额仍按未税值汇总、台账/CKD/三单仍可跳过坏行后提交、CKD 冲突被吞、未知成本沿用旧成本、三单导入可跨项目扣减库存。

## 一、审核范围（提交列表）

### 1. 固定基线与目标快照

- 基线：`origin/main = 4f8b6881a0ff367e73f8dc108ada137d3d2e2346`。
- 用户指定目标：`d5fd3764ff65bd798f9bcd6428fd9c7a33db1700`，父提交为 `94dec254b977b71ba8b4c3b552396be9f8910044`。
- 审核开始时已执行 `git fetch origin --prune`，当时本地、远端跟踪分支与目标均为 `d5fd376`。审核期间分支又前移到 `899a648`；该提交只改 `docs/maintenance/data-baseline-2026-08.md`，不属于用户指定目标，本报告的代码证据、差异和 WSL 测试均固定在 Git 对象 `d5fd376`。
- 比较范围：`git diff origin/main...d5fd376`，共 `43 files changed, 6738 insertions(+), 27 deletions(-)`。
- WSL 测试使用 `git archive d5fd376 backend` 导出的干净快照；测试后已删除本轮创建的临时目录，未覆盖开发工作区。
- 本轮唯一仓库写入为本报告；未修改被审代码、迁移、测试或业务文档。

### 2. 提交列表（时间正序）

1. `88f4796` — `feat(maintenance): ledger workbook import models, parser and apply (B2)`
2. `384bbb8` — `fix(maintenance): accept 报销明细. prefixed expense headers in ledger`
3. `f5d8fbc` — `fix(maintenance): align ledger migration with frozen test baselines and permission registry`
4. `a4eb91a` — `feat(maintenance): project front-stock ledger (B1)`
5. `769337b` — `feat(maintenance): no-return rules — project default + line override + exemption audit (B3)`
6. `740b2ad` — `docs(ai): codex reviewer prompt for maintenance rebuild branch`
7. `0807b2f` — `feat(maintenance): 氚云发货单导入 + 维保供货入前置库 (C1a/F1)`
8. `94dec25` — `feat(maintenance): 氚云三单通用导入层 + 返库单未用件收回 (C1b)`
9. `d5fd376` — `fix(maintenance): address codex review blockers (15 项全量整改)`

### 3. 口径依据

已重新核对 reviewer prompt 指定的五份业务资料。口径冲突时仍以 PRD §18/§19 与重建计划 §2/§2.1 为准：无现场收货、CKD 维保供货直接入前置库、领用不写前置库、无 SN 结存维度、返还率分母扣除不返还、台账合同额为含税、字段码优先、关键异常失败关闭、项目身份不得按展示名猜测。

## 二、五维度结论

| 维度 | 结论 | 决定性证据 |
|---|---|---|
| 业务口径一致性 | ❌ | Blocker 1–4、7–8、12 仍有实际错算或事实归属错误；C1b 又把坏品/未知状态等行当作可跳过项。 |
| 数据模型与迁移安全 | ❌ | Blocker 6、10、15 未关闭：并发窗口、整事务 rollback、跨批 FK、迁移检查后写入竞态仍在。 |
| 安全与权限 | ❌ | Blocker 5、9、13、14 未关闭；新增三单 apply 无项目范围门，可跨项目出前置库账。 |
| 测试质量 | ❌ | 新增测试均通过，但多处只测纯函数/简化 fixture/单会话；首轮点名的 HTTP 越权、并发、真实 166 列、DB 违规插入及失败点数据不变性没有覆盖。 |
| 回归 | ❌ | `git diff --check origin/main...d5fd376` 退出码 2，三个模型文件存在 trailing whitespace；Ruff 在 WSL 共享环境未安装，未宣称 Ruff 通过。 |

## 三、15 项 Blocker 逐项复核

### Blocker 1 — ❌ 台账含税金额链仍未闭合

- ✅ parser 已兼容 `订单金额(含税)`，raw/canonical 也能保存台账含税值：`backend/app/services/maintenance_ledger.py:97-107,365-405,645-659`。
- ❌ 没有执行“台账含税额 vs 销售未税额 × (1 + 税率)”对账；合同同时把销售单未税额写入 `contract_amount`、把台账值写入 `amount_inc_tax`：`backend/app/services/maintenance_ledger.py:629-695`。
- ❌ 项目读模型仍用 `contract_amount` 判缺失、求和和输出，却标注 `contract_amount_basis="inc_tax"`；目录聚合同样求和未税列：`backend/app/services/maintenance_project.py:234-280`、`backend/app/services/maintenance_project_operations.py:5494-5521`。因此 44,756 元样例仍会按 39,607.08 元展示/汇总。
- ❌ 新测试反而把台账金额改成 50,000 后断言正常更新为 50,000：`backend/tests/test_maintenance_ledger.py:278-306`，与首轮“差异进入异常且 canonical 零写”的通过标准相反。

### Blocker 2 — ❌ B2/CKD 仍不是完整 fail-closed 原子 apply

- ✅ B2 apply 现在会拒绝已落在合同/计划 raw 行上的 issues：`backend/app/services/maintenance_ledger.py:741-777`。
- ❌ preview 没有把所有关键失败制造成 issue：显式订单日期/终止日期解析失败、缺订单金额、计划金额解析失败均未完整报错；计划日期 issue 又在 `issue_rows` 计数之后追加：`backend/app/services/maintenance_ledger.py:351-427`。`planned_amount='坏值'` 会成为 `None`，随后按 `date_only` 节点继续写：`backend/app/services/maintenance_ledger.py:708-734`。
- ❌ 计划订单号不属于本批合同时被静默跳过，批次仍标 `applied`；报销行 issues 计入批次，但 apply 只检查合同/计划 issues：`backend/app/services/maintenance_ledger.py:429-460,750-766,817-826`。
- ❌ CKD 对未归属 WBDD、未知 PN、非法数量、未知数据状态继续 skip/ignore，并在捕获 `FrontStockError` 后无条件提交 `applied`：`backend/app/services/maintenance_ckd_import.py:386-448`。现有测试明确断言未知 WBDD/PN 可以跳过成功：`backend/tests/test_maintenance_ckd_import.py:217-248`。

### Blocker 3 — ❌ 台账仍未拥有稳定项目身份、期限、负责人和唯一计划事实

- ❌ 项目仍以 `(project_name)[:64]` 生成/查询 `project_code`：`backend/app/services/maintenance_ledger.py:477-505`；这与模型“display names are never relationship keys”约束相反：`backend/app/models/maintenance_project.py:26-33`。
- ❌ 新建项目在 `backend/app/services/maintenance_ledger.py:491-518` 提前返回，根本不会执行 `backend/app/services/maintenance_ledger.py:568-574` 的 `MaintenanceServicePeriod` upsert；同日期重导也不会刷新 period 的 `source/ledger_batch_id`：`backend/app/services/maintenance_ledger.py:609-619`。
- ❌ Excel 经理文本只写 `project_manager_id`，没有解析/维护真正用于可见范围的 `MaintenanceProjectUserAssignment`：`backend/app/services/maintenance_ledger.py:496,537-540`、`backend/app/services/maintenance_project_assignments.py:96-138`。
- ✅ ledger 已调用受控 milestone helper，已跟进节点变更会进入 `needs_review`。但经理工作簿和旧计划导入仍能写同一事实，helper 没有来源所有权或 expected-version 门，台账并非唯一写源：`backend/app/services/maintenance_collection_milestones.py:96-125`、`backend/app/services/maintenance_manager_workbook_adapter.py:866-925`、`backend/app/services/maintenance_collection_plan_imports.py:1128-1140`。

### Blocker 4 — ❌ `no_return` HTTP→DB→confirm 全链仍断

- ✅ 分类函数与 category resolve 已补充部分行级优先和 `exemption_source`：`backend/app/services/maintenance_bad_returns.py:50-115,1437-1455`。
- ❌ draft create 虽接收并把 `no_return` 放入请求指纹，构造 `MaintenanceSiteIssueLine` 时却没有赋值；legacy create 同样遗漏：`backend/app/services/maintenance_project_operations.py:2082-2118,2164-2197,2275-2301`。confirm 只能读取已落库值，故请求标记会丢失：`backend/app/services/maintenance_project_operations.py:922-950`。
- ❌ patch 虽落库 `no_return`，但幂等指纹的 line payload只含 `delivery_line_id/quantity`；同 key 改标记不会产生 payload 冲突：`backend/app/services/maintenance_project_operations.py:1386-1431,1461-1483`。
- ❌ 显式 `no_return=False` 且无品类仍返回 `pending_category`，没有形成“行级 False 覆盖项目默认”的必须返还事实；后续 category resolve 又不带原行/项目标记重新分类：`backend/app/services/maintenance_bad_returns.py:70-105,1437-1447`。
- ❌ 新增测试仍只有分类纯函数，没有首轮要求的 HTTP→DB→confirm→obligation 与同幂等键改标记用例：`backend/tests/test_maintenance_no_return_rules.py:38-124`。

### Blocker 5 — ❌ 实现子路径已补，但未达到权限验收门，且新增写路径重新暴露同类风险

- ✅ front-stock GET 已接项目范围和成本字段门；CKD API 已解析可见项目，service 在写入前拒绝范围外项目：`backend/app/api/maintenance_front_stock.py:38-103`、`backend/app/api/maintenance_ckd_import.py:121-155`、`backend/app/services/maintenance_ckd_import.py:398-405`。
- ❌ 新增 hardening 测试只覆盖 front-stock；成本脱敏用例没有库存行，仓库内没有 CKD 403/零写 API 用例：`backend/tests/test_maintenance_import_hardening.py:153-180`。因此首轮明确的 Admin/本项目/他项目及 403 零写验收尚未被证明。
- ❌ 同一高风险 action 下新增的 `doc-imports` 写路径完全没有项目范围门，见“新增 P0-1”；当前分支整体仍允许范围账号跨项目改前置库。

### Blocker 6 — ❌ 前置库/CKD 幂等与并发仍可吞冲突或丢前序行

- ✅ 流水唯一键已改为稳定 `(source_type, source_ref)`，顺序单会话下同 payload 重放返回旧流水、异 payload 抛 `FrontStockPayloadConflict`：`backend/app/models/maintenance_front_stock.py:114-150`、`backend/app/services/maintenance_front_stock.py:147-180`。
- ❌ CKD 捕获其父类 `FrontStockError` 后只记 `skipped_lines`，最终仍返回 200/applied；同稳定明细变 PN/数量不会向调用方暴露 409：`backend/app/services/maintenance_front_stock.py:38-51`、`backend/app/services/maintenance_ckd_import.py:424-448`。
- ❌ payload hash 缺 `project_id`、`occurred_at` 等业务字段：`backend/app/services/maintenance_front_stock.py:54-73`。同来源改项目或业务日期可被当作相同重放。
- ❌ `FOR UPDATE` 只锁已存在的流水/结存；空行并发窗口仍在。创建结存的唯一冲突分支执行整 session `db.rollback()`，会回滚本批此前成功行再继续：`backend/app/services/maintenance_front_stock.py:92-126,167-180`。CKD batch 自身也没有行锁/CAS：`backend/app/services/maintenance_ckd_import.py:340-346`。
- ❌ 测试仅覆盖单会话顺序重放，没有双事务并发 batch/新建 stock/同 Idempotency-Key：`backend/tests/test_maintenance_front_stock.py:173-181`、`backend/tests/test_maintenance_ckd_import.py:195-214`。

### Blocker 7 — ❌ 真实 166 列 CKD 字段契约仍未实现

- ✅ 精确重复中文列会拒绝；缺明细 Data ID 不再用物理行号伪造来源：`backend/app/services/maintenance_ckd_import.py:81-86,251-252,431`。
- ❌ parser 仍完全忽略字段码行，只使用第二行中文表头：`backend/app/services/maintenance_ckd_import.py:120-140`。这不满足“字段码优先”。
- ❌ 所谓真实表头 fixture 只有 33 列，字段码行只有一个占位单元格；既有 CKD fixture 也只有三个占位码：`backend/tests/test_maintenance_import_hardening.py:45-79`、`backend/tests/test_maintenance_ckd_import.py:29-42`。
- ❌ WBDD/销售订单别名仍以 `A or B` 静默择一，预留的两个 WBDD raw/canonical 字段没有完整保存；缺失/未知数据状态仍可入账或静默忽略：`backend/app/services/maintenance_ckd_import.py:210,225-237,386-393`。
- ❌ line issues 在数量/Data ID 问题追加前计数，preview 的 `issue_rows` 低报：`backend/app/services/maintenance_ckd_import.py:243-252,284-289`。

### Blocker 8 — ❌ CKD 真实库名和历史发生时间仍会写错

- ✅ 来源类型已改为 `ckd_shipment_line`；有效出库日期会传入 `occurred_at`：`backend/app/services/maintenance_ckd_import.py:425-438`。
- ❌ apply 仍用项目编码作为 `warehouse_name`，已解析保存的 Excel 主表/明细仓库原值不参与入账：`backend/app/services/maintenance_ckd_import.py:223,267,316-318,406,433`。同项目同 PN 的两个真实仓库仍会被合并。
- ❌ 出库日期解析失败不进入 issues，随后以 `occurred_at=None` 入账；前置库再回退为导入当前时间：`backend/app/services/maintenance_ckd_import.py:206-213,434-438`、`backend/app/services/maintenance_front_stock.py:191-192`。历史 91 天记录仍可能显示 0 天。
- ❌ CKD 测试没有“两库同 PN”或历史 91 天适配器验收；现有库名测试只直调通用前置库 service：`backend/tests/test_maintenance_front_stock.py:82-93`。

### Blocker 9 — ❌ 专用动作已建立，但写权限契约与审计仍不完整

- ✅ CKD 改用 `action_maintenance_doc_import`；该 key 已进入权限注册、非管理员模板默认 false 和 `HIGH_RISK_KEYS`：`backend/app/api/maintenance_ckd_import.py:20-22,44-53,121-129`、`backend/app/permissions.py:169-293,473-493`。
- ❌ apply 请求没有 `reason`、expected version/CAS；batch 也未加锁：`backend/app/api/maintenance_ckd_import.py:121-143`、`backend/app/services/maintenance_ckd_import.py:340-346`。
- ❌ 没有 CKD apply 专用 before/after 审计事件；前置库流水只保存变化量与变化后余额：`backend/app/models/maintenance_front_stock.py:95-154`。
- ❌ 权限图声明该 action 依赖 `data_purchase_cost`，但 CKD/doc 端点调用 `require_action(_ACTION_KEY)` 时没有传 `require_data`；接口层不能兜住旧 token/旧快照/数据库外形成的非法组合：`backend/app/permissions.py:326-345`、`backend/app/security.py:98-130`、`backend/app/api/maintenance_ckd_import.py:44-53,121-129,159-168`。
- ❌ 新测试只验证 key 出现在账号元数据列表，没有 action/data 矩阵、reason/version 或 before/after 审计测试：`backend/tests/test_pools_api.py:381-400`。

### Blocker 10 — ❌ downgrade guard 仍有竞态、覆盖缺口，测试未证明不变性

- ✅ B1/B2/B3/CKD 以及新增 C1b guard 均已放到本 revision 首个 DDL 之前：`backend/alembic/versions/b1e3f7d9c2a5_maintenance_front_stock.py:117-131`、`backend/alembic/versions/e7b3d9f2c1a4_maintenance_ledger_import.py:324-340`、`backend/alembic/versions/c3b5d9e1f7a2_maintenance_no_return_rules.py:76-94`、`backend/alembic/versions/d1e3f5a7c2b9_maintenance_ckd_import.py:134-149`、`backend/alembic/versions/e9f2d4b7a1c6_maintenance_doc_imports.py:113-128`。
- ❌ 五个 guard 都未先取得 `ACCESS EXCLUSIVE` 锁；并发写可发生在检查之后、DROP 之前。仓库已有正确的先锁再检查模式：`backend/alembic/versions/a6c8d2e4f1b7_maintenance_project_manager_assignment.py:147-171`。
- ❌ B2 guard 只查 raw 四表，却会删除项目/合同新增 canonical 字段；CKD guard 只查 CKD raw 表，不查已派生 `source_type='ckd_shipment_line'` 流水：`backend/alembic/versions/e7b3d9f2c1a4_maintenance_ledger_import.py:324-340,427-435`、`backend/alembic/versions/d1e3f5a7c2b9_maintenance_ckd_import.py:134-160`。
- ❌ 参数化测试的 `revision` 未使用；失败后在 `finally` 先 upgrade head 并删除事实，才断言 `version != parent`，没有在失败点断言 revision、表、行数和内容 hash 不变：`backend/tests/test_maintenance_import_hardening.py:183-294`。

### Blocker 11 — ❌ 权限实现已对齐，但首轮指定的回填/授权回归证据缺失

- ✅ e7 迁移、Python 模板现均为 admin 开、boss/其他关闭；台账 action 已纳入 `HIGH_RISK_KEYS`：`backend/alembic/versions/e7b3d9f2c1a4_maintenance_ledger_import.py:281-320`、`backend/app/permissions.py:169-293,473-493`。
- ❌ 没有新增“迁移后角色模板与存量账号值”断言，也没有用 `action_maintenance_ledger_import` 验证非 admin grant/revoke 失败。现有委派测试只靶向 `page_accounts/action_account_manage`：`backend/tests/test_permission_center.py:420-446`；对新 key 的唯一断言只是元数据列表：`backend/tests/test_pools_api.py:381-400`。
- 结论：实现子项可标 `✅`，但按首轮明确的“迁移后角色/账号断言 + 高风险授权测试”关闭标准，整项仍为 `❌`。

### Blocker 12 — ❌ 未知成本仍会被旧成本伪装为完整估值

- ✅ 当前 stock 行本身缺成本时，API 总额会返回 `null`；无成本权限时也会脱敏：`backend/app/api/maintenance_front_stock.py:60-100`。
- ❌ 对已有已知成本的 stock 再导入一批成本未知 CKD 时，入账逻辑保留旧 `unit_cost`，随后用“新增后的全部 qty × 旧单价”估值；未知新批数量被猜成旧成本，API 会判为 complete：`backend/app/services/maintenance_front_stock.py:180-196,221-267`、`backend/app/services/maintenance_ckd_import.py:425-441`。
- ❌ `value_completeness` 只看 `complete_ex`；未税全而含税缺时，`total_value_inc_tax=null` 但状态仍是 `complete`：`backend/app/api/maintenance_front_stock.py:72-99`。
- ❌ 新增测试只覆盖“无权限”和“空库存为 0/complete”，没有已知+未知混合入库或税基单边缺失：`backend/tests/test_maintenance_import_hardening.py:160-180`。

### Blocker 13 — ❌ 上传安全门仍发生在完整 multipart spool 之后

- ✅ openpyxl 已移到线程池；ZIP 增加成员数、总解压量、压缩比和路径检查：`backend/app/services/import_safety.py:16-69`。
- ❌ ledger、CKD、doc 三个 API 均先 `await request.form()`，之后才对选中的单个 `UploadFile` 执行 `read_limited`：`backend/app/api/maintenance_ledger.py:105-150`、`backend/app/api/maintenance_ckd_import.py:45-96`、`backend/app/api/maintenance_doc_import.py:55-107`。
- WSL 当前 Starlette `1.3.1` 实测：`Request.form` 默认 `max_files=1000`，且 `MultiPartParser.on_part_data` 只对非文件字段应用 `max_part_size`；无 Content-Length/多文件 part 可在业务限额前完整 spool。
- ❌ `validate_xlsx_zip` 没有 sheet/行/列/单元格、公式、externalLinks、嵌入对象限制：`backend/app/services/import_safety.py:40-64`。测试只有 helper 级 ZIP bomb 与路径穿越；名为 member limit 的用例实际只写了一个 traversal member：`backend/tests/test_maintenance_import_hardening.py:27-42`。

### Blocker 14 — ❌ GET 门已补，但 ledger owner 身份不稳定且无 HTTP 验收

- ✅ ledger、CKD、doc GET 均增加 action、owner/admin/boss 与 `Cache-Control: no-store`：`backend/app/api/maintenance_ledger.py:197-220`、`backend/app/api/maintenance_ckd_import.py:159-181`、`backend/app/api/maintenance_doc_import.py:160-183`。
- ❌ ledger 把 owner 存为每次动态计算的 `显示名（username）`；上传后若账号 display_name 变化，apply/GET 重算出的 owner 与批次不相等，原上传者会被 403：`backend/app/api/maintenance_ledger.py:73-78,149-155,182-188,209-220`。
- ❌ 全仓测试没有 `ledger-imports/ckd-imports/doc-imports` 路由级用例，未验证 page-only、他人 action、owner、admin、真实/伪造 UUID 与 no-store 响应。首轮要求的枚举验收没有完成。

### Blocker 15 — ❌ ORM/迁移约束仍允许跨批关系和非法计数

- ✅ plan sequence 已统一为 `0..24`，canonical `amount_inc_tax` ORM CHECK 已补：`backend/app/models/maintenance_ledger.py:163-170`、`backend/app/models/maintenance_project.py:162-171`。
- ❌ ledger、CKD、doc batch 的计数字段仍只有 default/nullable，没有非负 CHECK：`backend/app/models/maintenance_ledger.py:52-63`、`backend/app/models/maintenance_ckd_import.py:45-53`、`backend/app/models/maintenance_doc_import.py:46-54`。
- ❌ CKD line 的 `batch_id` 与 `head_row_id` 仍是两个独立 FK，数据库允许 line 属于 batch B、head 属于 batch A：`backend/app/models/maintenance_ckd_import.py:128-168`、`backend/alembic/versions/d1e3f5a7c2b9_maintenance_ckd_import.py:96-128`。
- ❌ apply 的异常筛查按 `line.batch_id`，实际处理却只按 `head_row_id`；跨批异常行可绕过 batch A 的筛查并随 A 入账：`backend/app/services/maintenance_ckd_import.py:347-358,377-415`。doc head/line 复制了同一独立 FK 缺陷：`backend/app/models/maintenance_doc_import.py:115-145`。
- ❌ hardening 测试没有任何直接 SQL 违规插入/`IntegrityError` 断言：`backend/tests/test_maintenance_import_hardening.py:183-294`。

## 四、新发现 Blocker（本轮新增）

### P0-1：三单 `doc-imports` 可跨项目扣减前置库

- apply API 只校验 action 与批次所有者，没有传入 `allowed_project_ids` 或执行项目范围门：`backend/app/api/maintenance_doc_import.py:132-150`。
- service 可按 WBDD、XSDD，甚至禁止作为身份键的 `project_name → project_code` 回退解析任意项目，并直接执行 `return_out`：`backend/app/services/maintenance_doc_import.py:379-409,436-493`。
- 影响：被授予 `action_maintenance_doc_import` 的非全量范围账号，可以上传本人批次并扣减他项目前置库。仓库没有 403/零写测试。

### P0-2：三单 apply 无视 preview issues，并把负结存/未知 PN/坏件静默跳过后标 applied

- preview 生成 `issue_rows`，但 apply 完全不检查 batch/row issues；未知项目、无明细键、非法数量、未知 PN、负结存都只增加 `skipped_lines`，最后无条件 `status='applied'` + commit：`backend/app/services/maintenance_doc_import.py:302-374,412-505`。
- apply 只检查 `data_status`，不校验返库类别或已保存的 `test_result`；只要结存足够，坏件也可按“未用件收回”扣账：`backend/app/services/maintenance_doc_import.py:361-366,436-493`。
- 测试明确固化“负结存异常被跳过、批次成功返回”的行为：`backend/tests/test_maintenance_doc_import.py:197-223`。

### P1-1：`action_maintenance_doc_import` 缺数据库权限回填，DB 模板/UI/运行时再次漂移

- Python 已把该 key 加入 `ALL_KEYS/HIGH_RISK_KEYS/UI`，但 `e9f2d4b7a1c6` 迁移只有表 DDL，没有 `sys_role_template/sys_user` 回填：`backend/alembic/versions/e9f2d4b7a1c6_maintenance_doc_imports.py:17-139`。
- 角色模板 API 对缺键执行 normalize 后显示 false，而 admin 运行时 `effective_for_user` 从 `_full()` 得到 true：`backend/app/api/role_templates.py:112-119`、`backend/app/permissions.py:927-945`。迁移后的数据库快照、UI 展示与实际运行权限不一致。

### P2-1：分支新增格式回归

`git diff --check origin/main...d5fd376` 返回 2：

- `backend/app/models/maintenance_ckd_import.py:13` trailing whitespace；
- `backend/app/models/maintenance_doc_import.py:13` trailing whitespace；
- `backend/app/models/maintenance_ledger.py:14` trailing whitespace。

## 五、建议清单（不替代 Blocker）

1. 为三类 import 建统一的 fail-closed orchestration：preview 生成结构化错误码，apply 在任何 canonical 写入前校验全部 batch/head/line issues、身份、状态和 payload 冲突；禁止 service 内 `except ...: skipped_lines += 1` 吞掉业务冲突。
2. 用脱敏真实导出制作 fixture：CKD 166 列字段码/中文名双表头、两个真实仓库、91 天历史日期、字段别名冲突；三单覆盖返库类别、检测结果、未知状态与跨项目。
3. HTTP 测试至少覆盖 Admin/本项目负责人/无关账号，分别验证 preview/apply/GET 的 200/403/404/409、403 零写、owner 改显示名仍可读，以及 `Cache-Control: no-store`。
4. 并发与迁移测试使用两个独立数据库会话；downgrade 失败时立即断言 revision、表、行数、内容 hash，再做清理。不要在断言前 upgrade/删除证据。
5. 前置库成本需按批次/层保留 unknown，或用可证明的成本层模型聚合；不能在新入库成本缺失时把旧单价扩展到新增数量。

## 六、复跑测试记录

### 1. B1/B2/B3/C1a/C1b 与 hardening 相关切片

```bash
python -m pytest \
  tests/test_date_loose.py \
  tests/test_maintenance_ledger.py \
  tests/test_maintenance_front_stock.py \
  tests/test_maintenance_no_return_rules.py \
  tests/test_maintenance_bad_returns_logic.py \
  tests/test_maintenance_ckd_import.py \
  tests/test_maintenance_doc_import.py \
  tests/test_maintenance_import_hardening.py -q
```

```text
76 passed, 1 warning in 47.86s
```

### 2. 权限、冻结基线、单 head 与回款迁移守卫

```bash
python -m pytest \
  tests/test_permission_center.py \
  tests/test_pools_api.py \
  tests/test_spec_and_misc.py \
  tests/test_migration_pool_seq.py \
  tests/test_maintenance_collection_reminders_migration.py -q
```

```text
116 passed, 1 warning in 66.85s
```

### 3. manual-pool / Alembic 零漂移 pytest 守卫

```bash
python -m pytest tests/test_migration_manual_pool.py -q
```

```text
15 passed, 1 warning in 37.12s
```

warning 为既有 `SAWarning: Can't validate argument 'dialect_options'`；该组零漂移断言通过。

### 4. Beta 总闸

```bash
python -m pytest tests/test_maintenance_beta_gate.py -q
```

```text
6 passed, 1 warning in 32.65s
```

### 5. Alembic head

```bash
python -m alembic heads
```

```text
e9f2d4b7a1c6 (head)
```

### 6. 格式与工具环境

```bash
git diff --check origin/main...d5fd376
```

结果：退出码 `2`，三个 trailing whitespace，见 P2-1。

WSL 共享 venv 未安装 Ruff：`python: No module named ruff`，因此本报告不宣称 `ruff check/format --check` 通过，也不把工具缺失误报为代码失败。

## 七、合并与生产门

### PR 合并结论

**❌ 不可合并。** 必须关闭上述 15 项遗留 Blocker 与 3 项新增 P0/P1 Blocker，并用能够命中本报告反例的测试证明，而不是只保持现有绿色用例。

最低复验集合仍包括：44,756/39,607.08/13% 对账与 50,000 零写；B2/CKD/doc 混合好坏行整批零 canonical 写；真实 166 列与字段码优先；两库同 PN；91 天库龄；同来源异项目/日期/payload 409；双会话并发；三类账号项目范围；`no_return` HTTP 全链；五条 downgrade 锁定与失败点内容不变；DB 直接违规插入；multipart 多文件和 XLSX 结构资源门。

### 生产发布结论

**❌ 不具备生产或灰度条件。** 即使后续达到可合并，还必须另过生产只读副本核对、事实表/权限快照备份、失败关闭 downgrade 演练、真实账号 200/403 验收及单项目 canary。当前跨项目写库存、金额错算与部分成功路径均属于直接阻断项。
