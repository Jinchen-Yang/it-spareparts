# 维保模块重建分支第四轮独立代码审核报告

报告标识时间：2026-08-16 01:49（Asia/Shanghai，按用户指定文件名）

完成复核时间：2026-08-16 02:10（Asia/Shanghai）

最终结论：**❌ 不可合并；不可进入生产或灰度。**

本轮新增 F2 不能验收：两个新端点绕过申请 owner scope；草稿、打回和未审核行均可导出；多版本复提会漏掉前版已批准行；所谓“四列”实际输出五列，并绕过已有 Excel 公式转义；成本区间读取的也不是 C1 已落库的 CKD 成本事实。

C4 的 round-3 修复只给 API 增加了 `admin/boss + data_profit` 门。直接跨项目读取在实现层已收窄，但金额口径、多明细聚合、三源全集、批次/状态/版本选择和分页全部未改；范围内也没有任何 C4 HTTP 权限测试。因此 N5 未关闭，N6 只能记为“代码已修、验收未闭环”。

`2b0e729` 没有修改任何测试文件。按第三轮已经确定的“正确实现 + 命中指定反例的测试”关闭门，本轮触达的 round-3 Blocker **没有一项达到完整关闭条件**；未触达项继续沿用第三轮的未关闭结论。

## 一、审核范围（提交列表）

### 1. 固定基线与目标快照

- 已执行 `git fetch origin`。
- 基线：`origin/main = 4f8b6881a0ff367e73f8dc108ada137d3d2e2346`。
- 第三轮固定对象：`f6c208cc3d5c4e5c994783ae1fd47c3a2301ad28`。
- 本轮审核对象：`adc9910da493da4530b2df8b707cc9c92f4f73d9`；审核时本地 HEAD 与 `origin/feat/maintenance-ledger-import` 一致。
- 本轮范围：`f6c208c..adc9910`，共 22 个文件，2,794 行新增、27 行删除。
- 审核依据：`.ai/review/reviewer-prompt.md` 指定的五份业务文档，以及 `.ai/review/review-20260816-0126-round3.md` 的关闭门和未关闭项。

审核开始时工作树干净。并行只读复核期间，共享工作树出现了本轮 reviewer 未产生的未提交修改；截至报告定稿至少包括：

- tracked：`backend/app/api/maintenance_front_stock.py`、`backend/app/main.py`、`backend/app/models/maintenance_doc_import.py`、`backend/app/services/maintenance_bad_returns.py`、`backend/app/services/maintenance_doc_import.py`、`backend/app/services/maintenance_front_stock.py`、`backend/tests/conftest.py`、`backend/tests/test_maintenance_bad_returns_api.py`、`backend/tests/test_maintenance_bad_returns_logic.py`、`backend/tests/test_maintenance_doc_import.py`、`backend/tests/test_maintenance_front_stock.py`；
- untracked：`backend/alembic/versions/a7c3e5f9b2d1_maintenance_rkd_return_facts.py`、`backend/alembic/versions/b9d1e7c3f5a8_maintenance_bad_salvage.py`、`backend/app/api/maintenance_bad_salvage.py`、`backend/app/api/maintenance_recovery.py`、`backend/app/models/maintenance_bad_salvage.py`、`backend/app/services/maintenance_bad_salvage.py`、`backend/app/services/maintenance_recovery.py`、`backend/tests/test_maintenance_bad_salvage.py`、`backend/tests/test_maintenance_recovery.py`。

这些内容不属于 `adc9910`，本报告没有审入。所有代码行号均以 `git show adc9910:<path>` 固定；Linux 测试使用 `git archive adc9910 backend` 解包到一次性目录，没有同步或覆盖共享工作树。本轮唯一仓库写入是本报告。

### 2. 第三轮之后的提交（时间正序）

1. `2b0e729d903406b92afd1240f2c10e3a7ad73807` — `fix(maintenance): round-3 review — AI 脱敏/外部总闸、C4 范围门、成本单侧、并发 savepoint`
2. `cdb733e77e70188e2b25f105b10f71d8761560da` — `feat(maintenance): 补库购物车增强证据 + 四列采购清单导出 (F2)`
3. `adc9910da493da4530b2df8b707cc9c92f4f73d9` — `docs(maintenance): PRD 审阅草案、重建计划、工作簿模板设计、Codex 审核报告`

## 二、逐项结论（五维度）

| 维度 | 结论 | 决定性证据 |
|---|:---:|---|
| 业务口径一致性 | ❌ | F2 未执行“审核通过后、仅累计批准意向、精确四列”；CKD 成本证据读成旧 WBDD 派生价；C4 在正式金额列尚未锁定时，直接比较台账金额、BXD 第一条明细和正式费用行。 |
| 数据模型与迁移安全 | ❌ | 前置库 savepoint 在失败后才开启，不能隔离唯一冲突；`source_ref` 查询/模型允许 256 字符，但插入仍截为 128；AI accept 在 proposal 临界区内调用会自行 `commit()` 的 preview store，行锁提前释放。 |
| 安全与权限 | ❌ | F2 evidence/export 均绕过既有 `_application_scope()`，销售账号可按 UUID 读取他人 PN、数量、价格并下载；C3 旧台账第二行会被误当 header 原文送入外部 LLM。 |
| 测试质量 | ❌ | 相关固定快照测试虽 85/85 通过，但新增 F2 测试反而固定了“draft 可导出”；`2b0e729` 零测试变更；owner、状态、多版本、四列、公式注入、C4 HTTP/聚合、双事务与真实大文件反例均缺失。 |
| 回归 | ❌ | 新 F2 端点绕过已有 owner/approved/export 安全路径；C3 修复破坏 ledger header 选择；新增 XML parser 不完整且仍全量蓄积；本轮 `git diff --check` 还重新引入 3 处 trailing whitespace。组合回归中的 11 个失败为 reviewer-prompt 已知共享 DB 隔离问题，单独进程 15/15 通过，不另报业务缺陷。 |

迁移侧有两项正向证据，但不足以改变上述结论：`python -m alembic heads` 仍为单 head `f1a2b3c4d5e6`；`test_migration_manual_pool.py::test_alembic_check_no_drift` 在 pytest 隔离数据库内通过。

## 三、Blocker 清单（必须修，按严重性排序）

### 1. Round-3 修复闭环矩阵

| Round-3 项 | 本轮结论 | 决定性原因 |
|---|:---:|---|
| N1 C3 脱敏/外部总闸 | ❌ | 总闸和 sample masking 已加，但 ledger 会把业务数据行当 header 原文外发；proposal list 仍只有 page 门；正常文件仍可直接调用 AI。 |
| N2 alias 执行层 | ❌ | ledger 读取错 sheet/header，且合同、计划、费用没有完整应用 alias；多个源列映射同一 canonical 仍静默择一。 |
| N3 文件大小/同步执行 | ❌ | AI 仍限 16 MiB；读取仍整 sheet `list()`；async 路由中仍同步执行 LLM 和 trial parse；新“大文件”路径也会累计全部行。 |
| N4 accept 原子幂等 | ❌ | proposal 的 `FOR UPDATE` 锁在 `store_preview()` 内部提交时提前释放，不同 Idempotency-Key 仍可生成两个 batch。 |
| N5 C4 确定性对账 | ❌ | service 和测试相对 `f6c208c` 零改动；金额口径、聚合、三源全集、状态/批次/版本均未修。 |
| N6 C4 项目范围 | ❌（代码已修、验收缺失） | `admin/boss + data_profit` 门已收窄直接泄露路径，但没有任何 HTTP 权限矩阵测试，未达到第三轮关闭门。 |
| Blocker 2 fail-closed apply | ❌ | ledger expense issue 门只是部分修复；CKD 空日期和 orphan plan 仍能通过并写/标 applied；无反例测试。 |
| Blocker 6 前置库并发/幂等 | ❌ | savepoint 顺序错误；`source_ref` 仍截断；同来源唯一冲突仍无稳定 replay/409；无双事务测试。 |
| Blocker 8 CKD 日期/仓库 | ❌ | 非空坏日期已阻断，但空日期仍回退当前时间；仓库仍由项目 code 派生；无两仓/缺日期反例。 |
| Blocker 12 单侧成本 | ❌（代码已修、验收缺失） | 双侧 aggregate 置空逻辑正确，但没有 ex-only/inc-only 经 service→API 为 incomplete 的测试。 |
| 其余第三轮未触达项 | ❌ | 本轮无实现或验收证据，沿用第三轮报告，不因提交说明自动关闭。 |

### Blocker 1 — P0：F2 两个新端点均存在跨申请 owner IDOR

- 契约要求非管理员只能读取本人申请：`docs/maintenance/import-field-contract.md:237-244`；既有正确入口 `_application_scope()` 已实现该规则：`backend/app/services/replenishment.py:120-139`。
- evidence API 虽取得 `username, role`，却未传给 service：`backend/app/api/replenishment.py:500-525`；service 直接 `db.get()`：`backend/app/services/maintenance_replenishment_evidence.py:174-183`。
- purchase-list API 没有执行 scoped identity：`backend/app/api/replenishment.py:528-551`；service 同样直接 `db.get()`：`backend/app/services/maintenance_replenishment_evidence.py:186-191`。
- 反例：销售 B 具备 Beta page、create action 和 price-data 权限，只要获知销售 A 的 application UUID，即可读取 A 的 PN、数量、历史价格、成本区间并下载采购清单；预期应与既有详情接口一致返回 404。
- 新测试只直接调用 service：`backend/tests/test_maintenance_replenishment_evidence.py:139-161`，没有 owner/other-owner/admin 的 HTTP 反例。

### Blocker 2 — P0：F2 未限制“审核通过后导出”，并破坏多版本复提语义

- 权威口径要求审核通过后只导出 `PN + 数量 + 采购金额 + 销售金额`：`docs/maintenance/维保模块PRD-审阅草案.md:694`、`docs/superpowers/plans/2026-08-15-maintenance-rebuild-plan.md:47,89`。
- `export_purchase_list()` 不检查 application/review 状态：`backend/app/services/maintenance_replenishment_evidence.py:186-210`；它只取 latest version 的全部行：`:151-171`，不读取 `ReplenishmentReviewLine.decision`。
- 仓库已有累计批准实现 `_approved_lines()`：`backend/app/services/replenishment.py:1123-1154`；既有 WBDD 导出还明确检查 `app.status == "approved"`：`:1157-1175`，新端点均未复用。
- 反例一：`draft`、`submitted`、`needs_revision` 均可下载。反例二：v1 中 A approved、B rejected，v2 修正 B 后 approved，正确结果是 A+B；当前只导出 v2 的 B。尚未复提时，当前又会把 v1 被拒的 B 一并导出。
- 新测试用 `application.status="draft"`、`version.status="draft"` 建数：`backend/tests/test_maintenance_replenishment_evidence.py:79-95`，随后断言导出成功：`:151-161`，把错误状态固化成绿色测试。

### Blocker 3 — P0：C4 金额口径、聚合和三源全集均未实现

- 正式计入列和审批原值仍待确认，不同金额层级不得混用，未确认时不得生成经营结论：`docs/maintenance/import-field-contract.md:168-175,255,269,275-276`；生产基线也确认三口径、三来源并存：`docs/maintenance/data-baseline-2026-08.md:60-62`。
- 当前直接比较 ledger `row.amount`、BXD 第一条明细 `line.amount` 和单条 `FProjectExpense.amount`：`backend/app/services/maintenance_expense_reconcile.py:51-68`。
- BXD 每单只 `.first()`：`:51-57,88-99`；formal 用 `{bxd_no: row}` 静默覆盖同号多行：`:38-42`；ledger 同号多行既不聚合又重复输出：`:24-26,45-84`。
- 可复现反例：BXD 明细 400+600、ledger/formal 1000，当前取 400 错报 mismatch；BXD 明细 1000+500、ledger/formal 1000，当前又错报 matched，实际明细合计为 1500。
- 结果集只从 ledger 起步，再补 BXD：`:43-103`。formal-only 永不输出；BXD+formal 且无 ledger 仍标 `bxd_only`，并把 `formal_amount` 写成 `None`：`:85-101`。
- 三个查询不选择已成功批次、最新版本或无 issue 事实，formal 也不筛审批完成状态：`:24-42`。pending、failed、旧重传和作废/审批中费用均可能参与；响应没有 batch/provenance：`:69-83`。
- 现有测试创建默认 pending 的 ledger/doc batch：`backend/tests/test_maintenance_expense_reconcile.py:22-40`，却断言 `matched`：`:132-138`。`2b0e729` 对 C4 只改 API，service 和测试相对 `f6c208c` 均为零 diff。

### Blocker 4 — P0：C3 ledger “脱敏修复”仍可把整行业务原文当表头外发

- `_read_headers_and_samples()` 对所有 `doc_type` 固定读取首个 sheet 第 2 行：`backend/app/api/maintenance_ai_fallback.py:73-93`。
- 合法旧台账第 1 行是表头，第 2 行是订单号、人员、项目名和金额等业务数据；新模板首 sheet 又是 `00_使用说明`。ledger 自己的确定性 parser 明确选择业务 sheet 第 1 行：`backend/app/services/maintenance_ledger.py:139-151,223-252`。
- 误取的 headers 在 prompt 中原样写入：`backend/app/services/maintenance_ai_fallback.py:97-101`，并原样保存到 `header_snapshot`：`:235-247`；只有 sample value 被 mask。总闸开启时，旧模板形成原文外发，新模板则形成无效 alias。
- ledger 新格式虽接收 `column_aliases`，合同、计划和费用并未完整应用：`backend/app/services/maintenance_ledger.py:223-283`；`_validate_proposal()` 也不拒绝两个源列映射同一 canonical：`backend/app/services/maintenance_ai_fallback.py:110-125`，违反重复 canonical 必须 fail-closed 的契约：`docs/maintenance/import-field-contract.md:31-35`。
- 正常文件仍可直接走 `/ai-fallback/propose`，没有主 parser 失败凭据：`backend/app/api/maintenance_ai_fallback.py:96-150`；proposal list 仍只有 page 门，却返回全库 filename/hash/creator/batch：`:230-239`、`backend/app/services/maintenance_ai_fallback.py:315-335`。

### Blocker 5 — P0：AI accept 的行锁在关键区中途释放

- proposal 加锁位于 `backend/app/services/maintenance_ai_fallback.py:279-305`。
- ledger、CKD、doc 的 `store_preview()` 分别在 `backend/app/services/maintenance_ledger.py:479`、`backend/app/services/maintenance_ckd_import.py:323`、`backend/app/services/maintenance_doc_import.py:397` 自行 `commit()`；此时 proposal 仍是 pending，但行锁已释放。
- 两个不同 Idempotency-Key 可各自提交一个 batch，随后覆盖同一 `accepted_batch_id`；batch 已提交而 proposal 更新失败时还会留下孤立可 apply batch。API 又在加锁前预加载 proposal：`backend/app/api/maintenance_ai_fallback.py:176-183`，未要求刷新已缓存状态。
- 没有双 Session/barrier 测试证明“同 proposal 不同 key 恰好一个 batch”。

### Blocker 6 — P0：前置库 savepoint 与长 `source_ref` 修复不成立

- `_get_or_create_stock()` 在 `flush()` 已抛 `IntegrityError`、Session 已进入失败态之后，才调用 `begin_nested()`，随后又执行 Session 级 `rollback()`：`backend/app/services/maintenance_front_stock.py:116-130`。savepoint 必须包住风险 flush；当前顺序不能保留外层事务此前成功行。
- 输入和查询允许完整 256 字符：`:155-183`；模型/迁移也为 256：`backend/app/models/maintenance_front_stock.py:108-110`、`backend/alembic/versions/b1e3f7d9c2a5_maintenance_front_stock.py:59-60`；实际 insert 仍写 `source_ref[:128]`：`backend/app/services/maintenance_front_stock.py:212-230`。
- 129–256 字符引用首次会被截断，顺序重放用全长查询必 miss，再在唯一键处失败；共享前 128 字符的不同引用仍相互碰撞。
- 同来源的新 ledger 行也无法靠对不存在行的 `FOR UPDATE` 防并发，唯一冲突没有稳定 replay/409；现有测试只有短引用、单 Session 顺序重放。

### Blocker 7 — P0：CKD/ledger fail-closed 仍是局部修补

- ledger expense issue 现已加入 apply gate：`backend/app/services/maintenance_ledger.py:818-836`，这是正确的局部修复；orphan plan 仍只计 `skipped_rows` 后继续把批次标 applied：`:888-900`。
- CKD 只对“非空但无法解析”的日期增加 issue：`backend/app/services/maintenance_ckd_import.py:238-245`；空日期仍通过，在 apply 时下传 `None`：`:486-490`，前置库再回退到当前时间：`backend/app/services/maintenance_front_stock.py:200-209`。
- CKD apply 仍用项目 code 派生仓库：`backend/app/services/maintenance_ckd_import.py:349-351,458`，没有保留 Excel 仓库事实。
- `2b0e729` 没有新增测试，缺少“好行+坏行零写”、空日期、orphan plan、两仓同 PN 等反例。

### Blocker 8 — P1：F2 的 CKD 成本区间和有效事实选择均错误

- 基线指定成本区间来自发货单成本列：`docs/maintenance/data-baseline-2026-08.md:87-93`；C1 已将其落在 `MaintenanceCkdLineRow.unit_cost`：`backend/app/models/maintenance_ckd_import.py:126-162`。
- 新代码却查询旧 WBDD 事实 `FMaintenanceLine.unit_cost`：`backend/app/services/maintenance_replenishment_evidence.py:104-119`。查询没有 batch/status/date/order，只任取 500 行再求 min/max，超过 500 行时区间还会随执行计划变化。
- `_stats_payload()` 丢弃聚合器已有的 `min/max/latest`：`:141-148`，采购/销售侧实际也没有给出“区间”。
- 最近采购/销售日期没有有效状态或未来日期上限：`:46-55`；高频 WBDD 没有有效状态和 `order_date <= today`：`:89-102`；替代建议没有过滤 active pool、active part 和 excluded/merged member：`:72-87`。
- 反例：已取消的昨日采购会消除 365 天提醒；未来 60 件 WBDD 会制造“高频”；归档池的 excluded PN 会被推荐；CKD 成本 90/110、旧 WBDD 派生成本 300 时，当前声称 CKD 区间 300–300。
- 新测试只有 active/happy path，没有建立任何 CKD 成本事实，也不断言 min/max、取消/未来状态或不可用替代件。

### Blocker 9 — P1：“四列”实际为五列，且未接入 UI、未复用导出安全层

- 实际表头为 `PN/数量/采购金额/销售金额/备注` 五列，并把 `special_note` 原样写入：`backend/app/services/maintenance_replenishment_evidence.py:192-210`，违反 PRD 的精确四列契约。
- 既有导出会调用 `_excel_safe()` 防止 `= + - @` 公式注入：`backend/app/services/replenishment.py:1047-1073`；新导出绕过该层。若备注以 `=WEBSERVICE(...)` 开头，下载文件会形成公式单元格。
- 新测试只断言 `rows[0][:4]`：`backend/tests/test_maintenance_replenishment_evidence.py:151-161`，没有断言 `sheet.max_column == 4`，也没有公式转义反例。
- 前端 API 仍只定义旧 manual-review/WBDD 导出：`frontend/src/api/replenishment.ts:172-180`；页面只展示半年均价并提供旧两个按钮：`frontend/src/pages/ReplenishmentBetaPage.tsx:95-104,183-202,477-490,698-710`。全仓前端没有调用 `/evidence` 或 `/exports/purchase-list.xlsx`，业务用户无法完成“一键四列导出”。

### Blocker 10 — P1：C4 范围门只有实现，没有验收；查询还无界且 N+1

- `2b0e729` 在 `backend/app/api/maintenance_expense_reconcile.py:31-48` 增加 `ctx.role in ("admin", "boss")` 和 `data_profit` 门；普通项目角色的直接全库 IDOR 在代码层已收窄，这一方向与 PRD 的管理层全项目/Admin 全量口径相容。
- 但范围内没有任何 C4 测试修改。现有文件只有两条 service 测试：`backend/tests/test_maintenance_expense_reconcile.py:132-157`，完全没有 TestClient/HTTP 验收。
- 至少缺 anonymous、admin、boss、普通项目负责人/销售即使具备 page+data、`data_profit=false`、inactive/伪 token、`Cache-Control: no-store` 矩阵。按第三轮门槛，N6 不能关闭。
- API 无 limit/cursor：`backend/app/api/maintenance_expense_reconcile.py:21-56`；service 全量加载三源，再对每个命中 ledger/BXD-only 单独查 line：`backend/app/services/maintenance_expense_reconcile.py:24-42,51-57,85-92`。查询数随数据量增长为 `3 + ledger 命中数 + BXD-only 头数`。

### Blocker 11 — P1：新增“大文件流式解析”不兼容标准 XLSX，也没有真正限内存

- `stream_first_sheet_rows()` 直接按 worksheet XML 文件名排序取第一个，不读取 workbook relationships/visible sheet；不解析 `t="s"` 的 `sharedStrings.xml`；最后仍把所有行累计到 `rows_out`：`backend/app/services/import_safety.py:77-123`。
- 仓库已有正确处理 relationships、shared strings、inline strings 和类型的参考实现：`backend/app/services/maintenance_warehouse_adapters.py:556-663`。
- 该路径仅接到 CKD 大于 8 MiB 的分支：`backend/app/services/maintenance_ckd_import.py:110-137`；AI 仍限 16 MiB：`backend/app/api/maintenance_ai_fallback.py:25,131,205`。
- 对真实 CKD 样例做只读聚合探针：文件 57,289,738 bytes，首个 sheet XML 506,443,959 bytes，共 69,300 行、约 1,150 万物理单元格；没有单元格超过当前 4,096 字符阈值。因此该阈值不会剥离真实膨胀负载，而 `rows_out` 仍会蓄积数百万值。探针只输出计数，没有输出任何业务单元格内容。
- `2b0e729` 没有 sharedStrings、隐藏首 sheet、峰值内存、超时或并发测试。

## 四、建议清单（可修可不修）

1. F2 evidence 明确返回统一 `as_of` 和 `evidence_digest`；若证据参与自动审核，应随申请版本冻结。当前同一 version/content digest 会随事实回填而改变审核依据：`backend/app/services/maintenance_replenishment_evidence.py:39-40`。
2. 分开返回 purchase/sales age。当前只要一侧超过 365 天就置 `inactive_365d=true`，却用两侧现存日期的最小 age 填 `inactive_days`：`:56-70`；“无采购、昨天有销售”会同时显示 inactive 和 1 天。
3. 在五份契约中确认 `HIGH_FREQUENCY_THRESHOLD=50` 的来源，并补上计划要求的自动审批规则；当前常量只有代码注释：`:34-36`。
4. F2 按整单 part IDs 批量聚合。申请上限 200 行，当前逐行约 7–8 次 SQL，最坏约 1,400–1,600 次；增加 query-count 上限测试。
5. 金额计算全程使用 `Decimal`，并在表头/API 标明未税/含税及“估算”性质；当前用 `float` 相乘再 `round()`：`:197-208`。
6. 新 purchase-list 导出应像已有导出一样写 `ReplenishmentAuditEvent`，否则页面“导出行为已留痕”的提示与事实不一致。
7. C4 响应增加来源 batch、版本、状态、业务时间和可下钻引用；用 cursor/limit 分页，并用一次聚合查询构造三源 union。
8. 修复本轮文档提交重新引入的 trailing whitespace：`.ai/review/review-20260815-2313-B1-B2-B3-C1a.md:3`、`.ai/review/review-20260816-0024-round2.md:3-4`。
9. 将 `test_migration_manual_pool.py` 的数据库状态与相邻回归文件完全隔离，消除组合顺序相关的 11 个已知失败；在修复前继续保留“组合跑 + 独立进程复跑”两组证据，不把环境失败误算为业务失败。

## 五、复跑测试记录（命令 + 结果尾部）

测试均固定到 `adc9910`：先用 `git archive adc9910 backend` 解包到 WSL 的 `mktemp` 一次性目录，再在该目录执行；没有使用当前共享工作树。

### 1. F2、C4 与 round-3 修复相关定向回归

```bash
python -m pytest \
  tests/test_maintenance_replenishment_evidence.py \
  tests/test_replenishment_beta.py \
  tests/test_maintenance_expense_reconcile.py \
  tests/test_maintenance_ai_fallback.py \
  tests/test_maintenance_ckd_import.py \
  tests/test_maintenance_front_stock.py \
  tests/test_maintenance_import_hardening.py \
  tests/test_maintenance_ledger.py \
  tests/test_date_loose.py -q
```

结果尾部：

```text
........................................................................ [ 84%]
.............                                                            [100%]
85 passed, 1 warning in 41.72s
```

结论：现有测试全绿，但不覆盖上述决定性反例；其中 F2 测试还把 draft 导出当成正确行为，因此不能作为业务验收证据。

### 2. 权限、迁移与冻结基线组合回归

```bash
python -m pytest \
  tests/test_permission_center.py \
  tests/test_pools_api.py \
  tests/test_spec_and_misc.py \
  tests/test_migration_pool_seq.py \
  tests/test_maintenance_collection_reminders_migration.py \
  tests/test_migration_manual_pool.py \
  tests/test_maintenance_beta_gate.py -q
```

结果尾部：

```text
11 failed, 126 passed, 2 warnings in 71.30s
```

11 个失败均发生在 `test_migration_manual_pool.py`，原因与第三轮和 reviewer-prompt 已记录的一致：同一 pytest 进程内前序 collection milestone 事实残留，阻断 manual-pool 迁移。它们不是本轮业务代码新缺陷，但说明组合套件仍不能提供一次性全绿证据。

### 3. 已知环境用例独立进程复跑

```bash
python -m pytest tests/test_migration_manual_pool.py -q
```

结果尾部：

```text
...............                                                          [100%]
15 passed, 1 warning in 31.88s
```

### 4. Alembic 单 head 与漂移

```bash
python -m alembic heads
```

结果：

```text
f1a2b3c4d5e6 (head)
```

直接在 WSL shell 执行 `python -m alembic check` 时，当前环境的 `DATABASE_URL` 指向仅容器内可解析的主机 `db`，以主机名无法解析失败，未得到独立命令的有效业务结论；没有把它误报为代码缺陷。相同检查由隔离数据库测试 `test_migration_manual_pool.py::test_alembic_check_no_drift` 执行并通过，且该文件独立进程为 15/15 通过。

### 5. Diff 格式检查

```bash
git diff --check f6c208c..adc9910
```

结果（退出码非 0）：

```text
.ai/review/review-20260815-2313-B1-B2-B3-C1a.md:3: trailing whitespace.
.ai/review/review-20260816-0024-round2.md:3: trailing whitespace.
.ai/review/review-20260816-0024-round2.md:4: trailing whitespace.
```

`2b0e729^..2b0e729` 与 `cdb733e^..cdb733e` 分别执行 `git diff --check` 均通过；三处格式回归来自 `adc9910` 纳入的历史报告。

### 6. 合并前必须新增并通过的最小反例集

- F2 两端点 owner / other-owner / admin 的 200/404；draft/submitted/needs_revision 全拒绝；approved 才允许。
- 两版本“首版一条通过、一条打回，复提通过”最终只含累计批准意向；`max_column == 4`；公式型 PN/文本不成为公式。
- F2 已取消、未来、零/负数量事实不改变 inactivity/high-frequency；applied CKD 成本必须压过旧 WBDD 派生成本；archived/excluded PN 不得成为替代项。
- C4 明细 400+600 与 1000、1000+500 与 1000、formal-only、BXD+formal 无 ledger、重复批次、pending/failed/作废状态；每条断言确定状态和 provenance。
- C4 anonymous/admin/boss/普通项目角色即使有 page+data、data-profit false、inactive/伪 token、no-store HTTP 矩阵。
- C3 ledger 旧/新模板的真实 sheet/header、prompt/header_snapshot 无业务原文、重复 canonical fail-closed；同 proposal 不同 key 的双事务测试。
- 前置库同新 stock/同 source 的双事务、129–256 字符引用顺序重放、共享前 128 字符的不同引用；CKD 空日期/两仓；ex-only/inc-only 经 API 均为 incomplete。
- 大 XLSX sharedStrings、visible-sheet 选择、峰值内存上限与超时测试。
