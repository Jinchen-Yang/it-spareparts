# 维保模块重建分支独立代码审核报告

审核时间：2026-08-15 23:13（Asia/Shanghai）  
最终结论：**❌ 不可合并；更不可进入生产或灰度。**

本结论不是因为测试红，而是因为现有绿色测试未覆盖多个可直接造成合同金额错误、跨项目越权、库存错账、来源事实丢失的路径。

## 一、审核范围（提交列表）

### 1. 固定基线与快照

- 基线：`origin/main = 4f8b6881a0ff367e73f8dc108ada137d3d2e2346`。
- 审核 HEAD：`origin/feat/maintenance-ledger-import = HEAD = 0807b2f607093e224fc0aa7ea1e5f649e2666ca0`。
- 最终复核前已执行 `git fetch origin --prune`；比较范围始终为 `git diff origin/main...0807b2f`。
- 差异规模：`34 files changed, 4421 insertions(+), 23 deletions(-)`；`git diff --check` 为 0。
- 审核期间开发方继续在工作区编写下一切片 `e9f2d4b7a1c6_maintenance_doc_imports.py` 等未提交文件。本报告只取 Git 对象 `0807b2f`，不包含这些未提交 D/C1 后续文件。
- WSL 测试使用 `git archive 0807b2f backend` 导出的干净快照，没有把本地 dirty worktree 混入测试。

### 2. 提交列表（时间正序）

1. `88f4796` — `feat(maintenance): ledger workbook import models, parser and apply (B2)`
2. `384bbb8` — `fix(maintenance): accept 报销明细. prefixed expense headers in ledger`
3. `f5d8fbc` — `fix(maintenance): align ledger migration with frozen test baselines and permission registry`
4. `a4eb91a` — `feat(maintenance): project front-stock ledger (B1)`
5. `769337b` — `feat(maintenance): no-return rules — project default + line override + exemption audit (B3)`
6. `740b2ad` — `docs(ai): codex reviewer prompt for maintenance rebuild branch`
7. `0807b2f` — `feat(maintenance): 氚云发货单导入 + 维保供货入前置库 (C1a/F1)`

### 3. 口径优先级说明

`docs/maintenance/import-field-contract.md:59,83,252-259` 仍保留旧的“在途→现场收货、领用影响前置库、PN/SN”描述；本报告以后写入且被业务方明确确认的 `docs/maintenance/维保模块PRD-审阅草案.md:651-698` 与 `docs/superpowers/plans/2026-08-15-maintenance-rebuild-plan.md:37-51` 覆盖旧段：无现场收货、CKD 维保供货直接入前置库、领用不写账、SN 不要求、RKD 为返还分子。因此未把“没有现场收货/没有 SN 结存维度”误报为缺陷。

## 二、逐项结论

### 1. 业务口径一致性：❌

#### 已符合的方向

- ✅ B1 流水类型为 `shipment_in/purchase_in/return_out/salvage_out`，没有在途或现场收货状态：`backend/app/models/maintenance_front_stock.py:31-34`。
- ✅ 现场领用确认返回 `inventory_effect="none"`，没有写前置库账本：`backend/app/services/maintenance_project_operations.py:1302-1333`。
- ✅ 前置库身份无 SN；CKD 的 SN 仅作为 raw 可选证据：`backend/app/models/maintenance_front_stock.py:37-50,75-80`、`backend/app/models/maintenance_ckd_import.py:131-153`。
- ✅ CKD 只对“维保供货”调用 `shipment_in`，非维保类别不入前置库；项目通过 WBDD→active assignment、PN 通过 `pn_std` 精确关联，没有按人员或项目名猜：`backend/app/services/maintenance_ckd_import.py:279-298,323-365`。
- ✅ B3 已建立项目默认、行级覆盖、义务豁免来源三个字段：`backend/alembic/versions/c3b5d9e1f7a2_maintenance_no_return_rules.py:16-73`。

#### 业务 Blocker 证据

1. **台账含税金额链路断裂。** 正式模板列为 `订单金额(含税)`，且 44,756 应与销售单未税 39,607.08×1.13 对账：`docs/maintenance/workbook-template-design.md:94-100`。解析器只认 `订单金额`，并且只强制存在订单编号：`backend/app/services/maintenance_ledger.py:49-54,214-236`；正式 v1 模板的金额会静默成为 `NULL`。即使旧字段成功解析，服务仍把销售未税额写入 `contract_amount`、台账含税额写入 `amount_inc_tax`：`backend/app/services/maintenance_ledger.py:537-570`；既有读模型继续汇总 `contract_amount` 却标注为含税：`backend/app/services/maintenance_project.py:234-280`、`backend/app/services/maintenance_project_operations.py:322-329,5512-5521`。结果是 44,756 元合同按 39,607.08 元展示和计算。

2. **B2 有异常仍部分落正式事实。** preview 仅把问题放进 raw 行：`backend/app/services/maintenance_ledger.py:309-434`；apply 不检查 `batch.issue_rows`/行级 issues，缺项目名还会创建“未命名项目”，坏行被跳过后整批标 `applied`：`backend/app/services/maintenance_ledger.py:456-489,672-725`。测试反而固化了“先建项目、再跳合同”的部分写入：`backend/tests/test_maintenance_ledger.py:318-345`。计划日期错误又在 `issue_rows` 计数之后追加，批次少计：`backend/app/services/maintenance_ledger.py:379-384`；订单日期、终止日期、计划金额、报销金额的解析失败也没有完整异常。

3. **项目身份、期限、负责人和回款计划没有成为台账持续拥有的事实。** 模型明确声明展示名不是关系键：`backend/app/models/maintenance_project.py:26-34`；B2 却把项目名截前 64 字符作为 `project_code` 查询：`backend/app/services/maintenance_ledger.py:456-459`。生产基线已经证明项目名存在前缀、截断和漂移：`docs/maintenance/data-baseline-2026-08.md:53-59`。既有项目更新不改负责人，生命周期只允许 `missing→非 missing`：`backend/app/services/maintenance_ledger.py:490-518`；期限没有写项目详情实际读取的 `MaintenanceServicePeriod`。回款节点更新也不切换 `source/ledger_batch_id`、不处理已跟进节点的待复核状态：`backend/app/services/maintenance_ledger.py:613-669`，绕开已有受控 helper `backend/app/services/maintenance_collection_milestones.py:94-121`。

4. **B3 行级不返还值在创建链路中被静默丢弃，判定顺序也错误。** API 接收 `no_return`，创建指纹也包含它：`backend/app/api/maintenance_project_operations.py:158-163`、`backend/app/services/maintenance_project_operations.py:2082-2118`；但新建行 `2177-2196` 和 legacy 新建行 `2286-2301` 都没有赋值。patch 落库路径虽赋值，幂等指纹却遗漏该字段：`backend/app/services/maintenance_project_operations.py:1461-1477`。分类函数注释要求“行级→项目默认→品类”，实现却先判断 `category_id is None`，从而忽略显式行标记和项目默认：`backend/app/services/maintenance_bad_returns.py:58-105`。品类待判定的人工解析路径又没有写 `exemption_source`，在新 CHECK 下 flush 会失败：`backend/app/services/maintenance_bad_returns.py:1428-1445`。

5. **前置库金额把未知误报成 0。** 单行缺成本正确返回 `value_ex_tax=None`：`backend/app/services/maintenance_front_stock.py:182-195`；API 聚合过滤所有 `None` 后仍返回 `total_value_ex_tax=0`，且没有完整性状态或含税总额：`backend/app/api/maintenance_front_stock.py:29-42`。这违反“缺失不按 0”与前置库含税金额口径：`docs/maintenance/workbook-template-design.md:58-64`、计划 `:105`。

6. **CKD adapter 与真实契约不兼容，且会写错库存。** 实现丢弃字段码，只按中文名精确匹配：`backend/app/services/maintenance_ckd_import.py:76-80,113-133`；契约要求字段码优先、去 `(必填)`、受控别名与重复字段失败关闭：`docs/maintenance/import-field-contract.md:29-42`。真实 `备件明细.备件PN(必填)`/`成本单价(必填)` 无法匹配，而测试使用 3 个占位字段码和去后缀简化表头：`backend/tests/test_maintenance_ckd_import.py:29-42`。服务还固定 `warehouse_name=""`：`backend/app/services/maintenance_ckd_import.py:353-365`，把不同 Excel 前置库合并；只保存 `数据状态` 却不筛作废/未知状态：`backend/app/services/maintenance_ckd_import.py:184-220,323-372`；保存出库日期却不传账本，B1 又以导入当前时间计算库龄：`backend/app/services/maintenance_ckd_import.py:189,212-213,353-365`、`backend/app/services/maintenance_front_stock.py:125-148`。历史全量导入后“超 90 天”指标会接近全错。

7. **CKD 同样允许异常部分成功。** 未归属 WBDD、未知 PN、非法数量、`FrontStockError` 都只是 `skipped_lines += 1`，其他行继续写，最后无条件 `status="applied"` 并 commit：`backend/app/services/maintenance_ckd_import.py:301-372`。数量异常又在 issue 计数后才追加：`backend/app/services/maintenance_ckd_import.py:223-230`。两个 WBDD/销售订单别名列通过 `A or B` 静默择一，模型预留的 `wbdd_parts_raw/wbdd_parts_no` 没有填充：`backend/app/models/maintenance_ckd_import.py:91-105`、`backend/app/services/maintenance_ckd_import.py:190,205-218`。

#### 当前切片边界（不能当成已完成）

- ⚠️ 正式返还率尚未落地：`official_rate_pct` 固定为 `None`，分子仍来自旧坏件单 warehouse-confirmed 状态，文案仍称“分子待业务确认”：`backend/app/services/maintenance_bad_returns.py:127-163,840-875`。PRD 已在 `:691,697-698` 确认 RKD/（领用−不返还）。该工作虽可留给计划 F3/C1 后续，但当前分支不得宣称正式返还率可验收或可生产。

### 2. 数据模型与迁移安全：❌

- ✅ 干净 `0807b2f` 快照只有一个 Alembic head：`d1e3f5a7c2b9`；专用临时库从空库升级到 head 后 `alembic check` 输出 `No new upgrade operations detected.`。
- ✅ B1 对项目/库存/来源的常用查询已有索引；CKD raw 表有 batch/head/order 索引：`backend/alembic/versions/b1e3f7d9c2a5_maintenance_front_stock.py:98-112`、`backend/alembic/versions/d1e3f5a7c2b9_maintenance_ckd_import.py:49-56,88-89,125-126`。
- ❌ B2 plan sequence 的 ORM CHECK 为 `1..24`，迁移允许 `0..24`，service 也把非法值写成 0：`backend/app/models/maintenance_ledger.py:157-164`、`backend/alembic/versions/e7b3d9f2c1a4_maintenance_ledger_import.py:163-165`、`backend/app/services/maintenance_ledger.py:391`。`amount_inc_tax` 非负 CHECK 只在迁移 `e7...py:37-41`，ORM `backend/app/models/maintenance_project.py:145-169` 未声明。Alembic autogenerate 没有发现这些 CHECK 语义差异，不能据其绿色结论豁免。
- ❌ B1 幂等唯一键为 `(kind, source_type, source_ref, part_id)`，命中后不比较项目、仓库、数量、成本和 payload：`backend/app/models/maintenance_front_stock.py:134-141`、`backend/app/services/maintenance_front_stock.py:96-115`。`kind` 与可变 `part_id` 都在键内，允许同一来源改 kind/PN 后重复入账。
- ❌ B1 余额的查找、创建、读改写没有 `FOR UPDATE` 或版本 CAS：`backend/app/services/maintenance_front_stock.py:53-78,117-150`；并发来源可 lost update，使余额与流水和不一致。CKD batch apply 也只是普通 `db.get`：`backend/app/services/maintenance_ckd_import.py:301-307`。
- ❌ CKD source ref 为 `CKD号 + data_id/seq/物理行号`，缺稳定 ID/序号时用易漂移行号；重放返回旧流水仍被计成 applied：`backend/app/services/maintenance_ckd_import.py:353-366`。改 PN 会因 `part_id` 不同而二次入账；改数量/项目/仓库会静默保留旧错账。
- ❌ 四条新迁移都不满足“downgrade 可逆且不删历史事实”：B1 无条件删结存和流水 `b1...py:115-132`；B2 在删 `ledger_batch_id` 后重建旧 source CHECK，并继续删 raw/含税事实 `e7...py:309-350`；B3 对 line/project 豁免的非硬盘行恢复旧 CHECK 会失败，成功时也删业务决策字段 `c3...py:76-95`；CKD downgrade 无条件删 batch/head/line，但保留已派生的前置库账和余额，导致来源证据消失 `d1...py:129-140`。
- ❌ CKD raw 模型没有批内单号/稳定明细冲突约束；line 的 `batch_id` 与 `head_row_id` 是两个独立 FK，不能保证 head 属于同一 batch：`backend/alembic/versions/d1e3f5a7c2b9_maintenance_ckd_import.py:91-123`。

### 3. 安全与权限：❌

#### 已符合的部分

- ✅ 新 routers 由 `backend/app/main.py:161-176` 统一叠加 `require_maintenance_beta`，该门要求实名 SysUser；不把 `_real_operator` 的 fallback 单独误报为可利用实名绕过。
- ✅ preview/apply 均有 page + action 门；SQL 查询使用 SQLAlchemy 参数化表达式，未发现动态 SQL 注入。
- ✅ `action_maintenance_warehouse_manage` 已在 `HIGH_RISK_KEYS`，不误报缺失：`backend/app/permissions.py:461-479`。

#### 安全 Blocker 证据

1. **B1 前置库读接口存在 IDOR 和成本泄漏。** `GET /projects/stable/{project_id}/front-stock` 只有登录与 `page_maintenance`：`backend/app/api/maintenance_front_stock.py:15-44`；没有 `require_maintenance_project_access`，也没有成本数据权限门。任意有页面权限账号可枚举 project_id，读取 PN、未税/含税成本、估值、来源引用、原因和操作人：`backend/app/services/maintenance_front_stock.py:154-229`。

2. **CKD apply 可跨项目写库存。** 端点注入 `_ctx` 却没有使用：`backend/app/api/maintenance_ckd_import.py:100-111`；service 对文件里的任意 WBDD 解析项目后直接写账：`backend/app/services/maintenance_ckd_import.py:279-291,331-365`。对照既有仓库 apply 明确限制 `FULL_SCOPE_ROLES`：`backend/app/api/maintenance_warehouse.py:245-263`。因此被授予 warehouse action 的项目范围账号可以写其他项目前置库。

3. **CKD 复用权限键与该权限公开契约相反。** 新端点使用 `action_maintenance_warehouse_manage`：`backend/app/api/maintenance_ckd_import.py:17-20`；该权限元数据明确承诺“不会修改库存、成本或返还率”，且要求乐观锁和前后值审计：`backend/app/permissions.py:806-813`。CKD apply 实际修改库存，且没有 version/reason/独立审计事件：`backend/app/api/maintenance_ckd_import.py:100-118`、`backend/app/services/maintenance_ckd_import.py:301-372`。

4. **B2/CKD 的 HTTP Idempotency-Key 均为空门。** 两个 preview 端点仅校验长度，随后不存、不查、不绑定 payload：`backend/app/api/maintenance_ledger.py:128-142`、`backend/app/api/maintenance_ckd_import.py:72-87`；apply 端点也没有 key。相同 key/不同文件会创建新批次，无法满足安全重放与冲突检测。

5. **台账权限数据库回填与代码模板冲突。** e7 迁移把 admin、boss 默认 action=true：`backend/alembic/versions/e7b3d9f2c1a4_maintenance_ledger_import.py:236-278`；Python boss 模板明确 false：`backend/app/permissions.py:166-184`。迁移后的生产权限快照与测试 reseed/运行时回退不一致，也偏离“项目经理+指定成员账号配置”。该 action 被 UI 放入“高风险管理能力” `backend/app/permissions.py:512-516`，却不在 `HIGH_RISK_KEYS` `:461-479`，非 admin 账号管理员仍可授予/撤销。

6. **批次读取权限过宽。** 台账与 CKD 的 GET 批次端点只有 page 门，可读文件名、hash、上传人、应用人和报告：`backend/app/api/maintenance_ledger.py:174-201`、`backend/app/api/maintenance_ckd_import.py:121-147`；没有 action、所有者或项目范围门。

7. **上传限制在资源消耗之后生效。** CKD 接受 256 MiB：`backend/app/services/maintenance_ckd_import.py:34`；API 先无限制 `request.form()`，再整文件读入，随后同步 `openpyxl` 解压解析：`backend/app/api/maintenance_ckd_import.py:26-38,53-83`、`backend/app/services/maintenance_ckd_import.py:104-160`。B2 也在完整 multipart 解析后才检查文件内容大小：`backend/app/api/maintenance_ledger.py:95-139`。两者都没有 ZIP 解压总量、压缩比、成员数、sheet/行列/单元格、公式、外链限制，存在内存/磁盘/event-loop DoS 风险。

### 4. 测试质量：❌

- ✅ 新增切片测试存在且在 Linux 干净快照通过：B2/B4 27 例、B1 12 例、B3/既有返还逻辑 9 例、CKD 8 例，共 56 passed。
- ❌ B2、B1、CKD 新测试全部以 service 直调为主，没有覆盖 HTTP 权限、实名、Beta、Idempotency-Key、上传大小和跨项目越权。仓库内没有 front-stock/ledger-imports/ckd-imports 的完整 API 失败路径测试。
- ❌ `test_maintenance_ledger.py:65-73` 使用旧台账字段名，无法发现正式模板 `订单金额(含税)` 不兼容；`:318-345` 固化了异常部分写入。
- ❌ B3 新测试只测分类纯函数：`backend/tests/test_maintenance_no_return_rules.py:38-111`，没有 API→DB→confirm→obligation 链；`:103-110` 还固化了无品类时忽略项目默认的错误结果。
- ❌ CKD 测试的字段码只有 3 个占位值，表头没有 `(必填)`：`backend/tests/test_maintenance_ckd_import.py:29-42`；未覆盖真实 166 列、重复列、字段码/中文名冲突、作废状态、Excel 原生日期、负数/零数量、异常整批零写。
- ❌ `test_apply_idempotent_across_reimport` 把旧流水重放仍断言 `applied_lines==1`：`backend/tests/test_maintenance_ckd_import.py:195-214`，掩盖误报；`test_apply_rejects_duplicate_apply` 的测试行列位错，PN/数量实际为空，只证明空批次状态冲突：`:251-260`。
- ❌ 三个 B1/B2/B3 迁移及 CKD 迁移均没有“seed 新事实→downgrade 失败关闭且数据 hash 不变”的专属测试；通用 `alembic check` 不能验证数据语义和历史事实保护。
- ❌ 缺少同幂等键不同 payload、双事务并发 apply/余额、类别解析后 `exemption_source`、权限迁移后存量账号值的测试。

### 5. 回归：⚠️

- ✅ `git diff --check origin/main...0807b2f` 通过。
- ✅ head 唯一且为 `d1e3f5a7c2b9`；`c8e2a4f6b1d3` 仍在祖先链。
- ✅ 权限中心、pools、单 head、冻结迁移祖先、collection reminders 共 116 passed；manual-pool/`alembic check` 守卫单独 15 passed；Beta 总闸 6 passed。
- ✅ 专用临时库完整 `upgrade head → alembic check` 通过。
- ⚠️ `test_maintenance_beta_gate.py:252-266` 的模块白名单没有 `app.api.maintenance_ledger`、`maintenance_front_stock`、`maintenance_ckd_import`；本次 main 确实挂了总闸，但未来移除依赖时守卫不会发现这些 routers。
- ⚠️ 将 `test_maintenance_collection_reminders_migration.py` 与 `test_migration_manual_pool.py` 放在同一 pytest 进程时出现 11 failed；在 `origin/main@4f8b6881` 用同一组合也得到同样 11 failed，单独跑 manual-pool 则 15 passed。因此这是可复现的既有跨文件状态隔离/顺序问题，不计为本分支新增失败，也不用于豁免本报告 Blocker。
- ⚠️ 本轮没有宣称全量 `pytest -q` 通过；只复跑了相关切片和用户点名的冻结/迁移/权限回归门。

## 三、Blocker 清单（必须修，按严重性排序）

### P0 / Critical

1. **统一含税合同金额链并兼容正式模板。** 解析 `订单金额(含税)` 与验收/巡检新列；以 canonical 含税额驱动项目总额、回款进度和导出；按税率对账。通过标准：39,607.08 未税、13% 税率、44,756 含税全链展示 44,756；改为 50,000 时进入逐行异常且 canonical 零写。

2. **B2 与 CKD 均改为 fail-closed 原子 apply。** preview 完整生成 sheet/行/字段/原值/错误码；任何关键身份、日期、金额、状态、关联冲突均使整批业务表零写且不得标 `applied`。通过标准：一个有效行+一个坏行的混合文件，raw 可保留，project/contract/milestone/front_stock/ledger 新增均为 0。

3. **禁止按项目名建立正式身份，并补齐期限/负责人/计划唯一事实源。** 只能使用稳定来源 ID 或已审计绑定；无法唯一映射失败关闭。台账重导必须同步 `MaintenanceServicePeriod`、负责人和 lifecycle；回款节点走唯一受控 helper 并保留当前 ledger batch、并发版本与已跟进待复核状态。

4. **修复 B3 全链。** `no_return=True/False/None` 必须在所有 create/patch 路径落库并进入幂等指纹；判定顺序严格为行级→项目默认→品类→pending；category resolve 写 `exemption_source`。通过标准：HTTP→DB→confirm→obligation 矩阵全部绿色，同 key 改标记返回 409。

5. **封住前置库 IDOR 和 CKD 跨项目写。** front-stock GET 接入项目访问门及成本字段门；CKD 全量 apply 仅允许 FULL_SCOPE 实名账号，或逐项目 enforce scope。通过标准：Admin/有项目负责人/无项目账号为 200/本项目 200/他项目 403，403 时零写且无可推导成本。

6. **重做前置库/CKD 幂等与并发。** 稳定来源事件身份不能含可变 PN/kind；绑定完整 payload digest；同 payload 重放原结果，异 payload 409 或进入可审计冲销。batch 与 stock 使用行锁/CAS/advisory lock。通过标准：双会话并发只一笔事实，余额等于流水和；改 PN/数量/项目/库名不得静默吞或双记。

7. **按真实 166 列契约实现 CKD adapter。** 字段码优先、中文名规范化、重复映射失败关闭；分别保留别名原值并校验冲突；只允许已确认有效状态；无 data ID/序号不得以物理行号伪造稳定键。使用脱敏真实导出做 fixture。

8. **保存 CKD 的真实库名、来源类型和业务发生时间。** 不得写空仓库；CKD 不能伪装成 `f_maintenance_line`；流水必须可反查 raw line，并区分 source occurred_at 与 imported_at。通过标准：同项目同 PN 两个库产生两条余额；历史 91 天 CKD 显示 91 天而非 0 天。

9. **修正 CKD 权限能力契约。** 当前 `action_maintenance_warehouse_manage` 承诺不改库存，却实际改账。应新增默认 false、纳入 `HIGH_RISK_KEYS` 的专用库存导入动作，或经正式决策修改并迁移权限契约；写操作必须有 reason、乐观锁和前后值审计。

10. **所有新迁移保护历史事实。** B1/B2/B3/CKD downgrade 在任何新事实存在时必须在 DDL 前失败关闭，或提供可证明无损的逆向迁移；不得删 raw 后保留孤立派生账。通过标准：每条 revision 均有 `upgrade→seed→downgrade` 测试，失败时 revision、表、行数和内容 hash 全部不变。

### P1 / High

11. **修复台账权限回填与高风险授权漂移。** 数据库迁移、Python 模板、存量账号与“项目经理+指定成员”配置口径必须一致；`action_maintenance_ledger_import` 纳入 admin-only grant/revoke 保护，并有迁移后角色/账号断言。

12. **未知成本不能返回 0。** 前置库总额返回 `null/unknown` 或明确 completeness；CKD 原始成本在税基未确认前保持 `unknown`，不得猜税率；确认后校验 `unit_cost×qty=amount` 并形成含税/未税可追溯链。

13. **补上传资源安全门。** multipart 总体/单文件流式限额；ZIP 成员数、解压总量、压缩比、sheet/行列/单元格、公式、外链和嵌入对象检查；重解析移出 async event-loop。413/422/ZIP bomb/多 part 必须有测试。

14. **修复批次元数据读取权限。** GET batch 至少与导入 action、所有者或全项目范围绑定，并返回 `Cache-Control: no-store`；普通页面账号枚举 UUID 必须 403/404。

15. **对齐 ORM/迁移约束并补 DB 直测。** plan sequence、`amount_inc_tax`、CKD batch/head/line 同批身份、计数非负、业务键冲突需要模型/迁移一致；直接 SQL 违规插入必须失败。

## 四、建议清单（可修可不修）

1. CKD apply 把“新入账、完全重放、冲突、跳过”拆成独立计数；不要让幂等 no-op 增加 `applied_lines`。
2. 把 `maintenance_ledger`、`maintenance_front_stock`、`maintenance_ckd_import` 加入 `test_maintenance_beta_gate.py` 的自动扫描集合，最好取消手写模块白名单，直接扫描 `/api/maintenance/*` Beta routes。
3. 将 `action_maintenance_ledger_import` 加入 `ACTION_ADDITIONAL_PAGE_DEPENDENCIES`，避免账号中心保存运行时必被 Beta 总闸拒绝的孤岛组合：`backend/app/permissions.py:360-374`。
4. 修正 B3 两个测试名与传值相反的问题：`backend/tests/test_maintenance_no_return_rules.py:58-77`。
5. `test_zero_or_negative_qty_rejected` 当前只测 0，补负数：`backend/tests/test_maintenance_front_stock.py:166-169`。
6. 增加带真实 embedded image 的 57 MiB 级 CKD 脱敏 fixture，验证附件剥离与资源上限；当前 `bytes/>4096` 过滤方向正确，但没有真实回归：`backend/app/services/maintenance_ckd_import.py:83-94`。
7. 明确纵向回款计划是否仍限制 24 期；当前 model/service 均固定 `1..24`，纵向化后不应无文档继续继承横向列数限制。
8. 将“正式返还率 = RKD/(领用−不返还)”作为 C1/F3 合流的独立生产门，不要用旧坏件单试算字段冒充正式指标。

## 五、复跑测试记录（命令 + 结果尾部）

### 1. 干净快照准备

```bash
git archive 0807b2f backend | ssh wsl \
  'tar -x -C /home/cloudlay-3080/Workspaces/it-data-pm/review-0807b2f-j0GG1d --strip-components=1'
```

测试完成后已删除本次创建的 WSL 临时快照目录；未改动开发方 `it-spareparts-b2` 工作区代码。

### 2. B1/B2/B3/B4/C1a 相关切片

```bash
ssh wsl 'cd <clean-0807b2f>/backend && PYTHONDONTWRITEBYTECODE=1 \
  <shared-venv>/python -m pytest \
  tests/test_date_loose.py \
  tests/test_maintenance_ledger.py \
  tests/test_maintenance_front_stock.py \
  tests/test_maintenance_no_return_rules.py \
  tests/test_maintenance_bad_returns_logic.py \
  tests/test_maintenance_ckd_import.py -q'
```

结果尾部：

```text
........................................................ [100%]
56 passed in 19.34s
```

### 3. 权限、冻结基线、单 head 与回款迁移守卫

```bash
ssh wsl 'cd <clean-0807b2f>/backend && PYTHONDONTWRITEBYTECODE=1 \
  <shared-venv>/python -m pytest \
  tests/test_permission_center.py \
  tests/test_pools_api.py \
  tests/test_spec_and_misc.py \
  tests/test_migration_pool_seq.py \
  tests/test_maintenance_collection_reminders_migration.py -q'
```

结果尾部：

```text
116 passed, 1 warning in 56.16s
```

### 4. manual-pool 与 Alembic 零漂移 pytest 守卫

```bash
ssh wsl 'cd <clean-0807b2f>/backend && PYTHONDONTWRITEBYTECODE=1 \
  <shared-venv>/python -m pytest tests/test_migration_manual_pool.py -q'
```

结果尾部：

```text
15 passed, 1 warning in 28.62s
```

warning 为既有 `SAWarning: Can't validate argument 'dialect_options'`；零漂移断言通过。

### 5. Beta 总闸

```bash
ssh wsl 'cd <clean-0807b2f>/backend && PYTHONDONTWRITEBYTECODE=1 \
  <shared-venv>/python -m pytest tests/test_maintenance_beta_gate.py -q'
```

结果尾部：

```text
6 passed, 1 warning in 47.83s
```

### 6. Alembic head 与直接零漂移检查

```bash
<shared-venv>/python -m alembic heads
```

```text
d1e3f5a7c2b9 (head)
```

在测试隔离器创建的专用临时 PostgreSQL 数据库执行：

```text
alembic upgrade head
alembic check
```

结果尾部：

```text
Running upgrade c3b5d9e1f7a2 -> d1e3f5a7c2b9, maintenance CKD shipment import...
No new upgrade operations detected.
```

临时数据库在 `finally` 中按所有权/OID 校验后删除。

### 7. 11 个顺序失败的 main A/B 对照

分支与 main 都运行：

```bash
python -m pytest \
  tests/test_maintenance_collection_reminders_migration.py \
  tests/test_migration_manual_pool.py -q
```

`origin/main@4f8b6881` 结果尾部：

```text
11 failed, 30 passed, 1 warning in 38.33s
```

失败均为前一个文件留下的 `project_manager_xls_v1` milestone 触发 c8 downgrade 失败关闭；分支得到相同 11 项，manual-pool 独立运行 15 passed。结论：既有跨文件隔离/顺序基线问题，不计为本分支新增回归。

## 六、合并与生产门

### PR 合并条件

必须关闭第三节全部 15 个 Blocker，并新增对应失败测试后复跑：相关切片、权限/项目范围、真实模板与真实 CKD 契约、并发、四条 downgrade、单 head、`alembic check` 全绿；同一组合不得新增 main 基线之外的失败。仅让现有 56 例继续通过，不构成关闭条件。

### 生产发布条件

即使修复后达到“可合并”，也仍需另过生产门：

1. 在生产只读快照/副本上备份四类新事实表、合同金额和权限快照；记录行数与 hash。
2. 演练 `upgrade`、有事实时的失败关闭 downgrade、回滚 image/feature flag；不得以删除历史事实回滚。
3. 抽样核对至少：44,756/39,607.08 含税样例、两个不同前置库同 PN、历史 90/91 天 CKD、同明细异 payload、行级 no_return 三态。
4. 用 Admin、有项目负责人、无项目账号三类真实账号验收 200/403 边界；CKD 全量导入只允许正式批准的 FULL_SCOPE 账号。
5. 先单项目 canary；观察重复来源冲突数、部分成功数（必须为 0）、未知 PN/WBDD、金额差异、库存余额与流水和、权限 403/409、上传资源占用。
6. RKD 分子接入前不发布“正式返还率”；成本税基未确认前不展示为 0。

**最终状态：不可合并。**
