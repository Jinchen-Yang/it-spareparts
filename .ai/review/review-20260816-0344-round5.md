# 维保模块重建分支第五轮独立代码审核报告

报告标识时间：2026-08-16 03:44（Asia/Shanghai，按用户指定文件名）

完成复核时间：2026-08-16 04:15（Asia/Shanghai）

最终结论：**❌ 不可合并；不可进入生产或灰度。**

本轮固定对象为 `9adffd0f21b1017da28fc57a131732d9e9067ddb`。F3–F6 均有确定性业务或事务 Blocker；D1 新增工作簿导出还引入财务/成本越权、返还状态关联键错误、公式注入和错误回款聚合。Round-4 的 11 项 Blocker 中，只有 **3 项完整关闭**（F2 approved/累计批准、精确四列与公式转义、CKD/ledger 原 Blocker 的 fail-closed）；其余 **8 项仍为 ❌**，其中 AI accept、front-stock savepoint、C3、C4 和流式解析均不能由现有绿色测试证明安全。

此外，WSL 独立迁移门禁稳定失败：`maintenance_bad_salvage` 与 `maintenance_collection_evidence` 已由迁移建表，却未注册到 `Base.metadata`，`alembic check` 明确生成 `remove_table` 差异。这不是已知环境用例，而是当前目标 SHA 的真实迁移漂移。

## 一、审核范围（提交列表）

### 1. 固定基线、round-4 锚点与目标

- 已执行 `git fetch origin`；最终复核时远端未再前移。
- 生产基线：`origin/main = 4f8b6881a0ff367e73f8dc108ada137d3d2e2346`。
- 用户给定起点：`f6c208cc3d5c4e5c994783ae1fd47c3a2301ad28`。
- Round-4 报告审核对象/本轮实际锚点：`adc9910da493da4530b2df8b707cc9c92f4f73d9`，见 `.ai/review/review-20260816-0149-round4.md:17-24`。
- 本轮最终对象：`9adffd0f21b1017da28fc57a131732d9e9067ddb`；本地 HEAD 与 `origin/feat/maintenance-ledger-import` 一致。
- 实际新增范围：`adc9910..9adffd0`，48 个文件，5,086 行新增、509 行删除。
- `f6c208c..9adffd0` 的 log 还包含已由 round-4 审过的 `2b0e729/cdb733e/adc9910`；本报告只把它们作为关闭门基线，不重复冒充本轮新增。

### 2. Round-4 之后的提交（时间正序）

1. `d04f864faea88ee00105430584365f7034d028e3` — `feat(maintenance): F3-F6 切片 + round-4 审核修复`
2. `3d88ca8348f7738a7322e53bda7c23bf5b7a07ab` — `docs(maintenance): round-4 审核响应与计划勾稽`
3. `85930add21367430dd42a632bb571e4d331626be` — `perf(maintenance): 三单大文件流式解析（C5）— 入库单 51MB 485s→24.2s`
4. `662ad827ca94416e4231739cfc0fe8d50721553d` — `feat(maintenance): 项目工作簿 v3 导出 (D1) — 六 sheet + 隐藏技术 sheet + 颜色契约`
5. `9adffd0f21b1017da28fc57a131732d9e9067ddb` — `docs(maintenance): 旧工作簿下线评估 (D4)`

### 3. 工作树边界

审核期间开发者持续推送，目标先后从 `3d88ca8` 前移到 `85930ad`、`662ad82`、最终 `9adffd0`；本报告已纳入全部前移提交。定稿时共享工作树另有一处本 reviewer 未产生的未提交修改：

- `docs/superpowers/plans/2026-08-15-maintenance-rebuild-plan.md`

该修改不属于 `9adffd0`，没有审入，也没有被覆盖。所有代码和文档行号均以 `git show 9adffd0:<path>` 为固定对象。本轮唯一仓库写入是本报告。

## 二、逐项结论（五维度）

| 维度 | 结论 | 决定性证据 |
|---|:---:|---|
| 业务口径一致性 | ❌ | F3 枚举/跳行：`backend/app/services/maintenance_doc_import.py:597-690`；F5 变卖不减账本：`backend/app/services/maintenance_bad_salvage.py:101-164`；F6 仍需手工 handle：`backend/app/services/maintenance_collection_reminders.py:814-845`；C4 直接分类未确认金额：`backend/app/services/maintenance_expense_reconcile.py:125-160`；D1 回款/返还错误：`backend/app/services/maintenance_project_workbook_v3.py:127-187,242-335`。 |
| 数据模型与迁移安全 | ❌ | F5/F6 模型未注册：`backend/app/models/__init__.py:75-95,195-207`；front-stock savepoint 顺序：`backend/app/services/maintenance_front_stock.py:107-118`；F6 唯一键/落盘事务：`backend/alembic/versions/c3e9d1b7f5a2_maintenance_collection_evidence.py:55-60`、`backend/app/services/maintenance_collection_evidence.py:184-217`；downgrade 竞态：同迁移 `:69-88`。 |
| 安全与权限 | ❌ | F4/F5/D1 均绕过独立数据组：`backend/app/api/maintenance_recovery.py:16-37`、`backend/app/api/maintenance_bad_salvage.py:73-91`、`backend/app/api/maintenance_project_workbook_v3.py:20-47`；D1 未做 safe text：`backend/app/services/maintenance_project_workbook_v3.py:276-435`；C3 可能外发伪表头：`backend/app/api/maintenance_ai_fallback.py:73-103`。 |
| 测试质量 | ❌ | 最终重点集 166/166 通过，但迁移门禁失败位置为 `backend/tests/test_migration_manual_pool.py:252-257`；并发双事务、故障注入、F3 废品/错类别、F4/F5/D1 低权限、F6 上传即关闭、D1 多合同/返还状态、隐藏首 sheet 均无有效反例，现有代表性 happy path 见 `backend/tests/test_maintenance_project_workbook_v3.py:179-270`。 |
| 回归 | ❌ | metadata 注册回归：`backend/app/models/__init__.py:75-95,195-207`；C5 可见 sheet 选择错误：`backend/app/services/import_safety.py:92-124`；D1 未复用既有脱敏和 safe text：`backend/app/api/maintenance_project_workbook_v3.py:20-47`、`backend/app/services/maintenance_workbook_export.py:101-110`。 |

### 2.1 F3–F6 新切片逐项验收

| 切片/验收项 | 结论 | 证据 |
|---|:---:|---|
| F3：RKD 原始行落独立返还事实表、`source_ref` 有唯一约束 | ✅ | 模型/迁移保留批次、表头、项目、PN、数量和来源引用，并对 `source_ref` 唯一：`backend/alembic/versions/a7c3e5f9b2d1_maintenance_rkd_return_facts.py:17-63`。 |
| F3：RKD 返还分子识别 | ❌ | 契约确认检测结果为“成品/坏品/废品”：`docs/maintenance/import-field-contract.md:133-140`；实现只接收“坏品/坏件/故障”，漏掉合法“废品”，且不限制入库类别：`backend/app/services/maintenance_doc_import.py:597-635`。现有测试还把“其他入库+坏品”固定为返还事实：`backend/tests/test_maintenance_doc_import.py:288-321`，采购/其他入库坏品可误增分子。 |
| F3：失败关闭与事实完整性 | ❌ | 缺单号、项目无法归属会静默 `continue`，缺 PN 被写成“(无PN)”，缺业务日期写 `NULL`，批次最后仍标 `applied`：`backend/app/services/maintenance_doc_import.py:600-609,655-690`；违反关键身份/日期异常不得静默跳过的统一契约：`docs/maintenance/import-field-contract.md:31-42,272-280`。 |
| F3：官方返还率与就绪状态 | ❌ | 分子按 project 全量 SUM，不与 PN/领用义务匹配；没有 RKD 数据的项目又由 `defaultdict(0)` 发布为官方 0，而不是“未导入/不完整”：`backend/app/services/maintenance_bad_returns.py:874-913`。因此“领用 A 10 件、RKD B 10 件”可显示 100%，生产尚未导入 RKD 的项目会显示伪 0%。 |
| F3：超 90 天未领用 | ❌ | 已计算 `age_days` 却不参与判断；只要有结存且从未领用就立即 `stale_90d=True`：`backend/app/services/maintenance_front_stock.py:306-321`。测试把新入账、从未领用直接断言为超 90 天：`backend/tests/test_maintenance_front_stock.py:272-280`。 |
| F4：好件/坏件/结存分源展示 | ✅ | 好件来自 `return_out`，坏件来自 RKD，结存来自前置库余额，三类没有混为一个状态：`backend/app/services/maintenance_recovery.py:20-75`。API 也执行项目 scope：`backend/app/api/maintenance_recovery.py:16-37`。 |
| F4：可执行清单与成本权限 | ❌ | 好件清单只返回 `source_ref/qty/time/reason`，没有 PN、part_id、仓库，无法形成可核对清单；`remaining_stock` 又原样返回含单位成本/估值的 `balance_rows`：`backend/app/services/maintenance_recovery.py:39-55,74-75`。API 只有 page+项目范围，没有 `data_purchase_cost` 脱敏：`backend/app/api/maintenance_recovery.py:16-37`。 |
| F5：独立事实、软作废与基础约束 | ✅ | 独立表不伪造采购/销售单，数量/收入/版本/作废状态有约束，作废保留事实：`backend/app/models/maintenance_bad_salvage.py:32-95`、`backend/app/services/maintenance_bad_salvage.py:167-189`。 |
| F5：变卖对前置库影响 | ❌ | 权威口径要求变卖为前置库出账：`docs/maintenance/维保模块PRD-审阅草案.md:655-658`、`docs/superpowers/plans/2026-08-15-maintenance-rebuild-plan.md:60-62,87-93`；登记仅插入 `MaintenanceBadSalvage`，完全未写 `salvage_out` 流水：`backend/app/services/maintenance_bad_salvage.py:101-164`。模型注释甚至写成“不写前置库账本”：`backend/app/models/maintenance_bad_salvage.py:1-7`。 |
| F5：贡献毛利历史真实性 | ❌ | 登记和每次列表都按“当前最近一笔领用成本”重算：`backend/app/services/maintenance_bad_salvage.py:75-98,162-164,192-220`；后续新增领用会改写过去变卖毛利。`part_id` 与 `pn` 也未校验一致，可用 A 的 part_id 配 B 的 PN 取 A 成本。 |
| F5：权限、幂等与审计参数 | ❌ | 列表/登记/作废返回 `cost_basis_inc_tax/margin/total_margin`，但读端只有 page+项目 scope：`backend/app/api/maintenance_bad_salvage.py:73-91`、`backend/app/services/maintenance_bad_salvage.py:50-72,211-223`。幂等键可空，check-then-insert 对不存在行的 `FOR UPDATE` 不能串行，并发 loser 被统一转 409而非稳定 replay：`backend/app/api/maintenance_bad_salvage.py:29-40,118-125`、`backend/app/services/maintenance_bad_salvage.py:128-161`。作废 body 要求 `reason`，调用却丢弃：`backend/app/api/maintenance_bad_salvage.py:42-47,151-153`。 |
| F6：文件验证、顺序重放、独立目录与 yml | ✅ | 单线程顺序路径复用 20MB 附件验证，写独立目录、计算 md5/sha256 并生成每文件 yml：`backend/app/services/maintenance_collection_evidence.py:121-228`；顺序重放与文件/yml 测试覆盖：`backend/tests/test_maintenance_collection_evidence.py:80-130`。 |
| F6：上传即关闭提醒 | ❌ | 权威规则是“回款提醒关闭=上传凭证”：`docs/maintenance/维保模块PRD-审阅草案.md:684-698`。上传 API 只保存并 commit：`backend/app/api/maintenance_collection_evidence.py:62-108`；提醒仍需另调 `handle`：`backend/app/services/maintenance_collection_reminders.py:814-845`。测试明确固化“先上传，再第二次 POST handle”：`backend/tests/test_maintenance_collection_evidence.py:133-166`。 |
| F6：并发 md5 去重与文件/DB 原子性 | ❌ | service 先查 md5 再生成随机 file_id：`backend/app/services/maintenance_collection_evidence.py:143-183`；DB 只唯一约束 `(milestone_id,file_id)`，没有 `(milestone_id,md5)`：`backend/alembic/versions/c3e9d1b7f5a2_maintenance_collection_evidence.py:55-60`，双请求可同时 miss 后双写。DB flush 后先落 binary、再落 yml，API 最后才 commit，且 rollback 不清理文件：`backend/app/services/maintenance_collection_evidence.py:184-217`、`backend/app/api/maintenance_collection_evidence.py:92-128`；sidecar/commit 故障会留下孤儿或半套文件。 |

### 2.2 审核期间新增的 C5、D1、D4

| 项目 | 结论 | 证据 |
|---|:---:|---|
| C5：大文件惰性逐行消费 | ✅ | worksheet 行改为 generator，调用方不再把全部业务行累计进 `rows_out`：`backend/app/services/import_safety.py:138-168`；CKD/doc 流式与普通路径一致性测试存在：`backend/tests/test_maintenance_doc_import.py:469-496`。 |
| C5：首个可见 sheet/标准 relationships | ❌ | `workbook.xml.rels` 的 `Relationship` 属 package relationships namespace，实现却用 officeDocument namespace查找，必然回退 XML 文件名排序；循环也没跳过 `state=hidden`：`backend/app/services/import_safety.py:92-124`。最终 WSL 探针返回 `SECRET-HIDDEN`。现有“visible sheet”测试只建一个可见 sheet，没有隐藏首 sheet：`backend/tests/test_maintenance_doc_import.py:449-466`。 |
| C5：严格内存上界与 AI 大文件路径 | ❌ | shared strings 仍完整装入 list：`backend/app/services/import_safety.py:126-136`；AI endpoint 仍限 16MiB 并用 openpyxl 全表 `list()`：`backend/app/api/maintenance_ai_fallback.py:25,73-103,142-165,228-230`。因此 round-4 的“真实大文件/AI 路径”未完整闭环。 |
| D1：sheet 名、颜色与项目 scope | ✅ | 00–06 与 98/99 hidden sheet、灰/黄表头和项目 scope 已实现：`backend/app/services/maintenance_project_workbook_v3.py:252-436`、`backend/app/api/maintenance_project_workbook_v3.py:20-37`；happy-path 测试覆盖 sheet 名和颜色：`backend/tests/test_maintenance_project_workbook_v3.py:179-238`。 |
| D1：独立数据权限与缓存 | ❌ | 新路由只有 `page_maintenance`+项目 scope，随后丢掉 `ctx`：`backend/app/api/maintenance_project_workbook_v3.py:20-37`；工作簿直接写合同额、回款、报销额、前置库估值：`backend/app/services/maintenance_project_workbook_v3.py:273-334,371-409`，没有 `data_purchase_cost/data_profit` 门。`no-store` 写入被丢弃的注入 Response，真正返回的新 Response 不含该 header：`backend/app/api/maintenance_project_workbook_v3.py:23,38-47`。 |
| D1：06 返还状态关联 | ❌ | `_obligation_map` 返回 `issue_line_id -> obligation`，却把这些 issue_line_id 传给按 `obligation_id` 查询的 `_bad_return_map`：`backend/app/services/maintenance_project_workbook_v3.py:127-174`；之后又用真正 `obligation.obligation_id` 读取：`:176-187`。真实返还单因此仍显示“待返还/空返还单号”。测试没有 seed obligation/bad return：`backend/tests/test_maintenance_project_workbook_v3.py:137-176,222-226`。 |
| D1：合同、回款与缺失值口径 | ❌ | 合同缺金额以 0 参与总额：`backend/app/services/maintenance_project_workbook_v3.py:315-317`；累计回款取全项目按月份排序的最后一条，不筛 confirmed，也不按合同取各自最新后求和：`:242-248,318-320`。仓库已有正确的“有效合同+每合同最新 confirmed”参考：`backend/app/services/maintenance_project_operations.py:4493-4529`。成本/成本率/完整性提示固定空，缺失成本行却拿前置库余额统计：`backend/app/services/maintenance_project_workbook_v3.py:321-335`，违反模板的缺失不按 0：`docs/maintenance/workbook-template-design.md:60-64`。 |
| D1：安全 XLSX 与回传协议 | ❌ | 项目名、人员、PN、事由、凭证和备注原样写入 openpyxl：`backend/app/services/maintenance_project_workbook_v3.py:276-310,349-367,377-409,419-435`；未调用仓库已有 `safe_xlsx_text()`：`backend/app/services/maintenance_workbook_export.py:101-110`，`=WEBSERVICE(...)` 会成为公式。98/99 只有说明、版本、时间、项目编号，没有稳定实体 ID、base version、行令牌或签名：`backend/app/services/maintenance_project_workbook_v3.py:424-436`，不满足隐藏技术 sheet 契约：`docs/maintenance/workbook-template-design.md:18-25`。 |
| D1：规模门与“导出/导入完成” | ❌ | contracts/expenses/WBDD/回款/领用/返还/结存全部 `.all()/list()`，普通 `Workbook` 再复制到 `BytesIO`，无 as_of/行数/字节预检：`backend/app/services/maintenance_project_workbook_v3.py:80-174,233-253,438-440`。全仓 v3 只有 GET；说明却宣称整本上传：`:255-264`，计划 D1 的“导出/导入”尚未完成：`docs/superpowers/plans/2026-08-15-maintenance-rebuild-plan.md:73-77`。 |
| D4：条件式下线门槛 | ✅ | 文档明确要求 D3 apply、行令牌/版本协议完成并通过审核后，才 deprecated/下线；旧审计事实不删：`docs/maintenance/legacy-workbook-retirement.md:13-24`。 |
| D4：当前替代能力陈述 | ❌ | 文档称新模板覆盖项目四表全部业务列，并把概览成本/成本率留空记录成决定：`docs/maintenance/legacy-workbook-retirement.md:7-12,26-28`；D1 实际丢失 WBDD 已有描述/退货/SN/成本字段并存在上述口径、权限、协议错误：`backend/app/services/maintenance_project_workbook_v3.py:342-390,424-436`。在 D1/D3 未闭环前不能据此确认替代完成。 |

### 2.3 Round-4 11 项 Blocker 真实闭环矩阵

| # | Round-4 Blocker | 本轮结论 | 决定性证据 |
|---:|---|:---:|---|
| 1 | F2 owner IDOR | ❌（实现已修，端点验收未闭环） | API 已把 identity 传入 scope service：`backend/app/api/replenishment.py:500-560`，service 复用 `_application_scope`：`backend/app/services/maintenance_replenishment_evidence.py:46-52,201-227`；但新增反例只直接调用 service，没有 TestClient 验证 owner/other-owner/admin 的依赖链：`backend/tests/test_maintenance_replenishment_evidence.py:201-240`。按 round-4 明确的 HTTP 关闭门，仍不能记为真实闭环。 |
| 2 | F2 仅 approved + 跨版本累计批准 | ✅ | 两入口均 `_require_approved` 并使用 `_approved_lines`：`backend/app/services/maintenance_replenishment_evidence.py:55-61,201-227`；累计批准实现：`backend/app/services/replenishment.py:1123-1154`；草稿拒绝和两版本累计反例：`backend/tests/test_maintenance_replenishment_evidence.py:151-240,243-256`。 |
| 3 | C4 单号聚合、三源全集、状态/版本/口径 | ❌ | 三源并集与单次 SUM 已修，但所有历史/重传 `applied` 批次被一起累计，无 latest/version 去重：`backend/app/services/maintenance_expense_reconcile.py:58-123`；BXD 不筛 head/line issues，formal 不关联成功 `SysImportBatch`：`:94-143`。金额缺失会因默认 `Decimal(0)` 变为 0：`:61-63,95-97,126-140`。更根本的是契约仍禁止选择正式金额列/审批原值，却直接用 `amount_inc_tax` 和 BXD 明细生成 matched/mismatch：`:125-160`、`docs/maintenance/import-field-contract.md:168-176,261-280`。 |
| 4 | C3 ledger 表头泄漏、alias、重复 canonical、主解析门 | ❌ | 主解析成功拒绝门和 proposal owner scope 已修：`backend/app/api/maintenance_ai_fallback.py:150-165,254-271`。但未识别 sheet 时仍把首 sheet 首行当 ledger header 原文：`:80-103`，parser 失败不等于该行就是表头，prompt 又原样写 headers：`backend/app/services/maintenance_ai_fallback.py:98-103`。新模板 contract/plan/expense 完全未应用 `column_aliases`，旧 expense 也未应用：`backend/app/services/maintenance_ledger.py:193-220,223-283`；原 canonical+一个 alias 造成的执行层碰撞仍由 `_header_index` 静默取首列：`:98-108`。 |
| 5 | AI accept 原子幂等 | ❌ | session advisory lock 包住了逻辑，但三个 `store_preview()` 在 proposal 仍 pending 时先 commit：`backend/app/services/maintenance_ai_fallback.py:277-339`、`backend/app/services/maintenance_ledger.py:472-480`、`backend/app/services/maintenance_ckd_import.py:320-327`、`backend/app/services/maintenance_doc_import.py:393-410`。批次 commit 后、proposal commit 前故障仍留下孤儿。Engine 是 QueuePool：`backend/app/db.py:11-17`；commit 可归还物理连接，而 session-level lock 绑定连接，重读/解锁可能换连接并泄漏锁。测试只是同一 Session 顺序两次：`backend/tests/test_maintenance_ai_fallback.py:333-375`。 |
| 6 | front-stock savepoint/长 source_ref | ❌ | 长引用已修并有测试：`backend/tests/test_maintenance_front_stock.py:283-325`。但新 stock 已 `db.add()`，随后调用 `begin_nested()`：`backend/app/services/maintenance_front_stock.py:107-118`；SQLAlchemy `begin_nested()` 会先 flush pending state，唯一冲突发生在 savepoint 建立和 `try` 之前。现有测试只有单 Session 顺序重放，无双事务/barrier。 |
| 7 | CKD/ledger fail-closed apply | ✅ | CKD 空日期写 issue，apply 对 head/line issue 整批拒绝：`backend/app/services/maintenance_ckd_import.py:238-250,383-405`；ledger expense issue 与孤儿计划均整批拒绝：`backend/app/services/maintenance_ledger.py:768-879`；对应测试存在：`backend/tests/test_maintenance_ckd_import.py:300-313`、`backend/tests/test_maintenance_ledger.py:399-436`。本结论只关闭 round-4 的 CKD/ledger 项；F3 新引入的 fail-open 另列为新 Blocker。 |
| 8 | F2 CKD 成本区间与有效事实 | ❌ | 已改读 applied CKD unit_cost，但查询只连 batch，不连/过滤 CKD head 的类别、状态、日期和 issues：`backend/app/services/maintenance_replenishment_evidence.py:143-163`；任何 applied 批次里的销售出库、作废头或异常头成本均可进入区间。现有测试的 CKD head 只写 `order_no`，没有状态/类别也照样被采纳：`backend/tests/test_maintenance_replenishment_evidence.py:348-380`。采购/销售最近日期与高频 SUM 也没有正数量门：`backend/app/services/maintenance_replenishment_evidence.py:71-89,127-141`。 |
| 9 | F2 精确四列 + 公式转义 | ✅ | 表头和每行均严格四列，并对 PN 调用 `_excel_safe`：`backend/app/services/maintenance_replenishment_evidence.py:216-249`；测试断言 `max_column == 4`、跨版本累计及 `=WEBSERVICE` 被转义：`backend/tests/test_maintenance_replenishment_evidence.py:243-256`。 |
| 10 | C4 HTTP 矩阵、N+1、分页/有界查询 | ❌ | HTTP 权限矩阵和三趟查询已修：`backend/tests/test_maintenance_expense_reconcile.py:240-310`、`backend/app/services/maintenance_expense_reconcile.py:58-172`。但 API 的 `limit` 是 service 全量构造之后再切片：`backend/app/api/maintenance_expense_reconcile.py:21-58`；没有 cursor/offset，既不能取第 501 行，也没有限制 DB/内存工作量，因此 round-4 的有界查询项未闭环。 |
| 11 | 大文件流式解析 | ❌ | C5 已惰性 yield，但 relationship namespace/hidden sheet 选择仍错，sharedStrings 仍全量驻留，AI 路径仍是 16MiB+openpyxl 全表；证据见 `backend/app/services/import_safety.py:92-168`、`backend/app/api/maintenance_ai_fallback.py:25,73-103`。最终 WSL 隐藏-sheet 探针直接反证“第一个可见 sheet”。 |

## 三、Blocker 清单（必须修，按严重性排序）

### Blocker 1 — P0：F5/F6 模型未注册，Alembic 零漂移门禁真实失败

- Alembic 只导入 `app.models` 触发 metadata 注册：`backend/alembic/env.py:8-10,24-25`。
- `app/models/__init__.py` 已导入 doc/RKD 模型，却完全未导入 `maintenance_bad_salvage` 和 `maintenance_collection_evidence`：`backend/app/models/__init__.py:75-95,195-207`。
- 最终 WSL 独立运行 `test_migration_manual_pool.py`，`test_alembic_check_no_drift` 报 `remove_table maintenance_bad_salvage` 和 `remove_table maintenance_collection_evidence`。这是可直接阻断 CI/发布迁移审计的确定性回归。

### Blocker 2 — P0：F4、F5、D1 泄漏成本、利润或合同/回款数据

- F4 原样返回余额中的单位成本/估值，API 无成本数据门：`backend/app/services/maintenance_recovery.py:39-75`、`backend/app/api/maintenance_recovery.py:16-37`。
- F5 返回成本基准、逐笔毛利和汇总毛利，API 同样只有 page+项目范围：`backend/app/services/maintenance_bad_salvage.py:50-72,192-223`、`backend/app/api/maintenance_bad_salvage.py:73-91`。
- D1 导出合同额、累计回款、报销额、前置库估值，只检查 `page_maintenance`：`backend/app/api/maintenance_project_workbook_v3.py:20-37`、`backend/app/services/maintenance_project_workbook_v3.py:273-334,371-409`。权限中心明确把采购成本和利润拆成独立数据组：`backend/app/permissions.py:13-22,254-273`。

### Blocker 3 — P0：F3 官方返还率分子不可信且无“未就绪”状态

- 检测枚举/入库类别错误、缺关键身份静默跳过：`backend/app/services/maintenance_doc_import.py:597-690`。
- 返还分子按 project 无条件求和，未导入时发布 0：`backend/app/services/maintenance_bad_returns.py:874-913`。
- 新入库未领用件被立即标为超 90 天：`backend/app/services/maintenance_front_stock.py:306-321`。

必须先锁定 RKD 返还类别/检测枚举，按可追溯 PN/义务校验分子，并在全量 RKD 未完成前输出 `incomplete/not_ready`，不得发布伪 0%/100%。

### Blocker 4 — P0：F5 变卖没有减前置库，且毛利会被未来事实改写

- 变卖登记完全不写账本：`backend/app/services/maintenance_bad_salvage.py:101-164`，与 `docs/superpowers/plans/2026-08-15-maintenance-rebuild-plan.md:60-62,87-93` 冲突。
- 成本每次从当前最近领用重算：`backend/app/services/maintenance_bad_salvage.py:75-98,192-220`；没有冻结成本证据、来源行或算法版本。
- part_id/pn 不校验，数量也不绑定可变卖的坏件返还事实；任意正数可登记为变卖。

### Blocker 5 — P0：F6 “上传即关闭”、并发去重和文件/DB 原子性均未成立

- 上传后仍需独立 handle：`backend/app/api/maintenance_collection_evidence.py:62-108`、`backend/app/services/maintenance_collection_reminders.py:814-845`。
- check-then-insert 没有 `(milestone_id,md5)` DB 唯一键：`backend/app/services/maintenance_collection_evidence.py:143-187`、`backend/alembic/versions/c3e9d1b7f5a2_maintenance_collection_evidence.py:55-60`。
- binary/yml 在事务 commit 前落盘，失败无补偿清理：`backend/app/services/maintenance_collection_evidence.py:184-217`、`backend/app/api/maintenance_collection_evidence.py:92-128`。

### Blocker 6 — P0：D1 工作簿返还状态、回款聚合和缺失值口径错误

- 06 表把 issue_line_id 当 obligation_id 查返还，真实返还单无法关联：`backend/app/services/maintenance_project_workbook_v3.py:127-187`。
- 02 表只取项目最后一条任意状态回款，而不是每份有效合同最新 confirmed 后求和：`backend/app/services/maintenance_project_workbook_v3.py:242-248,315-335`。
- 合同缺金额被当 0，总额/进度可形成错误经营结论；项目成本、成本率和完整性提示又固定留空，违反 `docs/maintenance/workbook-template-design.md:60-64`。

### Blocker 7 — P0：C4 在口径未确认时生成经营结论，且重传批次会重复累计

- 所有 applied 历史批次按单号一起 SUM，无 latest/version/stable-row 去重：`backend/app/services/maintenance_expense_reconcile.py:58-123`。
- BXD 不筛异常行，formal 不校验导入批次；缺金额被初始化成 0：`:94-143`。
- 契约明确正式计入金额列和审批完成原值待确认，确认前不得任选一列或生成经营结论：`docs/maintenance/import-field-contract.md:168-176,261-280`；当前却输出 matched/mismatch：`backend/app/services/maintenance_expense_reconcile.py:125-160`。

### Blocker 8 — P0：AI accept 仍不是一个原子事务，session advisory lock 还有连接泄漏风险

- batch 在 proposal 仍 pending 时先 commit，proposal 更新在第二次 commit：`backend/app/services/maintenance_ai_fallback.py:277-339`。
- commit 后故障会留下孤儿 batch；重试换 key 可再建批次。session-level lock 属于物理 PostgreSQL connection，而 QueuePool 下 commit 可归还连接：`backend/app/db.py:11-17`；finally 可能在另一连接 unlock，原锁留在池内。
- 测试只有单 Session 顺序重放，无双事务/barrier 和 batch commit 后故障注入：`backend/tests/test_maintenance_ai_fallback.py:333-375`。

### Blocker 9 — P0：front-stock “savepoint 修复”仍把唯一冲突留在 savepoint 外

- `db.add(stock)` 后才调用 `db.begin_nested()`：`backend/app/services/maintenance_front_stock.py:107-118`。SQLAlchemy 2.0 建 nested transaction 前会无条件 flush pending state，所以唯一冲突发生在 `try` 和 savepoint 建立之前。
- 现有 129–256 字符测试只验证顺序重放：`backend/tests/test_maintenance_front_stock.py:283-325`；没有两个 Session 同时创建相同 stock/source 的反例。

### Blocker 10 — P0：C3 仍可能外发伪表头业务原文，且 ledger alias 执行层不完整

- 未识别 ledger sheet 时直接取第一张表首行，非 ledger 固定取第二行：`backend/app/api/maintenance_ai_fallback.py:73-103`；parser 失败不能证明该行是 header。
- headers 原文进入 prompt：`backend/app/services/maintenance_ai_fallback.py:98-103`。
- 新台账三 sheet 和旧项目成本 sheet 没有应用 alias：`backend/app/services/maintenance_ledger.py:193-220,223-283`；原 canonical+alias 重复仍可被 `_header_index` 静默择一：`:98-108`。

### Blocker 11 — P1：C5 仍会选中隐藏首 sheet，且 AI/sharedStrings 路径无严格内存界

- 错误 relationship namespace 和未检查 hidden state：`backend/app/services/import_safety.py:92-124`；WSL 反例返回 `SECRET-HIDDEN`。
- sharedStrings 全量 list：`:126-136`；AI 使用 16MiB 上限和 openpyxl 全表列表：`backend/app/api/maintenance_ai_fallback.py:25,73-103`。

### Blocker 12 — P1：F2 仍缺有效 CKD 成本头门和 owner HTTP 验收

- CKD 成本查询不关联 head 的类别/状态/日期/issues：`backend/app/services/maintenance_replenishment_evidence.py:143-163`；测试用无类别/状态 head 仍断言采纳：`backend/tests/test_maintenance_replenishment_evidence.py:348-380`。
- owner 修复只有 service 直接测试：`backend/tests/test_maintenance_replenishment_evidence.py:201-240`，没有真实依赖链的 owner/other-owner/admin HTTP 200/404。

### Blocker 13 — P1：三个新 downgrade guard 存在检查后并发写入再 DROP 的丢事实窗口

- F3/F5/F6 均先 `IF EXISTS`，随后才 drop，没有先锁表：`backend/alembic/versions/a7c3e5f9b2d1_maintenance_rkd_return_facts.py:66-85`、`backend/alembic/versions/b9d1e7c3f5a8_maintenance_bad_salvage.py:83-102`、`backend/alembic/versions/c3e9d1b7f5a2_maintenance_collection_evidence.py:69-88`。
- 参考的 B1 downgrade 会先 `ACCESS EXCLUSIVE` 锁表再检查：`backend/alembic/versions/b1e3f7d9c2a5_maintenance_front_stock.py:117-145`。当前三项存在“检查为空→并发事务提交新事实→DROP TABLE”窗口。

## 四、建议清单（可修可不修）

1. D1 不要另写一套财务投影。复用已有项目 workspace 中“有效合同 + 每合同最新 confirmed 回款 + as_of + 数据权限”的查询：`backend/app/services/maintenance_project_operations.py:4493-4529`，并复用统一 field visibility。
2. 所有 XLSX 动态文本统一走 `safe_xlsx_text()`，并把 `Cache-Control: no-store`、`X-Content-Type-Options: nosniff` 写在真正返回的 Response；正确参考：`backend/app/services/maintenance_workbook_export.py:101-110`、`backend/app/api/maintenance_project_workbooks.py:240-250`。
3. D1 导出先做分 sheet COUNT/字节预检；超过阈值失败关闭或改 write-only/流式导出。隐藏 sheet 至少包含 export_id、project_id、scope/as_of、entity_id、base_version、row_token、client_row_id 和 HMAC，D3 再开放回填。
4. F5 登记应绑定具体 RKD/义务行及可售剩余数量，事务内同时追加 `salvage_out`，并冻结 cost basis、来源行、税口径和算法版本；作废时用反向流水恢复，不改写历史流水。
5. F6 用 DB 唯一约束实现 `(milestone_id, md5)` 并发去重；文件采用 staging→DB commit→原子 promote/outbox，失败有可重放清理任务。上传与 reminder handled 状态应在同一业务事务或可恢复状态机中完成。
6. C4 在财务确认状态/正式金额列前，只输出三源证据和 `unresolved_basis`，不要输出 matched/mismatch；确认后再以稳定行身份/批次版本去重，不以“所有 applied 批次求和”代替版本选择。
7. front-stock 并发测试使用两个真实 PostgreSQL Session + barrier；AI accept 测试加入两个 Session、不同 Idempotency-Key 以及 store commit 后故障注入。
8. D4 把“新模板覆盖全部业务列”和“成本率留空”改成显式未完成差异表；旧端点下线门除 D3 外还应包括权限矩阵、业务逐列 parity、最大规模和回滚演练。

## 五、复跑测试记录（命令 + 结果尾部）

所有最终测试均在固定对象 `9adffd0` 上执行。`9adffd0` 相对 `662ad82` 只有 D4 文档；backend 内容 SHA 与 `662ad82` 相同。

### 1. 按 reviewer-prompt 原样同步最新 backend 到 WSL

```bash
rsync -az \
  --exclude .git \
  --exclude node_modules \
  --exclude .venv \
  --exclude __pycache__ \
  /Users/yangjinchen/Code/IT_data/backend \
  wsl:~/Workspaces/it-data-pm/it-spareparts-b2/
```

同步完成后，所有下列命令均从 `~/Workspaces/it-data-pm/it-spareparts-b2/backend` 执行。

### 2. F2、F3–F6、round-4 修复、C5 与 D1 定向回归

```bash
.venv/bin/pytest \
  tests/test_maintenance_project_workbook_v3.py \
  tests/test_maintenance_bad_returns_api.py \
  tests/test_maintenance_bad_returns_logic.py \
  tests/test_maintenance_recovery.py \
  tests/test_maintenance_bad_salvage.py \
  tests/test_maintenance_collection_evidence.py \
  tests/test_maintenance_collection_reminders_api.py \
  tests/test_maintenance_replenishment_evidence.py \
  tests/test_maintenance_expense_reconcile.py \
  tests/test_maintenance_ai_fallback.py \
  tests/test_maintenance_front_stock.py \
  tests/test_maintenance_ckd_import.py \
  tests/test_maintenance_doc_import.py \
  tests/test_maintenance_ledger.py \
  tests/test_maintenance_import_hardening.py \
  tests/test_date_loose.py -q
```

结果尾部：

```text
......................                                                   [100%]
166 passed, 1 warning in 68.80s (0:01:08)
```

结论：现有 happy-path/顺序测试全绿，但没有覆盖本报告中的并发、权限、错误关联、隐藏 sheet 和故障窗口，不能据此关闭 Blocker。

### 3. 迁移独立门禁

```bash
.venv/bin/pytest tests/test_migration_manual_pool.py -q
```

结果尾部：

```text
............F..                                                          [100%]
FAILED tests/test_migration_manual_pool.py::test_alembic_check_no_drift
1 failed, 14 passed, 1 warning in 23.38s
```

核心异常：

```text
AutogenerateDiffsDetected: New upgrade operations detected:
('remove_table', 'maintenance_collection_evidence')
('remove_table', 'maintenance_bad_salvage')
```

结论：这是模型注册缺失导致的真实目标-SHA 漂移，不属于 reviewer-prompt 所述已知 11 个环境失败。

### 4. 权限、迁移与冻结基线组合回归

```bash
.venv/bin/pytest \
  tests/test_permission_center.py \
  tests/test_pools_api.py \
  tests/test_spec_and_misc.py \
  tests/test_migration_pool_seq.py \
  tests/test_maintenance_collection_reminders_migration.py \
  tests/test_migration_manual_pool.py \
  tests/test_maintenance_beta_gate.py -q
```

结果尾部：

```text
11 failed, 126 passed, 2 warnings in 51.00s
```

这 11 个组合失败仍是 round-4 已记录的共享 DB 状态问题：前序 collection milestone 事实触发 `c8e2a4f6b1d3` downgrade guard，均落在 `test_migration_manual_pool.py`。它们不另计为本轮业务缺陷。与之不同，上一节独立进程已越过该环境问题，并单独暴露 F5/F6 的 Alembic 漂移。

### 5. Alembic 单 head

```bash
.venv/bin/python -m alembic heads
```

结果：

```text
c3e9d1b7f5a2 (head)
```

结论：迁移链保持单 head；单 head 不等于 metadata 零漂移，独立 `alembic check` 仍失败。

### 6. 三个最小反例探针

在 WSL 同一最新 backend 上用纯内存合成数据执行：

```text
stream_first= SECRET-HIDDEN
formula_type= f
returned_cache_control= None
```

- `stream_first`：首 sheet hidden、第二 sheet visible，流式函数错误返回隐藏 sheet 内容。
- `formula_type=f`：openpyxl 会把未转义 `=WEBSERVICE(...)` 写成公式，证明 D1 必须使用 safe text helper。
- `returned_cache_control=None`：D1 修改的是未返回的注入 Response，真实下载响应没有 `no-store`。

### 7. Diff 格式检查

```bash
git diff --check adc9910..9adffd0
```

结果：退出码 0、无输出。

### 8. 合并前最小必补反例集

- F3：每种 RKD 入库类别 × 成品/坏品/废品；缺单号/PN/日期/项目；跨 PN/义务错配；未导入 RKD 返回 `not_ready`；新入库未领用不算 90 天。
- F4/F5/D1：`data_purchase_cost/data_profit` 独立开关、项目 owner/other-owner/admin 的 HTTP 矩阵，数值与汇总均不得侧漏。
- F5：变卖与 `salvage_out` 同事务；可售数量上限；part_id/pn 一致；冻结毛利；同 key 双事务稳定 replay；作废理由与反向流水。
- F6：上传即 handled；同 md5 双事务恰好一行/一个文件；binary/yml/DB 每个故障点的补偿与重放。
- C3/AI：未知 sheet 不外发数据行；新旧 ledger 三 sheet alias；original+alias duplicate fail-closed；accept 双事务及 batch commit 后故障。
- front-stock：两个 Session 同时创建相同 stock 和相同 source，外层事务此前写入不丢。
- C4：重复 applied 批次/重传版本、null 金额、head/line issues、failed SysImportBatch、formal-only；未确认口径只能 `unresolved_basis`；真实 cursor 分页。
- C5：隐藏首 sheet、非顺序 relationship target、sharedStrings 峰值内存、AI 大文件上限与超时。
- D1：多合同有效期/每合同最新 confirmed、void/unconfirmed 回款、真实 bad-return 关联、缺合同金额、公式/XML 控制字符、技术 token/HMAC、20k+ 行规模门、实际返回的 cache headers。
