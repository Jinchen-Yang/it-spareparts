# DEV-09A 验证记录

日期：2026-07-16  
基点：`origin/main@3f1a15eb4972a659068c9a69dc6f71ac3876deea`

## 自动化验证

- 后端全量：`973 passed, 2 skipped`
- 前端全量：`26 files, 224 passed`
- TypeScript：`tsc --noEmit` 通过
- 前端生产构建：`vite build` 通过
- 数据库迁移漂移：`alembic check` 返回 `No new upgrade operations detected.`

全量后端测试使用独立数据库 `spareparts_test_dev09`。共享测试库上曾出现其他并行任务造成的夹具干扰，未把该次结果当成业务失败或通过证据。

## 独立 SQL 对账

使用本地 staging 数据库中 365 天窗口内数据量最大的有效池（`group_id=9`），以独立 SQL 直接从采购/销售事实表计算，不复用应用服务统计函数。

| 侧别 | 加权均价 | 中位数 | 最低 | 最高 | 数量 | 订单数 | 行数 | 与 API |
|---|---:|---:|---:|---:|---:|---:|---:|---|
| 采购 | 1325.61 | 1230.00 | 500.00 | 11240.00 | 10108 | 549 | 556 | 七项一致 |
| 销售 | 1536.42 | 1592.92 | 707.96 | 21238.94 | 6001 | 1477 | 1508 | 七项一致 |

## 性能门槛

同一数据集对两个侧别各执行 30 次热请求：

| 侧别 | p50 | p95 | 最大值 | 800ms 门槛 |
|---|---:|---:|---:|---|
| 采购 | 22.9ms | 32.4ms | 34.9ms | 通过 |
| 销售 | 43.7ms | 61.1ms | 61.2ms | 通过 |

## 权限和数据质量

- 无 `data_pool_price_governance` 时，服务端结构化清空池统计、成员统计、约束、差额、最近原始记录和疑点数量；价格排序回退为 `pn/asc`。
- `open`、`source_changed` 样本进入正式统计并明确计数。
- `confirmed_source_error` 不进入正式统计，但最近原始记录保留并标成“确认源数据错误”。
- 池内所有有效成员始终返回；无样本成员显示“暂无正式参考样本”，不会从图表等价表消失。

## 尚未完成的人工门槛

当前子会话没有可用的应用内浏览器实例，因此没有把 1440px、768px、390px 的真实浏览器截图和键盘巡检冒充为已通过。合并前由主会话补做这三档真实浏览器验收；若发现问题，返修后重跑相应自动化测试。
