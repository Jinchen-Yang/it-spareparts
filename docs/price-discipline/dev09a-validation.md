# DEV-09A 验证记录

日期：2026-07-16
基点：`origin/main@3f1a15eb4972a659068c9a69dc6f71ac3876deea`

## 自动化验证

- 后端全量：`975 passed, 2 skipped`
- 前端全量：`26 files, 226 passed`
- TypeScript：`tsc --noEmit` 通过
- 前端生产构建：`vite build` 通过
- 数据库迁移漂移：`alembic check` 返回 `No new upgrade operations detected.`

全量后端测试使用独立数据库 `spareparts_test_dev09`。共享测试库上曾出现其他并行任务造成的夹具干扰，未把该次结果当成业务失败或通过证据。

## 独立 SQL 对账

使用本地 staging 数据库中 365 天窗口内数据量最大的有效池（`group_id=9`），以独立 SQL 直接从采购/销售事实表计算，不复用应用服务统计函数。

复审返修后于 2026-07-17 再次按 API 返回窗口 `2025-07-18 ~ 2026-07-17` 对账：

| 侧别 | 加权均价 | 中位数 | 最低 | 最高 | 数量 | 订单数 | 行数 | 与 API |
|---|---:|---:|---:|---:|---:|---:|---:|---|
| 采购 | 1328.69 | 1230.00 | 500.00 | 11240.00 | 10049 | 547 | 554 | 七项一致 |
| 销售 | 1542.09 | 1592.92 | 707.96 | 21238.94 | 5944 | 1474 | 1505 | 七项一致 |

## 性能门槛

同一数据集对两个侧别各执行 30 次热请求：

| 侧别 | p50 | p95 | 最大值 | 800ms 门槛 |
|---|---:|---:|---:|---|
| 采购 | 54.1ms | 72.7ms | 76.4ms | 通过 |
| 销售 | 88.9ms | 102.0ms | 102.6ms | 通过 |

## 权限和数据质量

- 无 `data_pool_price_governance` 时，服务端结构化清空池统计、成员统计、约束、差额、最近原始记录和疑点数量；价格排序回退为 `pn/asc`。
- `open`、`source_changed` 样本进入正式统计并明确计数。
- `confirmed_source_error` 不进入正式统计，但最近原始记录保留并标成“确认源数据错误”。
- 池内所有有效成员始终返回；无样本成员显示“暂无正式参考样本”，不会从图表等价表消失。
- `current_reference/latest_raw_record/quality_counts` 等独有派生容器已登记到
  `FIELD_GROUPS.pool_price_governance`；结构化净化之后再过递归脱敏第二防线。

## Standards 复审返修

- 自定义 HTML tooltip 对 PN、日期等动态文本统一使用 `escapeHtml`；恶意 PN 回归测试证明不会生成 `<img onerror>` 节点。
- 图表与等价表选择只保存 `part_id`，详情卡始终从当前 price-map 响应派生；响应更新后使用新统计，成员消失后清空选择。
- 图表不再打开旧池详情统计抽屉；固定详情卡只展示同源正式统计与原始追溯，进入型号使用稳定 `/parts?part_id=` 深链。
- 新增查询预算测试：池从 2 个成员增加到 20 个成员后，price-map SQL 数量保持不变且不超过 12 条。

## 尚未完成的人工门槛

当前子会话没有可用的应用内浏览器实例，因此没有把 1440px、768px、390px 的真实浏览器截图和键盘巡检冒充为已通过。合并前由主会话补做这三档真实浏览器验收；若发现问题，返修后重跑相应自动化测试。
