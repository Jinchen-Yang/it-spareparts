# 决策日志：维保整改八条口径

- 拍板日期：2026-09-02 ~ 09-03
- 来源：架构审计 + 负责人逐条确认
- 规则：这些口径是**唯一真值**。三个开发工具（zcode / codex / claude）开工前必读本文档；后续改动只能新增编号或 supersede，不能改历史条目。

---

## D-01 工作簿删行 = 作废

- **状态**：已拍板。
- **口径**：维保总表工作簿「删行」语义 = 作废，02 回款计划 / 03 备件明细 / 04 费用报销 / 05 实收回款 / 06 领用返还 五张表统一适用。作废的行不再进入任何统计与导出。
- **冲突**：`CONTEXT.md` 曾写「缺行不代表删除」，与代码实际执行相反，按本条目为准，`CONTEXT.md` 需同步修正。

## D-02 上传冲突：行级校验，不整本拒绝

- **状态**：已拍板。
- **口径**：上传只校验**用户改过的行**；未触碰行即使服务端已变也自动 rebase（不 409）。改同一行才冲突，冲突时**不阻止改动**，写入用户值，回执列明「覆盖了谁在何时的什么改动」，并落审计。
- **例外（唯一保留的拒绝）**：被作废的行，别人改了**不复活**——作废优先，回执提示「该行已被 xxx 作废，修改未生效」。

## D-03 全量修改权下放

- **状态**：已拍板，后续可再调。
- **口径**：项目负责人对**本人项目**工作簿全部字段可见可改（含成本与合同额）；销售限本人项目，成本与合同额仍受 `data_profit` 利润键控制。

## D-04 销售订单导入自动建项

- **状态**：已拍板。
- **口径**：保留「销售订单导入→建维保项目」能力，但从 `codex/sales-xsdd-auto-project`（233 文件 / 5.8 万行）中**切成独立小 PR 分批合入**，不整体合。

## D-05 维保项目唯一正规来源 = 销售订单导入

- **状态**：已拍板。
- **口径**：维保项目的正规来源只有一条——销售订单导入（按 XSDD 建项）。期限、显示名、负责人**直接从销售订单取**（见 `docs/reference/sales-order-columns.md`）。WBDD 需求单导入**只挂靠不建项**；手工建项入口封掉。销售订单上没有期限的，留给人工在项目信息编辑里填。

## D-06 订单编号格式

- **状态**：已拍板。
- **口径**：订单编号只认 `XSDD-8位日期-4位序号`（如 `XSDD-20260828-0120`）。非标准号**拒绝导入**并报错；数据库触发器不再对非标准号静默放行。上线前做一次存量核查。

## D-07 权限矩阵化

- **状态**：方案已定，待确认「仓库」岗位是否存在。
- **口径**：45 个权限键继续作底层真值，守卫层零改动；上面加「岗位 × 模块 × 看/改/钱」矩阵视图。基础岗位 6 个：管理员 / 老板 / 销售 / 采购 / 维保负责人 / 只读；可选 +仓库。

## D-08 生命周期词表统一

- **状态**：纯技术收敛，无需拍板。
- **口径**：项目生命周期统一用 `ongoing`（项目表现行词）；合同关系表的 `active`/`upcoming` 回填对齐。前端显示中文不变。

## D-09 报销「修复模式」删除侧：只在单合同、无排除行时执行

- **状态**：已拍板（2026-09-04）。
- **适用范围**：通用数据导入的报销页 `mode=upsert`（以本表为准）。**不改 D-01**——D-01 说的是维保总表工作簿的删行语义，另一条路径。
- **口径**：修复模式的「删除侧」（把本表未出现的旧报销行作废）只在**同时**满足两条时执行，否则整批不作废、退化为「覆盖同键、只增不删」，并在回执与预检里写明原因：
  1. 本表没有任何行因缺销售订单被排除（`missing_link = 0`）——有行被排除，本表就不完整，被排除行的旧行会落进别人贡献的合同范围里被误作废；
  2. 本表只触及**一个**销售订单——「以本表为准」的删除承诺只有在本表完整覆盖它触及的合同时才成立，文件自己证明不了；修复模式的设计形态是单合同工作簿报销页往返，多合同全公司导出触发合同级全量替换是实现副作用；
  3. 每张报销页都带**页级「销售订单」锚**（系统导出的项目工作簿报销页形态）——无锚的逐行表即使只写一个合同号也只是用户断言；不加这一条，把多合同导出按合同拆成多份单合同逐行表分次上传即可绕过第 2 条（对抗核验实跑证实）。
- **不做逐行豁免**：任何一次身份不匹配都不构成「旧行真的消失了」的证据（改过金额的内容签名对不上；旧行可能是源表还没有「数据ID」列的年代按内容键入库的）。
- **实现约束**：删除侧规则只写在 `backend/app/etl/expense_void.py` 一处，loader 执行、pipeline 门禁、precheck 预检都消费它——预演与执行不允许存在第二份实现。
- **依据**：2026-09-04 客户支付单事故；按回执提示过滤后重导实测作废 431 行、¥497,806.94 消失、批次报 success（PR #314 说明）。
- **代价**：多合同文件在修复模式下无法删旧行；要删请用对应项目的工作簿报销页逐个合同做。

## D-10 报销「修复模式」导入前作废预演：预演即承诺

- **状态**：已拍板（2026-09-04，用户选定顺序「先结构性规则 D-09，再预演」）。
- **适用范围**：通用数据导入 `mode=upsert`，且报销页会真的执行删除侧（D-09 三条全部满足：单合同、页级锚、无排除行）。抑制形态与非报销文件不适用，也不需要令牌。
- **口径**：预演不是描述，是**承诺**——要么真实导入的作废集合与预演**逐行一致**（同一批 raw_line_id、同状态、同金额、同合同号），要么这次导入根本不发生。三样东西兑现它：
  1. 作废判定只有一份实现（`expense_void.classify`），预演走无锁投影、导入走加锁实体，规则同源；
  2. 字节同一性：预演令牌把 `文件 sha256 + 作废指纹 + 合同` 用服务端 HMAC 绑在一起；提交时按**实际收到文件**的 sha256 认令牌，预演后改过文件即拒绝；
  3. 装载期指纹复核：loader 在无锁探针后与加锁重读后**各算一次**指纹，与令牌不符就在任何写入之前整批中止（`void_plan_drift`），连本表的同键覆盖也不落地，提示重新预演。
- **强制点在 HTTP 入口**：`/import/upload` 与 `/import/upload-batch` 对会作废的形态要求令牌（`void_preview_required`）；`run_import` 本身不强制，既有直接调用路径与用例不变，且两个公开入口都没有绕过预演的后门。
- **一批只允许一个会作废的报销页**：同批两个会作废的页，文件 #1 提交后文件 #2 的作废集合已变而两份预演都算自同一库态，逐文件独立事务之下不可能同时兑现；后端硬拒（`void_preview_multiple`），不交给前端判合同交集。
- **指纹刻意覆盖的范围**：作废行的 `(raw_line_id, 状态, 金额, 合同号)`、已作废集合、抑制原因、合同集合；**不**覆盖本表会同键覆盖的行——它们永远不在作废集合里，纳入只会制造无意义的拒绝。合同号必须在元组里：费用归集工作簿不取全局导入锁，能在探针→加锁重读的窗口里改合同号，该行仍会被作废，若指纹不看合同号则一声不响。
- **令牌 30 分钟过期**；过严拒绝的真实发生率上线后需观测。
- **已知残余**（本条不覆盖，如实记录）：探针→加锁重读窗口内被并发**插入**到该合同名下的新行本次不会被作废（不在 affected_ids 里），预演与实际一致，但「以本表为准」本身在这一点上是漏的；同键覆盖把 `data_status` 写成「已作废」这条第二通道不进 `expense_rows_voided`。
- **依据**：对抗核验（4 勘察 + 3 方案 + 2 证伪）与 2026-09-04 客户支付单事故；PR #313/#314/#315 的连续发现。

## D-11 报销幂等键形态冲突：原生数据ID接管旧键，同形态才是真重复

- **状态**：已拍板（2026-09-05，生产验收①发现，zcode 报告 + 对抗核验后落地）。
- **背景**：同一笔报销明细在不同导出视图下落成不同形态的幂等键（§17.4）：带「报销明细.数据ID」⇒ 原生 UUID 键；无数据ID有单号序号 ⇒ 复合键 `单号#序号@合同域hash`；两者皆无 ⇒ `EXP:` 内容键。批次 168 用旧视图落成复合键归因，客户新导出带数据ID，同一 (项目, 单号#序号) 出现两把键，归因去重守卫判为真重复整批 422——客户侧无解。
- **口径**（`maintenance_expense_integrity.duplicate_identity_verdict`，loader 归因守卫消费）：
  1. **接管**：既有归因是遗留形态、来行是原生键，且**同一合同域**（复合键的 `@hash` 正是为了让跨合同同名单号不互撞）⇒ 旧归因让位（删除 + 项目事实审计 `supersede`，before 含全部列、after 含接管行与差异）、旧键事实行软作废（导入覆盖审计）、随后为原生键建归因。旧键事实行**必须**作废：预算看板与合同工作簿按事实行汇总，只换归因会双计。
  2. **原生键权威**：既有原生、来行遗留 ⇒ 来行作废并跳过归因，不降级——否则月度大导出与手工薄来回传会让键形态反复横跳。**前提**：原生侧仍在计数（归因 approved、事实行生效、且不在本批缺行作废集合里）；否则退回整批拒绝——两把键都作废等于整条业务行从所有读模型消失且回执不报错。
  3. **真重复**：同形态不同键（如报销单退回重提换了数据ID）、跨合同域、或既有是手工独立归因（无事实行）⇒ 仍整批拒绝，422 详情与作业记录带冲突对（单号#序号、项目、两把键、原因）。
  4. **无序号内容键**（`expense_ref` 只有单号，与带序号原生行永不相等）：只在内容摘要（合同|日期|金额|事由|人员，与 transform 共用同一函数）逐字节相同时接管；对不上就回执 `unresolved`，不猜。
- **锁序**：先归因行 `FOR UPDATE`，flush 删除，再锁事实行——`autoflush=False` 下 delete 只是 pending，反过来会与按协议顺序加锁的费用归集工作簿 apply 成环（对抗核验实跑出 DeadlockDetected）。
- **回执**：`expense_rows_voided_by_identity` 单列（不进 `expense_rows_voided`，也不计入 D-09 抑制形态的「免于作废」），另有 `legacy_takeovers` / `legacy_skipped` / `legacy_unresolved` / `takeovers_amount_changed` / `legacy_siblings_active`（接管所在报销单仍有未覆盖的旧键行 ⇒ 本表可能只覆盖了该单一部分，提示核对）。不设金额门槛：源端更正金额是合法场景，等额重排又能绕过门槛；差异记审计、计数上报。
- **配套**：合同重算循环跳过「已作废且无归因」的事实行——否则接管留下的旧键行会让该项目此后所有合同修改/新建/归档被拒。
- **第三族：从未建归因的旧键孤儿行**（2026-09-05 验收③补充；`legacy_orphan_disposition` / `retire_orphan_raw`）。批次 168 时代有旧键事实行落库却没建成归因（当时归因失败），它们不在接管路径上（没有旧归因可让位），却仍以「已结束」生效：看板按事实行汇总双计，合同重算守卫把它当真重复而 400。口径：**旧键形态、无归因、其业务身份（同合同域 + 同单号#序号，或同内容摘要）已被仍在计数的原生键归因持有** ⇒ 带审计软作废（`SysAuditLog` `expense_identity/void`，`after.superseded_by` 指向原生行）。跳过而不作废等于永远留雷。两条路径都做：导入时原生行在归因前顺手清（回执 `expense_attribution_legacy_orphans_voided`，计入 `expense_rows_voided_by_identity`）；合同重算命中时就地清再继续，而不是 400。原生侧已作废/未 approved、跨合同域、既有归因不是原生键或无事实行支撑、孤儿本身是原生形态 ⇒ 仍按真重复拒绝。存量 3 条（`BXD-20260413-0019#1`、`BXD-20260312-0046#1`、`BXD-20260617-0026#1`，均 `@f02a8f6c`）由下一次客户原件跳过模式导入一次性清掉。
- **依据**：2026-09-05 生产验收①（`BXD-20260328-0004#1`：`…@f02a8f6c` × `1ab68a49-…`，项目 `5eee74ea-…`）；用客户原件回放：接管 1、看板单计、合同可改。

## D-12 需求单一键作废：不做两阶段复核，前端统一走 void-fast

- **状态**：已拍板（2026-08-19，用户拍板）。本条为补记——决策原记录在 `.ai/MAINT_V2_DELETE_LINK.md` §二 第 5、8 条，此前未进本文档。
- **口径**：
  1. **不做两阶段复核**（§二 第 5 条：「不做两阶段复核（甲方不在乎审核强度）」）。
  2. **一键作废统一走 `POST /maintenance/demands/void-fast`**（§二 第 8 条）：批量 `source_order_ids` + `reason`，单事务内完成快照校验 → 逻辑墓碑 → 挂靠停用，**跳过 7 秒 arm 窗口**；仍保留 `MAX_DELETE_HEADERS`=1000 / `MAX_DELETE_LINES`=20000 上限、版本 digest 冲突检测、**整批零删除**（任一单版本 / 归属 / 权限变化即整批不作废）、实名操作者（`_real_operator`）、访问日志与审计锚（同事务落一条 `status='executed'` 的 intent 作墓碑外键）。差异清单页与需求单列表的作废按钮统一走该端点。
  3. **两阶段 `delete-intents` API 保留不动、前端不用**：create / get / arm / execute / cancel 五个端点仍在，客户端函数仍在 `frontend/src/api/maintenanceDemands.ts`，但无任何页面调用。
  4. **恢复 = 管理员实名动作**：`POST /maintenance/demands/{source_order_id}/restore`，`require_admin` + `action_maintenance_demand_delete` + 实名操作者；恢复不复活挂靠，重挂走正常 assign 流程。
  5. **墓碑对稳定读模型的生效受 cutover 开关控制（记录事实，不断言生产取值）**：墓碑把 WBDD 从稳定读者（成本 / 导出 / 展示板 / 项目总表，都经 `active_demand_condition`）中剔除，以及恢复时清空旧成本证据、标记待重算，都只在 `maintenance_cutover_enabled` 打开时发生——开关关闭时 `active_demand_condition` 返回 `true()`（稳定读者照常消费该单），`restore_demand` 返回 `cost_state="stable_unchanged"`（不失效成本）；该开关默认 `false`（`backend/app/config.py`、`docker-compose.yml`、`.env.example`）。挂靠停用不受开关控制，作废即停用。
- **与既有文档的关系**：`docs/adr/0003-wbdd-logical-delete-tombstone.md` 第 3、4 节（服务端双确认、7 秒、HTTP 425）描述的是仍保留的两阶段 API，UI 层已被本条取代——ADR 顶部加状态说明、正文不改写；`CONTEXT.md`「维保需求单删除意图」词条按本条修正。
- **依据**：`.ai/MAINT_V2_DELETE_LINK.md` §二 第 5、8 条（2026-08-19 用户拍板）、§四 新接口 2；后端 PR #272、前端 PR #270（§六）；2026-09-06 代码核验：`backend/app/api/maintenance_demands.py`（`/void-fast`、`/delete-intents*`、`/{source_order_id}/restore`）、`backend/app/services/maintenance_demands.py` `void_fast` / `restore_demand` / `active_demand_condition`（`maintenance_cutover_enabled` 门）、`frontend/src/pages/maintenance/MaintenanceDemandsPage.tsx`（仅调 `voidFastMaintenanceDemands`）。

## D-13 验收开放给销售 / 项目经理 / 维保负责人，提交即生效，无独立审批

- **状态**：已拍板（2026-08-24 客户微信拍板；2026-08-25 补充去截止日门）。本条为补记。
- **口径**：
  1. **开放**：验收提交开放给销售（`sales`）、项目经理与维保负责人（`maintenance_manager` 角色）；模板默认带 `action_maintenance_acceptance_submit`，销售另开 `page_maintenance` + `own_maintenance_projects_only`（行键「自己销售 ∪ 自己负责」）。验收面板随维保页面渲染，不再挂 Beta 门。
  2. **免审批**：提交即生效——`submit_acceptance` 直接落 `approval_status=approved`（`approved_by`=提交人）；生效后仍可补附件 / 重新提交（版本乐观锁 + 幂等键不变）。独立审批环节、`review_acceptance` 服务与 `POST /acceptance-deliverables/{id}/review` 端点已删除；`action_maintenance_acceptance_review` 键留在注册表兼容历史快照，标「已废弃」。迁移 `a9e2f7c4d1b8` 把存量 `submitted + not_reviewed` 整体转 `approved`。
  3. **无截止日门**（2026-08-25，PR #283）：客户反馈「验收是没有截止日的，只是个上传的地方」——无交付行时首次上传自动建默认交付行，截止日只作展示（有则显示、无则「—」），不再是上传 / 提交的前置条件。
- **配套**（同日 CI 契约修正）：`sales` 的验收开放只走 DB（迁移改模板 + 账号快照），代码兜底模板保持历史冻结口径；`maintenance_manager` 为后期新角色不受冻结约束（§九「CI 契约修正」）。
- **依据**：客户原话「验收这个我觉得可以开放给销售 项目经理和维保负责的人」「不需要审批」（`.ai/MAINT_V2_DELETE_LINK.md` §九）；PR #278（2026-08-24 合入）正文；PR #283（2026-08-25 合入）正文；2026-09-06 代码核验：`backend/app/services/maintenance_acceptance.py` `submit_acceptance`（落 approved）与 `_locked_deliverable`（2026-08-25 注释），`backend/app/api/maintenance_acceptance.py` 无 review 路由，`backend/app/permissions.py` review 键文案「已废弃」。

## D-14 坏件返还率口径：分子已拍板，分母 / 官方字段发布 / 待判定处理待拍板

- **状态**：**部分拍板，部分待拍板**。本条只记录已定与未定，不替业务决定。
- **已拍板**：
  1. **分子 = 入库单（RKD）中返件类数量**，类别 ∈ {维保拆旧返件, 旧库退返}（2026-08-16 甲方口径；`docs/maintenance/REQUIREMENTS.md` #7、#10；`docs/maintenance/plan-v1.3-fullstack.md:351`「坏件回收 = rkd_inbound 入库单，类别∈{维保拆旧返件, 旧库退返}（业务已终确认）」）。代码同口径：`maintenance_bad_returns.calculate_return_rate` 的 `official_basis="rkd_inbound"`。
  2. **不返还标记行级默认**：项目级默认值 + 行级覆盖，返还率按行级排除（2026-08-15，REQUIREMENTS #32）。代码：`classify_return_obligation` 判定顺序 行级 → 项目默认 → 品类（硬盘豁免）→ pending。
  3. **坏件返还率进度条属二期**（2026-08-16，REQUIREMENTS #36：二期第二进度条 = 坏件返还率，v1 不做）。
- **待拍板**（文档与代码不一致或业务未答，逐条列出，本条不裁决）：
  - **分母**：文档写「需求单『需求』数量」（REQUIREMENTS #13，`docs/maintenance/ARCHITECTURE.md:47`）；代码用「义务应返量」= 已确认领用数量 − 不返还（行级 / 项目默认 / 硬盘品类豁免）（`backend/app/services/maintenance_bad_returns.py` `calculate_return_rate` 的 `required_quantity`，来自 `MaintenanceReturnObligation.required_quantity`）。两者不是同一个数。
  - **官方字段能否在业务确认前发布**：`CONTEXT.md`「官方返还率待确认」原写「确认前接口中的兼容官方字段必须保持空值」（2026-09-06 已按本条现状改写）；代码现状（`maintenance_bad_returns.calculate_return_rate` + `return_rates_for_projects` 的 RKD 导入判定）：**三条同时成立**才发布 `official_rate_pct`——项目已导入 RKD（`rkd_inbound` 批次 `applied` 且头行归属本项目）、无品类待判定行（`pending_quantity == 0`）、义务应返量 `required_quantity > 0`，此时 `official_basis="rkd_inbound"`、`status="available"`；未导入 RKD → `official_rate_pct=None`、`official_basis="rkd_not_imported"`（原本 available 的降为 `not_ready`）；有待判定行 → `status="basis_incomplete"`，应返量为 0 → `status="no_return_required"`，这两种都不发布官方率。业务确认前该不该发布，代码没有替业务回答。
  - **品类待判定（`pending_category`）由谁处理**：GitHub #208「编码前 HITL 确认」项至今未答（本文写作时 #208 仍 open、标签 `question`）；代码现状 `POST /maintenance/return-obligations/{id}/resolve-category` 仅管理员（`require_admin`）。
- **依据**：REQUIREMENTS #7 / #10 / #13 / #32 / #36；ARCHITECTURE.md §4；plan-v1.3-fullstack.md §2.4；GitHub #208；2026-09-06 代码核验 `backend/app/services/maintenance_bad_returns.py`、`backend/app/api/maintenance_bad_returns.py`。

## D-15 原生现场领用 / 返还义务 / 坏件返还流程搁置

- **状态**：已拍板（2026-09-06）。
- **口径**：
  1. **项目总表 06 表（`06_领用返还`）是当前唯一的领用录入 / 改行入口**——D-01（删行 = 作废，06 表在五张表之内）、D-03（修改权下放）延续。**整单作废另有入口**：项目面板『领用与返还』Tab 的「作废」按钮 → `POST /maintenance/site-issues/{id}/void`，对 `source=workbook`（06 表建的单）同样生效（2026-09-06；此前按 source 拒绝而 400），语义 = 整单明细软作废（`line.is_active=False`），与 06 删行一致，作废行退出 06 导出、面板行与已领用成本（D-01）。**06 表作废优先**（同一 PR 落地）：把已作废的单号 / 行重新写回 06 表，是行级「该行已被 xxx 作废，修改未生效」回执（D-02 唯一保留的拒绝），不是整本拒绝，也不复活。
  2. **原生 v2 领用 API 与坏件返还 API 保留；生产按端点分「失败关闭」与「可用」**：`/maintenance/site-issues/*`、`/maintenance/projects/stable/{id}/site-issues`、`/maintenance/return-obligations/*`、`/maintenance/bad-returns/*` 一律不删。生产（`environment == "prod"`）**失败关闭**的是走发货来源的录入链：建草稿 `POST /maintenance/site-issues/projects/{project_id}`（每行必填 `delivery_line_id`，候选只从仓库发货单投影，生产为空）、预览 `POST /maintenance/site-issues/{id}/preview`、确认 `POST /maintenance/site-issues/{id}/confirm`、更正确认 `PATCH /maintenance/site-issues/{id}`（对已确认单），服务守卫文案「生产确认只接受已确认仓库发货明细，合成来源已失败关闭」（更正为「生产更正确认只接受已确认仓库发货明细，合成来源已失败关闭」）；旧版直建 `POST /maintenance/projects/stable/{id}/site-issues` 的 `direct_api` 来源同样被挡（「生产现场领用必须使用已确认仓库发货明细的新版受控流程」）。生产**可用**的是读与作废：`POST /maintenance/site-issues/search`（『领用与返还』Tab 列表）、`POST /maintenance/site-issues/{id}/void`（见第 1 点）、`POST /maintenance/site-issues/projects/{project_id}/candidates/search`（生产无仓库发货来源时返回空候选，不报错）。领用确认只认仓库发货单导入派生的稳定发货明细（`warehouse_shipment_v1`，来自 `MaintenanceWarehouseDocument` shipment），生产没有这类发货数据，确认走不通。
  3. **0 引用前端组件删除**：`SiteIssueWorkflowPanel` / `BadReturnPanel` / `MaintenanceProjectCard` 等按 `docs/reports/2026-09/2026-09-03-重构决策审计报告.md` 阶段 1（「前端删 32 个死文件 + 11 个死测试，删后跑 vitest」）删除。
  4. **GitHub #207（现场领用 v2）/ #208（坏件返还义务与返还率）关为 deferred**（本文写作时两单仍 open：#207 `ready-for-agent`、#208 `question`；关闭动作待执行）。
- **重启条件**（三者齐备再议）：仓库发货单导入在生产落地；D-14 待拍板项定案；返还义务主键改为不依赖 `delivery_line_id` 的设计通过评审。
- **依据**（2026-09-06 代码核验）：
  - 整单作废对工作簿单生效：`backend/app/services/maintenance_project_operations.py` `void_site_issue` 不再按 `source` 拒绝、整单作废把明细一并 `is_active=False`、返还义务 voided 事件只对发过 outbox 事件的单发（2026-09-06；契约测试 `backend/tests/test_site_issue_v2_api.py` 工作簿单作废全链路：导出 / 统计 / 审计 / 幂等）；前端入口 `frontend/src/pages/maintenance/panel/SiteReturnTab.tsx` → `voidSiteIssue`。
  - 原生流程生产失败关闭：`backend/app/services/maintenance_project_operations.py` `_site_issue_is_production_blocked`（`environment == "prod"`）+ `_site_issue_sources_are_production_ready`（全部来源须为仓库发货适配器）；预览 / 确认 / 更正确认「生产确认只接受已确认仓库发货明细，合成来源已失败关闭」；`maintenance_warehouse_site_issue_bridge.synchronize_delivery_sources` 只从 `document_type == "shipment"` 的仓库单据投影候选。生产事实基准：REQUIREMENTS #0（2026-08-14）生产领用 / 返还 / 坏件 = 0 行。
  - 义务主键依赖发货明细：`MaintenanceReturnObligation.delivery_line_id` NOT NULL（`backend/app/models/maintenance_bad_return.py:38`），`obligation_id` 由 `(issue_id, delivery_line_id)` 派生，义务只由原生确认 / 更正 / 作废发出的 `MaintenanceSiteIssueReturnEvent` 投影；06 表建的领用行（`maintenance_project_master_workbook.py` 中 `MaintenanceSiteIssueLine(...)`，`source="workbook"`）不带 `delivery_line_id`，也不发返还事件——**06 行永远形不成返还义务**。
  - 返还率无可达页面：义务口径的 `return_rate` / `official_rate_pct`（`GET .../return-rate`、workspace 内嵌）前端只被 `MaintenanceProjectCard.tsx` 与 `BadReturnPanel.tsx` 消费，二者非测试引用为 0（审计报告 §抽检点名 BadReturnPanel / SiteIssueWorkflowPanel；MaintenanceProjectCard 经同日 grep 亦为 0）。`/maintenance/analytics` 页的 PN 级「坏返率」（`maintenance_analytics.py`：RKD 坏件数 ÷ 需求有效数）是另一指标，不走义务表，不受本条影响。

## D-16 收款单（SKD）定期导入的幂等与更新口径

- **状态**：已拍板（2026-09-06）。
- **口径**：
  1. **收款单号级台账是增量导入的前提**：新增收款单事实行，唯一键 = 合同（销售订单）× 收款单号；月度「已确认累计回款」快照由台账推导，不再由文件直接覆盖。
  2. **跨批次按收款单号去重**：同号同额同日 = 已知，跳过；同号不同额 / 不同日 = 冲突，需人工裁决，**不自动覆盖、不求和**（延续 REQUIREMENTS #56「合同、回款或其他事实冲突必须人工裁决，禁止自动求和 / 去重」、#57「禁止只按金额、月份或名称猜重，禁止自动求和」）。
  3. **已有月份金额不同 ⇒ 不再整批阻断**，改为行级「显式覆盖」：需用户逐行勾选确认，经审计版 `update_collection`（版本 CAS + 事实审计 `_fact_audit` + 已确认累计单调校验 `_validate_confirmed_collection_monotonicity`）写入；回执列明「覆盖了谁在何时的什么值」（D-02 风格）。未勾选的行不写。
  4. **网关必须归档上传原件**：与通用导入一致落 `sys_raw_file`（内容寻址，`storage_path={sha256}.xlsx`）。
  5. **05 表（`05_实收回款`）Excel 改月份同样走审计 + 单调校验**，与 API 路径同口径。
- **现状**（2026-09-06 代码核验，与口径的差距如实记录）：
  - 现有 `maintenance_bulk_import.ReceiptCollectionAdapter`（`file_type=maint_receipt`，「收款单累计实收」）只在**同一文件内**按 (销售订单, 收款单号) 判重（同额同日 `duplicate_same` 只计一次；不一致 `duplicate_receipt_conflict` 整单 fail-closed），跨批次无收款单号台账（`MaintenanceCollectionSnapshot` 只有月度快照 + `receipt_reference` 文本）；已有已确认月份与源累计不一致 `snapshot_conflict`、源文件缺生产已有月份 `incomplete_receipt_history` 均为硬阻断——即第 3 点要改掉的「整批阻断」。
  - `update_collection`（`backend/app/services/maintenance_project_operations.py`）已具备版本 CAS、`_fact_audit`、单调校验；`maintenance_bulk_import` 只算 sha256 做 (adapter, file_hash) 幂等，**不落** `sys_raw_file`（通用导入在 `backend/app/etl/pipeline.py` 落）。
  - 05 表 v2 写库走 `maintenance_expense_collection_workbook.apply`，直接改 `cumulative_amount` / `version += 1`，**无** `_fact_audit`、**无**单调校验——第 5 点为待实现。（2026-09-06 同 PR 已落地：改按合同**终态**判单调、新增 / 覆盖 / 作废均落 `_fact_audit`、无变化不计版本；05 表 `validate` 仍不跑单调校验，只有 `apply` 跑。）（2026-09-07 复核补丁，独立 05 上传与 V2 总表 05 表共用同一共享写路径：**作废优先**——命中已作废月份的 CREATE/UPDATE 不复活、不论状态列写什么（05 导出只带 confirmed 行，上传者看不到该月已作废），记入 `voided_rows` 行级回执「已被 xxx 于何时作废，修改未生效」，其余行照常写入；状态列留空 = 沿用原状态，不把 unconfirmed 翻成 confirmed；状态列只认 confirmed / unconfirmed / void，同合同同月重复行在加锁前拒成 422 `duplicate_month`（此前 V2 撞唯一约束 500）。作废快照的复活不走 Excel。）（2026-09-07 再复核：「留空 = 沿用原状态」此前只在独立 05 上传成立——V2 总表 05 解析器把状态留空折成 confirmed 再送共享写路径，导出不带的 unconfirmed 月补一行即被静默确认；现解析器留空传 None，两个入口口径一致，契约测试 `tests/test_maintenance_master_v2_receipts_shared_path.py`。）
- **依据**：2026-09-06 拍板；REQUIREMENTS #56 / #57（2026-08-28）；D-02（行级校验、覆盖回执）；代码坐标见上。
- **2026-09-06 复核后补充**（#288 实现落地，对抗审查后定案；`backend/app/services/maintenance_bulk_import.py` `ReceiptCollectionAdapter`、`backend/app/api/maintenance_project_batch_transfer.py`、迁移 `b7d3f9a1c5e2`）：
  1. **基线 / 建账规则**：月度累计 = 基线 + Σ台账生效收款(≤ 月末) + Σ本文件新收款(≤ 月末)；基线 = 覆盖起点（台账 ∪ 本文件最早月）之前最晚一条已确认快照。**合同尚无台账、而覆盖范围内某个已确认月份按本文件推导与快照不等 ⇒ 整合同 `seed_required`**（「该合同已有确认快照但尚无收款单台账，请先上传该合同的全量历史收款单导出建账」），不算任何累计值——从中间月份起增量导入只会把历史越算越少，唯一的"导入方法"是勾错误的降值。全量历史导出逐月复现旧快照 ⇒ `record_receipts` 入台账建账，快照不动；建账后增量导出按台账推导。（覆盖起点 / 基线措辞由 09-07 二次补充 #12 取代）
  2. **覆盖范围逐月对账**：覆盖起点及之后的每个候选月（台账月 / 文件月 / 既有快照月）都对账：无快照 `create`，相等 `noop` / `record_receipts`，不等 `update`（默认不勾选、需显式确认）。**推导值低于已确认值且该月没有台账收款支撑 ⇒ `cumulative_unverifiable` 行级阻断**，不提供覆盖（无从核实少了哪笔）；台账有支撑的月份保留显式覆盖。只有台账、没有快照的月份是回填（`ledger_backfill`，提示不阻断），不是漂移。
  3. **构成月规则**：一个月的累计含更早月份的新收款，那些月份必须同批勾选（`requires_months`）；预检判定「不勾某覆盖行守卫必拒」的也必须同批勾选（`depends_on_months`）。两者以 `depends_on_row_keys` 显式给前端，提示走 `hint_messages` 可见文字；应用缺任一依赖在**任何写入之前**硬拒（422 `invalid_selection`，预览仍可用），不再让守卫在事务中途 422、批次落成 failed。构成月被阻断的月份随之阻断（`constituent_blocked`）。
  4. **来源与批次**：批量导入写出 / 覆盖的快照 `source='bulk_import'` + `import_batch_id`（CHECK 约束扩展）；台账行带 `import_batch_id` / `source_sha256`；`SysImportBatch.file_hash` = 原件 sha256（多文件为各 sha256 排序后 sha256），同一原件分次勾选提交合法，`ux_batch_success_hash` 排除 `maint_bulk`；应用幂等按 `report_json.selection_hash`（表达式索引 `ix_batch_success_selection_hash`）。
  5. **单调性**：预览期以「本文件全部行应用后」的状态复算守卫，倒退即 blocked；应用写入顺序守卫安全（先按月升序写降值，再按月降序写新建 / 升值）。作废快照不进预检的邻月现值。
  6. **原件**：预览即归档到 `raw_file_dir`（内容寻址，与通用导入同一 `_archive`），但 `sys_raw_file` 行到应用成功才落——只预览不应用的批次不钉住原件，过宽限期由 `raw_archive_gc` 当孤儿回收。
  7. **作废语义**：覆盖范围内有已作废快照 ⇒ **整合同 fail-closed**（作废月的收款会被后续月份算进去却永远进不了台账）。作废快照不作废台账收款；台账行的更正只有裁决（`POST /maintenance/project-batch-transfer/receipt-rulings`，admin / boss + 实名 + `data_profit`）：旧行 `is_active=false` + `superseded_by` / 裁决人 / 时间 / 理由留档，另插更正行（`ruling_id`，无批次、无原件），`sys_audit_log` `collection_receipt/supersede`；**不动快照**，累计变化由下一次预览按台账推导成 update 行、由人显式勾选。唯一键只约束生效行；幂等 / 冲突判定只看生效行。
  8. **预览→应用 CAS 覆盖累计的全部输入**：预览按合同冻结指纹（台账生效行 + 基线 (collection_id, version) + 该合同全部快照 (月, 版本, 状态, 金额)），应用加合同锁后重算，不等即 409 `stale_preview`（「预览后台账/快照已变化，请重新预览」），并再按加锁库态 + 本批新收款复算每个月的累计与冻结值比对。同批两个文件触及同一合同 ⇒ 预览即 `cross_file_same_contract`（各文件的推导互不知情）。
  9. **实名门禁**：勾选含覆盖既有已确认累计的行、以及裁决端点，必须实名系统账号（`authn == sys_user`、非共享口令回退、账号有效，与 stable 项目 API `_real_operator` 同一实现）；共享口令 admin ⇒ 403「经营事实写入必须使用实名系统账号」。覆盖回执 / 审计 reason 点名原操作人（collection 事实审计最新一条，退回来源批次 `uploaded_by`）。
  10. **迁移**：`b7d3f9a1c5e2` 未发布，裁决列 / 偏唯一索引 / 表达式索引并入同一修订，单 head。downgrade 受保护：存在同 `(file_type, file_hash)` 的多条 success 批次（同一原件分次提交）时旧唯一索引建不回来，拒绝降级（`downgrade refused`）而不是半途 UniqueViolation。
  11. **已知残余**（如实记录）：同一合同的台账行按归一化销售订单号取（含已被取代关系的同号合同），换合同关系后旧台账仍计入；推导值**高于**已确认值而该月无台账支撑仍作显式覆盖提供（人负责）；裁决只改台账不改快照，裁决后的快照修正依赖人再预览并勾选；同批被裁决与入账走同一把合同锁串行，但裁决端点不校验裁决后的单调性（由下一次预览的守卫兜住）。
- **依据**：2026-09-06 对抗审查（预览→应用 TOCTOU、旧 05 表过渡、多文件同合同、作废月、实名门禁、冲突死胡同、降级撞索引、原件钉死 8 项）；REQUIREMENTS #56 / #57；D-02。
- **2026-09-07 再复核后补充**（第三轮对抗审查 16 项，probe 逐一复现后定案；下列条目 supersede 上一轮同号条目的对应措辞，历史条目原文不改；契约测试 `backend/tests/test_maintenance_receipt_ledger_review3.py`）：
  1. **不可核实对称（supersede 09-06 第 2 / 11 点）**：推导值与已确认值**不等**（高于、低于同论，REQUIREMENTS #57 对称）、且该月没有任何台账收款支撑 ⇒ `cumulative_unverifiable`，两个方向都不提供覆盖；出路只有两条——人工把快照改到推导值，或上传该月完整收款单导出使推导值与快照相等（`record_receipts` 入台账）。带新收款却被阻断的月份仍是**构成月**：依赖它的行 `constituent_blocked`，且不再展示任何"从其余月份算出来"的累计值。（"台账支撑"的定义与阻断传播由 09-07 二次补充 #12 / #13 取代）
  2. **依赖方向无关（supersede 09-06 第 3 点）**：本行累计经过的每个覆盖月（漂移 / 级联）都记入 `depends_on_months`，与单调方向无关，`create` / `update` / `record_receipts` 三种行都算（只勾本行会写出与人确认的现值互相矛盾的快照）；更晚的覆盖月仍只在不勾就被守卫拒时依赖。`record_receipts` 行同受 `requires_months` 约束：预览 `requires_earlier_months` 提示、应用缺构成月在任何写入之前硬拒。
  3. **作废与裁决（补充 09-06 第 7 点）**：基线之后、覆盖起点之前的已作废快照（基线跳过它、候选月也不含它）同样整合同 `snapshot_voided`——作废月的收款既不在台账也不在基线，后续累计会静默丢掉它。裁决后再预览同一文件，收款全部"已入账"但已确认快照与台账推导不等时，仍按台账出 `update` 行（默认不勾选），更正不会浮不上来。（跨月裁决的覆盖起点与影响面由 09-07 二次补充 #12 取代）
  4. **CAS 与锁序（补充 09-06 第 8 点）**：应用期每一种失效（逐项快照版本 / 金额、选中月新出现快照、合同关系版本、指纹、复算）一律 `code=stale_preview`、文案以「，请重新预览」结尾，HTTP 409；其它终态失败（422 `business_rule_violation` / `invalid_selection`、409 `apply_conflict`）批次落 failed，前端一律清空预览重预览。应用先按合同取台账锁，再按项目取工作簿并发锁（与 `update_collection` / `create_collection` / 05 表同一把、同一顺序），**然后**才读快照与台账——走正规入口的并发改值只能在锁上等待，进不了"读完到首次写入"的窗口。（09-07 二次补充 #4 取代）
  5. **实名门禁（supersede 09-06 第 9 点）**：任何写已确认快照或台账行的应用（新建、覆盖、登记入台账）以及裁决端点都必须实名系统账号，与 `POST /projects/stable/{id}/collections` 同一门禁；共享口令 admin ⇒ 403「经营事实写入必须使用实名系统账号」。原操作人取 collection 事实审计最新一条，退回来源批次 `uploaded_by` **只对数字批次号**（批量导入网关）生效；05 表写入的快照 `import_batch_id` 是 uuid，没有可回退的批次上传人——本 PR 之前由 05 表写入、从未进过事实审计的快照，覆盖回执 / 审计 reason 的原操作人显示 `-`（如实记录，05 表 apply 现已落 `_fact_audit`，此后的快照有审计可查）。
  6. **原件与暂存（补充 09-06 第 6 点）**：同一原件再预览复用归档时刷新 mtime，让 blob 重新进入 GC 宽限期（旧孤儿不再在预览→应用窗口里被回收）；应用落 `sys_raw_file` 前逐个核验 `storage_path` 仍是 sha256 相符的普通文件，否则 `ArchiveError` → 500 `archive_failed`、事实写入整体回滚、批次保持 processing（重试 / 重新预览均可）。归档暂存文件统一命名 `staging-*.part`（内存上传暂存与归档内部暂存同一形态），`raw_archive_gc` 在同一把锁下按 mtime 超过一天回收残片。
  7. **索引与迁移（supersede 09-06 第 10 点）**：`ix_batch_success_selection_hash` 改为**偏唯一**索引 `(file_type, report_json->>'selection_hash') WHERE status='success' AND file_type='maint_bulk'`，应用幂等查询不再带 `ORDER BY id LIMIT 1`（那会让规划器沿主键扫描过滤），取全部（≤1 行）后取最小 id；`upgrade` 与 `downgrade` 都 `SET LOCAL lock_timeout='5s'`。downgrade 另在两种情况下拒绝（`downgrade refused`，提示先导出）：台账表有任何行（含裁决留档行）；存在 `source='bulk_import'` 的快照。
  8. **公开行形态**：fail-closed / 冲突行（`seed_required`、`snapshot_voided`、`cross_file_same_contract`、`cumulative_unverifiable`、`constituent_blocked`、`collection_not_monotonic`、`order_level_fail_closed`、`receipt_conflict`）`match_state='invalid'`（不再 `ambiguous`）；同一行同一 code 只留一条；`hint_messages` 含 `receipt_conflict` / `receipt_known` 文字；`receipt_conflict` 行 `canonical` 带本文件的 `{receipt_no, receipt_date, actual_amount}`、`before` 带台账现值 `{receipt_no, receipt_date, actual_amount, import_batch_id}`，裁决界面按结构化字段对照。
  9. **已知残余（supersede 09-06 第 11 点）**：同一合同的台账行按归一化销售订单号取（含已被取代关系的同号合同），换合同关系后旧台账仍计入；裁决只改台账不改快照，快照修正靠下一次预览的 update 行由人勾选；裁决端点不校验裁决后的单调性（由下一次预览的守卫兜住）；05 表 apply 不写台账，05 表改值后的月份对导入而言是漂移（`ledger_drift` update 行）。（09-07 二次补充 #9 取代）
- **依据**：2026-09-07 第三轮对抗审查（逐项 CAS 码、锁序 TOCTOU、基线后作废月、漂移依赖方向、record_receipts 逃逸、不可核实构成月、裁决不浮现、高于无支撑、实名门禁范围、公开行形态、GC 复用竞态、索引失效、迁移 lock_timeout、暂存残片、受保护降级、文档 16 项）；REQUIREMENTS #56 / #57；D-02。
- **2026-09-07 二次补充**（第四轮对抗审查，probe 逐一复现后定案；下列条目 supersede 上一轮同号条目的对应措辞，历史条目原文不改、只追加取代指针；契约测试 `backend/tests/test_maintenance_receipt_ledger_review4.py`）：
  4. **应用失败的终态 / 非终态（supersede 09-07 第 4 点）**：**终态**失败——批次落 failed、前端清空预览、必须重新预览：409 `stale_preview`（文案以「，请重新预览」结尾）、409 `apply_conflict`、422 `business_rule_violation`、5xx `apply_failed`。**非终态**失败——批次保持 processing、预览与勾选照旧可用、前端原样显示 `detail.message`：403 `permission_denied`（实名门禁 / 权限）、422 `invalid_selection`（缺构成月 / 缺依赖覆盖行，在任何写入之前硬拒）、500 `archive_failed`（原件核验失败，事实写入整体回滚）。后端行为不变，本条只把口径写实；前端按此实现。锁序不变（先合同台账锁、再项目工作簿并发锁、然后读快照与台账）。
  9. **05 表改值与已知残余（supersede 09-07 第 9 点）**：05 表 apply 不写台账；**05 表改值后的月份：该月有台账收款 ⇒ `ledger_drift` update 行（默认不勾选）；该月无台账收款 ⇒ `cumulative_unverifiable` 阻断，须上传该月完整收款单导出或人工改回**。其余残余照旧：同一合同的台账行按归一化销售订单号取（含已被取代关系的同号合同），换合同关系后旧台账仍计入；裁决只改台账不改快照，快照修正靠下一次预览的 update 行由人勾选；裁决端点不校验裁决后的单调性（由下一次预览的守卫兜住）。
  12. **覆盖起点、基线与裁决影响面（supersede 09-06 第 1 点的基线措辞、09-07 第 1 / 3 点的裁决措辞）**：
     - **覆盖起点** = 下列各月的最早者：台账导入行的月份——**含被裁决取代的历史行**（裁决把收款移到别的月份后，原月份仍要按台账重新对账；否则原月份掉出覆盖范围，它的快照反而成了基线，移走的那笔被算两次）；台账生效行（含裁决更正行）的月份；本文件月份；`source='bulk_import'` 快照的月份。被再次裁决取代的裁决行不作锚点（它不是任何一次导出的证据），裁回原月份即恢复原覆盖范围。
     - **基线** = 覆盖起点之前最晚一条**已确认、非 `bulk_import`** 快照：台账推导出来的快照永远重新推导、不作基线。
     - **未确认快照是历史，不是基线**：基线之后、覆盖起点之前的未确认快照把覆盖起点拉到它那一月；覆盖范围内的未确认快照与已确认同论——推导值不等且合同尚无台账 ⇒ `seed_required`（文案「已有未确认快照但尚无收款单台账」）、不等且该月无台账支撑 ⇒ `cumulative_unverifiable`；相等的未确认月出 update 行把它确认掉；覆盖文案按真实状态写「已确认累计」/「未确认累计」。
     - **台账支撑**（`cumulative_unverifiable` 的"该月有台账收款"）= 该月有台账导入行（含被裁决取代的历史行——裁决本身说明了差在哪笔），或该月快照本就由台账推导（`source='bulk_import'`）；裁决更正行单独不构成支撑（裁决把收款移入台账建立之前的历史月份时，旧 05 表的该月累计是否已含这笔无从核实 ⇒ 阻断，出路是上传该月完整导出或裁回）。
     - **裁决影响面**：`rule_receipt.affected_months` 从 min(原收款月份, 新收款月份) 起重新推导，覆盖其后每个与推导值不等的已确认快照；跨月 / 改日期裁决不再报"无影响"。
  13. **上游作废与阻断传播（补充 09-06 第 2 / 09-07 第 1 点）**：
     - **`receipt_voided_upstream`**：源导出里「数据状态」非已生效（作废 / 空值 / 其他）的行，若其 (销售订单, 收款单号) 在台账仍有生效行且金额非 0 ⇒ 冲突（不是 `invalid_receipt`），整单 fail-closed，`hint_messages` 给出裁决路径（`POST /maintenance/project-batch-transfer/receipt-rulings` 裁为 0），`canonical` / `before` 与 `receipt_conflict` 同形；上游作废**不自动作废台账行**。台账生效行已裁为 0 的 ⇒ 该行「已知」跳过（`receipt_known`），同一导出即可再导，作废月按台账出 update 行（如 100→0）由人勾选；台账从未见过的非已生效行仍判 `invalid_receipt`（不放松）。
     - **阻断传播**：合同内任何一个月的行是冲突 / 阻断（`cumulative_unverifiable`、`collection_not_monotonic`、`seed_required`、`snapshot_voided`……），**不论该月有没有本文件新收款**，其后每个 `create` / `update` / `record_receipts` 行一律 `constituent_blocked`（`new_cumulative_amount=None`、`match_state='invalid'`、永不默认勾选）——累计逐月累加，经过对不上的月份就不成立。
     - **`record_receipts` 行带 info issue** `code='record_receipts'`（「累计不变，只把 N 笔新收款登记入台账」），前端标签按真实 code 打，不再猜 action。
- **依据**：2026-09-07 第四轮对抗审查（跨月裁决双算、基线丢失、阻断不传播、未确认历史不可见、上游作废死胡同、record_receipts 无 code、终态口径与文档 7 项）；REQUIREMENTS #56 / #57；D-02。

---

## 关联文件

- 事故调查：`outputs/2026-09-02-全量修改能力-事件调查与需求差异报告.md`
- 十维审计结论：`outputs/2026-09-03-重构决策审计报告.md`
- 销售订单字段口径：`docs/reference/sales-order-columns.md`
