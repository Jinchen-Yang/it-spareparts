# 维保核心需求增量表（实时更新）

> **每确认一条口径就追加一行，永不删除旧行**（改判也在原行标「已改判」并新增一行）。
> 本表与 [`ARCHITECTURE.md`](./ARCHITECTURE.md) 是维保业务的两本活文档；
> 归档历史文档的对照见 [`archive/README.md`](./archive/README.md)。

## 2026-08（v1.3 展示板立项期）

| 日期 | # | 口径 / 需求 | 状态 | 出处 |
| --- | --- | --- | --- | --- |
| 08-14 | 0 | 数据事实快照：需求单 19,046 单/38,483 行；成本覆盖 88.7%；发货↔需求命中 99.8%；生产领用/返还/坏件/回款=0 行；台账未导入生产 | 事实基准 | archive/data-facts-and-linkage-2026-08.md |
| 更早 | 1 | 项目身份 = 台账项目名（Excel 写法为准），前置库名一般=项目名 | 生效 | — |
| 更早 | 2 | 权限 = 勾选名单制（老板＋被勾选项目经理整套可见） | 生效（M0-B 细化见 #14） | — |
| 08-16 | 3 | 系统是后事实展示板，不是业务流程系统 | 生效 | 甲方口径 |
| 08-16 | 4 | 成本回填 = 采购价优先 → 无采购用销售价 → 时间就近 | 生效 | 甲方口径 |
| 08-16 | 5 | 预交付并入真实合同展示，不叫「合并」 | 生效 | 甲方口径 |
| 08-16 | 6 | 需求单状态列（已采/待供/待返/领用）只展示、不计算、不标注 | 生效 | 甲方口径 |
| 08-16 | 7 | 返还率分子 = 只有返件类（维保拆旧返件＋旧库退返） | 生效 | 甲方口径 |
| 08-16 | 8 | 报销对账正式金额列 = `amount_inc_tax`（含税计算值） | 生效 | 甲方口径 |
| 08-16 | 9 | 上传文件 = 需求单＋发货单＋返库单（入库单返件类）等，按需定期传 | 生效 | 甲方口径 |
| 08-16 | 10 | 好件收回 = 返库单（成品）；坏件回收 = 入库单（坏品/废品，返件类） | 生效 | 甲方口径 |
| 08-16 | 11 | 需求单可更改 = 展示层面的数据修正（改行/作废/补录/改归属），不是业务编辑 | 生效 | 甲方口径 |
| 08-16 | 12 | 冻结清单 9 项（AI 兜底/回款凭证/坏件变卖/工作簿 v3/前置库账本/收回清单/销售看板/报销对账 + 补库购物车） | 购物车已改判解冻，见 #25 | 甲方口径 |
| 08-16 | 13 | M0-A 需关注队列注册：**①超预算/预算余量 ③待返件多**；返件率分母 = 需求单「需求」数量 | 已实现 AB-2 | M0 签署清单 |
| 08-16 | 14 | M0-B 项目经理看**①全部项目**（改判，原计划默认②仅本人） | 已实现 AB-1 | M0 签署清单 |
| 08-16 | 15 | M0-C 成本字段正式名「已知申请估算成本(含税)」，布局按默认 | 生效 | M0 签署清单 |
| 08-16 | 16 | M0-D 事实粒度 = 项目＋PN 聚合（不做行级分配） | 生效（合并闸门放行） | M0 签署清单 |
| 08-16 | 17 | M0-E 发布迁移链 = ①整链发布（0 删表删列，4 处约束放宽重建） | 生效 | M0 签署清单 |
| 08-16 | 18 | F1 快照规则：全量 upsert/重传幂等/消失的单只进差异清单不删不隐藏/作废保留展示；修改通道 = 源头 Excel 重传覆盖，系统内不手工编辑 | 已实现 | M0 签署清单 |
| 08-16 | 19 | F2 数据陈旧提示：as_of > 45 天标「旧」，只提示不改库 | 已实现（保留） | M0 签署清单 |
| 08-16 | 20 | F3 改判：报销/回款**并入 v1**——`04_报销订单`＋`05_项目经理回款单`两张 sheet 合一的工作簿，下载→改→上传覆盖，不做严格对账、不做 BXD 对账、不进老板看板三槽 | 已实现 AB-3（后端；04 表无「备注」可编辑列，见增补包实现记录） | M0 签署清单改判 |
| 08-16 | 21 | F4 预交付不建独立档案，挂真实合同＋徽标 | 已实现 | M0 签署清单 |
| 08-16 | 22 | F5 自报与事实并排展示，服务端不产差异字段、前端无差异高亮 | 已实现 | M0 签署清单 |
| 08-16 | 23 | F6 台账 `not_counted`（included_in_total=false）v1 忽略、不进待办 | 未实现（备案） | M0 签署清单 |
| 08-16 | 24 | 归档项目带单仍出现在看板列表并标「已归档」；已空归档项目隐藏 | 已实现 | M0 清单附件 |
| 08-16 | 25 | PN 未标准化时互通池归属显示「—」（`in_pool: null`），不显示「不在池」 | 已实现 | M0 清单附件 |
| 08-16 | 26 | 补库购物车解冻（AB-4）：系统三查（通用池归属/半年购销/小众 PN）→ 系统审批通过 → 导出 Excel（PN/数量/描述/最近销售历史/池内最低价对比）交人工复核；**系统不建模人工审批** | 已实现 AB-4（后端；「小众」取零样本边界） | 业务确认 |
| 08-16 | 27 | 购物车结果**不进 WBDD**（WBDD 仍氚云导入）；购物车 = 独立记录、挂所选项目、与其他内容隔离、只记录展示、不进计算对账 | 已实现 AB-4（导出抬头明示隔离） | 业务确认 |
| 08-16 | 28 | 池内最低价无值时显示「—」 | 已实现 AB-4 | 业务确认 |
| 08-16 | 29 | 页面浓缩：维保前端页面砍到个位数、清理代码库维保页面、重头设计 | 已实现（22 页 → 2 页） | 业务确认 |
| 08-15 | 30 | 回款单口径 = 月度累计快照（报告月份＋累计金额），非逐笔流水 | 生效 | workbook-template-design |
| 08-15 | 31 | 回款计划唯一事实源 = 台账 `02_回款计划`，报销/回款工作簿不重复录计划 | 生效 | workbook-template-design |
| 08-15 | 32 | 不返还标记行级默认（项目级默认值＋行级覆盖），返还率按行级排除 | 生效 | workbook-template-design |
| 08-16 | 33 | 页面定稿 **2 页**：①维保主页（项目卡墙）②项目面板；报销/回款不独立页签，项目内 tab | 已实现 | 业务确认 |
| 08-16 | 34 | 项目卡内容：项目大标题 / XSDD 合同号 / 项目经理 / 合同总额；小字＝备件成本含税未税、缺失成本、维保备件采购数、回款预览；底部「进入面板」 | 已实现 | 业务确认 |
| 08-16 | 35 | 进度条 v1 **仅一条**＝成本÷合同额：<80% 绿、80–100% 黄（低盈利提醒）、>100% 红（亏钱）；有多少数据算多少 | 已实现 | 业务确认 |
| 08-16 | 36 | 二期第二进度条＝**坏件返还率**（返库分两类：前置库未用件返库、坏件返还）；v1 不做 | 二期 | 业务确认 |
| 08-16 | 37 | 项目墙默认显示**进行中**项目，已结束可筛出显示；一行 5 卡、下滑无限加载 | 已实现 | 业务确认 |
| 08-16 | 38 | 下载/上传共 3 处，**在哪下载就在哪上传**：①主页全局下载全项目备件行级表（时间预设：今天/昨天/本周/本月/自定义；改价补价后上传覆盖=真实源）②项目面板下载本项目总表（多 sheet）③各 tab 下载对应单 sheet | 已实现 | 业务确认 |
| 08-16 | 39 | 项目面板：顶部出库明细（生产版现状，按时间筛选；多合同并排或筛选）+ 子 tab（基础信息/备件成本/报销/回款＝表6 的 web 呈现）+ 项目名旁编辑项目基本信息（起止时间/负责人，与表6 联通） | 已实现 | 业务确认 |
| 08-16 | 40 | **一张项目总表（表6 多 sheet 工作簿）改所有数据**：缺价、报销、回款都走下载→改→上传覆盖，系统内不再手工散改 | 已实现（总表六 sheet） | 业务确认 |
| 08-16 | 41 | 项目卡小字「维保备件采购数」＝需求单里**已发货/已确认的数量**（建议列映射：库房发货＋直采直发；实现时与事实源「实发（发货单）」口径核对） | 生效 | 业务确认 |
| 08-16 | 42 | 原始单据上传（需求单/发货/返库/入库/台账）走 Admin 既有「数据中心→数据导入」入口，**不新建上传台**；回填类下载上传（备件行级表/项目总表/tab 单 sheet）只在主页和项目 tab 页 | 生效 | 业务确认 |
| 08-16 | 43 | 卡片状态三态 = 进度条颜色：**正常=绿、提醒=黄、报警=红**；「需关注」就是提醒——原需关注栏并入卡片状态，不再单列 | 生效 | 业务确认 |
| 08-16 | 44 | 旧页面**直接删除代码**（工作台 9 页、旧版 3 页、旧展示板 5 页），新 2 页直接替换上线；实现顺序先建新后删旧 | 已实现 | 业务确认 |
| 08-16 | 45 | 归属挂靠：在项目面板「基础信息」tab 点确认挂靠，或独立成项目（罕见）。**归属判定依据＝XSDD 销售订单**：同一销售订单的单子＝同一项目，不同销售订单＝不同项目 | **已验证（见 #46）** | 业务确认 |
| 08-16 | 46 | 生产验证结论（2026-08-16，生产库只读分析）：①WBDD 19,046 单 100% 挂 XSDD，363 个不同销售单，358 命中销售（5 个 2023 老单不在销售表，130 单）；②**XSDD→项目名**：312 个 1:1；51 个跨 2–4 个名，样本全是「期间/续签」写法差异＝同一项目（✅支持 #45）；③**项目名→XSDD**：唯一例外「兵装财务20240701-20250630北京神州新桥 整体维保」1 项目挂 2 个 XSDD（24 单，多合同项目）；④采购单 78,816 张，99.8% 带 XSDD，255/363 个维保 XSDD 有采购记录；⑤维保明细 11,600 行挂采购单号，100% 命中采购表；⑥发货/返库/入库/回款生产=0，台账=415 占位项目+7 测试合同，报销=14 行测试 | 事实基准 | 生产库验证 |
| 08-16 | 47 | 批准 `f_project_expense.remark` 迁移（**纯加法 nullable**，追加链尾保持线性）；`04_报销订单` sheet 加可编辑「备注」列（黄底），apply 回填 remark，报销 tab 展示 | 已实现（迁移 d6e1f4a8c3b5） | 业务确认 |
| 08-16 | 48 | 归属挂靠候选按 XSDD 预筛：候选＝未归属单中 **XSDD ∈ 本项目销售单集合** 的单（排最前），其余未归属单在其后；多合同项目把名下全部 XSDD 都算本项目键 | 已实现 | 业务确认 |
| 08-16 | 49 | **业务认账**：「维保备件采购数＝库房发货＋直采直发」的聚合豁免（铁律 3 只禁四列，这两列经 #41 明文授权；看守测试锁死豁免范围恰好两列）。无代码改动 | 生效（已实现） | 业务认账 |
| 08-17 | 50 | **项目周期从项目名称提取**（业务指示 2026-08-17）：名称嵌 `YYYYMMDD-YYYYMMDD` 段（连接符 `-`/`~`/空格混排；另支持 `YYYYMM-YYYYMM` 取起月首日/止月末日）。**台账周期仍是权威源**，名称仅在台账缺失时兜底；解析不出（无日期段/笔误/起止倒置）保持 missing，宁缺毋错。存量 415 个 missing 项目按名称一次性回填（迁移 `e8b2c6f4d1a7`，纯数据 UPDATE 零 DDL）：生产名称干跑 373/415 回填（152 进行中＋221 已结束），42 保持 missing（37 真无周期＋5 笔误）。快照语义：ongoing→ended 翻转靠下次台账导入刷新（导入侧带同款名称兜底，不会把回填打回 missing） | 已实现（#51 扩展为期限主数据） | 业务指示 |
| 08-17 | 51 | **现有数据即可确定项目名称/期限/合同额，不必等台账**（业务指示 2026-08-17，参照 v1.17 老版项目成本页口径）：①**期限进主数据**——`maintenance_project` 加 `period_from/to`（迁移 `f3b5d7c9e2a4` 纯加法），回填三层＝WBDD 挂靠聚合（min(maint_start)/max(maint_end)，单头日期非空率 99.97%，覆盖 411/415）→ 名称解析（#50）→ NULL；面板显示起止、「编辑基本信息」可改（#39 落地），改后按新期限重算 lifecycle；台账导入仍为权威覆盖，台账/名称都缺时不清空已有回填。②**合同额两层取数**——台账合同（included_in_total，权威）→ **XSDD 回退**：项目挂靠单据的 distinct XSDD 去销售表取 `max(amount_ex_tax×(1+tax_rate))` 跨单求和（生产可覆盖 402/415），带两个诚实标注：`contract_shared`（共用单，金额跨项目重复计入，只标注不擅自分摊——Q5 合同额仅参考）、`contract_incomplete`（销售表缺单，金额被低估——铁律 5 不静默按 0）。③WBDD 明细即备件成本页数据（已实现，known_apply_cost 取价瀑布） | 已实现 | 业务指示 |

## 状态约定

- **生效** = 口径已定，实现按此执行（可能已有代码）。
- **已实现** = 代码已落地并通过验收。
- **待实现 AB-x** = 见 `../releases/v1.23-addon-pack.md` 对应任务。
- **讨论中** = 已留 GitHub issue，待拍板。
