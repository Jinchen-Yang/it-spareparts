# DEV-07 验证记录

验证日期：2026-07-16

基点：`main@7e36c22de562b09c92ba1dd90a11ace89daeb5a2`

分支：`codex/dev07-pool-policy-coverage`

## 1. 自动化测试

- 后端全量：952 passed，2 skipped（基线 948，新增 4）。
- 后端定向：45 passed。
- 前端全量：199 passed（基线 191，新增 8）。
- 前端定向：43 passed。
- TypeScript：通过。
- Vite production build：通过。
- Alembic check：No new upgrade operations detected（本功能无迁移）。
- `git diff --check`：通过。

## 2. 独立 SQL 对账

本地生产快照验收库：40 个有效池，人工构造 10 个采购上限、15 个销售下限，其中 8 个双侧均设置。

| 指标 | API | 独立 SQL | 结果 |
|---|---:|---:|---|
| 有效池 | 40 | 40 | 一致 |
| 未设采购上限 | 30 | 30 | 一致 |
| 未设销售下限 | 25 | 25 | 一致 |
| 已设采购上限 | 10 | 10 | 一致 |
| 已设销售下限 | 15 | 15 | 一致 |
| 双侧均已设置 | 8 | 8 | 一致 |

四种缺失筛选 API / 独立 SQL：purchase 30 / sales 25 / either 32 / both 23，逐项一致。

## 3. 权限与失败态

- 治理可见 + 有池管理动作：覆盖卡可点击并跳转 `/pool-management?policy_missing=...`。
- 治理可见 + 无池管理动作：只读显示覆盖数字，0 个筛选按钮，提示联系管理员；未放宽路由。
- 治理不可见：不显示覆盖模块，不发 `/api/pools` 覆盖请求；后端显式缺失筛选返回 403。
- 缺失筛选请求模拟 500：旧表格行立即清空，URL 和筛选标签保留，显示持久错误与重试；重试后恢复正确 25 个池。

## 4. 真浏览器验收

Chromium + 真实前后端 + 生产快照数据：

| 页面/宽度 | 页面 viewport / scrollWidth | 结果 |
|---|---|---|
| 池管理 1440 | 1440 / 1440 | 通过 |
| 池管理 768 | 768 / 768 | 通过（宽表仅容器内滚动） |
| 池管理 390 | 390 / 390 | 通过，编辑抽屉 390px 全宽 |
| 老板看板 1440 | 1440 / 1440 | 通过 |
| 老板看板 390 | 390 / 390 | 通过 |

已验证：看板点击销售缺失 25 → 管理页 URL 筛选 → 共 25 个池 → 点编辑打开现有约束价抽屉；切到归档会清 URL，浏览器后退恢复缺失筛选时状态同步回“有效”。

截图（本机验收证据）：

- `/tmp/dev07-pool-management-1440.png`
- `/tmp/dev07-pool-management-768.png`
- `/tmp/dev07-pool-management-390.png`
- `/tmp/dev07-boss-1440.png`
- `/tmp/dev07-boss-390.png`

## 5. 对抗复审

最终结论：Standards PASS，Spec PASS，0 blocker，0 P1。

复审发现并关闭三项 P1：无管理入口死链、全局覆盖与时间范围语义混淆、筛选失败保留旧行。

## 6. 合并与生产判定

- 合并门：全量测试、独立 SQL、真实浏览器、对抗复审均已通过；等待 GitHub CI 全绿即可 squash 合并。
- 生产门：本功能无迁移，但合并后不自动部署。需单独部署授权、部署前备份、全权/受限账号冒烟、部署后错误日志与健康检查观察。
