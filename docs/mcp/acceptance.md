# 首版验收记录

日期：2026-09-16。全部业务验证使用本次测试创建的独立数据库、合成员工与合成单据；未连接生产数据。

| 验证 | 结果 |
|---|---|
| Python 3.11.16：MCP、真实浏览器、原生验收清单/费用回款、RBAC、登录与发布闸门 | 100项通过 |
| 最终MCP专项（含崩溃重试审计、31日边界、查询数据追溯和并发上传限额） | 21项通过 |
| 官方Python MCP客户端，真实HTTP连接 | 初始化、16工具发现、调用通过 |
| 员工浏览器流程 | 登录→上传→预览→确认→导出→下载校验→审计通过；桌面/390px手机无脚本错误、无页面横向溢出；失效链接登录恢复通过 |
| 迁移 | 单头 a16b7c8d9e01；alembic check无漂移；非空表降级被拦截 |
| 前端 | 类型检查及正式构建通过；全量828项通过 |
| 后端全量范围（分段完成） | 4523项：4515通过、8项按条件跳过；可选浏览器已另行实测通过 |
| 依赖 | 前后端生产依赖扫描均无已知漏洞 |
| 静态检查 | 新增MCP模块及模型通过Ruff；Python编译、JavaScript语法通过 |
| 容器 | 原有Dockerfile成功构建；容器内服务模块、worker和页面资源可导入 |
| Nginx代理 | 使用生产锁定的Nginx镜像，在新建隔离内部网络内验证语法、健康检查、页面/脚本、未授权拦截和no-store响应头，均通过；测试容器/网络已清理 |
| Compose | opt-in mcp-worker配置解析通过；两个MCP开关默认false |

关键断言：预览不入账；原生业务并发变更使旧预览失效；重复提交只产生一份回执；审计失败回滚业务；其他员工无法访问本人暂存；PAT不能确认；撤销授权阻止后台执行与待确认预览；租约恢复不会执行写入任务；重试耗尽仍落审计；过期字节清理保留审计元数据。

前端首次并行全量运行出现1处既有页面时序失败（PartSearchPage）；该文件22项单独复测、随后全量828项复跑均通过。后端首次全量运行在432项通过后因环境缺少ShellCheck中止；已临时安装检查工具，原失败测试单独通过，并重新运行全量。随后在4142项通过、7项跳过后发现依赖锁文件格式/SBOM未刷新；已按仓库生成器修复，失败项复测通过，剩余370项中369通过、1跳过。另有4个收尾新增MCP用例已随最终21项专项通过。按测试函数及参数化用例数核对覆盖范围，无遗漏。

基础镜像从Docker守护进程下载时停滞，备用镜像源出现IPv6连接超时。随后以仓库Dockerfile固定的相同摘要取回镜像并载入，原Dockerfile构建成功；没有更换镜像版本或降低校验要求。

未验收范围：员工真实WorkBuddy桌面安装与使用、生产发布、真实客户单据回传。浏览器与官方SDK验证不能替代这些记录。当前不具备“已生产上线”的状态。

截图（均为合成数据）：

- [桌面工作台](acceptance/desktop.png)
- [手机工作台](acceptance/mobile.png)

实际工具契约：`backend/app/mcp/tools.json`。WorkBuddy连接器源码及5个Skills：`connectors/workbuddy/partflow/`。安装包由该目录生成，包含说明文档，不包含令牌。

[机器可读验收摘要](acceptance.json)。
