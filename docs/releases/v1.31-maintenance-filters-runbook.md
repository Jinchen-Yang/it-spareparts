# v1.31.0 业务类型与可折叠筛选

## 交付范围

- 项目基本信息可选择整体维保、备件维保、算力运维、非维保或未标注；未标注提交空串清除。只提交用户修改的业务类型，保留历史自由文本，沿用 version 乐观锁与 reason 审计。
- 维保主页期限导航常驻，类型、状态、排序与关键词可折叠；首次展开，浏览器记住折叠状态。
- 已应用条件以可关闭蓝色胶囊展示，支持键盘删除；角标统计类型、状态、已提交关键词，排序和期限不计数。业务类型空选或全选均为全部。
- 继续复用现有自动继承、回填 API、清单导出与批量移交筛选参数；无后端代码或迁移变更。

## 验证记录

实现提交：`5bda77b`。

- 前端全量：78 文件、828 测试通过（`/tmp/v131-frontend-full.log`）。
- 类型检查与生产构建通过（`/tmp/v131-build.log`）；修正测试查询类型后，主页专项 10 项再次通过。
- 后端业务类型写入、回填、卡墙与导出专项：49 项通过（`/tmp/v131-backend-targeted.log`）。
- 后端全量、真实浏览器、Excel 验收和 PR CI 的最终状态记录在 PR；未完成的检查不得写成通过。
- 浏览器使用独立随机测试数据库与端口 5189/8012，不修改已有演示数据库。
- Chromium 实测通过：修改业务类型、清空与恢复；回填唯一来源 1 项，冲突 1 项与无来源 1 项均不改，重复执行填充 0 项；折叠记忆、键盘删除关键词、类型筛选及 Excel 下载通过，页面/API 错误 0。390px 视口下筛选面板边界 33–357px。仅验证本次筛选面板，原有卡墙在窄屏仍为固定多列。
- XLSX 实际 1 条项目记录，业务类型整体维保，已知申请成本含税 226、未税 200，与隔离夹具一致；数据库审计确认类型修改/清空/恢复均记录真实验收账号和修改原因。证据 `/tmp/v131-qa/browser.log`、`filtered-projects.xlsx`、`check-evidence.py`。
- 折叠时卸载筛选控件，避免下拉浮层残留；筛选值保存在父组件，重新展开不丢失。

## 生产执行顺序

1. PR CI 通过，按仓库正常保护规则合并，不绕过审批。
2. 通过 SSH 别名 `ybwznt`（经 `relay-vps`），核对 `/opt/it-spareparts` 代码、服务与迁移头。保留服务器本地部署配置。
3. 完成数据库备份、校验和与 pg_restore 对象清单验证，记录备份位置；保留旧镜像用于回退。
4. 快进到已合并提交，构建应用和前端，核对 alembic current / heads，一致后启动服务并检查健康。
5. 用现有实名管理员正常登录，调用 GET `/api/maintenance/projects/stable/business-type-backfill/preview`，报告能填、冲突和无法推导数量。
6. 调 POST `/api/maintenance/projects/stable/business-type-backfill/apply`，reason 写明版本与本次补齐目的；只填空值且来源唯一的项目。执行端会重新预览，不覆盖人工修改。
7. 复核未标注数量、类型分布、审计条数与再次预览；无法推导的项目留给业务补充资料。
8. 按业务类型筛选并导出项目清单，勾选业务类型、已知申请成本（含税）、已知申请成本（未税），核对导出范围、列与金额。生产验收不得随意改客户项目充当测试数据。
9. 验证折叠、胶囊删除、刷新记忆、编辑字段回显与导出，记录最终发布提交及 release 标签。

## 部署前基线（2026-09-15）

- 生产提交 `cf7da047`（v1.30.0），迁移头 `e3a7b9c2d4f6`。
- 活跃项目 618 个，业务类型全部为空。
- 只读服务预览：候选 618、可填 476、冲突 0、无法推导 142。执行前必须重新调用 API，使用当时实际结果。
- 生产配置的管理员凭据与实名账号匹配，可使用正常登录流程；不得输出口令或令牌。
- 正常登录后的 GET API 预览与只读服务预览一致。发布前备份 `/var/backups/spareparts/db-20260914-1616.dump`，50,443,294 字节、1,276 对象，完整性及 SHA256 校验通过（服务器 UTC 时间）。

## 回退

前端问题可切回部署前镜像；本版没有数据库结构变更。业务类型回填为独立数据治理，记录审计，不随前端回滚自动清空；如确需撤回，必须逐项核对审计及后续人工编辑，禁止整库覆盖或批量清空。
