# v1.36.0 维保人工维护与页面批量

> 状态：**Release Candidate 文档，尚未完成最终 freeze、Required CI、合并、部署或生产验收。**
> 本文以 2026-09-20 当前开发树为准；待并行分支合流后，须按最终 SHA 重新核对能力与迁移头。

## 一、发布主干

v1.36 把维保事实维护拆成四条彼此诚实的通道：

1. **需求**：维护需求单及需求明细；页面人工值用字段级 override 保护，不让后续 WBDD 导入静默覆盖。
2. **回款**：维护项目合同下的月度累计实收快照；合同从当前项目关系中选择。
3. **领用**：页面登记明确的人工领用事实，来源为 `page_manual`；它不是仓库发货，也不改变库存。
4. **返还 / 坏件**：维护实际收到的返件台账；件况可为成品、坏品、废品或留空，全部有效收货按既有返还台账口径计数。

页面批量只是对既有单行命令的**逐行编排**，不是一个跨行数据库事务。Excel 仍是独立的文件导入 / 回传通道，不能把 Excel 能力写成“页面批量”。

## 二、当前集成边界（2026-09-20）

当前分支 `feat/v1.36-manual-maintenance` 的 HEAD 为 `305b36e`：

| 范围 | 当前状态 |
|---|---|
| 需求后端、页面单条直建 / 编辑 / 撤销 override | 已进入当前 HEAD；仍待最终全量回归与 Required CI |
| 回款页面单条登记 / 修改 / 作废 / 恢复 | 已进入当前 HEAD；仍待最终全量回归与 Required CI |
| 返还单条维护、逐件 SN、页面批量登记、批量修改 / 作废 | 已进入当前 HEAD；仍待最终全量回归与 Required CI |
| 需求页面批量新增 / 修改 | 在 `/tmp/it-spareparts-demand-batch-8DHPXr/worktree` 收口，**未进入当前 HEAD，待合流与验收** |
| 回款页面批量新增 / 修改 / 作废 | 在 `/tmp/codex-v136-collection-batch` 收口，**未进入当前 HEAD，待合流与验收** |
| 页面人工领用 `page_manual` | 在 `/tmp/codex-v136-manual-site-issue` 收口，**未进入当前 HEAD，待合流与验收** |

`/tmp/codex-v136-receipt-batch-maintenance` 的返还批量维护提交已以等价提交进入当前 HEAD。以上“已进入”只表示代码集成状态，不代表验收、CI 或生产通过。

## 三、能力表

| 业务 | 处理对象 | 页面单条 | 页面批量 | Excel / 文件通道 | 明确限制 |
|---|---|---|---|---|---|
| 需求 | 需求单及 `f_maintenance_line` 明细 | 独立创建 PAGE 来源需求；编辑数量、退货数量、SN、描述、PN；逐字段撤销后恢复源值；需求单可软作废 / 恢复 | **待合流**：逐行创建独立 PAGE 需求单；选中行逐行修改 | 既有 WBDD / 项目总表导入继续使用 | 成本字段与单头不在行编辑白名单；PN 按 `pn_std` / `pn_raw` / `part_id` 身份组处理；修改与撤销使用 canonical snapshot digest 做 OCC |
| 回款 | 项目合同下的月度累计实收快照 | 合同编号下拉；登记、修改、软作废、恢复；恢复先回“待确认” | **待合流**：逐行新增、修改、作废；已作废记录仍走单条恢复 | 项目总表 **05 实收回款**页继续负责 Excel 批量回传 | 合同必须属于当前项目且有效；累计金额与确认状态沿用既有单调校验；PATCH 使用 `version` OCC |
| 人工领用 | 一张 `MaintenanceSiteIssue` 及其明细行 | **待合流**：预览成本后登记、更正、作废；单号可自动生成 | 没有“多张领用单页面批量”；一张页面领用单可含最多 200 条明细 | 项目总表 **06 领用返还**页继续支持人工领用 Excel 回传 | `source=page_manual`；复用 06 的成本瀑布和返还义务修正；库存影响为 none；不创建 `delivery_line_id`，不伪造仓库发货 |
| 返还 / 坏件 | `maintenance_rkd_return_line` 实际收货事实 | 登记、修改、转移、软作废；需求单关联可空；逐件 SN 可选 | 粘贴多行逐条登记；选中有效记录逐条修改或作废 | 既有 RKD / 入库单导入继续处理来源单据 | 页面不提供作废记录恢复；来源导入证据与手工登记来源保持可追溯；旧坏件统计的冻结过滤口径不因全件况收货而扩大 |

## 四、输入与并发契约

### 4.1 返还来源单号、凭据与 SN

- 返还台账 `evidence_ref` 前端、API、service 与数据库统一为最大 **16384** 字符；数据库类型为 `Text`。
- SN 使用多行输入，每行一个；单条返还最多 **1000** 个 SN，去空白后不允许重复。
- 有 SN 时，数量必须等于 SN 个数。
- 页面批量粘贴中，无 SN 的数量必须写成 `qty:<数量>`；纯数字永远按 SN 文本处理，不猜成数量。
- 整机返还沿用整机数量规则，不为组成项虚构逐件 SN。

### 4.2 幂等、冲突与部分成功

- 新增类命令冻结归一化 payload 与行级幂等键；成功行不重发，重试复用原 key。
- 修改 / 作废类命令冻结读时 `version` 或 canonical digest；发生冲突后不自动升级 token 覆盖他人结果。
- 无响应、超时或 5xx 记为“结果未知”，不得伪称成功或失败；须用原请求重试或刷新核对。
- 4xx 明确拒绝只说明该次请求未接受；如果同一行此前已经是“结果未知”，后续 4xx 不能反推首次一定未落库。
- 所有页面批量都是逐行执行，可出现部分成功；**不承诺跨行原子事务或整批回滚**。

### 4.3 需求字段保护

- 页面只允许维护白名单字段：数量、退货数量、SN、描述、PN。
- override 保存人工值、导入源值及操作者；WBDD 重导跳过仍受保护的字段。
- 撤销 override 后恢复 `source_value`，并重新允许后续 Excel 导入覆盖。
- PN 是一个身份组；修改或撤销必须同步校验 canonical PN 与 `part_id`，禁止留下字符串与型号身份错位。
- canonical JSON 的 SHA-256 digest 是页面 GET、PATCH 和撤销操作共用的 OCC token。

### 4.4 人工领用与仓库闸门

- 页面人工领用只记录用户明确提交的领用事实，来源固定为 `page_manual`，不冒充 `direct_api` 或仓库交付。
- 成本仍走 06 人工领用既有瀑布；缺价保持空值并提示，不编造 0 成本。
- 返还义务及更正审计复用既有 06 规则。
- `direct_api` / `production_ready` 等仓库适配器原闸门保持关闭条件不变；不得为了页面领用绕开仓库生产就绪校验。
- 页面领用不改变库存，也不得把需求单伪造成仓库发货记录。

## 五、数据库迁移

目标迁移单链已按文件声明核实：

```text
e3a7b9c2d4f6
  -> b5e8d1a7c3f9  返还逐件 SN JSONB
  -> c8f2b5d9a4e7  需求字段级 manual_override
  -> d9a4c6e2f8b1  返还 evidence_ref varchar(128) -> Text
  -> e7c1f9a3b5d2  人工领用 page_manual 来源与 create 命令
```

当前 HEAD `305b36e` 只到 `d9a4c6e2f8b1`；`e7c1f9a3b5d2` 随人工领用隔离分支，合流后才是 v1.36 目标 head。最终 freeze 必须重新执行单 head、从生产基线升级和 `alembic check`。

各迁移都含防丢数据降级守卫：已有逐件 SN、字段 override、超长凭据或 `page_manual` 事实时拒绝破坏性 downgrade。日常回滚只切应用，不执行 `alembic downgrade`。

> 升级 `c8f2b5d9a4e7` 后，升级前已 armed 但未 execute 的需求单两阶段删除 intent，其 version digest 会变旧；操作人员应重新创建 intent，不得强行复用旧摘要。

## 六、验证状态

本文件创建时：

- 尚未进行最终全量 backend pytest、完整迁移验证、frontend vitest / `tsc && vite build`；
- 尚未创建最终 PR，也没有 Required CI 结果；
- 尚未合并 `main`、创建 `v1.36.0` tag 或部署生产；
- 尚未完成真实账号点按和公网版本验收。

本版本元数据任务只做 JSON 版本一致性检查与 `git diff --check`，不替代上述验证。并行分支中的局部测试不能写成最终发布验收。

## 七、生产基线、备份与回滚准备

- 生产代码基线：`3ce1c6a7b7d2e958abc64f72a50eb11fa2ffd4c0`，标签 `v1.35.0`。
- 当前 `origin/main`：`fe4e0c1518fc0364be490df5963237f500dae6e0`，相对生产基线只增加 v1.35 部署文档。
- 已验证的**预备**备份：`/var/backups/it-spareparts-releases/v1.36.0-preflight-20260920T0655Z`，包含 `db.dump`、`config.tar.gz`、`uploads.tar`；全备恢复演练已做。
- 持久异机副本：`/home/cloudlay/.local/state/it-spareparts/release-backups/v1.36.0-preflight-20260920T0655Z`。
- 上线前仍必须创建 fresh backup；不得把上述预备备份冒充最终上线备份。
- 旧应用只读回滚脚本：预备备份目录内 `rollback-app-readonly.sh`；旧镜像 `rollback-v135-pre-v136-20260920`。
- 只读回滚保留当前新数据库及 schema，不用旧 dump 覆盖上线后的新增数据。旧 v1.35 应用不理解新人工字段，因此回滚后业务写暂时返回 503；GET / HEAD / OPTIONS 与登录可用。恢复写服务必须前向修复或另行确认兼容方案，不自动降数据库。

## 八、正式发布闸门

1. 合流需求批量、回款批量、页面人工领用，并按最终 tree 做 freeze；重新核对本文能力表和迁移单链。
2. 走 PR，Required CI 两项全绿：
   - backend pytest + 迁移升级 / 单 head / `alembic check`；
   - frontend vitest + TypeScript 检查 + Vite production build。
3. squash merge 到 `main`，记录精确 release SHA；不得用开发分支 SHA 代替。
4. 用精确 SHA 制作 bundle，送至 `ybwznt:/opt/it-spareparts`。
5. 先 build 新镜像但不切容器；其他服务保持不动。
6. 启用写保护并停止 app，创建并核验 fresh backup。
7. 执行 migration upgrade / check，确认目标单 head 后切换新 app / frontend 镜像。
8. 用真实登录验证 8 个只读 API，并核验公网入口版本与关键页面。
9. 只有真实证据完成后，才回填最终 SHA、CI、备份、迁移、登录验收、部署时间与 tag；失败或未执行项必须原样记录。

## 九、回退原则

应用故障优先切换 `rollback-v135-pre-v136-20260920` 并进入只读模式；不删除 v1.36 新事实，不执行数据库 downgrade，不影响同机其他服务。若需要恢复业务写，必须发布理解 v1.36 schema 的前向修复镜像，或经过单独批准的兼容方案。
