# IT 备件智能管理系统 — 产品需求文档（PRD）

**文档版本**：v1.0  
**创建日期**：2026-06-30  
**所属项目**：IT 备件智能管理系统  
**客户单位**：中石化完井研究中心  
**文档状态**：正式发布

---

## 目录

1. [背景与立项依据](#一背景与立项依据)
   - 1.1 [客户背景](#11-客户背景)
   - 1.2 [现有痛点](#12-现有痛点)
   - 1.3 [立项目标](#13-立项目标)
2. [用户画像与需求](#二用户画像与需求)
   - 2.1 [用户角色](#21-用户角色)
   - 2.2 [核心用户故事](#22-核心用户故事)
3. [问题定义与核心挑战](#三问题定义与核心挑战)
   - 3.1 [核心问题：型号身份碎片化](#31-核心问题型号身份碎片化)
   - 3.2 [核心问题：成本口径裂脑](#32-核心问题成本口径裂脑)
   - 3.3 [核心挑战：AI 与业务数据的安全集成](#33-核心挑战ai-与业务数据的安全集成)
4. [产品功能规格](#四产品功能规格)
   - 4.1 [数据导入系统](#41-数据导入系统)
   - 4.2 [型号主数据治理](#42-型号主数据治理)
   - 4.3 [成本与利润分析](#43-成本与利润分析)
   - 4.4 [库存管理](#44-库存管理)
   - 4.5 [AI 智能体](#45-ai-智能体)
   - 4.6 [替代品管理](#46-替代品管理)
   - 4.7 [账户与权限管理](#47-账户与权限管理)
5. [技术架构决策](#五技术架构决策)
   - 5.1 [架构原则](#51-架构原则)
   - 5.2 [技术选型理由](#52-技术选型理由)
   - 5.3 [数据库核心表结构](#53-数据库核心表结构)
6. [工程化路线图](#六工程化路线图)
   - 6.1 [已完成（P0/P1）](#61-已完成p0p1)
   - 6.2 [P2：知识库（RAG）](#62-p2知识库rag)
   - 6.3 [P3：工作流引擎](#63-p3工作流引擎)
   - 6.4 [P4：Skill 可插拔](#64-p4-skill-可插拔)
7. [验收标准总览](#七验收标准总览)
8. [遗留与待定](#八遗留与待定)

---

## 一、背景与立项依据

### 1.1 客户背景

中石化完井研究中心是中国石油化工集团旗下的专业研究机构，承担油气井完井工程技术研究、工程服务与技术支撑任务。中心 IT 部门负责管理数百台服务器、存储设备、网络设备及其配套备件，包括内存条、硬盘、网卡、CPU、HBA 卡、光模块等各类 IT 耗材。

IT 备件采购与管理业务具有以下特点：

- **物料种类繁多**：涵盖服务器配件、存储介质、网络设备配套耗材等数千个 SKU，涉及华为、Dell、HP、Cisco、NetApp 等主流品牌。
- **型号编码复杂**：同一物理器件在不同供应商、不同采购批次中可能使用完全不同的型号编码（PN），导致数据治理难度极高。
- **采购频率高、金额大**：年度备件采购预算规模可观，成本核算精度直接影响利润核算与经营决策。
- **多系统分散管理**：历史上依赖氚云（泛微低代码平台）、Excel 表格、电子邮件等多种工具进行数据管理，信息孤岛严重。

### 1.2 现有痛点

#### 痛点一：型号数据混乱

同一个备件可能有十几种写法。以一块服务器内存为例，其在不同渠道中的型号记录可能包括：

- 厂商官方型号（MPN）：`MTA18ASF4G72PDZ-3G2R`
- 供应商自定义编码：`HY-DDR4-32G-3200-RDIMM`
- 客户系统编码：`MEM-RH2288V3-32G`
- 氚云录入变体：`MTA18ASF4G72PDZ-3G2R V2.0`、`mta18asf4g72pdz-3g2r`

由于不同采购员录入习惯不同，同一型号在系统中被分散记录为多个独立的 PN（料号）。这导致：

- 库存数据离散，无法汇总同款型号的真实库存量
- 查询时必须记忆多个变体写法，极易遗漏
- 历史数据无法追溯到真正的物理同款

经数据分析，现有数据中 **21,750 至 23,152 个型号（占比约 94%）存在恒等别名问题**，即同一物理器件被登记为多个独立 SKU。

#### 痛点二：成本核算失真

成本走**移动加权平均法**，但由于型号碎片化，同款型号被分散为多个独立 SKU，各自维护一个独立的成本池：

- 同款内存，因为 PN 写法不同，系统将其视为两个不同型号，分别计算加权平均成本
- 当一个 PN 的库存先售完时，从另一个 PN 出货，但成本基数错误，导致毛利核算失真
- 管理层拿到的毛利分析报表数据不可信，无法作为经营决策依据

这是一个**根本性的数据质量问题**：成本口径的正确性完全依赖型号主数据的唯一性，而后者当前处于严重碎片化状态。实际上 21,750/23,152（94%）的型号存在恒等别名问题，加权平均被污染，毛利分析数据不可信。

#### 痛点三：采购决策依赖人工经验

销售和采购人员在处理客户询价时，必须手动查询多个系统：

1. 打开氚云查库存
2. 打开历史 Excel 表查上次采购价
3. 在历史邮件中搜索供应商报价
4. 人工综合以上信息给出报价

这个流程平均耗时 **15–30 分钟 / 条询价**，对于 20 条以上的批量询价单，往往需要大半天时间处理。此外，历史成交价散落在进出口记录里，无系统化提取，完全依赖个人记忆和经验，人员流动时知识流失严重。

#### 痛点四：数据导入摩擦

氚云系统（泛微低代码平台）导出的采购/销售明细为 Excel 格式，且具有以下特殊性：

- **双表头格式**：第一行为氚云内部字段 ID（`F0000001` 等），第二行才是人类可读字段名
- **数据质量参差**：存在空行、合并单元格、编码不规范等问题
- **无法直接入库**：必须经过人工清洗、格式转换后才能录入

这个转换过程完全依赖人工操作，每次导入耗时数小时，且容易引入人为错误。

#### 痛点五：管理透明度不足

管理层（老板级别）无法实时获取以下经营信息：

- 哪类备件最赚钱（毛利排行）
- 哪个客户贡献最大（客户价值分析）
- 库存是否存在积压风险（库龄分析）
- 各销售员业绩对比

目前经营分析全靠月底人工整理报表，数据滞后 **15–30 天**，且人工处理过程中容易引入统计口径不一致的问题。

#### 痛点六：AI 能力未被利用

团队已有基本的 AI 使用意识（了解 ChatGPT / 文心等工具），但面临以下障碍：

- **数据隔离**：LLM 无法访问内部价格数据和库存数据，只能依赖通用知识，给出的价格建议严重脱离实际
- **没有工具接口**：缺乏将内部数据系统化地提供给 AI 的机制
- **权限顾虑**：担心将成本数据暴露给不应知道的角色（如销售不应看到采购成本）

### 1.3 立项目标

用一套统一的信息管理系统替代 Excel + 氚云导出 + 人工查价的工作流程，实现以下核心目标：

**目标一：型号数据统一治理**
- 建立型号主数据（`dim_part`）唯一身份体系
- 提供主数据清洗、合并、标准化工作台
- 支持别名管理，实现"多文本写法 → 单一物理器件身份"的准确映射

**目标二：成本核算口径统一**
- 成本聚合以 `part_id`（商品身份）为键，而非 PN 文本
- 支持移动加权平均 + FIFO 双轨并行
- 合并操作后自动触发历史成本重算，保证口径一致性

**目标三：AI 辅助报价**
- 智能体可实时访问库存数据、历史成交价、供应商信息
- 支持单条询价（30 秒内出结果）和批量询价（Excel 上传 → 自动处理）
- 严格的权限隔离：销售账号无法通过 AI 接口获取成本 / 毛利数据

**目标四：管理驾驶舱**
- 利润、库存、客户三维实时分析
- 可按时间范围、产品类别、销售员等维度过滤
- 管理层直接查看，无需等待月报整理

---

## 二、用户画像与需求

### 2.1 用户角色

| 角色 | 英文标识 | 预计人数 | 核心诉求 | 数据访问范围 |
|------|----------|----------|----------|-------------|
| 管理员 | `admin` | 1 | 系统配置、账户管理、主数据治理 | 全量数据 + 系统配置 |
| 老板 | `boss` | 1-2 | 经营分析、利润报表、全量数据查看 | 全量数据（含成本/毛利） |
| 销售 | `sales` | 3-5 | AI 报价助手、自己负责客户的历史价格查询 | 客户数据（仅自己客户）、价格（不含成本） |
| 采购 | `purchaser` | 2-3 | 供应商比价、成本核算、采购记录查询 | 全部采购数据、成本数据 |
| 只读 | `readonly` | N | 型号查询、库存查询 | 基础产品信息、库存数量 |

**角色权限层级**：

```
admin
  └── 全部权限 + 系统管理

boss
  └── 全量数据查看（含成本/毛利/供应商）
  └── 经营分析面板

purchaser
  └── 采购记录 CRUD
  └── 成本数据可见
  └── 供应商信息可见

sales
  └── 自己客户的销售记录查看
  └── 价格可见（不含成本/毛利）
  └── AI 报价助手访问

readonly
  └── 型号信息查看
  └── 库存数量查看
  └── 无价格/成本权限
```

### 2.2 核心用户故事

#### 销售场景

**场景 S1：单条快速报价**

> "客户问我 RH2288HV3 服务器内存 32G DDR4 的价格，我打开 AI 助手，直接问'华为 32G DDR4 报多少钱'，助手查库给我历史成交价和建议售价，30 秒出结果。"

验收条件：
- 输入自然语言问题，AI 在 30 秒内返回结构化报价信息
- 返回内容包括：当前库存数量、近 90 天历史成交均价、建议售价区间
- 销售账号查询结果中不包含采购成本和毛利字段

**场景 S2：批量询价处理**

> "客户发来一份 20 个型号的询价单，我把 Excel 上传给 AI，助手自动批量查价，生成回复邮件。"

验收条件：
- 支持上传 Excel 格式询价单（最多 100 行）
- AI 自动识别型号列，逐行批量查价
- 生成包含报价信息的 Excel 文件，可直接下载发送给客户
- 整个流程在 2 分钟内完成（20 行询价单）

**场景 S3：历史价格查询**

> "客户问上次买这个型号是多少价格，我直接在系统里搜，看到所有历史订单记录和价格。"

验收条件：
- 仅显示该销售员负责的客户的历史记录
- 按时间倒序排列
- 支持按型号、客户、时间范围筛选

#### 采购场景

**场景 P1：供应商历史价格查询**

> "我需要知道这批内存上次从哪个供应商买的、买了多少钱，直接在系统里搜型号就能看到历史采购记录。"

验收条件：
- 搜索型号（支持模糊匹配和别名匹配）
- 返回所有历史采购记录：供应商、采购日期、数量、单价
- 多个别名写法可归一到同一型号查询

**场景 P2：型号主数据合并**

> "同一个型号被录成了三个不同的 PN，库存分散了，我需要在治理工作台合并它们。"

验收条件：
- 治理工作台显示疑似重复型号候选
- 操作合并后，库存、采购、销售历史行全部重指到合并后的主型号
- 成本自动重算，合并后的加权平均成本正确

#### 管理场景

**场景 M1：经营分析实时查看**

> "我想知道上半年哪类产品毛利最高、哪个销售贡献最大，直接在分析面板看，不用等月报。"

验收条件：
- 利润分析面板支持按型号、销售员、客户三个维度聚合
- 支持自定义时间范围过滤
- 数据实时（无延迟批处理）

**场景 M2：库存资产查看**

> "IT 采购这块的库存成本是多少、占用了多少资金，实时可查。"

验收条件：
- 库存总览显示：型号、数量、单位成本（加权平均）、总资产价值
- 支持按品类、品牌过滤
- 数字与成本引擎的计算口径一致

---

## 三、问题定义与核心挑战

### 3.1 核心问题：型号身份碎片化

#### 问题本质

IT 备件没有像商品条码（EAN/UPC）那样的全球唯一标识。同一个物理器件可以有：

| 编码类型 | 示例 | 说明 |
|----------|------|------|
| 厂商官方型号（MPN） | `MTA18ASF4G72PDZ-3G2R` | 厂商定义，相对稳定 |
| 供应商自定义编码 | `HY-DDR4-32G-3200-RDIMM` | 各供应商不同，无法跨商家统一 |
| 客户系统编码 | `MEM-32G-2666-RD` | 客户内部编码，与实物无直接关系 |
| 版本变体写法 | `MTA18ASF4G72PDZ-3G2R V2.0` | 含 V-code 版本前缀，被视为不同型号 |
| 大小写变体 | `mta18asf4g72pdz-3g2r` | 同一字符串的大小写差异 |

系统必须解决**"多文本 → 单身份"**的映射问题。与传统商品管理不同，这个映射：
- 不能依赖一次性人工处理（新数据持续进来）
- 不能完全依赖精确匹配（型号写法差异太大）
- 必须在数据层解决（不能只靠展示层合并）

#### 解决方案设计

```
dim_part 表（型号主数据）
├── id          INTEGER  -- 商品身份主键（不可变，与文本解耦）
├── pn_std      TEXT     -- 当前标准写法（可改名，但 id 不变）
├── brand       TEXT     -- 品牌
├── category    TEXT     -- 品类
└── merged_into INTEGER  -- 合并指针（NULL=主型号，非 NULL=已合并）

part_alias 表（别名映射）
├── id          INTEGER
├── part_id     INTEGER  -- 指向 dim_part.id（合并后路径压缩，链长恒≤1）
├── alias_text  TEXT     -- 原始别名文本（入库时录入）
└── source      TEXT     -- 来源（import / manual / governance）
```

**归一流程**：新型号文本入库时，`part_resolver` 先查 `part_alias` 精确匹配，未命中则通过 `pg_trgm` 模糊召回候选，Python rerank 后决定是否自动归一或加入治理队列。

**合并语义**：
- 合并 A → B：`A.merged_into = B.id`，A 的所有 alias 重指 B，所有事实行（采购/销售/库存）`part_id` 更新为 B
- 路径压缩：禁止链长 > 1，合并时强制检查并压缩
- 取消合并：恢复 A 独立，重新重放历史行

### 3.2 核心问题：成本口径裂脑

#### 问题本质

移动加权成本的计算公式为：

```
新加权平均成本 = (当前库存量 × 当前单位成本 + 新入库数量 × 新采购单价)
                  ÷ (当前库存量 + 新入库数量)
```

这个公式的正确性**完全依赖"当前库存量"和"当前单位成本"基于同一物理器件身份聚合**。

当前问题：同款内存条被登记为 PN=`A` 和 PN=`B` 两个独立 SKU，各自维护独立的成本池：

```
SKU-A: 库存 10 个，加权成本 ¥800/个
SKU-B: 库存  5 个，加权成本 ¥820/个

真实情况：这是同一款内存，合计库存 15 个，真实加权成本 ¥806.67/个

当 SKU-A 出货 10 个后，继续从 SKU-B 出货：
  → 系统记录毛利按 SKU-B 成本 ¥820 计算
  → 但实际进货混合了两批，真实成本应按 ¥806.67 计算
  → 毛利失真 ¥13.33/个
```

#### 解决方案设计

```
核心约束：所有事实表（购入行/销售行/库存行）的 part_id 列 NOT NULL

成本重算流程（cost.replay()）：
1. 按 part_id 收集所有采购行（时间升序）
2. 模拟逐批入库，维护移动加权平均
3. 按时间顺序插入销售行，计算出货 COGS
4. 最终写入 cost_snapshot 表

合并触发重算：
- 合并操作完成 → 事实行重指 part_id → 异步触发 cost.replay()
- 取消合并 → 事实行恢复 part_id → 重新触发 cost.replay()
- 支持手动触发（大批量合并后）
```

**FIFO 模式**（备用）：按采购批次先进先出，适用于批次管理要求严格的场景，与加权平均双轨并行，结果写入独立字段供对比。

### 3.3 核心挑战：AI 与业务数据的安全集成

#### 挑战描述

LLM 要有效辅助报价，必须访问以下内部数据：
- 内部成本价格（只有采购和老板可见，销售不可见）
- 历史销售价格（销售只能看自己客户）
- 库存数量（只读账号可见）

同时，LLM 本身不具备权限识别能力——如果直接将内部数据塞入 prompt，权限隔离就成了摆设。

#### 解决方案设计

**原则：权限在数据层，不在 prompt 层**

```python
# 工具函数层（不是 prompt 层）过滤权限
def search_parts(query: str, ctx: AgentContext) -> list[dict]:
    rows = db.query(Part).filter(...)  # 基础查询
    return [apply_field_visibility(row, ctx.user_role) for row in rows]

def apply_field_visibility(row: dict, role: str) -> dict:
    if role in ("sales", "readonly"):
        row.pop("cost_avg", None)       # 隐藏成本
        row.pop("gross_margin", None)   # 隐藏毛利
        row.pop("supplier", None)       # 隐藏供应商
    return row
```

**行级过滤（销售只见自己客户）**：

```python
# own_customers_only 账号：查询自动加 WHERE 条件
if user.own_customers_only:
    query = query.filter(
        SaleRow.customer_id.in_(user.customer_ids)
    )
```

**安全边界**：
- LLM 只能通过工具函数访问数据，没有直接数据库访问通道
- 工具函数的返回值已经过权限过滤，LLM 看不到被隐藏的字段
- 即使发生 prompt injection 攻击，LLM 也无法绕过数据层权限

---

## 四、产品功能规格

### 4.1 数据导入系统

#### 功能描述

将氚云系统导出的 Excel 文件（采购明细 / 销售明细）自动化解析并入库，消除人工处理环节。

#### 支持的导入类型

| 导入类型 | 数据来源 | 目标表 |
|----------|----------|--------|
| 采购明细 | 氚云采购模块导出 | `purchase_items` |
| 销售明细 | 氚云销售模块导出 | `sale_items` |
| 库存初始化 | 历史 Excel 快照 | `inventory_items` |

#### 关键技术特性

**双表头自动检测**

氚云导出的 Excel 具有特殊的双表头格式：

```
行 1（氚云内部 ID）: F0000001  F0000002  F0000003  ...
行 2（人类可读字段）: 采购日期  供应商    型号       ...
行 3+（数据行）:     2024-01   华为       32G DDR4  ...
```

系统通过正则 `F\d{7}` 匹配第一行，自动识别双表头格式，提取字段映射关系，无需人工配置列名。

**PN 标准化（Strategy B）**

入库前对型号字符串执行以下标准化操作：
1. 去除 V/v 版本前缀（`V2.0-SFP28` → `2.0-SFP28`，或按规则保留）
2. 统一大写
3. 去除首尾空白
4. 符号规范化（全角 → 半角）

**幂等入库**

每一条导入行以 `SHA256(氚云数据ID + 文件哈希)` 作为唯一键：
- **Skip 模式**（默认）：键冲突时跳过，不更新
- **Upsert 模式**：键冲突时更新除主键外的所有字段

这保证同一文件导入两次不会产生重复数据。

**导入后自动处理**

导入完成后自动执行：
1. 品牌/品类回填（基于型号文本的规则匹配）
2. 质量扫描（检测新导入数据中的质量问题）
3. 别名归一尝试（将新型号文本匹配到已知 `part_id`）

#### API 接口

```
POST /api/v1/import/upload
  Content-Type: multipart/form-data
  Body: { file: <xlsx>, type: "purchase|sale|inventory", mode: "skip|upsert" }

Response:
{
  "total": 150,
  "inserted": 143,
  "skipped": 5,
  "updated": 2,
  "errors": [...],
  "import_id": "imp_20240630_abc123"
}
```

#### 验收标准

| 验收项 | 标准 |
|--------|------|
| 双表头识别 | 上传氚云采购明细，自动识别双表头，无需人工配置字段映射 |
| 解析完整性 | 所有数据行正确解析（错误行记录到 `errors` 列表，不影响其余行） |
| 幂等性 | 同一文件导入两次，第二次全部 skip，`inserted=0` |
| V-code 处理 | 含版本前缀的型号正确标准化 |
| 性能 | 500 行 Excel 导入耗时 < 10 秒 |

---

### 4.2 型号主数据治理

#### 功能描述

建立型号主数据（`dim_part`）的唯一身份体系，提供可操作的工作台界面，消除历史数据中的型号碎片化问题。

#### 4.2.1 候选发现

系统通过两路召回机制自动发现疑似重复型号候选：

**召回路径一：pn_compact 重复组**
- 对型号标准写法（`pn_std`）去除所有非字母数字字符后取前缀（`pn_compact`）
- 相同 `pn_compact` 的型号分为一组
- 过滤条件：`pn_compact` 长度 ≥ 5 且非纯数字

**召回路径二：模糊匹配**
- 对标记为 `needs_review` 的型号，使用 `pg_trgm` 计算与候选列表的相似度
- 仅相似度 `score ≥ 0.70` 的候选进入治理队列

**质量过滤**：
- `pn_compact` 长度 < 5 → 过滤（避免 "DDR4"、"HDD" 等通用词误触发）
- 纯数字 → 过滤（序列号类编码不适用合并逻辑）

**队列排序**：候选按业务量（采购+销售总量）降序排列，高频型号优先审核。

#### 4.2.2 合并工作台

**候选列表界面**：

| 字段 | 说明 |
|------|------|
| 候选 A / 候选 B | 两个疑似重复型号的标准写法 |
| 相似度分数 | pg_trgm 计算的字符相似度（0–1） |
| A 的业务量 | 采购数量 + 销售数量总计 |
| B 的业务量 | 同上 |
| 候选来源 | compact 重复 / 模糊匹配 |

**操作**：

| 操作 | 说明 |
|------|------|
| 合并（选主型号） | 将 B 合并入 A（或 A 合并入 B），选择哪个作为标准写法保留 |
| 拒绝 | 确认两者不是同一器件，候选失效，不再出现 |
| 独立标记 | 标记为独立型号（`is_distinct=True`），跳过后续模糊匹配 |
| 批量确认独立 | 对一组候选批量标记独立，避免逐条点击 |

**合并后自动触发**：
1. 事实行重指 `part_id`（采购行、销售行、库存行）
2. 别名表更新（被合并型号的所有 alias 重指主型号，UPSERT 避免冲突）
3. 路径压缩（确保 `merged_into` 链长 ≤ 1）
4. 成本重算（`cost.replay()` 异步执行）

#### 4.2.3 别名管理

**别名来源**：
- 导入时自动发现的变体写法
- 人工手动添加
- 合并操作产生的恒等别名

**审核操作**：

| 操作 | 说明 |
|------|------|
| 审核通过（approve） | 确认别名归属正确，状态 `pending → approved` |
| 审核拒绝（reject） | 别名归属错误，标记为 `rejected` |
| 重指（reassign） | 将别名从当前型号重指到正确型号 |

**合并时别名处理**：合并操作产生的恒等别名（`A.pn_std` 作为 `B` 的别名）使用 UPSERT 操作，避免触发唯一键冲突。

#### 4.2.4 数据质量监控

**质量扫描检测项**：

| 问题类型 | 检测规则 | 严重程度 |
|----------|----------|----------|
| 品牌占位符 | `pn_std` 或 `brand` 含"待定"、"xxxxx"等占位符文本 | 中 |
| 无规格型号 | 型号字符串过短（< 4 字符）或仅含通用词 | 低 |
| 疑似重复 | `pn_compact` 重复但尚未处理的型号组 | 高 |
| 缺失品牌 | `brand` 为空且无法从型号推断 | 低 |
| 孤立库存 | 有库存记录但 `part_id` 未归一到任何主型号 | 高 |

**质量分（0–100）计算**：

```
质量分 = 品牌完整度得分（30 分）
        + 规格完整度得分（20 分）
        + 无疑似重复得分（30 分）
        + 无占位符得分（20 分）
```

**问题列表操作**：
- 关闭（dismiss）：确认该问题不需处理
- 已修复自动关闭：修复后下次扫描自动关闭对应问题单

**验收标准**：

| 验收项 | 标准 |
|--------|------|
| 治理效率 | 治理工作台可在 1–2 天内完成 1078 个待审型号的全部审核 |
| 合并正确性 | 合并后 30 项回归测试全部通过 |
| 成本一致性 | 合并后重算结果与合并前分组求和等价（误差 < ¥0.01） |
| 路径压缩 | 任意时刻 `merged_into` 链长 ≤ 1 |

---

### 4.3 成本与利润分析

#### 功能描述

基于统一的 `part_id` 口径，提供多维度利润核算与实时分析能力。

#### 成本方法

**主方法：移动加权平均（Moving Weighted Average）**

```
每次采购入库时更新：
新单位成本 = (旧库存数量 × 旧单位成本 + 入库数量 × 采购单价)
              ÷ (旧库存数量 + 入库数量)

出货时：
COGS    = 出货数量 × 出货时刻的当前单位成本
毛利    = 销售金额 - COGS
毛利率  = 毛利 ÷ 销售金额 × 100%
```

**备用方法：FIFO（先进先出）**

按采购批次先进先出，每个批次独立追踪库存和成本，出货优先消耗最早入库的批次。与加权平均双轨并行，结果存储在独立字段，供管理层对比参考。

#### 功能点

**利润总览面板**

支持以下三个聚合维度（可叠加过滤）：

| 维度 | 说明 |
|------|------|
| 按型号 | 显示每个 `part_id` 的营收、成本、毛利、毛利率 |
| 按销售员 | 汇总每个销售员名下的业绩 |
| 按客户 | 汇总每个客户的贡献 |

支持过滤条件：
- 时间范围（任意起止日期）
- 品类（内存/硬盘/网卡/CPU 等）
- 品牌
- 销售员（老板视角可选全部）

**利润排行榜**

Top N 最赚钱型号，支持按毛利额、毛利率两种排序方式，支持自定义 N（10 / 20 / 50 / 全部）。

**排除集**

支持将特定型号标记为"不计利润"（如内部测试件、维保附件），标记后该型号的销售行不计入利润统计，但仍保留在库存和采购记录中。

**手动触发重算**

提供手动触发按钮，在大批量合并操作完成后强制重算所有利润数据。

#### API 接口

```
GET /api/v1/profit/overview
  Query: start_date, end_date, group_by=part|sales|customer, brand, category

GET /api/v1/profit/ranking
  Query: top_n, sort_by=margin_amount|margin_rate, start_date, end_date

POST /api/v1/profit/replay
  Body: { part_ids: [...] }  # 空数组 = 全量重算
```

#### 验收标准

| 验收项 | 标准 |
|--------|------|
| 口径一致性 | `part_id` 分组 vs `pn` 分组逐行等价（零回归测试） |
| COGS 一致性 | 库存资产价值与利润 COGS 使用相同成本基准 |
| 重算幂等 | 重复触发重算，结果不变 |
| 性能 | 全量重算（5 万行事件）耗时 < 30 秒 |

---

### 4.4 库存管理

#### 功能描述

实时查询库存数量、成本价值，支持按 `part_id` 聚合显示（合并后多条记录合并展示）。

#### 功能点

**库存列表**

| 列 | 说明 |
|----|------|
| 型号（pn_std） | 标准写法，可点击查看详情 |
| 品牌 | 品牌名 |
| 品类 | 产品大类 |
| 仓库 | 库位/仓库名 |
| 数量 | 当前库存数量 |
| 单位成本 | 移动加权平均成本 |
| 总价值 | 数量 × 单位成本 |
| 最后更新 | 最近一次库存变动时间 |

**聚合规则**：同一 `part_id` 在不同仓库的库存分行显示；被合并的型号归并到主型号下汇总展示（合并前的各 PN 不再单独出现）。

**成本回填（backfill_costs）**：合并操作后，系统自动将库存成本更新到新 `part_id` 口径，保证库存价值统计的一致性。

**库存搜索**：支持按型号文本（含别名）、品牌、品类、数量区间过滤。

#### API 接口

```
GET /api/v1/inventory
  Query: part_id, brand, category, warehouse, min_qty, max_qty, page, page_size

GET /api/v1/inventory/summary
  Response: { total_value, total_skus, zero_stock_skus, top_categories }
```

---

### 4.5 AI 智能体

#### 功能描述

接入内部业务数据的 LLM 对话助手，支持多轮对话、工具调用，按用户角色进行权限隔离。

#### 工具列表（10 个）

| 工具名 | 功能说明 | 可用角色 |
|--------|----------|----------|
| `search_parts` | 模糊搜索型号（支持别名归一） | 全部 |
| `get_part_overview` | 型号详情（含合并重定向、历史价格） | 全部 |
| `get_profit_ranking` | 利润排行（top N 型号） | boss / admin |
| `list_recent_purchases` | 近期采购记录（含供应商、单价） | purchaser / boss / admin |
| `inspect_file` | Excel 文件预览（返回前 N 行结构） | 全部 |
| `read_file_rows` | 分批读取文件行（大文件分页） | 全部 |
| `read_document` | 读取文档内容（PDF/Word） | 全部 |
| `lookup_prices_bulk` | 批量查价（输入型号列表，返回历史价格） | 全部 |
| `write_excel` | 生成 Excel 报表（返回下载链接） | 全部 |
| `write_report` | 生成 Word 报告（返回下载链接） | boss / admin |

**权限说明**：工具返回数据前执行 `apply_field_visibility()`，按角色隐藏敏感字段（成本/毛利/供应商）。

#### 7 个标准场景

| 场景编号 | 场景名称 | 描述 |
|----------|----------|------|
| A | AI 报价 | 销售员询问某型号当前售价建议 |
| B | 价格压缩分析 | 采购询问同款型号供应商间价格对比 |
| C | 型号解释 | 解释型号规格含义（如 DDR4-3200-RDIMM-32G 各参数含义） |
| D | 批量询价处理 | 上传客户询价单 Excel，批量查价并生成回复文件 |
| D2 | 拆整机 BOM | 将整机（如服务器）拆解为配件清单及各件价格 |
| D3 | 配整机 | 根据客户需求，推荐配件组合并汇总报价 |
| E | 经营分析 | 老板询问经营数据，生成分析报告 |

#### 会话管理

**服务端持久化**：

```sql
chat_session: id, user_id, title, created_at, updated_at, stopped
chat_message: id, session_id, role, content, tool_calls, created_at
```

**历史窗口控制**：
- 最多携带 20 条历史消息进 LLM 上下文
- 单条消息超过 8000 字符时，服务端自动截断（截断点在 token 边界）

**并发控制**：
- 同一会话同时只允许一轮生成
- 新请求到达时若已有生成中的请求，返回 `HTTP 409 Conflict`

**中断语义**：
- 用户主动取消（Cancel）：`stopped=True` 落库，当前 AI 响应截断
- 网络断开：worker 继续跑完整答案并落库，用户重连后可查看完整回复

**共享口令回退登录限制**：使用回退登录（`fb=True`）的账号，无法使用 AI 智能体（返回 `403 Forbidden`），必须使用正式账号登录。

#### API 接口

```
POST /api/v1/chat/sessions
  Body: { title: "新会话" }
  Response: { session_id, title }

GET /api/v1/chat/sessions/{session_id}/messages
  Response: [ { role, content, created_at }, ... ]

POST /api/v1/chat/sessions/{session_id}/messages
  Body: { content: "华为 32G DDR4 内存多少钱？" }
  Response: SSE stream (text/event-stream)
    event: chunk, data: { text: "..." }
    event: done,  data: { message_id: "..." }

DELETE /api/v1/chat/sessions/{session_id}/current
  （取消当前生成）
```

#### 验收标准

| 验收项 | 标准 |
|--------|------|
| 报价响应速度 | "华为 RH2288 32G 内存多少钱？"→ 30 秒内返回历史成交均价和建议售价 |
| 批量处理能力 | 上传 20 行询价单 Excel → 批量查价 → 生成含报价的 Excel 文件，< 2 分钟 |
| 权限隔离 | 销售账号查询返回结果中不含成本/毛利字段（代码层验证） |
| 并发控制 | 同一会话第二次请求返回 409 |
| 幂等恢复 | 断网重连后，历史消息完整，无内容丢失 |

---

### 4.6 替代品管理

#### 功能描述

管理型号间的替代关系，支持查询某型号的可替代备选方案，辅助采购和销售决策。

#### 数据模型

```sql
part_substitutes:
  id           INTEGER PRIMARY KEY
  part_a_id    INTEGER REFERENCES dim_part(id)
  part_b_id    INTEGER REFERENCES dim_part(id)
  direction    ENUM('both', 'a_to_b', 'b_to_a')  -- 替代方向
  status       ENUM('active', 'pending', 'deprecated')
  notes        TEXT    -- 替代说明（如"规格兼容但接口不同，需确认"）
  created_by   INTEGER REFERENCES users(id)
  created_at   TIMESTAMPTZ

CONSTRAINT: part_a_id < part_b_id  -- 规范序，防双向数据二义
```

**规范序约束**：强制 `part_a_id < part_b_id`，确保同一对型号只有一条记录，`direction` 字段明确替代方向。

**替代方向说明**：

| direction | 含义 |
|-----------|------|
| `both` | A 和 B 互相可替代 |
| `a_to_b` | A 可以替代 B（但 B 不一定能替代 A） |
| `b_to_a` | B 可以替代 A |

#### 功能点

- 查询某型号的所有替代品（正向 + 反向合并查询）
- 在型号详情页展示替代品列表
- AI 工具 `search_parts` 在库存不足时自动提示可替代型号
- 替代关系审核（`pending → active`）

---

### 4.7 账户与权限管理

#### 功能描述

多角色访问控制（RBAC），支持字段级权限和行级过滤，确保各角色只能访问其权限范围内的数据。

#### 权限模型

**角色-权限矩阵**：

| 权限项 | admin | boss | purchaser | sales | readonly |
|--------|:-----:|:----:|:---------:|:-----:|:--------:|
| 系统配置 | ✓ | - | - | - | - |
| 账户管理 | ✓ | - | - | - | - |
| 主数据治理 | ✓ | - | - | - | - |
| 全量数据查看 | ✓ | ✓ | - | - | - |
| 成本/毛利字段 | ✓ | ✓ | ✓ | - | - |
| 供应商字段 | ✓ | ✓ | ✓ | - | - |
| 价格字段 | ✓ | ✓ | ✓ | ✓ | - |
| 采购记录 CRUD | ✓ | 查看 | ✓ | - | - |
| 销售记录查看 | ✓ | ✓ | ✓ | 仅自己客户 | - |
| 库存查看 | ✓ | ✓ | ✓ | ✓ | ✓ |
| AI 智能体 | ✓ | ✓ | ✓ | ✓ | - |

**字段级权限（field_visibility）**：

以下字段组独立控制可见性：

| 字段组 | 包含字段 | 默认可见角色 |
|--------|----------|-------------|
| cost | `cost_avg`, `cost_fifo`, `cogs` | admin, boss, purchaser |
| price | `sale_price`, `price_history` | admin, boss, purchaser, sales |
| margin | `gross_margin`, `margin_rate` | admin, boss |
| supplier | `supplier_name`, `supplier_contact` | admin, boss, purchaser |

**行级过滤（own_customers_only）**：

账号级开关（`users.own_customers_only BOOLEAN`），启用后：
- 该账号的所有销售记录查询自动附加 `WHERE customer_id IN (用户绑定的客户列表)`
- AI 工具调用同样受此限制

#### 账户安全

**登录锁定**：
- 连续登录失败 5 次 → 账号锁定 15 分钟
- 锁定期间任何登录尝试均返回 `423 Locked`（不更新失败计数）
- 15 分钟后自动解锁

**Token 即时吊销**：
- `users.token_version` 整数字段，每次修改密码或管理员手动吊销时递增
- JWT 中携带 `token_version`，验证时检查是否与数据库一致，不一致则视为已失效

**共享口令回退登录**：
- 提供紧急访问的回退登录机制（`fb=True` 参数）
- 回退登录账号禁用 AI 智能体、禁止数据写入操作

---

## 五、技术架构决策

### 5.1 架构原则

**原则一：商品身份 = `dim_part.id`**

禁止以 PN 文本作为聚合键或过滤键。所有事实表（采购行、销售行、库存行）的 `part_id` 列设为 `NOT NULL`，在数据库层强制约束。任何按型号汇总的查询必须 `GROUP BY part_id`，不得 `GROUP BY pn_std`。

**原则二：权限在数据层**

`apply_field_visibility()` 在工具/API 层执行，不依赖 prompt 层的指令来隐藏数据。这确保即使 LLM 被 prompt injection，也无法绕过权限限制。

**原则三：服务端为唯一事实源**

会话记录、工作流定义、成本快照全部存储在 PostgreSQL 中。客户端（前端/AI 客户端）不持有任何关键业务状态，可随时重建。

**原则四：幂等操作**

所有导入、回填、重算操作均设计为幂等：重复执行不产生副作用，不需要担心"跑了两次"的问题。这是系统可靠性的基础。

**原则五：路径压缩恒保持**

型号合并的 `merged_into` 指针链长度恒 ≤ 1。每次合并操作时强制检查并压缩，防止链式合并导致的性能退化和语义混乱。

### 5.2 技术选型理由

| 技术 | 版本 | 选型理由 |
|------|------|----------|
| **FastAPI** | 0.111+ | 原生异步（async/await），SSE 流式输出天然支持，Pydantic v2 提供类型安全的请求/响应验证 |
| **PostgreSQL** | 15 | 事务保证合并操作原子性；`pg_trgm` 扩展提供 GIN 索引模糊搜索（型号搜索核心）；成熟的 JSONB 支持（EAV 规格数据） |
| **pg_trgm** | 内置 | 字符三元组相似度计算，支持 `%` 模糊运算符和 `<->` 距离运算符，是型号模糊匹配的技术基础 |
| **SQLAlchemy** | 2.0 | ORM 类型安全，async session 支持，复杂查询可 `text()` 兜底，与 Alembic 原生集成 |
| **Alembic** | 最新 | 数据库迁移版本控制，`single-head` 测试保证迁移链完整无分叉 |
| **React + AntD** | React 18 / AntD 5 | 企业内部系统的标准组件库，数据表格、表单、分页等组件开箱即用，减少 UI 开发量 |
| **Vite** | 5+ | 极速开发服务器和构建工具，TypeScript 支持，与 React 生态无缝集成 |
| **uv** | 最新 | Python 包管理器，比 pip 快 10–100 倍，锁文件确保依赖一致性 |
| **OpenAI 兼容 API** | — | 设计为可切换 DeepSeek / Qwen / 其他厂商模型，不锁定单一 LLM 提供商；通过 `OPENAI_BASE_URL` 环境变量切换 |
| **Docker Compose** | v2 | 统一开发和生产环境，Postgres + 后端 + 前端一键启动 |
| **GitHub Actions** | — | CI 自动化：pytest 后端测试 + TypeScript 编译 + Vite 生产构建 |

### 5.3 数据库核心表结构

```sql
-- 型号主数据（身份锚）
dim_part (
  id           SERIAL PRIMARY KEY,
  pn_std       TEXT NOT NULL UNIQUE,     -- 标准型号写法
  pn_compact   TEXT,                      -- 去符号版本，用于重复检测
  brand        TEXT,
  category     TEXT,
  merged_into  INTEGER REFERENCES dim_part(id),  -- NULL=主型号
  is_distinct  BOOLEAN DEFAULT FALSE,             -- 已确认独立，跳过模糊匹配
  needs_review BOOLEAN DEFAULT FALSE,
  created_at   TIMESTAMPTZ DEFAULT now()
);

-- 别名映射
part_alias (
  id          SERIAL PRIMARY KEY,
  part_id     INTEGER NOT NULL REFERENCES dim_part(id),
  alias_text  TEXT NOT NULL,
  source      TEXT,
  status      TEXT DEFAULT 'approved',
  UNIQUE(alias_text)
);

-- 采购行（事实表）
purchase_items (
  id            SERIAL PRIMARY KEY,
  part_id       INTEGER NOT NULL REFERENCES dim_part(id),
  pn_raw        TEXT,                   -- 原始录入型号（保留）
  supplier      TEXT,
  qty           INTEGER NOT NULL,
  unit_price    NUMERIC(12,4),
  purchase_date DATE,
  source_type   TEXT DEFAULT 'purchase', -- purchase / warranty
  import_key    TEXT UNIQUE              -- 幂等键
);

-- 销售行（事实表）
sale_items (
  id             SERIAL PRIMARY KEY,
  part_id        INTEGER NOT NULL REFERENCES dim_part(id),
  customer_id    INTEGER,
  salesperson_id INTEGER,
  qty            INTEGER NOT NULL,
  unit_price     NUMERIC(12,4),
  sale_date      DATE,
  import_key     TEXT UNIQUE
);

-- 库存行（事实表）
inventory_items (
  id          SERIAL PRIMARY KEY,
  part_id     INTEGER NOT NULL REFERENCES dim_part(id),
  warehouse   TEXT,
  qty         INTEGER NOT NULL,
  cost_avg    NUMERIC(12,4),  -- 移动加权平均成本
  cost_fifo   NUMERIC(12,4)   -- FIFO 成本
);

-- 治理候选
governance_candidate (
  id         SERIAL PRIMARY KEY,
  part_a_id  INTEGER NOT NULL REFERENCES dim_part(id),
  part_b_id  INTEGER NOT NULL REFERENCES dim_part(id),
  score      NUMERIC(4,3),    -- 相似度分数（0.70–1.00）
  source     TEXT,            -- compact_dup / fuzzy_match
  status     TEXT DEFAULT 'pending',  -- pending / merged / rejected / distinct
  reviewed_at TIMESTAMPTZ,
  reviewed_by INTEGER REFERENCES users(id)
);
```

---

## 六、工程化路线图

### 6.1 已完成（P0/P1）

- [x] **基础 CRUD**：型号 / 采购 / 库存 / 替代品的增删改查 API
- [x] **数据导入 ETL 流水线**：氚云 Excel 双表头自动解析、PN 标准化、幂等入库
- [x] **成本引擎**：移动加权平均 + FIFO replay，按 `part_id` 聚合
- [x] **利润分析**：三维聚合（型号/销售/客户）+ 排除集 + 手动重算
- [x] **AI 智能体**：10 工具 + 7 场景 + 服务端会话 + SSE 流式输出
- [x] **RBAC**：5 角色 + 字段级权限 + 行级过滤 + 登录锁定 + token 吊销
- [x] **型号主数据整改**：`dim_part.id` 身份锚 + 合并 + 路径压缩 + 质量扫描
- [x] **治理工作台**：候选审核 + 别名管理 + 质量问题列表
- [x] **CI/CD**：GitHub Actions（pytest + tsc + vite build）
- [x] **规格查询**：`product_specs` EAV 表 + `/parts/search` 规格过滤接口

### 6.2 P2：知识库（RAG）

**用途**：产品资料、兼容性矩阵、供应商档案、SOP 文档的语义检索，增强 AI 助手的专业知识深度。

**典型问题场景**：
> "这块 HBA 卡和我们的存储阵列兼容吗？"→ AI 需要查询内部兼容性文档，而不是依赖 LLM 的通用知识。

**核心设计**：

```sql
-- 知识库三层数据模型
kb_collection (id, name, description, visibility, owner_id)
kb_document   (id, collection_id, title, source_url, content_hash)
kb_chunk      (id, document_id, chunk_index, content, embedding VECTOR(1536))
```

**检索策略**：`pgvector` 向量检索 + `pg_trgm` 关键词检索混合，结果使用 RRF（Reciprocal Rank Fusion）融合后选 top-K 进 LLM 上下文。

**权限控制**：`collection.visibility` 过滤后才将文档内容加入 LLM 上下文，确保敏感文档（如内部定价策略）不泄露给低权限用户。

**新增工具**：`search_knowledge(query, collection_ids)` 接入智能体工具列表。

**基础设施依赖**：PostgreSQL 镜像从 `postgres:15` 升级到 `pgvector/pgvector:pg15`（已在 Docker Compose 中规划）。

**里程碑**：
1. pgvector 扩展部署 + embedding 生成 pipeline
2. 知识库 CRUD API + 文档上传（PDF/Word/TXT）
3. `search_knowledge` 工具 + 智能体集成
4. 权限过滤验证

### 6.3 P3：工作流引擎

**用途**：将重复性业务流程（询价 → 查价 → 回填 → 发采购单）显式建模为可执行的 DAG 工作流，减少人工操作步骤和遗漏风险。

**典型问题场景**：
> 销售收到客户询价单 → 转给采购查价 → 采购查完回填价格 → 销售核对发报价 → 若客户确认则生成采购单。
> 这个流程现在完全依靠人工记忆和沟通，容易卡在某个环节。

**核心设计**：

```sql
-- 工作流三层数据模型
wf_definition (id, name, dag_json, version)
wf_run        (id, definition_id, status, input_json, started_at)
wf_run_step   (id, run_id, node_id, status, output_json, started_at)
```

**节点类型**：

| 节点类型 | 说明 |
|----------|------|
| `tool` | 调用系统工具（如 `lookup_prices_bulk`） |
| `llm` | 调用 LLM 生成文本（如撰写报价邮件） |
| `branch` | 条件分支（如"库存充足 → 直接报价 / 库存不足 → 查替代品"） |
| `human` | 挂起等待人工确认（如"请确认报价后再发送"） |

**`human` 节点实现**：复用治理工作台的"待办列表"交互组件，工作流挂起时在用户的待办列表中生成一条任务，用户点击确认后工作流继续。

**智能体集成**：新增 `run_workflow(definition_id, input)` 工具，AI 可以触发预定义工作流作为复合操作。

**里程碑**：
1. 工作流引擎 core（DAG 执行、状态机）
2. human 节点 + 待办列表 UI
3. 询价工作流模板（首个预定义工作流）
4. `run_workflow` 工具 + 智能体集成

### 6.4 P4：Skill 可插拔

**用途**：新业务场景（如新产品线的报价规则、新客户的特殊定价逻辑）无需修改代码，由管理员在配置页面通过 UI 配置即可生效。

**典型问题场景**：
> 现有 A–E 场景是硬编码在代码中的。如果要新增一个"按客户级别自动打折"的报价场景，需要修改后端代码并重新部署。

**核心设计**：

```sql
skill (
  id            SERIAL PRIMARY KEY,
  name          TEXT UNIQUE NOT NULL,
  display_name  TEXT,
  manual_md     TEXT,        -- 场景手册（Markdown），注入 LLM system prompt
  tool_names    TEXT[],      -- 该场景允许使用的工具列表
  kb_collections INTEGER[], -- 关联的知识库集合
  roles         TEXT[],      -- 有权使用该 Skill 的角色
  enabled       BOOLEAN DEFAULT TRUE
)
```

**迁移计划**：现有 A–E 场景的 system prompt 和工具列表迁移为 `skill` 表记录，保持向后兼容。

**扩展点**：
- `@tool` 注册器升级：工具注册时携带角色过滤 + 分类元数据（`category: "查询" | "生成" | "分析"`）
- MCP 远期集成：支持将 MCP 工具翻译进工具注册器，外部工具接入无需修改核心代码

---

## 七、验收标准总览

| 功能域 | 关键验收指标 | 优先级 |
|--------|-------------|--------|
| **数据导入** | 氚云 Excel 无人工干预自动解析；重复导入幂等（skip 模式零写入） | P0 |
| **型号治理** | 1078 待审型号可在 1–2 天内完成审核；合并后 30 项回归测试全部通过 | P0 |
| **成本利润** | `part_id` 口径 vs `pn` 口径逐行等价（零回归测试）；COGS 与库存成本口径一致 | P0 |
| **AI 助手** | 30 秒内返回历史成交价；20 行询价单批量处理 < 2 分钟；SSE 流式输出正常 | P1 |
| **权限隔离** | 销售账号无法获取成本/毛利字段（代码层验证）；行级过滤仅见自己客户 | P0 |
| **系统稳定性** | 同一会话无并发生成（409）；断网不丢消息；导入 500 行 < 10 秒 | P1 |
| **CI/CD** | 所有 PR 必须通过 pytest + tsc + vite build 三项检查 | P0 |
| **数据迁移** | Alembic 迁移链 single-head，无分叉；所有迁移可顺序回放 | P0 |

---

## 八、遗留与待定

### 8.1 当前遗留问题

| 问题 | 描述 | 建议处理 |
|------|------|----------|
| **维保工单备件** | `maintenance_parts` 暂无数据源，本期不建表；维保体现为采购 `source_type=维保需求`（不计成本） | P2 评估是否引入独立维保模块 |
| **前端路由** | 现为基于 state 的路由（`useState` 切换页面），页面不支持直链和浏览器前进/后退 | P2 引入 `react-router`，需重构路由层 |
| **沙箱代码执行** | AI 场景中的代码执行（如 Python 数据分析）暂不支持；LLM 生成代码但不执行 | P3 独立容器沙箱，不在 API 进程内 exec |
| **LLM 用量统计** | 当前不记录 token 用量和费用；无法分析 AI 成本 | P2 加 `tokens_in / tokens_out / provider` 字段 |
| **评测框架** | 无固定问题集和期望工具轨迹，提示词改动可能导致现有场景退化 | P3 建立 LLM-as-Judge 评测 pipeline |
| **Docker 网络** | dev 库容器按旧 compose 配置以 `0.0.0.0:5432` 暴露，存在安全风险 | 建议重建容器，限制为 `127.0.0.1:5432` |

### 8.2 已知技术债

| 债务 | 影响 | 处理建议 |
|------|------|----------|
| 部分历史 `part_id` 为 NULL | 合并前导入的旧数据行无 `part_id`，影响成本口径 | 运行 `backfill_part_id` 脚本补全 |
| 前端 state-based routing | 用户无法分享页面链接；刷新丢失当前页 | P2 迁移至 react-router |
| 无完整的 E2E 测试 | 修改路由/UI 组件后无自动化回归保障 | P2 引入 Playwright E2E 测试套件 |
| pgvector 未启用 | RAG 功能（P2）依赖 pgvector，当前镜像未包含该扩展 | 升级 Docker 镜像（已规划） |

### 8.3 外部依赖风险

| 依赖 | 风险描述 | 缓解措施 |
|------|----------|----------|
| **LLM API 可用性** | 依赖外部 LLM API（OpenAI 兼容），存在服务不可用风险 | 使用 `OPENAI_BASE_URL` 支持快速切换至本地部署模型（如 Qwen） |
| **氚云数据格式** | 氚云升级可能改变 Excel 导出格式，导致双表头解析失败 | 导入时记录原始文件和解析日志，便于格式变更时快速修复 |
| **PostgreSQL 版本** | pgvector 要求 PostgreSQL ≥ 11，pg_trgm 为标准扩展无版本风险 | 锁定 `postgres:15-alpine` 镜像版本 |

---

*文档结束*

---

**文档变更历史**

| 版本 | 日期 | 变更说明 | 作者 |
|------|------|----------|------|
| v1.0 | 2026-06-30 | 初始版本，覆盖 P0–P4 全部功能域 | 产品团队 |
