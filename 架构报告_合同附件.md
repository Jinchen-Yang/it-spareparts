# IT 备件智能管理系统 — 架构与交付报告

> **本文件用途**：合同附件，明确双方对系统架构、分期交付内容、部署要求、验收标准的共同认知。
> **版本**：v1.0  **日期**：2026-05-25
> **代码仓库**：`https://github.com/Jinchen-Yang/it-spareparts`（私有）

---

## 一、项目背景与目标

### 1.1 业务痛点

- 氚云系统中**采购、销售、库存**三类数据需重复人工对账，无法快速回答"这个型号该报多少价"。
- 备件型号(PN)技术性强，**新员工识别困难**，培训周期长。
- 成本与售价不透明，**销售提价/采购压价缺少数据依据**。

### 1.2 项目目标(两期)

| 期 | 目标 | 形态 |
|---|---|---|
| **一期** | 把氚云三类导出数据汇入独立系统，提供"型号全景查询、成本利润计算、库存与数据治理"等核心查询能力。 | Web 工具(管理端+查询端) |
| **二期** | 在一期数据底座上构建 **AI 定价助手**：销售/采购用自然语言提问，AI 自动调用一期的查询/利润接口、并把技术型号翻译为人话。 | 钉钉/Web 对话式 Agent |

> **关键定位**：一期是 AI 助手的"数据地基与工具集"，不是终点；二期是面向最终用户(销售/采购)的智能交互层。

---

## 二、技术架构

### 2.1 总体分层

```
┌──────────────────────────────────────────────────────┐
│  浏览器(管理员/只读用户)                                │
│   ├ 数据导入页   ├ 型号查询页   ├ 利润分析页              │
│   └ 库存查询页   └ 数据治理页   └ 登录页                 │
└────────────────────────┬─────────────────────────────┘
                         │ HTTPS / JSON
┌────────────────────────┴─────────────────────────────┐
│  FastAPI 后端 (Python 3.11+)                          │
│   ├ API 层: auth / parts / profit / inventory /      │
│   │         substitutes / governance / import        │
│   ├ 服务层: part_overview / cost / profit /          │
│   │         inventory / substitute / governance      │
│   ├ ETL: reader → cleaner → transform → loader →     │
│   │      pipeline (双表头 / ffill / 幂等 / 审计)      │
│   ├ 安全: auth(HMAC token) + 权限钩子(预留)            │
│   └ 数据模型: SQLAlchemy 2.0 + Alembic 迁移           │
└────────────────────────┬─────────────────────────────┘
                         │
┌────────────────────────┴─────────────────────────────┐
│  PostgreSQL 15                                       │
│   ├ 维度表 dim_*: 型号/供应商/客户/PN 别名             │
│   ├ 事实表 f_*: 采购/销售/询价(头+行 拆分)              │
│   ├ 库存表 inventory: PN × 仓库 快照                   │
│   ├ 系统表 sys_*: 导入批次/错误/原始文件/审计日志      │
│   └ 替代料 part_substitute                            │
└──────────────────────────────────────────────────────┘
原始 Excel 文件按 SHA256 哈希名归档于本地卷
```

### 2.2 技术栈(固定)

| 层 | 选型 | 选型理由 |
|---|---|---|
| 后端语言 | Python 3.11+ | Excel 解析/数据清洗生态最强，FastAPI 性能足够 |
| Web 框架 | FastAPI | 异步、自动 OpenAPI 文档 |
| ORM | SQLAlchemy 2.0(声明式) | 稳定、显式、社区成熟 |
| 迁移 | Alembic | 数据库结构版本化 |
| 数据处理 | pandas + openpyxl | 仅支持 `.xlsx`(旧 `.xls` 不受 zip 容器约束，需另存为 `.xlsx`) |
| 数据库 | PostgreSQL 15 | 工程稳定、JSONB、TEXT[]、偏唯一索引、备份成熟 |
| 前端 | React 18 + Vite + TypeScript + Ant Design 5 | 标准企业级组合 |
| 包管理 | uv(Python) / npm(前端) | |
| 部署 | Docker Compose(app + db + volumes) | 一键启动、便于交付移交 |

> **关于性能与重构**：当前规模(2 万采购 + 5 万销售 + 3 万库存，约 20 内部用户)下，FastAPI + PostgreSQL 完全可承担，**不建议重构为 Rust/Go**——真正瓶颈是数据库查询，不是应用语言；语言重构将带来重大重写成本而无明显收益。当未来需要扩展时，正确的优化路径是：加索引 → 异步任务队列 → 物化视图，而非更换语言。

### 2.3 数据库设计要点

- **三类粒度**：订单头(`f_*_order`)与明细行(`f_*_line`)拆分，幂等键为氚云的"数据ID(不可修改)"。
- **金额精度**：全部 `NUMERIC(14,2)`/`NUMERIC(14,3)`/`NUMERIC(5,4)`，禁止浮点。
- **PN 标准化**：氚云内部 V 码 token(`V[0-9A-Z]{10,}`)被识别并截断；含空格的歧义 PN 保留原值并标 `needs_review`，通过 `part_alias` 表保留人工修订能力。
- **库存口径**：以氚云产品库存表的真实数量为准(`source_qty`)，每次导入覆盖；人工修正写入独立列(`manual_qty + is_qty_overridden`)，源系统与人工值并存可追溯。
- **审计**：库存修正、替代料、型号排除等所有人工操作写 `sys_audit_log`(实体类型/前后值/原因/操作人/时间)。

---

## 三、第一期交付内容(已实现)

### 3.1 功能清单

#### A. 数据导入管道

- [x] **三类 Excel 自动识别**：采购订单 / 销售订单 / 产品库存(按列名特征判定)。
- [x] **双表头解析**：自动跳过氚云的 `F\d{7}` 编码行，识别中文列名。
- [x] **续行 ffill**：多明细订单中主表字段为空的续行自动补齐到所属订单。
- [x] **字段清洗**：
  - PN 标准化(去 V 码 token)；
  - 金额定点数(修浮点毛刺如 `79.0999…`)；
  - 税率统一(`13.0%`/`0.13` → `0.1300`)；
  - 日期容错(`2026-03-31` 与 `2025/6/19 12:19:27` 都识别)；
  - 供应商名去全角括号后缀生成 normalized 名；
  - 采购类型按真实导出值标准化(销售订单/维保需求/指定采购/其他)。
- [x] **幂等去重**：以氚云"数据ID(不可修改)"为唯一键，同文件重传被 SHA256 拦截(同 success 文件唯一)，失败可重试。
- [x] **两种导入模式**：
  - `skip`(默认)：已存在的明细行跳过；
  - `upsert`(修复模式)：已存在的明细按新文件更新可修复字段(数量、单价、税率、状态等)；利润派生字段不被覆盖。
- [x] **导入报告**：源行/新增/更新/跳过/错误/未生效 计数，异常行预览，原始文件按哈希归档。
- [x] **异常容错**：坏文件(非真正的 `.xlsx`)给出明确中文提示，不阻塞其他操作。
- [x] **上传进度条**：上传百分比 + 上传完成后"处理中"提示。
- [x] **导入历史**：可点开查看任一批次的完整报告与错误明细。

#### B. 型号全景查询

- [x] **PN 模糊搜索**：按 `pn_std` 或描述命中。
- [x] **基本信息**：描述、品牌、品类、规格、单位、`needs_review` 标记。
- [x] **采购历史**(近 20，可分页查全部)：采购单号、日期、供应商、数量、单价、**采购类型**(维保行明确标橙提示"不计入成本")。
- [x] **销售历史**(近 20)：销售单号、日期、客户、数量、含税单价。
- [x] **库存表**：按仓库列出，含展示数量/源数量/人工修正值/单位成本/库存金额。
- [x] **替代料**：列表 + 添加(管理员，写审计)。
- [x] **利润概览**：两种成本法**并排显示**(单位成本 + 平均毛利率)。
- [x] **历史询价区间**(待 Step 4 询价数据导入后启用)：最小价/最大价/最近价/次数。

#### C. 成本与利润计算

- [x] **两种成本计价方法并排呈现**：
  - **移动加权平均**：每次进货刷新滚动均价；
  - **先进先出 FIFO**：按时间扣最老批次。
  - 两种方法每笔销售的单位成本同时计算并落库，利润页同时显示两套毛利/毛利率，可对比。
- [x] **COGS 严格按已售扣减**：每笔销售的成本 = 单位成本 × 销售数量；**未售部分作为库存资产价值另列**，**不计入毛利**(避免"卖 30 个却减掉 100 个的进货款"这种错误)。
- [x] **税口径**：默认按**不含税**(`ex_tax`)算毛利——销售单价多为含税(13%)，采购多为 0 税；混算会虚高毛利。
- [x] **维保采购不计成本**(已与客户口头确认)：`COST_PURCHASE_TYPES` 排除"维保需求"，所有成本路径(引擎/兜底/库存反算)统一遵守。
- [x] **期初/扣穿兜底**：销售早于采购历史窗口、或销售量超采购量时，用该型号最近采购价兜底；并区分 `computed`(真实匹配) / `fallback`(估算) / `none`(无成本) 三种来源，便于辨识。
- [x] **重算**：规则改变后一键 `POST /api/profit/recompute` 刷新；采购/销售导入成功后自动触发。
- [x] **三维度聚合**：按型号 / 销售员 / 客户。
- [x] **CSV 导出**：含两种成本法所有列；防 Excel 公式注入(`=+-@` 转义)。

#### D. 库存与替代料

- [x] **库存查询**：仓库筛选 + PN/描述搜索 + 分页表格。
- [x] **人工修正**：管理员可在行内弹窗中改 `manual_qty` / `safety_stock`，必填"修改原因"，自动写入审计；可随时"撤销人工修正"恢复源系统值。
- [x] **库存单位成本/资产价值自动回填**：采购/库存导入后，按"按型号加权平均不含税采购价"计算并填入；无采购记录的型号留空显示"未计算"，绝不臆造数字。
- [x] **替代料**：型号全景页直接添加；以 `(part_id_a, part_id_b)` 排序后唯一，杜绝双向重复；写审计；自关联自动拒绝。

#### E. 数据治理(质量闭环)

- [x] **异常汇总卡**：非标候选型号数 / 已排除 / 待复核 / 无成本行 / 负毛利行 / 估算成本行。
- [x] **非标型号识别**：自动揪出"一批备件"、"配件"、"打包"等笼统型号，以及不含字母数字的纯中文 PN。
- [x] **排除/恢复**：管理员可"排除"某型号(必填原因，写审计)，该型号销售行不再计入营收/利润 KPI；可随时恢复。
- [x] **重算入口**：治理页一键触发利润重算，立刻反映在利润分析页。
- [x] **审计可追溯**：所有排除/恢复/库存修正/替代料新增均落 `sys_audit_log`。

#### F. 用户与权限

- [x] **最小登录**：管理员 + 只读用户(基于 HMAC token，无需额外服务)。
- [x] **权限钩子预留**：身份上下文、数据范围过滤、字段脱敏、访问审计四个扩展点已布到所有敏感接口的调用链，默认关闭(`ENABLE_RBAC=False`)。**接入企业身份(钉钉/SSO)与字段级 RBAC 是二期内容**，现在做了二期再启用时不用重构接口。

#### G. 安全基线

- [x] **生产环境默认弱口令/密钥自检**：`ENVIRONMENT=prod` 时若 `ADMIN_PASSWORD` / `SECRET_KEY` / 数据库密码仍为默认值，**应用拒绝启动**；开发环境只告警不阻断。
- [x] **数据库端口不暴露公网**：Docker Compose 中 PostgreSQL 端口绑定 `127.0.0.1:5432`，仅本机可达。
- [x] **健康检查不泄露细节**：`/health/db` 异常时只回报 "unreachable"，详细错误写服务端日志。
- [x] **CSV 导出防公式注入**：以 `= + - @` 起始的单元格自动转义，避免 Excel 自动执行公式。

### 3.2 已交付数据规模(基于客户真实数据实测)

| 数据 | 规模 | 时间覆盖 |
|---|---|---|
| 采购订单 | 39,969 单 / 45,440 行 | 2024-03 → 2026-05 |
| 销售订单 | 37,352 单 / 47,648 行 | 2023-12 → 2026-05 |
| 库存(PN×仓库) | 28,165 条 | 当前快照 |
| 标准化型号 | 22,962 个 |  |
| 库存总资产价值 | 约 ¥5,028 万 |  |
| 利润成本匹配率 | computed ≈ 78% | 剩余为估算或无历史 |

### 3.3 关键交付价值证据

- **数据治理实测**：排除 1 个非标打包型号("一批备件"，将整批 ¥400 万采购成本错套到单件销售上)后，**整体毛利从账面 -262 万 翻正为 +893 万 ≈ 7% 毛利率**——证明系统具备识别并修正数据脏点的能力。
- **两种成本法并排**：同一份销售数据，移动加权 vs FIFO 毛利率差异在管理层维度可直接对比(典型差异 0–3 个百分点，个别型号差异 >50%，能识别数据异常)。
- **幂等与可修正**：同份文件重传被去重；源系统修字段后用 `upsert` 模式重导可正确更新旧数据(实测把人为改坏的 `unit_price=99999` 还原为真值 ¥1,566,361)。

---

## 四、第二期规划(AI 定价助手)

### 4.1 北极星

**给销售/采购提供自然语言交互的智能助手**：

- **销售场景**:
  - "老王问的 ST8000NM000A 报 2200 行不行？" → AI 自动调用型号全景、列出近 N 单成交价、当前库存、毛利空间、给出报价建议区间。
  - 销售看不到精确成本(权限收紧)，看到的是"达标/未达标"的毛利档位。
- **采购场景**:
  - "我现在要进 50 个 ST8000NM000A，目标价多少合理？" → AI 拉出历史采购均价、最近 3 次成交价、当前库存与销售速率，给出压价空间建议。
- **新员工场景**:
  - "这个 ST8000NM000A 是什么？" → AI 解释："希捷 8TB 7200 转 3.5 寸企业级 SATA 硬盘，服务器存储用"，并附上同型号近期成交与库存。

### 4.2 二期技术路线

| 模块 | 内容 |
|---|---|
| LLM 接入 | Anthropic Claude API(支持 tool use)；含 prompt cache 节省成本 |
| 工具层 | 把一期的 `/api/parts/overview`、`/profit`、`/inventory`、`/governance` 等接口包装成 LLM 工具 |
| 权限收紧 | 启用 `ENABLE_RBAC=True`(钩子已预留)：销售只看客户、不看成本；采购只看供应商、不看销售单价；老板看全量 |
| 接入方式 | 优先钉钉机器人(企业内部 IM)；可选企业微信、Web 聊天窗 |
| 审计 | 所有 AI 查询写访问日志，包含查询人、问题、调用工具、返回字段、是否触发脱敏 |
| 反推风险防护 | 销售即便能看毛利率，也只能看到分档(如"20–30%")，不能看精确值——避免根据自己售价反推出成本 |

### 4.3 二期交付前置条件

- 一期数据治理基本稳定(非标型号已批量标注、`needs_review` PN 人工复核完毕)。
- 客户确认权限矩阵(销售/采购/老板各自可见字段)。
- 客户提供 LLM 接入授权(Anthropic API key 或类似)及钉钉企业账号(若选钉钉机器人方案)。

---

## 五、后续可选扩展(第三期及以后，不属于本合同强制范围)

> 以下为来自架构评审的候选增强项，本合同不承诺实现；可作为后续二次开发对接。

| 类别 | 内容 |
|---|---|
| 权限与身份 | 完整 RBAC 角色管理、对接钉钉 SSO、Token 撤销机制、多租户/多公司隔离 |
| 任务编排 | 大文件后台异步导入(Celery/RQ)、利润重算任务化、失败重试与告警 |
| 数据资产 | 库存快照历史(每次导入留全量历史，可回看任一天库存)、利润口径版本固化(每次重算附规则版本号) |
| 业务建模 | 退货/换货/调拨/盘点 显式建模；替代料分组、品牌限制、有效期；询价时效与成交转化追踪 |
| 主数据治理 | 客户/供应商别名合并、停用、审核流程；型号合并(同一物理件不同 PN 写法) |
| 报表与决策 | 采购比价、自动报价建议、库存预警、低周转分析 |
| 观测性 | 结构化日志、关键指标 Prometheus 暴露、慢查询告警 |
| 灾备 | 异地备份、读副本 |

---

## 六、部署与运维要求

### 6.1 服务器最低配置(基于一期数据规模 + 二期预留)

| 项 | 最低 | **推荐** | 说明 |
|---|---|---|---|
| CPU | 2 核 | **4 核** | 计算密集仅在 recompute(几秒) |
| 内存 | 4 GB | **8 GB** | PostgreSQL 1–2 GB + 应用 300 MB + 系统 1 GB + 缓冲 |
| 系统盘 | 40 GB SSD | **60–80 GB SSD** | 数据库当前 ~100 MB，多年增长 + Docker 镜像 + 日志 + 备份 |
| 公网带宽 | 3 Mbps | **5 Mbps+** | Excel 上传约 15 MB，并发 5–10 人 |
| 操作系统 | Linux | **Ubuntu 22.04 LTS** | 长期支持 |
| 备份盘 | 同上独立卷 | 强烈推荐 | 用于 `pg_dump` 与文件归档 |

### 6.2 部署步骤(概要)

1. 安装 Docker、Docker Compose、Git、防火墙(只开 22 / 80 / 443)。
2. `git clone` 仓库到服务器(私有仓库需 deploy key 或 PAT)。
3. 复制 `.env.example` → `.env`，填入：
   - `POSTGRES_PASSWORD`: 强随机 ≥ 16 位
   - `ADMIN_PASSWORD`: 强随机
   - `SECRET_KEY`: 32 字节随机串
   - `ENVIRONMENT=prod`(确保默认值自检生效)
4. `docker compose up -d` + `docker compose exec app alembic upgrade head`。
5. Nginx 反向代理 + Let's Encrypt 免费 HTTPS 证书。
6. 配置 `pg_dump` 每日备份 cron，保留近 7 日。
7. 系统自动安全更新(`unattended-upgrades`)。

> 交付时提供完整的部署手册与一键脚本。

### 6.3 客户责任(部署后)

- 服务器物理安全与网络可用性。
- 应用层密码、密钥的妥善保管。
- 定期检查备份完整性。
- 氚云导出格式稳定性(若氚云列名变更，需联系开发方更新映射)。

---

## 七、待客户确认的业务口径(交付前清单)

下列业务规则均为 **`config.py` 中可配置项**，已设默认值；客户确认后改配置即生效，不需修改代码。

| # | 决策项 | 当前默认 | 客户拍板 |
|---|---|---|---|
| B1 | 计入营收的销售业务类型 | 仅"备件销售" |  |
| B2 | 维保采购是否计入成本 | **否(已确认)** | ✅ |
| B3 | 维保是否单独算账(自有营收=整体维保/单次维修) | 否(切出去) |  |
| B4 | FIFO/移动加权 排队范围 | 按型号全局 |  |
| B5 | 期初库存成本口径 | 最近采购价兜底 |  |
| B6 | 毛利含税口径 | 不含税(`ex_tax`) |  |
| B7 | 询价重复行处理 | 按内容去重 |  |
| C1 | 销售可见成本范围 | 完全不可见 |  |
| C2 | 销售可见毛利率 | 仅分档(达标/区间) |  |

---

## 八、验收标准(一期)

### 8.1 业务功能

- [ ] 三类真实 Excel(采购/销售/库存)均能识别并正确导入；重复成功文件被拒；失败文件可重试；坏文件给出明确中文提示。
- [ ] 任意 PN 查询，型号全景页能展示采购史/销售史/库存/替代料/两种成本法毛利/询价占位。
- [ ] 利润可按型号/销售员/客户三维度查看；两种成本法并排；负毛利/零价格/无成本/估算成本/已排除型号均可识别可筛。
- [ ] 改 `config.py` 业务口径后调用 `recompute` 即生效，无需改代码。
- [ ] 库存重导后 `source_qty` 更新；人工修正后 `manual_qty` 保留且有审计记录。
- [ ] 数据治理页能识别"一批备件"等非标型号；排除后该型号销售行不再污染利润 KPI。
- [ ] `docker compose up` 一键启动；含数据库持久卷与备份脚本。

### 8.2 性能与规模

- [ ] 单次导入 5 万行 Excel 在 60 秒内完成(含清洗/入库/利润重算)。
- [ ] 型号全景查询响应 < 500 ms(数据库现有规模)。
- [ ] 三维度利润聚合响应 < 2 秒(全量数据)。

### 8.3 安全

- [ ] `ENVIRONMENT=prod` 且任何默认密钥/弱口令存在时，应用拒绝启动。
- [ ] 数据库端口不绑定 `0.0.0.0`，仅本机访问。
- [ ] CSV 导出对公式注入字符做转义。
- [ ] 关键写操作(库存修正/替代料/排除/导入)有审计记录，含操作人与原因。

---

## 九、源代码与知识产权

- 完整源代码托管于私有仓库 `Jinchen-Yang/it-spareparts`(GitHub)，交付时按约定向客户开放访问或转交所有权。
- 数据库迁移文件(Alembic)与部署脚本随代码一并交付。
- 第三方开源依赖均为 MIT/BSD/Apache 等宽松许可，无 GPL 传染风险。

---

## 十、风险与依赖说明(诚实)

| 风险 | 说明 | 缓解 |
|---|---|---|
| 数据完整性依赖客户导出 | 当前 78% 销售行能匹配真实成本，剩余 22% 因采购历史未覆盖只能估算或无成本 | 持续导入更早期采购数据；治理页"估算成本行"始终可见 |
| 氚云导出格式变化 | 若氚云列名/字段变化，需更新映射表(`etl/mapping.py`) | 列名校验已就位；变更时单点修改 |
| 部分文件非真正 `.xlsx` | 实测氚云某些导出实为 `.xls` 或 HTML 改后缀 | 已加友好提示并要求"另存为 `.xlsx`" |
| 非标型号需持续维护 | "一批备件"类笼统型号需人工逐一确认排除 | 治理页 + 审计已支持，由客户业务人员定期维护 |
| 利润数字可信度 | 整体利润依赖数据治理质量；新增非标型号、新采购类型会影响 | 治理页 + 异常计数提供持续监控 |

---

## 附录：模块/接口清单

### A. 后端 API(部分)

```
POST /api/auth/login                      登录
POST /api/import/upload?mode=skip|upsert  上传 Excel(管理员)
GET  /api/import/batches                  导入历史
GET  /api/parts/search?q=                 型号搜索
GET  /api/parts/overview?pn_std=          型号全景
GET  /api/parts/purchases / sales         分页明细
GET  /api/profit?dimension=...            三维度利润聚合
POST /api/profit/recompute                重算利润(管理员)
GET  /api/profit/export                   CSV 导出
GET  /api/inventory                       库存列表
PUT  /api/inventory/{id}                  人工修正(管理员)
GET  /api/governance/summary              数据治理汇总
GET  /api/governance/parts?kind=          非标/待复核/已排除型号
PUT  /api/governance/exclude              排除/恢复(管理员)
GET  /api/substitutes?pn_std=             替代料
POST /api/substitutes                     新增(管理员)
```

### B. 前端页面

| 页面 | 路径 | 用户 |
|---|---|---|
| 登录 | `/login` | 所有人 |
| 数据导入 | 顶部菜单 | 管理员 |
| 型号查询 | 顶部菜单 | 登录用户 |
| 利润分析 | 顶部菜单 | 管理员 |
| 库存查询 | 顶部菜单 | 登录用户 |
| 数据治理 | 顶部菜单 | 管理员 |

### C. 关键数据库表(16 张)

```
维度  dim_part, part_alias, dim_supplier, dim_customer
事实  f_purchase_order, f_purchase_line, f_sales_order, f_sales_line, f_part_inquiry
状态  inventory, part_substitute
系统  sys_import_batch, sys_import_error, sys_raw_file, sys_audit_log
版本  alembic_version
```

---

**文档结束**
