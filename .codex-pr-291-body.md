## 结论

统一“合同总额”为明确的含税口径；无法从可靠来源证明含税金额时不再猜 13%，而是显式标记 `incomplete`。同时闭环项目总表回传、备件成本、上传交互、权限与页面刷新链路。

## 生产只读诊断

- 当前合同关系 531 条；旧 f7 曾改动 497 条。
- 其中 64 条可由明确双税事实证明，433 条没有安全的含税证据，应在受控修复时清空并标记不完整。
- 本 PR 不直接更新生产业务数据，也不以固定 13% 重新猜值。
- 有挂靠维保需求单的活动项目 438 个：219 个成本完整且为正，203 个部分缺成本，16 个全部缺成本。
- 223 个项目只有往年需求单；旧“仅本年”默认范围会把它们错误显示为 0。

## 改动

### 1. 合同金额

- API、项目卡片、面板、老板看板、CSV 与工作簿统一使用 canonical `amount_inc_tax`。
- 保留 `amount_ex_tax`；legacy `contract_amount` 输出按含税语义。
- 销售回退只接受成功批次、明确未税额与税率；经济值冲突时 fail-close。
- `contract_amount_inc_tax` 登记到全局利润脱敏字段组；scoped sales / 无利润权限用户不能通过项目聚合或 CSV 旁路读取合同额。

### 2. 项目总表

- 模板升级至 2.6.0；唯一、当前、未共享合同允许修改“合同总额（含税）”。
- 隐藏稳定 ID、版本与导出金额，使用 HMAC 元数据、整本 revision/OCC、CAS、审计和幂等重放。
- `validate` 与 `apply` 分离；整本过期返回 409 且零写入。
- V1 兼容入口也重新校验 URL 项目与隐藏行 ID，关闭跨项目写入路径。

### 3. 备件成本

- 项目卡片默认按项目全生命周期需求单计算，不再只取本年。
- 人工覆盖优先；真实 0、部分缺失、全部缺失分别表达，缺证据不再伪装为 0。
- 项目卡片、面板、看板、CSV 与工作簿复用同一双税成本口径。

### 4. 上传、权限与即时刷新

- 修复上传入口点击无反应、同一文件无法重选，以及 void/restore 状态语义。
- DB apply 成功后等待父页面与已挂载 tab 的刷新屏障；明确区分“已提交但 UI 刷新失败”。
- 请求按 `projectId` 与序号隔离，旧请求不能覆盖新项目；子页失败时清掉旧数据。
- 修复跨项目读取/写入与回款计划集合越权，并补齐超 50/200 行分页路径。

### 5. 相邻维保缺陷

- 回款凭证先完成文件落盘，再提交 DB 与关闭提醒；失败回滚，历史缺文件可 replay 修复。
- 报销对账先在 SQL 分页 `bxd_no`，再加载本页三源明细。
- 费用归属、台账导入和项目总表统一锁顺序、来源指纹与 workbook revision，避免并发丢写和陈旧工作簿覆盖。
- 修复 WBDD 幂等 advisory lock 将真实 NUL 传给 PostgreSQL 导致上传在业务逻辑前失败；复合锁键改为无歧义、text-safe 的 canonical JSON。
- WBDD 上传与管理员成本重算遇到并发归属变化时整体回滚并返回可重试 409，不再误报 500。

## 数据修复与迁移边界

- f7 对新环境为安全 no-op。
- a9 只新增 append-only 修复运行/条目审计与保护，不直接 `UPDATE` 业务表。
- b6 固化费用归属完整性、来源指纹与 workbook revision/data_version 原子推进；离线 SQL 可生成，Alembic 保持单 head。
- 生产修复必须绑定 f7 前备份生成完整 manifest：497 = 64 保留 + 433 清空/incomplete。
- 第二人复核 manifest 后才允许 CAS 执行；版本冲突行拒绝覆盖。
- a9 两张表已注册到 ORM metadata，`alembic check` 零漂移；append-only trigger 仍由迁移管理。

## 自动化验证

- [x] 前端 65 files / 677 tests
- [x] 前端 TypeScript + Vite build
- [x] Linux 隔离 PostgreSQL：合同、成本、权限、项目总表、刷新与公共依赖分片回归
- [x] remediation、b6 完整性迁移与 offline SQL 定向回归；Alembic 单 head / metadata drift 检查
- [x] 合成数据人工 E2E：下载真实 XLSX，合同含税额 1130→2260，validate/apply、DB version/audit、项目卡片与面板即时读回一致
- [x] 合成数据附件 E2E：落盘 SHA、DB deliverable/business_file/audit、幂等重放与前端附件列表一致
- [x] `compileall`、`git diff --check`、差异 secrets 扫描
- [x] Kimi Code K3 对 b6 迁移/锁序/offline SQL 独立审计：无 P0/P1；另一个独立子代理完成整体差异复核，结论同为无业务 P0/P1
- [x] 当前候选 SHA GitHub Actions：后端 3914 passed / 6 skipped，迁移与 ORM 零漂移、生产依赖扫描通过；前端构建、677 项行为测试和生产依赖扫描通过

## 发布与真实验收 Gate

- [ ] 独立 reviewer APPROVE 后合并（必需 CI 已通过；作者账号不能自批）
- [ ] 部署后核对 deployed SHA 与 Alembic single head
- [ ] 从真实 f7 前备份完成 restore 取证，停 app/import writers，生成并双人复核 manifest，再 CAS 执行生产修复
- [ ] 迁移命令外置 `statement_timeout=120s` / `lock_timeout=5s`；任一迁移或修复核验失败立即中止
- [ ] 明确回滚只使用冻结时点备份或 forward fix；b6 downgrade 不作为业务数据回滚
- [ ] 真实账号执行：下载总表 → 改合同额 → validate/apply → DB/审计 → 卡片/概览/老板看板/导出即时刷新
- [ ] 真实项目复验“有需求单但成本 0”、部分缺成本、人工覆盖与各消费者一致
- [ ] 上传按钮、同文件重试、失败提示及刷新屏障验收
- [ ] 完成 #268 的两个 tracer bullet；#268 在此之前保持开放

> 注意：本 PR 当前只具备本地/隔离库验证证据；尚未合并、部署或执行生产 remediation，也尚未完成真实账号验收与观察窗口。

Fixes #255
Fixes #256
Related to #268
